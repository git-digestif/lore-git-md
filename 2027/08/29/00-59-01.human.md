This is the final patch submission in the `defaultBranchName` advice documentation fix series. The author, Vsevolod Myalitsin, has sent **v3** of the patch, incorporating all previously agreed-upon changes. The patch now uses `NULL` for `advise()` calls that aren’t tied to an `advice_setting` entry, resolving the last major technical hurdle (backward compatibility) with a clean, idiomatic solution suggested by Jeff King and endorsed by Junio C Hamano.

The patch corrects the on-screen instructions for disabling the `defaultBranchName` advice message, recommending a globally scoped configuration command (`git config --global advice.defaultBranchName false`) instead of a repository-local `set`. This aligns with the intended user experience and avoids confusion for users who want the setting to apply globally by default. The implementation introduces a `scope_hint` field in the `advice_setting` struct, set to `CONFIG_SCOPE_GLOBAL` for `defaultBranchName` and left as `CONFIG_SCOPE_UNKNOWN` for other keys, making the system extensible for future advice keys that may require different scopes (e.g., `--system`).

The patch touches only `advice.c` and is self-contained, with no changes to on-disk formats or test coverage. The architectural direction—using `scope_hint` and passing the entire `advice_setting` struct to `vadvise()`—has been settled, and the backward-compatibility mechanism is now in place. The only remaining loose end is a minor syntax preference debate (`--global` vs `set --global` in the displayed command), but this is not a blocker for merging. The patch is well-motivated, thoroughly reviewed, and ready for Junio to pick up. Expect it to appear in `next` soon.