This is a brief but pointed review comment in the ongoing `clang-format` adoption discussion. Brian m. carlson flags a practical compatibility issue with Ævar’s second RFC patch: the `BitFieldColonSpacing: None` rule requires clang-format v12 or newer, but Debian stable (a common development platform) ships only v11. Since some Git developers actively use Debian stable, the patch would effectively lock them out of the formatting toolchain.

The objection is narrow but consequential: it surfaces a real-world constraint that could stall the entire `clang-format` effort if not addressed. Brian suggests dropping the patch for now and revisiting it later, implying that platform support should take precedence over incremental style improvements. No alternative solution is proposed, and the comment does not engage with the broader debate about Randall’s pluggable-formatter counterproposal or Junio’s line-wrapping concerns. It is a surface-level review focused solely on the immediate compatibility blocker.