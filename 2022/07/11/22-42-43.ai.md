This is a brief reply in the ongoing `clang-format` adoption thread, specifically addressing the second patch in Ævar Arnfjörð Bjarmason’s four-part RFC series. The patch proposes adding `BitFieldColonSpacing: None` to Git’s `.clang-format` file to enforce the project’s preferred bit-field syntax (`unsigned foo:1` instead of `unsigned foo : 1`), but brian m. carlson raises a platform-compatibility blocker: the rule requires clang-format v12 or newer (released April 2021), which is unavailable on Debian stable (clang 11). He argues that Debian stable should remain a viable development target and recommends dropping the patch for now, deferring reconsideration until the compatibility constraint is resolved.

The reply surfaces a concrete trade-off between incremental progress and platform inclusivity. Ævar’s patch reduces the formatting delta by ~36 lines, but the version requirement excludes developers on older but still-supported distributions. brian’s position aligns with his broader advocacy for broad platform compatibility (e.g., his earlier opposition to `clang-format` due to NonStop’s lack of support), though here the concern is narrower: a specific rule’s version constraint rather than the tool’s availability. The reply does not engage with the broader debate about `clang-format` vs. Randall S. Becker’s pluggable-formatter counterproposal, nor does it propose an alternative (e.g., dynamic version detection or a fallback rule). It simply flags the incompatibility and suggests deferral, leaving the next steps open.

Key technical details: the `BitFieldColonSpacing: None` rule is a minor but measurable improvement to Git’s `.clang-format` configuration, and the version constraint (clang-format v12+) is a hard blocker for some contributors. The reply does not address whether the rule could be reintroduced later (e.g., via a conditional in the Makefile or a separate CI enforcement target) or how the project might balance incremental progress with platform support. The tone is pragmatic and deferential, avoiding advocacy for or against the broader `clang-format` proposal. The attachment (a PGP signature) is routine and not substantive.