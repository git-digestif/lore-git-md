This is a substantive review comment in the ongoing discussion about adopting an automatic code formatter for Git. Phillip Wood engages directly with the technical feasibility of using `clang-format` to match Git’s existing style, raising concerns about inconsistent struct formatting and the potential for excessive churn if the tool’s output is adopted as-is. He illustrates his point with a concrete example from a past patch (Han-Wen Nienhuys’ refs backend work), showing how `clang-format` reformats struct definitions in ways that diverge from Git’s current conventions—sometimes placing one field per line, other times packing multiple fields per line with irregular indentation. His critique highlights a specific pain point: `clang-format`’s handling of structs may not align with Git’s manual style, and tweaking the rules to improve it has proven difficult in the past.

Phillip’s feedback is grounded in prior experimentation; he notes that he tried adjusting `.clang-format` rules to address similar issues but made no progress. This suggests the problem is not trivial and may require either deeper configuration changes or acceptance of suboptimal formatting. His concern about churn—mass reformatting of existing code—echoes a recurring theme in the thread: even if `clang-format` can be made to mostly match Git’s style, the remaining deviations could still disrupt readability and ongoing development. The example diffs underscore the practical stakes: adopting the tool as-is would force large-scale changes to struct layouts, which could complicate reviews and merges for in-flight patches.

The comment does not propose an alternative or endorse Randall S. Becker’s pluggable formatter counterproposal, but it adds weight to the argument that `clang-format`’s current output is not yet ready for adoption. It also implicitly challenges the thread’s framing of the trade-off between automation and style deviations—Phillip’s example suggests the deviations may be more significant than "occasional slightly suboptimal" outcomes. For a future AI summarizer, the key technical details are the struct formatting inconsistency, the risk of churn, and the difficulty of rule-tweaking, all of which remain open questions in the broader discussion.