This is a brief but pointed reply in the ongoing discussion about adopting `clang-format` for Git’s codebase. Brian m. carlson, a strong advocate for the `clang-format` proposal, responds to Ævar Arnfjörð Bjarmason’s patch that disables the `ColumnLimit` rule in `.clang-format`. The patch removes enforcement of Git’s 80-character line limit, reducing the formatting delta by ~60% but leaving unresolved whether to reintroduce a stricter limit later.

Brian’s position is clear: he supports *some* line-length limit, whether 80 characters (Git’s traditional guideline) or a longer one like 132 (a common terminal width). His objection is to dropping the limit entirely, which he sees as inconsistent with the project’s existing review practices. He implies that if the community is unwilling to enforce a line-length rule—even a relaxed one—then reviewers should stop nitpicking line length in patches. This frames the discussion as a binary choice: either automate enforcement (via `clang-format`) or abandon the guideline altogether.

The reply does not engage with the practical challenges Ævar raised (e.g., whether `clang-format` can replicate Git’s nuanced line-wrapping conventions) or Junio’s recent concern about the tool’s rigidity. Instead, it focuses on the principle of consistency: if Git cares about line length, it should enforce it systematically. Brian’s suggestion of 132 characters as an alternative hints at a compromise, but he does not push for it strongly, deferring to the community’s preference for 80.

This is a surface-level review—it comments on the patch’s intent rather than its implementation—but it carries weight given Brian’s role in the original proposal. The reply nudges the discussion toward a concrete decision: pick a limit (80 or 132) and enforce it, or stop policing line length in reviews. It does not address Randall S. Becker’s counterproposal for a pluggable formatter, nor does it offer a path forward for resolving the remaining formatting delta. For now, the question of line-length enforcement remains open.