This is a reply in the ongoing design discussion about adopting an automatic code formatter for Git, specifically addressing Ævar Arnfjörð Bjarmason’s RFC patch series to refine `.clang-format` compatibility with Git’s style. The email is a surface-level review from brian m. carlson, who supports the goal of automated formatting but raises a targeted concern about the patch’s removal of the `ColumnLimit` rule.

brian’s reply reaffirms his support for enforcing an 80-character line limit, aligning with Git’s documented `CodingGuidelines`. He argues that if the project is going to enforce line length at all—whether in code review or via automation—it should pick a specific limit rather than dropping enforcement entirely. He suggests two options: either stick with the traditional 80-character limit (requiring some line rewrapping) or adopt a longer limit like 132 characters (a common terminal width) to reduce churn. His preference is not dogmatic; he is open to either choice but insists on consistency. The reply does not engage with the broader debate about `clang-format` vs. Randall S. Becker’s pluggable formatter counterproposal, nor does it address Junio’s nuanced concern about replicating Git’s flexible line-wrapping conventions. It is narrowly focused on the immediate question of whether to enforce a line-length rule and, if so, which one.

The tone is pragmatic and collaborative, framing the discussion as a choice between two reasonable options rather than a fundamental disagreement. brian does not propose a concrete next step (e.g., a follow-up patch to reintroduce a `ColumnLimit`) but signals his willingness to contribute to whichever direction the project chooses. His position contrasts with Ævar’s incremental approach (removing the rule to make progress on other formatting dimensions) and Elijah Newren’s broader argument that the formatter should automate existing style requirements without imposing new ones. The reply adds a small but clear constraint to the discussion: if line-length enforcement is to be part of the automated formatter, the project must pick a specific limit. This leaves the door open for future patches to reintroduce a `ColumnLimit` (either 80 or 132) but does not resolve the tension between Git’s documented guideline and the reality of its existing codebase.