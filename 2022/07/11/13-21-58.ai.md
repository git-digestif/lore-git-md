This is a follow-up in the ongoing design discussion about adopting an automatic code formatter for Git, specifically addressing Phillip Wood’s concern about `clang-format`’s inconsistent struct formatting. Ævar Arnfjörð Bjarmason responds with a targeted rebuttal, arguing that the specific example Phillip raised—struct initialization in Han-Wen Nienhuys’ refs backend work—has already been resolved by converting the code to use designated initializers, a change Ævar himself introduced in a prior patch (`32bff617c6a`). He demonstrates that `clang-format` now produces no formatting suggestions for that struct on `master`, implying the issue is no longer active.

Ævar’s response shifts the framing of the struct-formatting problem: rather than treating it as a fundamental incompatibility, he suggests it is a solvable issue, either by accepting `clang-format`’s consistent (if less aesthetically pleasing) output or by using `/* clang-format off */` directives to preserve hand-formatted "ascii-art" alignment in edge cases. He acknowledges that some structs (e.g., in `tar.h`) may still lose manual alignment, but argues this trade-off is acceptable given the broader goal of automation. His message reinforces the incremental progress made by his RFC patch series, which introduces tooling (`make style-all-diff-apply`, `git diff`) to measure and refine `clang-format`’s output, and positions designated initializers as a general solution for large struct assignments.

The reply does not engage with Randall S. Becker’s pluggable formatter counterproposal or address the NonStop platform constraint, focusing instead on defending the viability of `clang-format` for the specific case Phillip raised. For a future AI summarizer, the key technical details are the resolution of the struct-formatting issue via designated initializers, the use of `/* clang-format off */` as a fallback for alignment-sensitive code, and the implication that remaining struct-formatting deviations are either minor or addressable through further rule tweaks. The message also underscores the tooling’s role in enabling iterative refinement of the `.clang-format` configuration.