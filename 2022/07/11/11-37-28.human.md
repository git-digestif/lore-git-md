This is the fourth and final patch in Ævar Arnfjörð Bjarmason’s RFC series refining Git’s `.clang-format` configuration to better match the project’s existing style. The patch disables indentation of `goto` labels, a small but measurable adjustment that trims the formatting delta by about 300 lines (from 13,042 insertions/deletions to 13,039). The change aligns `clang-format` with Git’s long-standing convention of left-flushing `goto` labels, which are currently indented by a single space in many places.

The patch is incremental and pragmatic, continuing the series’ goal of reducing the gap between `clang-format` output and Git’s manual style without requiring disruptive changes to the codebase. It does not address the larger debate between adopting `clang-format` and Randall S. Becker’s pluggable-formatter counterproposal, but it provides another data point: even minor tweaks to the formatter’s rules can shrink the remaining diff, suggesting that further refinements might close the gap entirely.

The commit message is concise and factual, noting the before-and-after impact on the total diff size. The patch itself is trivial—just three lines added to `.clang-format`—but it is part of a larger tooling effort that includes Makefile targets (`style-all-diff-ok`, `style-all-diff-apply`) to test formatting across the entire codebase. These targets allow the community to assess whether the remaining deviations are acceptable or fundamental, and whether future enforcement (e.g., via CI) is feasible.

For a busy reader, this patch signals that the `clang-format` proposal is making measurable progress. The cumulative effect of the series—disabling column limits, relaxing bit-field spacing, and now unindenting `goto` labels—has reduced the formatting delta by roughly 60%, from ~32,000 to ~13,000 lines. The remaining diff is still substantial, but the trend suggests that further rule tweaks might bring it within an acceptable range. Whether the community will tolerate minor deviations (e.g., suboptimal formatting in edge cases) or demand perfect alignment remains an open question, but this patch moves the discussion forward with concrete evidence.