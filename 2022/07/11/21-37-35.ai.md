This is a surface-level review from Junio C Hamano on Ævar Arnfjörð Bjarmason’s RFC patch series refining Git’s `.clang-format` configuration. Junio’s reply focuses narrowly on the `ColumnLimit` rule and its implications for Git’s line-wrapping conventions.

The email questions whether `clang-format` (or any automated formatter) can handle Git’s nuanced line-wrapping guidelines, which allow flexibility around the 80-column limit based on context. Junio notes that Git’s documented style does not enforce a rigid cutoff—developers are encouraged to wrap at "good" places, even if that means exceeding 80 columns slightly (e.g., wrapping at 82 instead of 65). He asks whether an automated tool can replicate this judgment, suggesting skepticism about `clang-format`’s ability to do so.

This is not a rejection of the patch or the broader `clang-format` proposal, but rather a targeted concern about one aspect of the formatting rules. The question remains open: whether to accept `clang-format`’s rigid line-breaking behavior, reintroduce a stricter `ColumnLimit` later (e.g., via a separate `make style` target), or abandon line-length enforcement altogether in the automated formatter. Junio does not propose a solution, but his observation highlights a potential limitation of the tool that the community may need to address if the proposal moves forward.

The review does not engage with the patch’s substance (disabling `ColumnLimit` to reduce the formatting delta) or the broader discussion about automation, platform compatibility, or Randall S. Becker’s counterproposal. It is a focused, technical nit about a specific rule’s alignment with Git’s style.