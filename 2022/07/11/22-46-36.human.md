This is a brief but pointed reply from Junio C Hamano in the ongoing discussion about adopting `clang-format` for automatic code formatting in Git. Junio responds directly to brian m. carlson’s suggestion that the project could enforce either an 80- or 132-character line limit, but his concern is more nuanced than a simple preference for one limit over the other.

Junio’s core message is that Git’s existing line-wrapping conventions are not rigidly tied to a fixed column limit. Instead, they allow flexibility—wrapping at "good" places even if it slightly exceeds 80 columns—based on human judgment about readability. He warns against using automation as a justification for producing "unreadable mess" (e.g., lines consistently stretching to 130 columns) or, worse, dismissing reviewer feedback about readability issues that a formatter cannot address. His tone is cautionary, not prescriptive: he does not reject the idea of a longer line limit outright, but he insists that automation should not become an excuse for poor style.

This reply adds a new dimension to the discussion. While brian framed the question as a binary choice between 80 and 132 characters, Junio highlights that Git’s documented style is not purely mechanical—it includes subjective judgments about readability that a tool like `clang-format` may not replicate. This suggests that even if the project adopts a formatter, some manual review may still be needed for line-wrapping decisions, or that the formatter’s configuration may need further refinement to better match Git’s conventions. The reply does not propose a solution, but it raises a potential limitation of `clang-format` that could require additional tooling (e.g., a separate `make style` target for line-length enforcement) or targeted exemptions for alignment-sensitive code.

For a busy reader, this is a clear signal that the discussion is not just about picking a line limit—it’s about whether `clang-format` can faithfully reproduce Git’s style at all. Junio’s concern is substantive, not surface-level, and it may require further patches or tooling to address. The thread remains open, with no consensus yet on how to reconcile automation with Git’s nuanced conventions.