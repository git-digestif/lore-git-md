Here’s the human-readable summary for a developer catching up on the thread:

The `git history squash` feature, proposed by Harald Nordgren, is now complete and ready for integration. This new subcommand folds a range of commits into the oldest one while preserving descendant history, offering a more efficient alternative to the conflict-prone rebase workflow. The series has reached its 15th iteration, addressing all prior feedback, including strict autosquash marker handling, shape-based validation, protection for local branches, and both interactive and non-interactive (`--no-edit`) workflows.

Junio Hamano has signaled intent to merge the series ("Will replace" in v7), and the latest patch (4/8 of v15) adds the subcommand’s foundation—usage, revision parsing, and input validation—without introducing new behavior. The broader `git history` topic is already in Junio’s integration pipeline, and this patch aligns cleanly with it. Key design choices include rejecting operations that would leave local branches dangling (with advice to use `--update-refs=head`), enforcing strict case-sensitive matching for autosquash markers, and mirroring `git rebase -i --autosquash`’s template for interactive editing. The series is now feature-complete, with all technical concerns resolved and test coverage verifying edge cases like merge commits, ref protection, and message construction. Expect it to graduate to `next` in the next "What’s cooking" report.