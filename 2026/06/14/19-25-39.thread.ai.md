Here is the updated dense AI thread summary incorporating the new email:

---

Proposal to fold commit ranges into a single commit, implemented as `git history squash` standalone subcommand. **Series v15 by Harald Nordgren is now feature-complete and ready for integration**, addressing all prior feedback including the final autosquash marker resolution policy, shape-based validation, **ref protection for local branches**, **the `--no-edit` workflow**, and **the default interactive message-editing workflow**. **Marked "Will replace" by Junio C Hamano in v7**, signaling intent to queue for the next release.

**Finalized implementation (v15)**:
- **Patch 4/8 (Junio's ack)**: Adds the subcommand’s entry point (`cmd_history_squash`), usage string (accepting revision ranges like `HEAD~3..HEAD` and flags `--dry-run`, `--update-refs`, `--[no-]edit`), and `setup_squash_revisions()` to validate/configure the revision walk. Enforces at least one `BOTTOM` revision (e.g., `A..B` but not `B` alone) and warns if user-supplied rev-list options would alter walk order. **Minimal and focused**: touches `builtin/history.c`, `Documentation/git-history.adoc`, and `t/t9902-completion.sh` (shell completion). Junio’s ack confirms alignment with the broader `git history` effort, now in the integration pipeline.
- **Autosquash marker resolution policy**: **Strict case-sensitive matching enforced** (rejecting all uppercase OIDs) via `starts_with()` replacement in `squash_check_autosquash_subject()`, aligning with Git’s historical convention of emitting only lowercase hexadecimal OIDs (`bc/restrict-hex-to-lowercase` precedent). **Test coverage for strict matching added**.
- **Shape-based validation**: **Patch 5/8 introduces `resolve_squash_range()`**, which enforces invariants on the revision range: at least two commits, exactly one tip, no root commits, and no merges with external parents. Uses `SQUASH_SEEN` and `SQUASH_TIP` flags to track commits in the range and the tip commit. **Error messages cite offending commit/parent OIDs** and guide users toward valid usage. **Test coverage verifies all error conditions** (empty ranges, single commits, root commits, multi-tip ranges, merges with external parents).
- **Ref protection (Patch 6/8)**: **Detects and rejects operations that would leave local branches dangling** by enumerating branches descended from the squashed range but not including its tip. Uses `ref-filter` (`FILTER_REFS_BRANCHES`) to list blocking branches alphabetically in the error message, with an `advice()` hint (`ADVICE_HISTORY_UPDATE_REFS`) pointing to `--update-refs=head`. **Limited to local branches**, matching the scope of other history-rewriting commands. **Test coverage verifies detection logic and advice suppression**.
- **`--no-edit` workflow (Patch 7/8)**: **Completes the non-interactive path** for the feature, handling autosquash markers (`fixup!`, `squash!`, `amend!`), preserving authorship and merge parents, and replaying descendants. Key behaviors:
  - **Autosquash validation**: Rejects any `fixup!`/`squash!`/`amend!` whose target is outside the range, unless the entire range consists of markers for a single target. **Strict case-sensitive matching enforced**.
  - **Message construction**: Builds the squashed commit message from the oldest commit’s message, incorporating `amend!` messages targeting it and appending `squash!`/`fixup!` messages (or rejecting them if they would be discarded).
  - **Merge parent preservation**: Retains the full parent list of the original merge commit.
  - **Descendant replay**: Uses the existing history-rewrite machinery (`handle_reference_updates()`), respecting `--dry-run` and `--update-refs` flags.
  - **Reflog accuracy**: Records the full revision expression (e.g., `squash: updating start..HEAD`).
- **Default interactive message-editing workflow (Patch 8/8)**: **Mirrors `git rebase -i --autosquash`** by opening an editor with a template that:
  - Groups autosquash markers under their targets (e.g., `fixup! alpha` appears under `alpha`).
  - Comments out `fixup!` messages (users uncomment to retain them).
  - Retains `squash!` message bodies.
  - Replaces the target’s message with `amend!` content unless a preceding `squash!` requires both bodies.
  - Uses existing sequencer helpers (`add_squash_combination_header`, `add_squash_message_header`) for consistent formatting.
  - **Test coverage verifies editor invocation, empty-message aborts, marker grouping, and integration with `--no-edit`**.
- **Mechanical fixes**: Typo correction (`squshed` → `squashed`) in adjacent comment.
- **Refactoring**:
  - Patch 1/8 introduces `first_parent_tree_oid()` helper in `builtin/history.c`, consolidating three nearly identical code blocks for resolving the tree OID of a commit’s first parent (or the empty tree for root commits). No behavior changes; purely mechanical reduction of duplication.
  - Patch 2/8 extends `commit_tree_ext()` (used by `git history fixup` and `git history split`) to accept an optional `message_template` parameter. When provided, the template overrides the commit message body of the `commit_with_message` argument; when `NULL`, the original behavior (reusing the commit’s message) is preserved. **Purely mechanical and backward-compatible**: existing callers pass `NULL`, so their behavior is unchanged. This prepares the ground for the squash logic (patches 4–8).
  - Patch 3/8 refactors squash-message template helpers (`add_squash_combination_header()`, `add_squash_message_header()`, `squash_subject_comment_len()`) from `sequencer.c` into `sequencer.h` as reusable functions, along with the `todo_item_flags` enum. **Purely mechanical**: no behavior changes; the logic is unchanged but now accessible to `git history squash` (patch 8/8).

**Core problem**: Efficiently collapse a linear or merge-commit-containing range into its oldest commit while preserving descendant history, avoiding the repeated conflict stops of a rebase-based approach. **Finalized behavior**:
- **Shape-based validation**: Rejects ranges that are empty, single-commit, contain root commits, have multiple tips, or include merges with external parents. **Error messages cite offending commit/parent OIDs**.
- **Ref protection**: Rejects operations if any local branch descended from the squashed range would be left dangling, with an `advice()` message (`ADVICE_HISTORY_UPDATE_REFS`) suggesting `--update-refs=head`. **Blocking branches are listed alphabetically in the error message**.
- **Autosquash handling**: Ranges whose oldest commit is a `fixup!`/`squash!`/`amend!` are **grouped under their targets in the default template**, matching `git rebase -i --autosquash`. **Strict case-sensitive matching enforced** (rejecting uppercase OIDs).
- **`--no-edit` workflow**: Creates the squashed commit without opening an editor, incorporating `amend!` messages targeting the oldest commit and appending `squash!`/`fixup!` messages (or rejecting them if they would be discarded).
- **Default interactive workflow**: Opens an editor with a template that mirrors `git rebase -i --autosquash`, allowing users to edit the squashed commit message interactively. **`fixup!` messages are commented out by default**, `squash!` bodies are retained, and `amend!` replaces the target’s message unless a preceding `squash!` requires both.
- **Rev-list options**: Fully validated and standardized; preserves the `boundary` invariant.
- **Merge-parent preservation**: The squashed commit retains the full parent list of the original merge commit.
- **Reflog message accuracy**: Records the full revision expression.

**Implementation details**:
- **Files touched**: `builtin/history.c` (1100+ insertions, including `first_parent_tree_oid()`, `commit_tree_ext()`, `setup_squash_revisions()`, `resolve_squash_range()`, `cmd_history_squash()`, **ref protection logic**, **`--no-edit` workflow helpers**, and `build_squash_message()`), `sequencer.c`/`sequencer.h` (refactored squash-message helpers), `advice.c`/`advice.h` (**new `ADVICE_HISTORY_UPDATE_REFS` key**), `object.h`, `Documentation/git-history.adoc`, `t/t3455-history-squash.sh` (1550+ lines, including **ref protection tests**, **`--no-edit` workflow tests**, and **interactive workflow tests**), `t/t9902-completion.sh` (1 line).
- **New helpers**: `first_parent_tree_oid`, `resolve_squash_range`, `squash_check_subject`, `squash_check_autosquash_subject`, `squash_amend_message`, `squash_squash_message`, `setup_squash_revisions`, `--[no-]edit`, `--update-refs=(branches|head)`, `ADVICE_HISTORY_UPDATE_REFS`, `SQUASH_SEEN`, `SQUASH_TIP`, `SQUASH_AMEND_TARGET`, `build_squash_message`, `add_squash_combination_header`, `add_squash_message_header`, `squash_subject_comment_len`, `is_autosquash_subject`, `skip_one_autosquash_prefix`, `truncate_message_to_subject`, `subject_data` struct.
- **Test coverage**: Range parsing, shape-based validation (empty ranges, single commits, root commits, multi-tip ranges, merges with external parents), merge commit handling, descendant replay, authorship preservation, **default interactive message editing** (editor invocation, empty-message aborts, autosquash marker grouping, `fixup!`/`squash!`/`amend!` handling), **ref protection (detection logic, advice suppression)**, `--dry-run` prediction accuracy, input validation, fixup/squash/amend edge cases (including strict case-sensitive matching), CLI/documentation style compliance, reflog message accuracy, and **`--no-edit` workflow** (autosquash marker resolution, merge handling, edge cases).

**Resolved technical concerns (v4–v15)**:
1. **Input validation**: Handled by `resolve_squash_range()` and `setup_squash_revisions()` (patches 4–5/8).
2. **Range resolution**: The base is always the oldest in-range commit’s parent.
3. **Merge commit handling**: Rejects merges with external parents or more than two parents but allows fully contained merges.
4. **`fixup!`/`squash!`/`amend!` commits**: Folded like any other commit, with markers grouped under their targets in the default template. **Strict case-sensitive matching enforced**.
5. **Code maintainability**: Patches 1–5/8 refactor existing code (including `first_parent_tree_oid()`, `commit_tree_ext()`, squash-message helpers, the `git history squash` skeleton, and shape-based validation); Patches 6–8/8 implement the core functionality in a decomposed structure.
6. **Ref-handling design**: **Resolved**: Rejects operations if any local branch descended from the squashed range would be left dangling, with an `advice()` message guiding users to `--update-refs[=head]`. **Blocking branches are listed alphabetically in the error message**.
7. **Default message editing**: Implements the feature using a template that matches `git rebase -i --autosquash`, with **interactive editing enabled by default** (Patch 8/8).
8. **Template consistency**: Resolved by adopting a template that matches `git rebase -i --autosquash`.
9. **CLI/documentation style**: Addressed Junio’s feedback.
10. **`amend!` message handling**: Incorporates `amend!` messages targeting the first folded commit into the default squashed commit message.
11. **Reflog message accuracy**: Records the full revision expression.
12. **Rev-list option sanitization**: Preserves the `boundary` invariant.
13. **Test suite robustness**: Junio’s fixup and v14’s `git rev-parse --short` replacement resolve fragility issues.
14. **Trailer ordering**: Phillip’s `Signed-off-by:` now appears above his `Helped-by:`.
15. **Autosquash marker resolution policy**: **Resolved in favor of strict case-sensitive matching** (rejecting all uppercase OIDs).
16. **Ref protection**: **Resolved**: Detects and rejects operations that would leave local branches dangling, with a clear error message and advice pointing to `--update-refs=head`.
17. **`--no-edit` workflow**: **Resolved**: Completes the non-interactive path, handling autosquash markers, merge parents, and descendant replay.
18. **Default interactive workflow**: **Resolved**: Implements `git rebase -i --autosquash`-style template editing, with `fixup!` messages commented out, `squash!` bodies retained, and `amend!` logic handling conflicts with `squash!`.

**Design resolution**:
- **Default behavior**: Reject the operation if any local branch descended from the squashed range would be left dangling, with an `advice()` message (`ADVICE_HISTORY_UPDATE_REFS`) guiding users to `--update-refs[=head]`.
- **`--update-refs` flag**: Supports `=head` to retarget only the current branch to the squashed commit.
- **Input syntax**: Accepts multiple arguments (parsed like `git rev-list`).
- **Merge commit handling**: Rejects merges with external parents or more than two parents but allows fully contained merges.
- **Shape-based validation**: Uses `resolve_squash_range()` to reject malformed histories (empty ranges, single commits, root commits, multi-tip ranges, merges with external parents). **Error messages cite offending commit/parent OIDs**.
- **Default message editing**: Opens an editor with the autosquash-style template by default (Patch 8/8).
- **`--no-edit` workflow**: Creates the squashed commit without opening an editor, incorporating `amend!` messages targeting the oldest commit and appending `squash!`/`fixup!` messages (or rejecting them if they would be discarded).
- **Autosquash strictness**: `--no-edit` folds reject any operation that would discard a `squash!` or `amend!` message unless the target is also in the range, and **strict case-sensitive matching is enforced**.

**Participant positions**:
- **Harald Nordgren**:
  - **Technical contributions**: Addressed all feedback in v15, including the `starts_with()` replacement, test coverage for strict matching, shape-based validation (`resolve_squash_range()`), **ref protection (Patch 6/8)**, **`--no-edit` workflow (Patch 7/8)**, **default interactive workflow (Patch 8/8)**, and the `first_parent_tree_oid()`, `commit_tree_ext(message_template)`, squash-message helper refactorings, and **Patch 4/8 skeleton**. **Series now feature-complete**.
  - **Process contributions**: Engaged with Phillip’s procedural demands and resolved test robustness.
- **Junio C Hamano**:
  - **Signaled intent to queue the series** with "Will replace. Thanks." in v7.
  - **Resolved the case-sensitivity policy** in favor of strict matching, citing `bc/restrict-hex-to-lowercase` and Git’s historical convention.
  - **Acknowledged Patch 4/8** as technically sound and aligned with the broader `git history` effort, now in the integration pipeline.
- **Phillip Wood**: **Substantive review** arguing for permissive matching (consistency with `git rebase --autosquash`), but **deferred to Junio’s resolution**.
- **Kristoffer Haugsbakk**: **Clarified trailer ordering**.
- **D. Ben Knoble**: **Endorsed future integration** with `git rebase --autosquash` via `git replay`.

**Status**:
- **Technical status**: **Series is now feature-complete (v15)**. All feedback addressed, including **ref protection for local branches**, **the `--no-edit` workflow**, and **the default interactive message-editing workflow**.
- **Process status**: **Ready for integration**. Junio’s "Will replace" sign-off and the resolution of all feedback indicate intent to merge.
- **Integration status**: **Cooking in `seen`** and likely to graduate to `next` in Junio’s next "What's cooking" report.

**Related work**:
- **`git rebase --autosquash`**: The series’ autosquash behavior is designed to match this command’s template generation, but **strict case-sensitive matching is enforced** (rejecting uppercase OIDs).
- **`bc/restrict-hex-to-lowercase`**: brian m. carlson’s prior work establishing Git’s convention of emitting only lowercase hexadecimal OIDs, cited by Junio as justification for strict matching.
- **`git rebase --update-refs`**: The `--update-refs=head` escape hatch aligns with this command’s ref-update behavior, though `git history squash` rejects dangling refs by default rather than silently updating them.

---