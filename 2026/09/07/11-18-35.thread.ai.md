Mode: ai
Thread identity: Patch series introducing `--ref-storage-format=` flag to standardize naming of Git’s reference storage format options. Series version: v1 (11 patches). Originating author: Patrick Steinhardt.

Core problem or goal: Resolve inconsistent naming between the command-line flag `--ref-format=`, the repository extension `refStorage`, the environment variable `GIT_REFERENCE_BACKEND`, and the common documentation term “ref storage format.” The series renames the flag to `--ref-storage-format=` in `git init` while preserving `--ref-format=` as a hidden backward-compatibility alias, enabling future `--object-storage-format=` without further naming clashes.

Current status: Under review. Series v1 posted; no prior versions. No merge status reported.

Key technical details:
- Files touched: 15, primarily documentation (`Documentation/git-init.txt`, `git-clone.txt`, etc.) and test scripts (`t/t0033-safe-directory.sh`, `t/t0034-root-safe-directory.sh`, etc.).
- New flag: `--ref-storage-format=` (replaces `--ref-format=` in `git init`).
- Old flag: `--ref-format=` retained as hidden alias for backward compatibility.
- No new symbols; no on-disk format changes.
- Test coverage: updated to use new flag name.

Open questions and loose ends: None raised in this patch; series-wide questions (e.g., whether the new name sufficiently disambiguates from future object-storage flags) may surface in later patches or replies.

Participant positions: None recorded yet; thread root only.

Related work: None mentioned.