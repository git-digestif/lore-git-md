Thread identity: standalone bug report about `git worktree add "" HEAD` deleting the `.git` directory on Windows. Originating author: Paul DE TEMMERMAN. Thread type: critical bug report (no patch series or RFC).

Core problem or goal: `git worktree add` with an empty string argument (`""`) as the worktree path on Windows deletes the entire workspace, including the `.git` directory, instead of failing gracefully. This is a high-severity, platform-specific (Windows) data-loss bug in the worktree subsystem.

Current status: newly reported; no prior discussion or resolution. No integration status (not applicable to bug reports). Version: Git for Windows 2.55.0.windows.5.

Key technical details:
- Subsystem: `git worktree` (specifically the `add` subcommand).
- Files likely involved: `builtin/worktree.c`, Windows-specific path handling in `compat/`.
- Platform: Windows (64-bit, OpenSSL 3.5.7, SHA-1/SHA-256).
- Trigger: empty string argument (`""`) passed as the worktree path.
- Impact: deletion of the `.git` directory (not just the worktree).
- Expected behavior: graceful failure (e.g., error message) without destructive side effects.

Open questions and loose ends:
- Root cause: path validation or directory creation logic in the worktree code.
- Scope: whether this affects other platforms or only Windows.
- Reproducibility: whether the issue is consistent across all Windows environments or specific to certain configurations.
- Mitigation: whether there are existing workarounds or safeguards (e.g., pre-validation of arguments).

Participant positions: none yet (initial report). Future replies may include:
- Confirmation or denial of the bug by other Windows users.
- Technical analysis of the code path (e.g., `builtin/worktree.c`).
- Proposed fixes (e.g., argument validation, Windows-specific path handling).
- Reviewed-by/Acked-by from maintainers or domain experts (e.g., Johannes Schindelin, Phillip Wood).

Related work: none mentioned. This is a self-contained issue.