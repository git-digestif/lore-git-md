Thread identity: standalone bug report about `git worktree add "" HEAD` deleting the `.git` directory on Windows. Originating author: Paul DE TEMMERMAN. Thread type: critical bug report (no patch series or RFC).

Core problem or goal: `git worktree add` with an empty string argument (`""`) as the worktree path on Windows deletes the entire workspace, including the `.git` directory, instead of failing gracefully. This is a high-severity, platform-specific (Windows) data-loss bug in the worktree subsystem.

Current status: confirmed and fix queued for `master`. The bug is acknowledged as real and under active resolution. A patch series (Lars Schneider, 2026-08-25) addressing the root cause is linked, with the fix expected to be included in the next Git release. No explicit merge date or graduation from `next` is stated; integration status remains "queued for `master`" pending Junio's "What's cooking" report.

Key technical details:
- Subsystem: `git worktree` (specifically the `add` subcommand).
- Files likely involved: `builtin/worktree.c`, Windows-specific path handling in `compat/`.
- Platform: Windows (64-bit, OpenSSL 3.5.7, SHA-1/SHA-256); confirmed reproducible on Git for Windows 2.55.0.windows.5.
- Trigger: empty string argument (`""`) passed as the worktree path.
- Impact: deletion of the `.git` directory (not just the worktree).
- Expected behavior: graceful failure (e.g., error message) without destructive side effects.
- Fix details: not specified in this email, but likely involves argument validation or Windows-specific path handling.

Open questions and loose ends:
- Root cause: path validation or directory creation logic in the worktree code (now addressed by the patch series).
- Scope: whether the fix addresses all edge cases (e.g., other invalid path arguments) or remains Windows-specific.
- Reproducibility: whether the issue is consistent across all Windows environments or specific to certain configurations (original reporter's system details remain relevant for regression testing).
- Mitigation: none mentioned beyond the upcoming fix.

Participant positions:
- **Paul DE TEMMERMAN**: original reporter; identified the bug and provided system details.
- **Tuomas Ahola**: confirmed the bug's existence, linked the ongoing fix effort (Git for Windows issue #6346, Lars Schneider's patch series), and stated the fix is queued for `master`.
- **Lars Schneider**: author of the patch series (2026-08-25) addressing the root cause (not yet reviewed or merged in this summary).

Related work:
- Git for Windows issue #6346 (tracking the bug and fix).
- Lars Schneider's patch series (2026-08-25) on the mailing list.