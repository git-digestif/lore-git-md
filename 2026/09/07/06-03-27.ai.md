This is a CI/build system discussion advancing the thread about updating Git's CI infrastructure. Patrick Steinhardt replies to Jeff King's follow-up about the `linux32` and `linux-TEST-vars` jobs, agreeing with Peff's pragmatic stance but proposing a concrete alternative for 32-bit coverage: Alpine Linux.

Steinhardt notes that Alpine still supports 32-bit platforms and is already tested in Git's CI via the `linux-musl-meson` job. He suggests extending that job to include 32-bit testing, or even merging the `linux32` job into it. This would mean losing 32-bit glibc coverage, but he implies that might be an acceptable trade-off given the shrinking support landscape. For the `linux-TEST-vars` job, he agrees it could be updated to the oldest in-scope Ubuntu LTS, though he laments the lack of a stable tag to avoid manual updates.

The tone remains constructive and low-stakes: Steinhardt is offering a potential path forward rather than demanding immediate action. The discussion now has a concrete proposal (Alpine Linux) for the 32-bit job, which could resurface if the current `linux32` job starts failing or if someone volunteers to implement the change. The `linux-TEST-vars` job remains a minor maintenance task with no urgency. No new patches are introduced; this is still an informational exchange about future CI maintenance.