This is a small but sensible documentation fix. The patch updates the advice message that appears when Git suggests configuring the default branch name. Currently, the message tells users to run `git config set advice.defaultBranchName false`, but this only sets the configuration for the current repository—it won’t affect new repositories. The patch changes the advice to recommend `git config --global` instead, which is the correct way to set a default for all repositories.

The change is minimal: it modifies the `turn_off_instructions` string in `advice.c` to conditionally include `--global` when the advice key is `defaultBranchName`. The logic is straightforward and avoids overcomplicating the message for other advice keys, which still use `set`.

The commit message is clear and explains the problem well. This isn’t a bugfix in the strictest sense—nothing is broken—but it prevents user confusion by giving accurate instructions. The patch is from a first-time contributor, but the implementation is clean and focused. No tests are included, but none are needed for a string change like this.

If you care about UX polish or have ever been frustrated by Git’s advice messages, this is worth a quick look. It’s unlikely to be controversial.