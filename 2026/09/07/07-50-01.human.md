This is a brief follow-up in the ODB refactoring series, specifically addressing Justin Tobler’s review of patch 5/13. The email is a short exchange between Patrick Steinhardt and Justin about the clarity of the commit message explaining why the last call to `odb_add_submodule_source_by_path()` in `submodule-config.c` is being removed.

Justin confirms he now understands the historical context: the 2019 workaround (commit `d9b8b8f896`) was needed because `config_with_options()` could only read blobs from `the_repository`, so submodule object databases had to be registered as in-memory sources to make `.gitmodules` blobs accessible. He suggests the commit message could be clearer but does not object to its substance. Patrick offers to rewrite it if needed but is inclined to leave it as-is for now.

The exchange is purely about documentation clarity, not technical correctness. Justin’s earlier review already signaled approval of the patch itself ("Nice to have one less workaround"), and this reply does not raise new concerns or objections. The patch remains uncontroversial and part of a well-reviewed series that continues the ODB abstraction and `the_repository` removal efforts. No action is required beyond Patrick’s optional commit message tweak.