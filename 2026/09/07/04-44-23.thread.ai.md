Thread identity
Subject: stash.index=true + --autostash during fast-forward merge leaves redundant stash entry and MERGE_AUTOSTASH ref
Series version: none (standalone bug report)
Originating author: Eli Barzilay
Thread type: bug report

Core problem or goal
A bug in Git 2.55.0 (and current master) causes `git merge --ff-only --autostash` (and by extension `git pull` with `rebase.autoStash` and `pull.rebase`) to leave behind a redundant stash entry and fail to clean up the `MERGE_AUTOSTASH` ref when `stash.index=true` is set and a staged change exists at merge time. The operation succeeds but emits an error message, and the stash list contains an extra entry.

Current status
Under review. The bug is confirmed, reproducible, and well-analyzed. No patch has been posted yet.

Key technical details
- Subsystems: stash, merge, ref management
- Files touched by the bug: `builtin/merge.c`, `sequencer.c`, `builtin/stash.c`, `branch.c`
- Symbols involved: `MERGE_AUTOSTASH` ref, `remove_merge_branch_state()`, `reset_head()`, `stash_apply_index()`
- Old behavior: `MERGE_AUTOSTASH` ref is deleted prematurely during `stash apply --index`, causing a duplicate stash entry and a later "ref not found" error
- New behavior: (not yet implemented) `MERGE_AUTOSTASH` ref must be preserved until the autostash apply completes
- Test coverage: reproduction script provided; no formal test in the suite yet

Open questions and loose ends
- Which of the two suggested fixes (shield `MERGE_AUTOSTASH` during apply, or avoid branch-state cleanup in `stash`'s `reset_head()`) will be chosen
- Whether the fix will be backported to the maintenance track
- No Reviewed-by or Acked-by trailers yet

Participant positions
- Eli Barzilay (author): reported the bug, provided reproduction script, traced the code path, and suggested two plausible fixes. Position is substantive: the analysis engages with the ref-management layering violation and explains why the rebase backend is unaffected.

Related work
- None mentioned in the thread so far. The bug is self-contained.