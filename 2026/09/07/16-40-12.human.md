This is the latest review exchange in the ongoing effort to defer auto maintenance until sequencer operations (`git rebase`, `git cherry-pick`, `git revert`) complete. The series is now in its third iteration and is technically complete, addressing all prior feedback from Phillip Wood, Patrick Steinhardt, and Junio C Hamano.

In this email, Phillip Wood and Thomas Bachem finalize the design choice to move the `run_auto_maintenance()` call out of the sequencer and into the built-in commands (`builtin/rebase.c` and `builtin/revert.c`). This aligns with the apply backend’s existing behavior and consolidates maintenance into a single exit path per command, simplifying the rule to "the command runs maintenance once when done." Phillip agrees this approach avoids scattering maintenance calls across multiple code paths and accommodates the different execution flows for single and multiple picks.

The discussion also confirms test improvements: Thomas will extend the existing test (rather than duplicating it) to assert that auto maintenance does *not* run during intermediate steps (e.g., before `git rebase --continue`), and will switch from `grep` to `test_grep` for better failure output. These changes address Phillip’s earlier suggestions about test hygiene and clarity.

No open technical objections remain. The only unresolved items are minor polish (commit message phrasing, test consolidation) and the design shift to delegate maintenance to the built-in commands, which now has Phillip’s approval. The series looks well-crafted, thoroughly reviewed, and ready for integration once the final tweaks are incorporated.