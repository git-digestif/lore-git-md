This is the first patch in Beat Bolli's three-part series targeting OpenSSL compatibility and correctness in `git imap-send`. It prepares the codebase for OpenSSL 4.1 by addressing a function rename: `ASN1_STRING_length()` is being replaced with `ASN1_STRING_get_length()` in the upcoming release. The patch adds a preprocessor macro to map the new name to the old one when compiling against pre-4.1 versions, ensuring the code continues to work across both old and new OpenSSL releases.

The change is minimal and surgical—just six lines added to `imap-send.c`, with no behavior change or new symbols introduced. The commit message is clear and forward-looking, explicitly calling out the OpenSSL version boundary (0x40100000L) and the motivation (avoiding breakage when 4.1 ships). This is a textbook example of proactive compatibility work: small, self-contained, and addressing a real future problem rather than a hypothetical one.

For the AI summarizer brief, note the key technical detail: the macro is defined only for `OPENSSL_VERSION_NUMBER < 0x40100000L`, so it will not interfere with 4.1 or later. The patch touches only the host-matching logic in the TLS certificate verification path, leaving the rest of `imap-send` untouched. No new tests are added, but the existing test suite should continue to pass since this is purely a compile-time compatibility shim.