This is the latest review exchange in the v3 series that defers auto maintenance until sequencer operations (`git rebase`, `git cherry-pick`, `git revert`) complete. The series is technically complete and addresses all prior feedback; this email resolves the final design question about where to place the `run_auto_maintenance()` call.

Phillip Wood approves the author’s proposal to move the maintenance call out of the sequencer (`sequencer.c`) and into the built-in commands (`builtin/rebase.c` and `builtin/revert.c`). This consolidates maintenance into a single exit path per command, aligning with the apply backend’s design and Patrick Steinhardt’s preference for a unified exit point. The trade-off is moving logic out of the sequencer, but it simplifies the rule to “the command runs maintenance once when done.” Phillip also agrees with the author’s plan to extend the existing test (rather than duplicating it) to assert that auto maintenance does *not* run during intermediate steps, and to switch from `grep` to `test_grep` for better failure output.

No open technical objections remain. The only remaining work is polishing the commit message and tests, which the author has already committed to addressing in the upcoming v3. The series is now effectively ready for integration, pending the final patch submission.