This is a **bugfix** addressing a memory leak in `git history reword --dry-run`, introduced by the merge of Patrick Steinhardt’s `ps/receive-pack-shallow-optim` branch. The discussion here clarifies why the fix—previously proposed by Kaartic Sivaraam in June 2026—was not merged despite being technically correct.

Kaartic acknowledges the patch is sound but explains the delay: Jeff King (Peff) pointed out the commit message was misleading, noting the leak only occurs when the commit buffer isn’t cached in the commit slab. Kaartic intended to identify the exact conditions triggering uncached commits but got sidetracked. He now plans to revisit and resend the patch soon.

The technical details remain unchanged from Patrick’s earlier summary: the leak stems from a missing `repo_unuse_commit_buffer()` call in `commit_tree_ext()` (`builtin/history.c`), and the fix is a single-line addition. The unresolved question is *why* some commits bypass the slab cache—likely tied to repository configuration (e.g., `i18n.commitencoding`) or packfile state. This dependency explains why the leak doesn’t reproduce universally.

For follow-up, watch for Kaartic’s reroll, which should include a corrected commit message and possibly a test case. The patch itself is uncontroversial, but the thread highlights the importance of precise commit messages in Git’s review process. No regressions are expected, but broader testing (e.g., with varied `i18n.commitencoding` settings) could help confirm the fix’s robustness. The leak remains unmerged in `master`, `next`, and `seen`.