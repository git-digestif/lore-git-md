This thread is a minor but necessary documentation cleanup. Brigham Campbell posted a one-line patch to fix conjoined bullet items in `Documentation/config/maintenance.adoc` by adding a `++` line to force proper list separation. The patch is purely mechanical, touching only AsciiDoc formatting, and has no functional impact.

Patrick Steinhardt reviewed the patch and found the fix itself correct—calling it a "simple and obvious fix"—but suggested a clearer commit message that explicitly references the rendered `git-config(1)` man page rather than just the source file. No code changes were requested, and the patch remains uncontroversial. Given its trivial nature and the lack of objections, it’s likely to be merged once the commit message is adjusted to Patrick’s suggestion. No other participants have weighed in yet.