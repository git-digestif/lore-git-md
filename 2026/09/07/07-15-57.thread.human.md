This thread is a minor but useful documentation cleanup. Brigham Campbell noticed two bullet items in `Documentation/config/maintenance.adoc` that were incorrectly run together and fixed the formatting by inserting the AsciiDoc `++` separator to force them into separate list items. The patch is a one-line change, purely mechanical, and doesn’t affect any code behavior—just the rendered man page appearance.

The fix aligns with the ongoing effort to standardize AsciiDoc formatting across Git’s documentation, though it wasn’t explicitly tied to Jean-Noël Avila’s larger synopsis-style conversion work. Since it’s a standalone patch with no functional impact, it’s likely to be uncontroversial and could be merged quickly if no one objects. No reviews or follow-ups have appeared yet.