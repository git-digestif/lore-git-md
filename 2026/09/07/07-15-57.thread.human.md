This thread is about a small documentation fix to `Documentation/config/maintenance.adoc`, where two bullet items were incorrectly conjoined in the AsciiDoc source. Brigham Campbell’s patch adds a `++` line to force proper list separation, restoring the correct structure in the rendered `git-config(1)` man page. The change is uncontroversial and touches only documentation formatting.

Patrick Steinhardt reviewed the patch early on, calling it a “simple and obvious fix” but suggested tweaking the commit-message subject to reference `git-config(1)` rather than the source file. The patch has since received additional scrutiny from Kristoffer Haugsbakk, who raised a compatibility question: the issue appears to be processor-specific. Asciidoctor (used by GitHub and git-scm.com) already renders the list correctly without the patch, while classic AsciiDoc (the Python implementation) requires it. This discrepancy doesn’t block the patch but adds context about its necessity—whether it’s fixing a general formatting error or targeting a specific processor.

The patch remains under review with no objections or requested changes. The original author may need to clarify the intended scope of the fix, but the change itself is still likely to be merged once the minor subject-line suggestion is addressed.