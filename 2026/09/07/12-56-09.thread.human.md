Here’s the human-mode narrative summary for a developer catching up on the thread:

This thread is a small but important fix to the advice subsystem, correcting the on-screen instructions for disabling the `defaultBranchName` advice message. The goal is to recommend `git config --global` (or possibly `git config set --global`) instead of the current `git config set`, ensuring the setting applies globally rather than just to the current repository. Vsevolod Myalitsin is the author, and the discussion has focused on balancing simplicity with long-term maintainability.

The latest version of the patch adopts Junio C Hamano’s architectural feedback, replacing the initial `global_hint` boolean with a more general `scope_hint` field in the `advice_setting` struct. This allows future advice keys to suggest different configuration scopes (like `--system`) without further refactoring. The `vadvise()` helper now receives the entire struct, narrowing its interface and making the code easier to extend. The core behavior remains unchanged: the displayed command will recommend a global setting, but the exact syntax (`--global` vs `set --global`) is still unresolved, pending Ben Knoble’s preference.

The thread is now in the implementation phase, with no technical objections to the goal. Vsevolod plans to send a v3 incorporating Junio’s suggestions, which should address the maintainability concerns. The patch touches only `advice.c` and is low-risk, so no new test coverage is planned. The next step is the v3 submission, after which the remaining syntax debate can be settled.