This thread is a simple maintenance fix to a Perl script used for development checks. Tuomas Ahola submitted a one-line patch to `Documentation/lint-gitlink.perl`, replacing the Perl 5.34.0+ regex shorthand `{,8}` with the equivalent `{0,8}` to ensure compatibility with Git’s minimum Perl version (5.26.0). The issue was discovered when Tuomas ran the linting script on a system with an older Perl version, which rejected the shorthand syntax.

Junio C Hamano, the maintainer, reviewed and accepted the patch, confirming he will queue it for merging. The fix is uncontroversial, requires no tests, and touches only a single line in a non-production script. The thread is now fully resolved, with no open questions or further revisions expected. The patch will land in an upcoming integration cycle.