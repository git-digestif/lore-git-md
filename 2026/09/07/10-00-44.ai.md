This is a reply in the ongoing ref-storage terminology unification series. The email exchanges a brief clarification about the `argh` placeholder in the `--ref-storage` option definition.

Patrick acknowledges Karthik’s observation that the help text placeholder (`argh`) still uses `"format"` instead of `"ref-storage"`, but defends the choice: with the series now moving to `--ref-storage-format=`, the placeholder `"format"` actually becomes more appropriate (`--ref-storage-format=<format>`). He also notes the pre-existing inconsistency in error messages, where Git would report `"unknown ref storage format"` despite the option being named `--ref-format=`.

The discussion is purely about presentation and does not affect the patch’s technical substance. The resolution—keeping `"format"` as the placeholder—is consistent with the series’ broader shift to `--ref-storage-format=`, which will be implemented in v2. No new issues are raised, and the thread remains focused on the series’ core goal of unifying terminology.