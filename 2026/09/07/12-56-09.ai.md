This is a small but user-facing documentation fix in the advice subsystem. The patch updates the on-screen instructions for disabling the `defaultBranchName` advice message: instead of suggesting `git config set advice.defaultBranchName false` (which only affects the current repository), it now recommends `git config --global advice.defaultBranchName false` (which sets the default for all future repositories). For all other advice keys, the original `git config set` wording is preserved.

The change is contained to `advice.c` and modifies only the string formatting logic in the `vadvise()` helper. The conditional logic checks whether the advice key is `defaultBranchName` and substitutes `--global` for `set` in that case. The rest of the advice machinery is untouched.

The commit message is clear and concise, explaining both the problem (the current suggestion is "useless" for the intended purpose) and the solution. No test or documentation updates are included, but the change is self-explanatory and unlikely to break anything.

This looks like a well-motivated, low-risk improvement that addresses a real usability papercut. The patch is small, focused, and ready for review.