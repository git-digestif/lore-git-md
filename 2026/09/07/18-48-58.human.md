This is a small but sensible portability fix for a development helper script. The patch updates `Documentation/lint-gitlink.perl` to replace the Perl 5.34+ regex shorthand `{,8}` with the equivalent but more widely supported `{0,8}`. Git's minimum Perl version is 5.26, so the original syntax would fail on older installations even though the script itself is not part of the core runtime.

The change is trivial—one line swapped for another—and the commit message clearly explains the motivation: consistency with Git's stated Perl version requirement. No tests are needed because this is a developer tool, not user-facing code. The patch is well-targeted and unlikely to be controversial. If you care about Perl compatibility or development tooling, it's worth a quick look; otherwise, it can safely be skipped.