This is a CI/build system patch. Patrick Steinhardt replies to Jeff King’s patch that updates Git’s CI workflows to use Debian 12 instead of Debian 11, now that Debian 11 is out of LTS support. The patch itself is straightforward and uncontroversial—it updates the `jobname` and `image` fields in `.github/workflows/main.yml` and `.gitlab-ci.yml` to point to `debian:12` and adjusts the comments to reflect the new support window (until 2028-06-30).

Steinhardt’s reply is brief but raises a practical suggestion: instead of hardcoding `debian:12`, the CI jobs could use the `oldstable` tag, which currently points to Debian 12 and would automatically track the next oldstable release when Debian 13 becomes stable. This would reduce future churn by avoiding manual updates each time a new Debian release reaches oldstable status. The suggestion is minor and doesn’t block the patch—it’s more of a forward-looking optimization—but it’s a sensible one that aligns with the project’s preference for reducing maintenance overhead.

The thread remains focused on the immediate goal of keeping CI on a supported distribution, and Steinhardt’s feedback doesn’t introduce new controversy or technical risk. The patch is already queued in `seen` with Junio’s intent to merge, and this reply doesn’t change that trajectory. If anything, it might prompt a small follow-up tweak before the patch graduates to `next`. For a busy reader, this is a low-stakes but well-motivated maintenance update with a minor refinement still in play.