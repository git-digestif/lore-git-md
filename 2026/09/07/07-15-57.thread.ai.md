Thread identity
Subject: [PATCH v2] config: fix conjoined bullet items in git-config(1)
Series version: v2 (standalone patch)
Originating author: Brigham Campbell (first-time contributor)
Thread type: documentation cleanup

Core problem or goal
Fix incorrect AsciiDoc formatting in `Documentation/config/maintenance.adoc` where two bullet items were incorrectly conjoined. The change adds a `++` line between the items to force a new list item, restoring proper list structure in the rendered `git-config(1)` man page. The patch targets classic AsciiDoc (Python implementation), as Asciidoctor (Ruby implementation) already renders the list correctly without the change.

Current status
Under review; v2 posted in response to Junio’s "What's cooking" request for a reroll. The patch is uncontroversial, touches only documentation, and is a strong candidate for integration into `next`. The author has incorporated Patrick Steinhardt’s subject-line suggestion (explicitly referencing `git-config(1)`) and added a link to the v1 discussion. No code changes from v1. Patrick Steinhardt has explicitly approved the patch ("This version looks good to me"), providing an Acked-by. Jeff King (Peff) has now contributed a surface-level review clarifying processor-specific behavior (classic AsciiDoc vs. Asciidoctor) and offering workflow guidance for documentation contributors, but did not object to the patch or request changes. No merge event to `next` or `master` has been reported; the patch remains under review pending Junio’s next integration cycle.

Key technical details
- File: `Documentation/config/maintenance.adoc`
- Line: 43
- Change: one-line addition (`++`) to separate bullet items
- Subsystem: documentation (AsciiDoc formatting)
- Rendered output: `git-config(1)` man page
- Processor dependency: classic AsciiDoc requires the fix; Asciidoctor does not
- Historical context: the conjoined bullet was introduced in commit d9bccf2e ("builtin/maintenance: introduce 'geometric' strategy", 2025-10-24)
- Workflow tooling: `./doc-diff` with `--asciidoc` and `--asciidoctor` flags can be used to compare rendering outputs; direct comparisons between processors (`--from-asciidoctor --to-asciidoc`) generate significant noise due to stylistic differences
- No new symbols, CLI options, or on-disk format changes
- No test coverage (expected for documentation-only patches)

Open questions and loose ends
- Processor compatibility: Kristoffer Haugsbakk’s question about whether the patch should target classic AsciiDoc, Asciidoctor, or both remains unresolved. The author’s commit message clarifies that the patch is scoped to classic AsciiDoc, and both Patrick Steinhardt and Jeff King have now confirmed the processor-specific behavior (classic AsciiDoc requires the fix; Asciidoctor does not). No explicit confirmation or objection to this scope has followed, and the clarification is not blocking integration.

Participant positions
- Junio C Hamano: maintainer; requested a reroll in "What’s cooking" report, prompting v2. Position implies readiness for integration once the reroll is posted; no new feedback in this update.
- Patrick Steinhardt: substantive review; confirmed the patch fixes the rendering issue in the tested context, called it a “simple and obvious fix,” and explicitly approved the v2 patch ("This version looks good to me"). Position unchanged; no blocking concerns.
- Jeff King (Peff): surface-level review; clarified the processor-specific behavior (classic AsciiDoc vs. Asciidoctor) and provided workflow guidance for documentation contributors using `./doc-diff`. Did not object to the patch or suggest changes. Position unchanged.
- Kristoffer Haugsbakk: surface-level review; raised a compatibility question about processor-specific behavior but did not object to the patch. Noted that git-scm.com (Asciidoctor) already renders the list correctly, implying the patch may not be universally necessary. Position unchanged.
- Brigham Campbell: author; incorporated Junio’s reroll request and Patrick’s subject-line suggestion in v2. Added a link to the v1 discussion and proactively requested an Acked-by from Patrick or Kristoffer, signaling readiness for integration. Position unchanged.

Related work
Part of the ongoing documentation synopsis-style conversion effort led by Jean-Noël Avila, though not explicitly referenced in the patch. The historical context ties to the geometric maintenance strategy introduced in d9bccf2e. The workflow guidance provided by Jeff King (`./doc-diff` with `--asciidoc`/`--asciidoctor` flags) is relevant to future documentation patches.