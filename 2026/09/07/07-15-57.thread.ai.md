Thread identity
Subject: [PATCH] config/maintenance.txt: fix conjoined bullet items
Series version: standalone patch
Originating author: Brigham Campbell
Thread type: documentation cleanup

Core problem or goal
Fix incorrect AsciiDoc formatting in `Documentation/config/maintenance.adoc` where two bullet items were incorrectly conjoined. The change adds a `++` line between the items to force a new list item, restoring proper list structure in the rendered `git-config(1)` man page.

Current status
Under review; no prior versions. The patch is uncontroversial and touches only documentation, so it is likely to be merged if no further objections are raised. Patrick Steinhardt has suggested a minor improvement to the commit-message subject line but has not requested code changes.

Key technical details
- File: `Documentation/config/maintenance.adoc`
- Line: 43
- Change: one-line addition (`++`) to separate bullet items
- Subsystem: documentation (AsciiDoc formatting)
- Rendered output: `git-config(1)` man page
- No new symbols, CLI options, or on-disk format changes
- No test coverage (expected for documentation-only patches)

Open questions and loose ends
- Commit-message subject line wording: Patrick suggests rewording to explicitly reference `git-config(1)` rather than the source file name.

Participant positions
- Patrick Steinhardt: surface-level review; confirms the patch fixes the rendering issue and calls it a “simple and obvious fix.” Suggests a clearer subject line but does not block the patch.

Related work
Part of the ongoing documentation synopsis-style conversion effort led by Jean-Noël Avila, though not explicitly referenced in the patch.