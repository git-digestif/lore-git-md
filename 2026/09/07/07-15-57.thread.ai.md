Thread identity
Subject: [PATCH] config/maintenance.txt: fix conjoined bullet items
Series version: standalone patch
Originating author: Brigham Campbell (first-time contributor)
Thread type: documentation cleanup

Core problem or goal
Fix incorrect AsciiDoc formatting in `Documentation/config/maintenance.adoc` where two bullet items were incorrectly conjoined. The change adds a `++` line between the items to force a new list item, restoring proper list structure in the rendered `git-config(1)` man page. The patch targets classic AsciiDoc (Python implementation), as Asciidoctor (Ruby implementation) already renders the list correctly without the change.

Current status
Under review; no prior versions. The patch is uncontroversial and touches only documentation. Kristoffer Haugsbakk’s compatibility question about processor-specific behavior remains unresolved but does not block acceptance. Patrick Steinhardt previously suggested a minor improvement to the commit-message subject line (explicitly referencing `git-config(1)` rather than the source file) but has not requested code changes. The author has declined to reroll for this trivial change, inviting the committer to make editorial adjustments if needed.

Key technical details
- File: `Documentation/config/maintenance.adoc`
- Line: 43
- Change: one-line addition (`++`) to separate bullet items
- Subsystem: documentation (AsciiDoc formatting)
- Rendered output: `git-config(1)` man page
- Processor dependency: classic AsciiDoc requires the fix; Asciidoctor does not
- Historical context: the conjoined bullet was introduced in commit d9bccf2e ("builtin/maintenance: introduce 'geometric' strategy", 2025-10-24)
- No new symbols, CLI options, or on-disk format changes
- No test coverage (expected for documentation-only patches)

Open questions and loose ends
- Commit-message subject line wording: Patrick suggests rewording to explicitly reference `git-config(1)` rather than the source file. The author has declined to reroll, leaving this as a committer discretion item.
- Processor compatibility: Kristoffer Haugsbakk raised a question about whether the patch should target classic AsciiDoc, Asciidoctor, or both. The author’s response implies the patch is scoped to classic AsciiDoc, but this has not been explicitly confirmed. No objections or further discussion have followed.

Participant positions
- Patrick Steinhardt: surface-level review; confirms the patch fixes the rendering issue in the context it was tested and calls it a “simple and obvious fix.” Suggests a clearer subject line but does not block the patch. Position unchanged.
- Kristoffer Haugsbakk: surface-level review; raises a compatibility question about processor-specific behavior (classic AsciiDoc vs. Asciidoctor) but does not object to the patch. Notes that git-scm.com (Asciidoctor) already renders the list correctly, implying the patch may not be universally necessary. Position unchanged.
- Brigham Campbell: author; acknowledges Patrick’s subject-line suggestion but declines to reroll for such a trivial change. Invites the committer to make editorial adjustments if needed. Position unchanged.

Related work
Part of the ongoing documentation synopsis-style conversion effort led by Jean-Noël Avila, though not explicitly referenced in the patch. The historical context ties to the geometric maintenance strategy introduced in d9bccf2e.