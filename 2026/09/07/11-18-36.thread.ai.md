Existing AI thread summary (absent, this is the thread root):

Thread identity
Subject: [PATCH v2 2/11] clone: rename `--ref-format` to `--ref-storage-format`
Series version: v2
Originating author: Patrick Steinhardt
Thread type: patch series (ref-storage terminology cleanup)

Core problem or goal
Standardize and unify Git’s ref storage terminology by renaming the `--ref-format=` option in `git clone` to `--ref-storage-format=`, matching a prior change to `git init`. The old option name remains as a hidden alias for backward compatibility.

Current status
Under review; this is the second patch in an 11-patch v2 series.

Key technical details
- Files touched: `builtin/clone.c`, `Documentation/git-clone.txt`, seven test scripts (`t/t5601*`, `t/t5606*`, `t/t5702*`)
- New symbol: `--ref-storage-format=` (CLI option)
- Old symbol: `--ref-format=` (now a hidden alias)
- Subsystem: ref storage, CLI interface
- On-disk format: unchanged
- Test coverage: option parsing, alias functionality, error messages

Open questions and loose ends
None yet raised; the patch is mechanical and does not alter behavior.

Participant positions
None yet recorded.

Related work
Part of Patrick Steinhardt’s ongoing ref-storage terminology cleanup; depends on a prior change to `git init` (not shown in this patch).