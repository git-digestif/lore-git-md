This thread is a critical bug report about `git worktree` on Windows. Paul DE TEMMERMAN discovered that running `git worktree add "" HEAD` (passing an empty string as the worktree path) deletes the entire workspace, including the `.git` directory—a catastrophic data loss scenario. The bug is specific to Windows and affects Git for Windows 2.55.0.windows.5.

The report is clear and actionable: it provides exact reproduction steps (initialize a repo, commit a file, then run the problematic command) and highlights the severity of the issue. No prior discussion or patch series is associated with this report yet. The expected next steps would be confirmation from other Windows users, analysis of the worktree path validation logic (likely in `builtin/worktree.c`), and a fix to ensure the command fails gracefully when given an empty path. Given the destructive nature of the bug, this is likely to receive urgent attention from maintainers and contributors familiar with the worktree subsystem or Windows compatibility.