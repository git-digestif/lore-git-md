This is a documentation patch. It fixes a formatting issue in the AsciiDoc source for the `maintenance` configuration documentation, where two bullet items were incorrectly joined without a separator. The change adds a `++` line between the two bullet points to ensure they render as distinct items in the output.

The patch is minimal, touching only one line in `Documentation/config/maintenance.adoc`. The commit message is clear and specific about the problem being addressed. This is a straightforward fix that aligns with the ongoing effort to maintain consistent AsciiDoc formatting across Git's documentation.

No controversy or deeper technical discussion is expected here—this is purely a presentation-layer correction. The patch is well-targeted and unlikely to draw significant review attention, but it’s the kind of small improvement that helps keep the documentation polished.