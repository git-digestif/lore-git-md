**Summarizer brief mode**

This is patch 7/11 in Patrick Steinhardt’s v2 series standardizing Git’s terminology for "ref storage format" and enabling URI support. The patch extracts URI-parsing logic from `setup.c` into a new reusable function `ref_storage_format_by_uri()` in `refs.c`/`refs.h`, eliminating 36 lines of duplicated code. The function parses URIs in the format `<format>[://<payload>]` (e.g., `files://path/to/repo`) and returns the storage format (`enum ref_storage_format`) while optionally populating a `payload` output parameter. The patch updates three call sites in `setup.c` to use the new function, improving error messages to show full URIs (e.g., "unknown ref storage format: 'files://invalid'") instead of truncated names. No behavior changes are introduced; this is a pure refactoring to prepare for URI payload support in patch 11. Files touched: `refs.c`, `refs.h`, and `setup.c`. Key symbols: `ref_storage_format_by_uri()`, `GIT_REFERENCE_BACKEND_ENVIRONMENT`, `discovery.format.ref_storage_payload`. The patch retains backward compatibility and aligns with the series’ goal of centralizing URI handling for future object storage extensions.