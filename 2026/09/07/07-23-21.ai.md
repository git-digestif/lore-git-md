This is a **follow-up discussion** in the ongoing ODB alternates refactoring series, focusing on the architectural implications of the `init_db()` interface. Patrick Steinhardt responds to Justin Tobler’s review of patch 1/8, engaging with the suggestion to eliminate "skip" flags in favor of explicit repository setup.

The exchange clarifies two key points. First, Patrick acknowledges the awkwardness of `init_db()`’s growing flag-based interface and proposes a more radical refactoring: lifting repository initialization messages out of `init_db()` entirely (since `git clone` always passes `QUIET` and never allows preexisting repositories) and renaming the function to `create_repository()` to better reflect its narrowed scope. This would move command-specific logic (e.g., printing messages, handling preexisting repos) to the commands themselves, simplifying the interface. Second, Patrick explains that deferring `setup_reference()`—which configures alternates, not refs—is necessary because alternates can only be written after the ODB exists. This resolves Justin’s question about the deferred reference setup, though the commit message will be updated to clarify the alternates-specific purpose.

The discussion is forward-looking and collaborative, with Patrick embracing Justin’s architectural feedback while noting practical constraints (e.g., preserving `git init`’s initialization message). No code changes are proposed in this reply, but the conversation may influence future iterations of the series or broader refactoring of repository initialization. The tone remains constructive, and the focus is on interface clarity rather than immediate implementation. No other reviewers have weighed in on this specific point, and the series remains under review pending final feedback on later patches.