Thread identity: standalone patch fixing a Perl regex compatibility issue in `Documentation/lint-gitlink.perl`; author Tuomas Ahola; thread type maintenance fix.

Core problem or goal: replace the `{,8}` Perl regex quantifier shorthand (introduced in Perl 5.34.0) with the equivalent `{0,8}` to maintain compatibility with Git's minimum Perl version (5.26.0). The script is a development helper, not production code.

Current status: accepted for merging; Junio C Hamano has explicitly stated he will queue it. The patch is uncontroversial, touches a single line, and requires no tests. No prior versions exist.

Key technical details: file `Documentation/lint-gitlink.perl`, line-level change from `{,8}` to `{0,8}`. No new symbols, no on-disk format changes, no CLI or config key modifications. The issue was discovered by running `make -C Documentation lint-docs` on a system with Perl older than 5.34.0, triggering a syntax error.

Open questions and loose ends: none; the patch is complete, self-contained, and fully resolved. Junio’s merge is the only remaining action.

Participant positions: Junio C Hamano (maintainer) reviewed, accepted, and confirmed intent to queue the patch. Tuomas Ahola authored the patch and provided context on the discovery method.

Related work: none.