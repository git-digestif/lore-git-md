Thread identity: standalone patch fixing a Perl regex compatibility issue in `Documentation/lint-gitlink.perl`; author Tuomas Ahola; thread type maintenance fix.

Core problem or goal: replace the `{,8}` Perl regex quantifier shorthand (introduced in Perl 5.34.0) with the equivalent `{0,8}` to maintain compatibility with Git's minimum Perl version (5.26.0). The script is a development helper, not production code.

Current status: accepted for merging; Junio C Hamano will queue it. No prior versions; patch is uncontroversial, touches a single line, and requires no tests.

Key technical details: file `Documentation/lint-gitlink.perl`, line-level change from `{,8}` to `{0,8}`. No new symbols, no on-disk format changes, no CLI or config key modifications.

Open questions and loose ends: none; the patch is complete and self-contained. Junio’s curiosity about how the issue was discovered does not affect the fix’s validity or merge status.

Participant positions: Junio C Hamano (maintainer) reviewed and accepted the patch, stating he will queue it. Tuomas Ahola authored the patch.

Related work: none.