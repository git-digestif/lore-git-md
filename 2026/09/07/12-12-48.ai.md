This is a **bugfix** for a memory leak in `git history reword --dry-run`, now confirmed as ready to proceed after a minor documentation clarification. Patrick Steinhardt acknowledges Kaartic Sivaraam’s earlier fix (identical to his own) and the reason it stalled: the commit message inaccurately claimed the leak was universal, when in fact it only occurs when the commit buffer isn’t cached in the commit slab. Kaartic has committed to resending the patch with a corrected message and possibly a test case.

The fix itself remains a single-line addition in `builtin/history.c` (`commit_tree_ext()`), adding a `repo_unuse_commit_buffer()` call to balance the `repo_get_commit_buffer()` allocation. The leak is tied to repository conditions—likely `i18n.commitencoding` settings or packfile state—that prevent commit buffers from being cached in the slab. This explains why it doesn’t reproduce universally (e.g., Patrick couldn’t trigger it on his machine).

No technical objections remain; the patch is uncontroversial and narrowly scoped to the immediate leak. The thread now shifts to logistics: Kaartic’s reroll will include the corrected commit message and may add a test case. The leak is still present in `master`, `next`, and `seen` as of this email. Watch for the reroll, which should resolve the last procedural hurdle.