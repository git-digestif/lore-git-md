This thread is about a three-patch series from Beat Bolli that fixes correctness and forward-compatibility issues in `git imap-send`'s TLS certificate verification logic. The series addresses deprecated OpenSSL API usage, a latent memory-safety bug in ASN1_STRING handling, and stricter RFC 6125 compliance for certificate validation. It’s currently under review, with no prior versions or maintainer decisions.

The key technical focus is on `imap-send.c`, where the patches modernize OpenSSL usage, eliminate undefined behavior, and enforce stricter certificate validation rules. However, two significant issues have surfaced: Junio C Hamano identified a memory-safety bug in the second patch that must be fixed before the series can proceed, and brian m. carlson raised concerns about the third patch’s incomplete RFC 6125 compliance (missing IP SAN handling) and questioned why Git continues to implement certificate verification manually instead of using OpenSSL’s built-in functions.

The series is still in early review, with no substantive approvals yet. The memory-safety bug in patch 2 is a blocker, and the architectural questions around patch 3 may require broader discussion. Junio’s latest reply was a minor style nit about comment formatting, indicating the technical review is still ongoing. The thread’s next steps will likely involve a v2 addressing the memory-safety issue and potentially revisiting the RFC 6125 compliance approach.