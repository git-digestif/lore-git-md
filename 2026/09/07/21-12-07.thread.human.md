This thread is a three-patch series from Beat Bolli that fixes OpenSSL compatibility and correctness issues in `git imap-send`'s TLS certificate verification. The series is under initial review and targets three specific problems: replacing a deprecated OpenSSL 4.1 function, fixing a latent bug in ASN1_STRING handling, and aligning certificate validation with RFC 6125.

The first patch, just posted, is a forward-compatibility shim for OpenSSL 4.1. It replaces `ASN1_STRING_length()` with `ASN1_STRING_get_length()` by adding a preprocessor macro that maps the new name to the old one when compiling against pre-4.1 versions. The change is minimal—just six lines in `imap-send.c`—and ensures the code works across both old and new OpenSSL releases without altering behavior. The patch is proactive and self-contained, addressing a real future compatibility issue rather than a hypothetical one.

No reviews or maintainer decisions have been recorded yet, and the series remains in its initial version. The patches touch only `imap-send.c` and do not introduce new tests, as the existing test suite does not cover the edge cases these fixes address. The focus is on correctness and forward-compatibility, with no changes to on-disk formats or user-visible behavior.