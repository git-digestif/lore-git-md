This is a brief follow-up in the ongoing memory-leak fix for `git history reword --dry-run`. Patrick Steinhardt acknowledges Kaartic Sivaraam’s explanation that the identical fix he just proposed was already discussed in June 2026 but stalled due to a misleading commit message. The message incorrectly implied the leak was universal, when in fact it only occurs when the commit buffer isn’t cached in the commit slab—likely tied to repository settings like `i18n.commitencoding` or packfile state.

No new technical content is introduced here; the patch itself remains a single-line addition of `repo_unuse_commit_buffer()` in `builtin/history.c`. The thread’s focus is now on Kaartic’s plan to resend the patch with a corrected commit message and possibly a test case. The leak is still present in `master`, `next`, and `seen`, and the fix is uncontroversial. Watch for Kaartic’s reroll to see if the updated message clarifies the leak’s environment-dependent nature.