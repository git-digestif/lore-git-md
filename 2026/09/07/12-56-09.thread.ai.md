Thread identity: patch series (single patch), originating author Vsevolod Myalitsin, thread type user-facing documentation fix in the advice subsystem.

Core problem or goal: correct the on-screen instructions for disabling the `defaultBranchName` advice message to recommend `--global` instead of `set`, ensuring the setting applies globally rather than just to the current repository. All other advice keys retain the original `git config set` wording. The revised design now uses a `global_hint` field in the `advice_setting` struct for cleaner extensibility.

Current status: revised version posted in response to Ben Knoble’s review; still under review. No objections raised; the change is now structurally cleaner and future-proof.

Key technical details:
- File touched: `advice.c`
- Subsystem: advice machinery
- Change: adds a `global_hint` boolean field to the `advice_setting` struct, set to `1` for `defaultBranchName` and `0` for all other keys. The `vadvise()` helper now conditionally substitutes `--global` for `set` when `global_hint` is true.
- Symbols: `advice_setting` struct extended with `global_hint`; no functions renamed or introduced.
- Behavior: old: `git config set advice.defaultBranchName false` (repository-local); new: `git config --global advice.defaultBranchName false` (global default). The displayed command may still adopt `set --global` if the author accepts Ben Knoble’s syntax preference.
- On-disk format: unchanged.
- Test coverage: none added; change remains self-contained and low-risk.

Open questions and loose ends:
- Whether to use `--global` or `set --global` in the displayed command (Ben Knoble’s preference for the latter remains unresolved).
- No further technical objections or requests for changes.

Participant positions:
- Vsevolod Myalitsin: author; proposed the change and revised the design to use a `global_hint` field in response to review feedback.
- Ben Knoble: reviewed the patch substantively; supports the goal and the revised design, but prefers the `set --global` syntax for clarity. No objections to the structural improvement.