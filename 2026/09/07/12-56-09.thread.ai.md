Thread identity: patch series (single patch), originating author Vsevolod Myalitsin, thread type user-facing documentation fix in the advice subsystem.

Core problem or goal: correct the on-screen instructions for disabling the `defaultBranchName` advice message to recommend `--global` instead of `set`, ensuring the setting applies globally rather than just to the current repository. The design now uses a configurable field in the `advice_setting` struct to allow future advice keys to specify their preferred scope, improving extensibility.

Current status: under active review; revised version posted in response to Ben Knoble’s review, now with additional architectural feedback from Junio C Hamano. No objections to the core goal, but open questions about the best way to structure the code for long-term maintainability. The unresolved syntax preference (`--global` vs `set --global`) from Ben Knoble remains pending, though Junio’s comments shift focus to the underlying design.

Key technical details:
- File touched: `advice.c`
- Subsystem: advice machinery
- Change: adds a `global_hint` boolean field to the `advice_setting` struct, set to `1` for `defaultBranchName` and `0` for all other keys. The `vadvise()` helper conditionally substitutes `--global` for `set` when `global_hint` is true.
- Junio’s proposed alternative: replace `global_hint` with a more general `config_scope` field (e.g., `CONFIG_SCOPE_GLOBAL`, `CONFIG_SCOPE_SYSTEM`) to future-proof for other scopes, and pass the entire `advice_setting` struct to `vadvise()` to narrow the interface.
- Symbols: `advice_setting` struct extended with `global_hint` (or potential `config_scope`); no functions renamed or introduced.
- Behavior: old: `git config set advice.defaultBranchName false` (repository-local); new: `git config --global advice.defaultBranchName false` (global default). The displayed command may still adopt `set --global` if the author accepts Ben Knoble’s syntax preference.
- On-disk format: unchanged.
- Test coverage: none added; change remains self-contained and low-risk.

Open questions and loose ends:
- Architectural direction: whether to use a boolean `global_hint` or a more general `config_scope` field, and whether to pass the entire `advice_setting` struct to `vadvise()` for better encapsulation.
- Syntax preference: unresolved debate between `--global` and `set --global` in the displayed command (Ben Knoble’s preference for the latter).
- No technical objections to the core goal or the revised design’s intent.

Participant positions:
- Vsevolod Myalitsin: author; proposed the change and revised the design to use a `global_hint` field in response to review feedback. Has not yet responded to Junio’s architectural suggestions.
- Ben Knoble: reviewed the patch substantively; supports the goal and the revised design, but prefers the `set --global` syntax for clarity. No objections to the structural improvement. Surface-level acknowledgment of reply formatting (bottom-posting) in the latest email; no new technical input on Junio’s points.
- Junio C Hamano: maintainer; raised substantive architectural questions about future-proofing and interface cleanliness. Suggests replacing `global_hint` with a `config_scope` field and passing the entire `advice_setting` struct to `vadvise()` to narrow the interface. No objections to the core goal, but emphasizes the importance of a maintainable design.