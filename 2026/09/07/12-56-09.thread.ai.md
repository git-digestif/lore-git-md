Thread identity: patch series (single patch), originating author Vsevolod Myalitsin, thread type user-facing documentation fix in the advice subsystem.

Core problem or goal: correct the on-screen instructions for disabling the `defaultBranchName` advice message to recommend a globally scoped configuration command (`--global` or `set --global`) instead of a repository-local `set`, ensuring the setting applies globally by default. The design uses a configurable `scope_hint` field in the `advice_setting` struct to allow future advice keys to specify their preferred configuration scope (e.g., `--global`, `--system`), improving extensibility and maintainability.

Current status: architectural direction and backward-compatibility mechanism settled; ready for v3 patch. The author has incorporated Junio C Hamano’s feedback to replace the `global_hint` boolean with a `scope_hint` field (e.g., `CONFIG_SCOPE_GLOBAL`) and to pass the entire `advice_setting` struct to `vadvise()` for better encapsulation. The backward-compatibility requirement for the existing `advise()` function is now resolved: Junio and Jeff King agreed to pass `NULL` to `vadvise()` to indicate the absence of a matching advice config, avoiding the need for an ad-hoc struct. The unresolved syntax preference (`--global` vs `set --global`) from Ben Knoble remains pending but is not a blocker for merging.

Key technical details:
- File touched: `advice.c`
- Subsystem: advice machinery
- Change: replaces the `global_hint` boolean in the `advice_setting` struct with a `scope_hint` field (e.g., `CONFIG_SCOPE_GLOBAL`, `CONFIG_SCOPE_SYSTEM`), set to `CONFIG_SCOPE_GLOBAL` for `defaultBranchName` and left as `CONFIG_SCOPE_UNKNOWN` (default) for other keys. The `vadvise()` helper now receives a pointer to the entire `advice_setting` struct, narrowing its interface and making it unaware of specific advice keys. To maintain backward compatibility with the existing `advise()` function, `NULL` is passed to `vadvise()` when no advice config is associated with the message.
- Symbols: `advice_setting` struct extended with `scope_hint`; no functions renamed or introduced.
- Behavior: old: `git config set advice.defaultBranchName false` (repository-local); new: `git config --global advice.defaultBranchName false` (or `git config set --global ...` if Ben Knoble’s syntax preference is adopted). The displayed command will use the scope hint to recommend the appropriate flag.
- On-disk format: unchanged.
- Test coverage: none added; change remains self-contained and low-risk.
- Broader subsystem implications: Jeff King noted that most `advise()` calls are wrapped in `if (advice_is_enabled(ADVICE_FOO))`, meaning they do not show turn-off instructions (introduced in 2020), while only `advise_if_enabled()` calls do. Junio confirmed this inconsistency is intentional and not a blocker for the current patch, but it could be revisited in the future.

Open questions and loose ends:
- Syntax preference: unresolved debate between `--global` and `set --global` in the displayed command (Ben Knoble’s preference for the latter).
- Subsystem design: the inconsistency of turn-off instructions (only shown for `advise_if_enabled()`) remains an open design question, but it is not a blocker for this patch.

Participant positions:
- Vsevolod Myalitsin: author; proposed the change and revised the design to use a `scope_hint` field and pass the entire `advice_setting` struct to `vadvise()` in response to Junio’s architectural feedback. Plans to send a v3 patch incorporating the agreed-upon `NULL` pointer approach for backward compatibility.
- Ben Knoble: reviewed the patch substantively; supports the goal and the revised design, but prefers the `set --global` syntax for clarity. No objections to the structural improvements or the backward-compatibility resolution.
- Junio C Hamano: maintainer; raised substantive architectural questions about future-proofing, interface cleanliness, and backward compatibility. Suggested replacing `global_hint` with a `scope_hint` field and passing the entire `advice_setting` struct to `vadvise()`. Agreed with Jeff King on the `NULL` pointer approach for backward compatibility. No further objections to the core design.
- Jeff King (Peff): proposed the `NULL` pointer approach for backward compatibility, resolving the last major technical obstacle. Also noted the broader advice subsystem design inconsistency (turn-off instructions only for `advise_if_enabled()`), but agreed it is not a blocker for this patch.
- R4NC (Vsevolod Myalitsin): surface-level reply confirming intent to send v2 (now v3) and acknowledging mailing-list etiquette; no new technical content.