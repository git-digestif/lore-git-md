Thread identity: patch series (single patch), originating author Vsevolod Myalitsin, thread type user-facing documentation fix in the advice subsystem.

Core problem or goal: correct the on-screen instructions for disabling the `defaultBranchName` advice message to recommend `--global` instead of `set`, ensuring the setting applies to all future repositories rather than just the current one. All other advice keys retain the original `git config set` wording.

Current status: initial version (`[PATCH]`) posted; under review. No prior versions or merge status reported.

Key technical details:
- File touched: `advice.c`
- Subsystem: advice machinery
- Change: modifies string formatting in `vadvise()` helper to conditionally substitute `--global` for `set` when the advice key is `defaultBranchName`
- Symbols: none renamed or introduced; only the displayed command string is altered
- Behavior: old: `git config set advice.defaultBranchName false` (repository-local); new: `git config --global advice.defaultBranchName false` (global default)
- On-disk format: unchanged
- Test coverage: none added; change is self-contained and deemed low-risk

Open questions and loose ends: none identified; patch is small, focused, and appears ready for review.

Participant positions: none recorded yet; thread root only.

Related work: none mentioned.