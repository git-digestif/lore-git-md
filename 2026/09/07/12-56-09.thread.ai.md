Thread identity: patch series (single patch), originating author Vsevolod Myalitsin, thread type user-facing documentation fix in the advice subsystem.

Core problem or goal: correct the on-screen instructions for disabling the `defaultBranchName` advice message to recommend `--global` instead of `set`, ensuring the setting applies globally rather than just to the current repository. All other advice keys retain the original `git config set` wording.

Current status: initial version (`[PATCH]`) posted; under review. Ben Knoble’s review suggests stylistic and structural improvements but does not oppose the change.

Key technical details:
- File touched: `advice.c`
- Subsystem: advice machinery
- Change: modifies string formatting in `vadvise()` helper to conditionally substitute `--global` for `set` when the advice key is `defaultBranchName`
- Symbols: none renamed or introduced; the displayed command string is altered
- Behavior: old: `git config set advice.defaultBranchName false` (repository-local); new: `git config --global advice.defaultBranchName false` (global default) or, per Ben Knoble’s suggestion, `git config set --global advice.defaultBranchName false` (modern form)
- On-disk format: unchanged
- Test coverage: none added; change is self-contained and deemed low-risk

Open questions and loose ends:
- Whether to adopt the more explicit `set --global` syntax (Ben Knoble’s preference).
- Whether to refactor the logic into a helper function (e.g., `is_global(key)`) for future extensibility, as suggested by Ben Knoble.

Participant positions:
- Vsevolod Myalitsin: original author; proposed the change to use `--global` for `defaultBranchName`.
- Ben Knoble: reviewed the patch; supports the goal but suggests using `set --global` and refactoring the logic into a helper function for maintainability. Review is substantive (engages with design and future extensibility).