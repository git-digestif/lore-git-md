This is a documentation patch addressing a minor AsciiDoc formatting issue in `Documentation/config/maintenance.adoc`. The patch itself is already under review and looks uncontroversial, but Patrick Steinhardt replies with a surface-level review suggesting a small improvement to the commit message.

Patrick notes that the commit message could be clearer by explicitly mentioning that the affected section appears in `git-config(1)` (the rendered man page) rather than just referencing the source file. He provides an example of how the message could be reworded but says it’s not worth a reroll—meaning the patch is acceptable as-is, though the suggestion could be incorporated if the author chooses to send a v2. He also confirms that the misrendering is visible in the man page and that the fix works as intended.

The patch remains a straightforward, one-line documentation cleanup with no functional changes. It’s part of the broader effort to improve AsciiDoc consistency in Git’s documentation, though it wasn’t explicitly tied to that effort in the original submission. No objections have been raised, and the fix is likely to be merged once any minor commit message tweaks are addressed (if the author decides to make them).