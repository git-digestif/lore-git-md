Thread identity: Bug report – `.gitignore` trailing-slash pattern fails to ignore symlinks, leading to silent data loss during `git pull`. Originating author: AIKSXD ax (aiksxd@outlook.com). Thread type: bug report (no patch series, no RFC).

Core problem or goal: A `.gitignore` entry ending in a slash (`dir/`) does **not** ignore a symlink of the same name (`dir`), allowing the symlink to be staged and committed. When another user pulls, Git silently overwrites their local **directory** with the incoming **symlink**, destroying all data inside the directory. The report seeks either (a) a change to `.gitignore` semantics so trailing-slash patterns also ignore symlinks, or (b) a conflict detection during `git pull` that refuses to overwrite without user confirmation.

Current status: Under review. No patch posted yet; the report is the thread root. No maintainer decision or merge status recorded.

Key technical details:
- Files touched: `.gitignore` semantics, `git pull` conflict resolution.
- Subsystems: ignore handling, checkout/update machinery.
- Behavior change: Either (1) extend `.gitignore` to treat `dir/` as matching a symlink `dir`, or (2) add a safety check in `git pull` that aborts when a local directory would be replaced by a symlink.
- On-disk format: unchanged.
- Test coverage: reproduction case provided in the report; no formal test script yet.

Open questions and loose ends:
- Which of the two proposed fixes (or a third alternative) will be adopted?
- Is the `.gitignore` change backwards-compatible, or will it break existing workflows that rely on the current behavior?
- Should the safety check be opt-in (configurable) or always-on?
- Are there other Git commands (e.g., `git checkout`, `git merge`) that exhibit the same silent-overwrite behavior?

Participant positions: None yet; the thread is currently a single-email report. No reviews, acks, or objections recorded.