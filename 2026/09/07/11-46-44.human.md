This is a follow-up in the memory leak discussion for `git history reword --dry-run`. Kaartic Sivaraam acknowledges that he previously proposed the same fix Patrick Steinhardt just sent—adding a missing `repo_unuse_commit_buffer()` call in `commit_tree_ext()`—but the patch stalled because Jeff King (Peff) pointed out the commit message was misleading. Specifically, the leak only occurs when the commit buffer isn’t cached in the commit slab, and Kaartic never identified the exact conditions that trigger this case. He now plans to revisit the issue and send a revised patch soon.

The thread confirms the fix itself is correct and uncontroversial, but the commit message needs to clarify the leak’s scope: it’s not a general issue with `git history reword`, but one that surfaces only when the commit buffer isn’t slab-cached. This explains why the leak is environment-dependent (e.g., `i18n.commitencoding` settings or packfile configurations) and why it didn’t reproduce for Patrick. The discussion also reveals that the fix has been floating around since June 2026, suggesting it’s a low-risk, high-priority candidate for merging once the commit message is tightened. No new technical objections or edge cases were raised in this reply.