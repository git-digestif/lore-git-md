This is a **regression fix discussion** in the sequencer subsystem, continuing the thread about restoring `git commit --amend` during interactive rebase. Elijah Newren replies to Junio C Hamano, refining the plan for a targeted fix rather than a full revert of the regressing commit (6257588252).

Elijah agrees with Junio’s workflow justification for allowing `git commit` and `git commit --amend` during interactive rebase, but proposes a more surgical approach: teaching `git commit` to recognize a new pseudoref, `REBASE_HEAD`, modeled on how it already handles `CHERRY_PICK_HEAD`. This would let `git commit` preserve authorship information from the original commit without requiring a full revert, addressing the immediate regression while avoiding the `MERGE_MSG` race condition. The idea is to extend the existing `read_commit_message()` and `sequencer_post_commit_cleanup()` logic to consume `REBASE_HEAD` after a successful commit, mirroring the `CHERRY_PICK_HEAD` pattern.

The email does not include code or test modifications, but it clarifies the technical path forward: introducing `REBASE_HEAD` as a new pseudoref in `.git/`, analogous to `CHERRY_PICK_HEAD`, and modifying `git commit` to read and consume it. This approach would sidestep the architectural complexity of modifying the sequencer’s state management or outright rejecting plain `git commit` during conflict resolution, deferring that broader discussion until after Git 2.56. Elijah notes that `git am` would be harder to adapt because it lacks a dedicated pseudoref, but the immediate focus is on interactive rebase.

The thread is now converging on this targeted fix as a middle ground between a full revert and a deeper architectural change. The proposal is well-motivated and leverages existing patterns, suggesting it is likely to be accepted if implemented cleanly. The broader question of rejecting plain `git commit` during conflict resolution remains deferred, but this email advances the immediate regression fix in a pragmatic direction.