This is a brief, substantive review comment in the ongoing discussion about the `parse-options` early-scanning API series. Junio C Hamano acknowledges Christian Couder’s decision to drop the `git bisect` conversion patches from v2, agreeing that detecting `--` can wait until `bisect_start()` adopts the full `parse_options()` API. His reply is minimal but **signals approval of the narrower scope** and removes a potential blocker for the series. The comment is **not a technical objection**—it simply notes that the current approach (deferring the `git bisect` conversion) is reasonable and aligns with his vision of a future where `--` detection comes "for free" as a side effect of `parse_options()`. This clears the way for the remaining patches (the core API and `git fast-import` bugfix) to proceed without further debate on this point. The thread’s focus now shifts back to the API’s design and usability.