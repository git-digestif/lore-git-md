This is a **regression fix discussion** for `git commit --amend` during interactive rebase, specifically whether the fix should be expanded or the original regression reverted entirely before Git 2.56 final.

Junio C Hamano steps in to ask whether the safest path forward is to **revert the regressing commit (6257588252) entirely** in the upcoming -rc2, rather than merging the current targeted fix. His question is pragmatic: the release is imminent, the fix is still under discussion, and reverting would restore the pre-2.56 behavior while giving the project more time to design a cleaner solution post-release. He does not take a position on the architectural question Phillip Wood raised (whether the fix should cover `git cherry-pick`, `git merge`, and `git revert`); his focus is purely on release risk management.

The thread remains focused on the 2.56 timeline. No new code or tests are proposed here—this email is a release-engineering checkpoint. The patch under review (Patrick Steinhardt’s v1) is still the only candidate on the table, but Junio’s question suggests it may not be merged as-is.