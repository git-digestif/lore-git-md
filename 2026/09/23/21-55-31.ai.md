This is the maintainer's **merge confirmation** for the v5 bugfix series restoring old OIDs in the reference-transaction hook for batched deletions. Junio explicitly states that all three patches in the series now build cleanly at every intermediate step (`1/3`, `1/3--2/3`, and the full series) with `DEVELOPER=1`, addressing the build-breakage issue identified in v4. The series is **technically complete** and ready for integration.

The email is brief and procedural: it confirms the mechanical fix (updating all `refs_delete_refs()` call sites in patch 1/3) and notes that Junio has adopted a new workflow to verify intermediate buildability, a direct response to prior criticism. There is no new technical feedback or open questions—this is a green light for merging to `next`.

The series remains a high-priority regression fix for a 5-year-old bug affecting external tools (Gerrit, GitLab, CI systems) that rely on the hook. It restores pre-2.31 behavior (passing resolved old OIDs to the hook) while preserving the performance gains of the original optimization, using `REF_TRANSACTION_ALLOW_FAILURE` to resolve the core design conflict. All prior feedback from Junio, Karthik Nayak, and others has been addressed, and the final tree is identical to v4. The fix is narrowly scoped to the refs subsystem and four built-in commands (`git branch`, `git tag`, `git remote`, `git fetch`), with no on-disk format changes. Test coverage is comprehensive, including new tests for partial prune scenarios and UI consistency. A strong candidate for `next`.