This is a **release-management discussion** in the ongoing thread about the `git commit --amend` regression during interactive rebase. Junio C Hamano and Elijah Newren now agree on a short-term plan for Git 2.56: revert the regressing commit (6257588252) entirely rather than merging Patrick Steinhardt’s targeted fix or implementing Elijah’s alternative of recording the original `HEAD` in `rebase-merge/stopped-head`.

Junio frames the architectural question—whether to restrict plain `git commit` during conflict resolution for sequencer-based commands—as a post-3.0 effort, not something to tackle in the 2.56 cycle. His rationale is pragmatic: the current fix is not robust (Elijah demonstrated a race condition with `git reset`), and the broader consistency question across `rebase`, `am`, `cherry-pick`, and `revert` remains unresolved. Reverting the regressing commit gives the project a clean slate for a more considered approach after the release.

The email advances the thread by settling the immediate release-management question: the revert will be prepared as -rc2 material. No code changes are proposed here, but the decision directly affects what will land in Git 2.56. The discussion about the architectural direction (rejecting plain `git commit` during conflict resolution) is explicitly deferred, with Junio and Elijah aligned on treating it as a major, post-3.0 change rather than a 2.56 blocker.