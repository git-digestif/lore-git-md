This is a **regression fix discussion** for the `git commit --amend` behavior during interactive rebase, now focused on agreeing the revert plan and exploring longer-term architectural options. Junio C Hamano’s email is a short, clarifying reply to Patrick Steinhardt’s earlier message, where Patrick agreed to revert the regressing commit (6257588252) for Git 2.56 and floated the idea of fixing `git commit` itself to preserve authorship information during conflict resolution.

Junio’s reply is light on new substance but sharpens the discussion: he questions what Patrick means by “fix git-commit(1) itself,” suggesting a mechanism where `git commit` would read and then clear a file in `.git/rebase-merge/` to override authorship. This is not a concrete proposal—Junio is probing whether the idea is feasible or just a vague thought. The tone is neutral, treating the suggestion as an open design question rather than a near-term plan.

The thread remains in agreement that the regressing commit will be reverted for the 2.56 release, deferring the broader architectural question (whether to reject plain `git commit` during conflict resolution) until after Git 2.56 ships. Junio’s question here is a small step toward that future discussion, but no code or test changes are proposed in this email. Readers following the thread can expect the revert to land soon, with the deeper design debate to resume later.