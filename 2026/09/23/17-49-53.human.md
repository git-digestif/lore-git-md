This is a **release-management follow-up** in the ongoing regression fix thread for `git commit --amend` during interactive rebase. Elijah Newren agrees with Junio C Hamano’s proposal to **revert the regressing commit (6257588252) entirely** for the Git 2.56 release cycle, deferring both Patrick Steinhardt’s targeted fix and the broader architectural consistency question until after the release ships.

The email is brief and decisive, endorsing Junio’s pragmatic approach to prioritize release stability over partial fixes. No new technical details or alternatives are introduced; this is purely a process-level agreement. The thread’s next steps now hinge on Junio’s integration decision, with the revert likely to appear in `next` shortly and graduate to `master` before the final 2.56 release.