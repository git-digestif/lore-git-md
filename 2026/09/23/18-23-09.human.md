This is a **regression fix discussion** for `git commit --amend` during interactive rebase, now pivoting toward a full revert of the regressing commit (6257588252) for Git 2.56. The email is Patrick Steinhardt’s reply to Elijah Newren’s substantive review, which identified a critical flaw in the proposed fix and proposed reverting the original commit entirely.

Patrick acknowledges the race condition Elijah pointed out—where `git reset` removes `MERGE_MSG` without advancing `HEAD`, allowing an unsafe `git commit --amend -a`—and agrees that reverting the original commit is the safest short-term solution for the 2.56 release cycle. He expresses reluctance about the broader architectural proposal to outright reject plain `git commit` during conflict resolution for sequencer-based commands (`rebase`, `am`, `cherry-pick`, `revert`), citing his own workflow (verifying merge results before continuing) and the risk of breaking existing user habits. However, he concedes that reverting is the pragmatic choice given the unresolved design questions and the urgency of stabilizing the release.

The email advances the thread by confirming consensus on the revert, deferring the deeper architectural discussion until after Git 2.56 ships. No new code or test changes are proposed here; the focus is on aligning with Junio and Elijah’s preference for a clean revert rather than a partial fix. The broader question of whether plain `git commit` should be disallowed during conflict resolution remains open, with Patrick noting that fixing `git commit` itself to preserve authorship information would be a much harder change. The tone is pragmatic and collaborative, prioritizing release stability over incremental fixes.