This is a **substantive follow-up review** in the ongoing regression fix thread for `git commit --amend` during interactive rebase. Phillip Wood now broadens the scope, asking whether the same overzealous safety check that broke `git rebase --interactive` also affects `git cherry-pick`, `git merge`, and `git revert`.

The core question is architectural: the fix currently modifies only `sequencer.c` logic used by `git rebase`, but the problematic commit 6257588252 ("sequencer: refuse to amend during conflict resolution") was written generically and could plausibly block amendments in any sequencer-based operation. Phillip does not claim a bug exists in those other commands—only that the same reasoning ("do we want to allow amending a conflict resolution commit?") should be applied consistently across all sequencer users. No test failures or user reports are cited; this is a proactive design consistency check rather than a discovered regression.

The thread remains focused on the 2.56 release cycle, and the patch under review is still the single-patch v1 from Patrick Steinhardt. No new code or test changes are proposed in this email; it is purely a question about whether the fix should be extended to cover more commands.