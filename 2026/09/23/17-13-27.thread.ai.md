Thread identity
Subject: [PATCH 0/2] ci: reduce resource exhaustion in GitHub Actions
Series version: v1 (initial submission)
Originating author: Tamir Duberstein
Thread type: patch series (CI/build system)

Core problem or goal
Address resource exhaustion in GitHub Actions CI jobs running the full test suite on Linux runners. The series targets two specific issues: (1) `t4205-log-pretty-formats.sh` generates enormous output that triggers OOM kills when `diff` compares it, and (2) default `make` and `prove` parallelism (`-j10`) is too aggressive for the Linux runner’s 2 CPU cores, causing ENOSPC errors during large clone/repack tests.

Current status
Under review (v1 posted). No prior versions. No maintainer decision recorded.

Key technical details
- Patch 1: Replace `test_cmp` (which uses `diff`) with `test_cmp_bin` (which uses `cmp`) in `t/t4205-log-pretty-formats.sh` and delete the two large temporary files immediately after comparison, reclaiming ~4 GiB of memory. The OOM kill (SIGKILL) is due to `diff` memory usage; `cmp` is a lightweight alternative.
- Patch 2: Dynamically set `-j` for `make` and `prove` to the runner’s CPU count (`JOBS=$(nproc)`) when `CI_OS_NAME` is `linux`, matching GitLab CI policy. The change is a four-line addition to `ci/lib.sh`. The motivation is to prevent ENOSPC errors during concurrent large fixtures (clone/repack) on 2-CPU runners.
- Files touched: `.github/workflows/main.yml`, `t/t4205-log-pretty-formats.sh`, `ci/lib.sh`.
- No new symbols, CLI options, or on-disk format changes.
- Test coverage: unchanged (long-running tests remain enabled).

Open questions and loose ends
None identified. The series is presented as a pragmatic fix with clear technical rationale and no unresolved objections.

Participant positions
- Tamir Duberstein (author): posted v1, described the changes as minimal and addressing real CI bottlenecks. Provided additional context for both patches: OOM kill due to `diff` memory usage (Patch 1), and ENOSPC errors due to over-aggressive parallelism (Patch 2).
- No reviews or objections recorded yet.

Related work
None mentioned.