This is a procedural ping on the configurable submodule fetch errors series. Paulius Zaleckas sends a second gentle reminder that his v6 patch series—adding `fetch.submoduleErrors` and `--submodule-errors=(fail|warn)`—has been sitting in Junio’s `seen` branch marked “Needs review” for over a month without further feedback. He specifically asks Ramsay Jones and Jean-Noël Avila, who reviewed earlier iterations, to re-engage. There is no new code or technical discussion in this email; it is purely a nudge to move the series toward `next`. The patches remain unchanged since v6, which already addressed all prior review points, including Junio’s mechanical requests.