This is a **regression fix discussion follow-up** in the `sequencer.c` subsystem, continuing the thread about restoring `git commit --amend` functionality during interactive rebase conflict resolution.

Patrick Steinhardt clarifies his earlier suggestion about modifying `git commit` to preserve authorship information during conflict resolution. He confirms Junio C Hamano’s interpretation: the idea would involve `git commit` reading and overriding authorship from a file in `.git/rebase-merge/`, then removing that file after use. Patrick acknowledges this would complicate the sequencer’s state management and concedes the behavior might not even qualify as a "fix," since `git commit` is technically doing what it always does—just with an outcome that users may find surprising in this context.

This exchange does not introduce new code or test changes, nor does it alter the agreed plan to revert the regressing commit (6257588252) for Git 2.56. It merely elaborates on a speculative future direction that is now explicitly deferred. The broader architectural question—whether to reject plain `git commit` during conflict resolution for sequencer-based commands—remains untouched, with all participants treating it as a post-2.56 effort. The thread’s immediate focus remains the revert, and this reply adds no new technical constraints or open questions.