This is a **substantive technical reply** in the review of Christian Couder’s `[PATCH v2 1/6]` ("parse-options: add early_scan_options()"). The exchange focuses on a key design decision: whether the early-scanner API should handle argument skipping internally or leave it to the caller.

Junio argues that the API would be **easier to use** if the callee (`find_early_scan_option()`) skipped separate arguments (`--option value`) itself, rather than requiring the caller to do it. His preference is for the machinery to "scan the options reliably" with minimal caller involvement. Christian initially resisted, noting that the callee lacks access to `argv`/`argc` and the current index, but Junio’s reply suggests this is a solvable implementation detail rather than a fundamental limitation.

The discussion also clarifies a **subtle correctness fix** in v2: the stuck-argument handling (`--option=value`) now mirrors `parse_options()`’s logic by checking `!(opt->flags & PARSE_OPT_NOARG)` instead of `takes_value`. This ensures consistent behavior for optional arguments (`PARSE_OPT_OPTARG`) and last-argument defaults (`PARSE_OPT_LASTARG_DEFAULT`), addressing a bug in v1 where the scanner would incorrectly reject stuck values for these cases.

The tone is **constructive and collaborative**, with Junio’s "opaque hint" remark acknowledging Christian’s responsiveness to feedback. The outcome is likely to be a v3 patch where the callee takes on the responsibility of skipping arguments, simplifying the API’s usage. This change would not affect the core functionality but would make the API more ergonomic for future adopters.

The broader series (introducing `early_scan_options()` to fix a `git fast-import` bug) remains under review, but this exchange resolves a concrete implementation concern in the preparatory refactoring patch. The `git fast-import` bugfix itself (patch 3/6) is uncontroversial and likely to land once the API design is finalized.