This is a **maintainer query** in the ongoing regression fix thread for `git commit --amend` during interactive rebase. Junio C Hamano asks whether the safest course for the 2.56 release cycle is to **revert the regressing commit (6257588252) entirely** rather than merging Patrick Steinhardt’s targeted fix, deferring any further work until after Git 2.56 ships.

The question is framed as a release-management trade-off: the current fix is narrowly scoped to `git rebase --interactive`, but Phillip Wood’s follow-up has raised the architectural question of whether the same safety check affects other sequencer-based commands (`git cherry-pick`, `git merge`, `git revert`). Junio’s reply suggests that the broader consistency question makes him hesitant to merge the fix as-is, preferring to remove the problematic code altogether and revisit the design in the next cycle. No new code or test changes are proposed; this is purely a process decision about what to include in the upcoming release candidate.