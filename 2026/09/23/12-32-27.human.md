This is a minor review reply in the ongoing `imap-send` OpenSSL compatibility series. Patrick Steinhardt acknowledges Beat Bolli’s clarification about the OpenSSL 4.1 compatibility macro in patch 1: the old `ASN1_STRING_length()` function will be hidden when `OPENSSL_API_COMPAT` is defined for OpenSSL 4.1 or later, and Git’s `DEVELOPER=1` build treats deprecation warnings as errors, causing a build failure. Patrick now agrees the patch is justified but suggests the commit message should explicitly mention the `OPENSSL_API_COMPAT` mechanism to make the rationale clearer.

This reply resolves the immediate question about patch 1’s necessity but does not address the larger unresolved issues in the series: the memory-safety bug in patch 2 (with two competing fixes still under discussion), the incomplete RFC 6125 compliance in patch 3 (missing IP SAN handling), or the architectural question about manual vs. OpenSSL-native certificate verification. The series remains under review with no maintainer decisions or merge status reported.