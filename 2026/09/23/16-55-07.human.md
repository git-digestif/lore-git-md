This is a design discussion reply in the ongoing thread about improving `git fetch` behavior in shallow repositories. D. Ben Knoble chimes in from the sidelines to endorse Junio C Hamano’s proposed "priming" mechanism for the matching-inspired fetch mode, while suggesting a refinement: the initial `git fetch <remote> <branch>` should still perform the usual mapping to create a remote-tracking branch (e.g., `refs/remotes/<remote>/<branch>`), rather than being a no-op. This would preserve the familiar `git fetch` behavior while still requiring the user to explicitly set up tracking branches, aligning with Junio’s goal of avoiding unnecessary fetches in shallow repositories.

The suggestion is minor but pragmatic: it reduces the number of steps users must perform (eliminating the need to manually specify the full refspec like `main:refs/remotes/second/main`) while maintaining the core idea that users must explicitly opt into fetching specific branches. This keeps the solution lightweight and user-friendly without sacrificing the performance benefits for shallow repositories. The discussion remains focused on refining the matching-inspired fetch mode, with no new objections or alternative proposals introduced.