This is a **post-merge procedural update** from Junio C Hamano. The `http.sslVerifyStatus` feature, previously merged to `master` on 2025-06-12, has been **reverted from `next`** due to test infrastructure issues reported by SZEDER Gábor. The core functionality remains intact in `master`, but Junio is holding the topic out of `next` to allow time for the test failures to be addressed. The revert is not a rejection of the feature itself—Junio explicitly states the topic can "mature" and return later—but a precaution to avoid integration instability while the test suite is fixed.

The email links to Gábor’s detailed report, which identified two problems in the new `t5585-http-ssl-ocsp.sh` test: a permissive prerequisite check that passes when `git ls-remote` fails for unrelated reasons, and an invalid expectation that a revoked certificate should be accepted without `http.sslVerifyStatus` on systems where libcurl checks CRLs by default. Junio’s decision to revert from `next` (rather than `master`) signals confidence in the feature’s design but acknowledges that the test suite needs to be more robust before the topic can graduate to the next release cycle.

No new technical concerns are raised; the focus is purely on test correctness. The author (graysongordon-gl) or another contributor will need to post a follow-up patch to fix the prerequisite logic and adjust the test expectations. Until then, the feature remains in `master` but is temporarily excluded from `next`.