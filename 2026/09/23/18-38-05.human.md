This is a follow-up in the ongoing thread about Tamir Duberstein’s two-patch performance series for `git ls-files`, which aims to reuse the untracked cache populated by `git status`. The series is still under review, with no prior versions or external feedback yet.

The email is a brief correction from the author, retracting an earlier accidental send of incomplete benchmark data. Tamir clarifies that the first patch (fixing ignore-file hash computation to prevent spurious untracked cache invalidation) is in good shape, but the second patch (teaching `git ls-files` to reuse the untracked cache) is still being refined. The earlier message included noisy benchmark results that showed inconsistent performance for literal queries, which Tamir now disavows as unintended and unrepresentative.

No new technical details or design changes are introduced here—this is purely a procedural note to disregard the previous benchmark noise. The core goal of the series remains unchanged: eliminating redundant directory traversals by reusing the untracked cache, with the first patch addressing correctness and the second delivering the primary speedup. The author’s focus is now on stabilizing the second patch before resubmitting.