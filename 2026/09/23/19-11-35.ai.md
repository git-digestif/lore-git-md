This is a **regression fix discussion** in the `sequencer.c` subsystem, continuing the thread about restoring `git commit --amend` functionality during interactive rebase conflict resolution. Elijah Newren’s reply to Junio C Hamano clarifies and advances the speculative future direction for addressing the architectural tension between `git commit` and sequencer-based commands (`rebase`, `am`, `cherry-pick`, `revert`).

Elijah endorses Junio’s suggestion to modify `git commit` to consume a new pseudoref, `REBASE_HEAD`, analogous to how it currently handles `CHERRY_PICK_HEAD`. This would allow `git commit` to preserve authorship information during an interactive rebase without requiring users to manually specify `--reset-author` or similar flags. He notes that this approach is already implemented for cherry-pick via `read_commit_message("CHERRY_PICK_HEAD")` and `sequencer_post_commit_cleanup()`, making it a natural extension. The proposal would involve reading the original `HEAD` from `REBASE_HEAD` (or a similar file in `.git/rebase-merge/`) and consuming it after a successful commit, ensuring the sequencer state remains consistent.

Elijah acknowledges that extending this mechanism to `git am` would be more challenging, as `git am` does not currently use a dedicated pseudoref and instead relies on the `author-script` and `final-commit` state files. This suggests that any broader architectural change to unify behavior across sequencer-based commands would require significant refactoring, reinforcing the decision to defer such work until after Git 2.56.

The email does not introduce new code or test modifications, but it provides a concrete technical direction for a future fix. Key references for follow-up discussion include the `REBASE_HEAD` pseudoref, the `CHERRY_PICK_HEAD` precedent, and the distinction between `git commit` and `git am` state management. The thread remains focused on the immediate revert of commit 6257588252 for Git 2.56, with this exchange laying groundwork for post-release architectural improvements. No new symbols or on-disk format changes are proposed yet.