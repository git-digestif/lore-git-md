This is a **thread update** in the `parse-options` early-scanning API series. Junio C Hamano acknowledges Christian Couder’s decision to drop the `git bisect` conversion patches from v2, agreeing that detecting `--` can wait until `git bisect` adopts the full `parse-options` API. This reply **removes the last substantive objection** to the series’ scope, clearing the way for the remaining patches (the core API and `git fast-import` bugfix) to advance. The exchange is brief and **resolves the open question** about whether the API’s value for `git bisect` justified its inclusion; the answer is now a clear "no" for v2, but the door remains open for future adoption. No new technical concerns are raised, and the thread’s trajectory is now **focused on the API’s design and the `git fast-import` bugfix**. Junio’s tone is neutral and **accepting of the revised scope**, suggesting the series is now more likely to proceed to `next` once the remaining design questions (e.g., argument-skipping responsibility) are addressed.