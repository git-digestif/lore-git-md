Thread identity: regression fix for `git commit --amend` during interactive rebase, introduced in Git 2.56-rc0; single-patch series from Patrick Steinhardt.

Core problem or goal: restore the ability to amend a commit created after resolving a conflict during an interactive rebase, which was broken by an overzealous safety check in commit 6257588252.

Current status: under review; patch v1 posted. No prior versions. The fix is targeted for the 2.56 release cycle to address a regression before final release.

Key technical details: modifies `sequencer.c` to check for the presence of `MERGE_MSG` as a proxy for whether a conflict resolution has already been committed, allowing amendments to proceed if the file exists. Adds a regression test in `t/t3404-rebase-interactive.sh` covering the scenario: start interactive rebase, induce conflict, resolve and commit, then amend the new commit. No new symbols or on-disk format changes.

Open questions and loose ends: Patrick Steinhardt expresses dissatisfaction with the indirectness of the `MERGE_MSG` check ("fishy") and invites suggestions for a cleaner approach. No alternative implementations have been proposed yet.

Participant positions: Patrick Steinhardt (author) -- pragmatic fix with test coverage, acknowledges architectural shortcomings but prioritizes regression fix for 2.56.

Related work: depends on commit 6257588252 (the regression source), which aimed to prevent accidental amendments during conflict resolution. No other in-flight topics mentioned.