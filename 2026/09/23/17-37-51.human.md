This is the final patch in the v6 series adding path-related keys to `git repo info`. The author acknowledges a **substantive edge-case discrepancy** between the new `path.cdup` key and `git rev-parse --show-cdup` when the current working directory is outside the repository’s working tree. The fix is straightforward: adopt the same logic used in `git rev-parse` to return the absolute path to the working tree root in this scenario. This aligns with the series’ goal of consolidating repository path information and resolves the last high-weight issue blocking the series.

The patch itself remains unchanged in this reply; the discussion focuses solely on the edge case. The author’s proposed fix (adding the `is_inside_work_tree()` check and falling back to `repo_get_work_tree()`) is mechanically sound and matches the existing `git rev-parse` implementation. No further design objections or mechanical issues are raised in this email. The series is now feature-complete, mechanically clean, and correctness-bug-fixed, pending this final adjustment.

Reviewers should note that this change is localized to `builtin/repo.c` and does not affect the rest of the series. The fix is uncontroversial and directly addresses Junio’s concern about script compatibility and user expectations. Once applied, the series will be ready for final review and potential merging.