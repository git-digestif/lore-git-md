This is a **regression fix discussion** in the `sequencer.c` subsystem, specifically about restoring `git commit --amend` functionality during interactive rebase conflict resolution. Junio C Hamano’s reply to Patrick Steinhardt advances the thread by clarifying the architectural implications of the proposed revert and the broader design question.

Junio agrees with Patrick’s and Elijah Newren’s consensus to revert the regressing commit (6257588252) for the Git 2.56 release cycle, prioritizing stability. He reinforces the workflow justification: allowing `git commit` (and thus `git commit --amend`) during interactive rebase enables users to split commits, reword messages, or inspect changes before continuing, which is a valuable and widely used capability. This aligns with the project’s reluctance to break established workflows without strong justification.

Junio questions Patrick’s suggestion about "fixing `git commit` itself to not reset authorship information," seeking clarification on the mechanism. Patrick’s idea appears to involve overriding authorship based on state stored in `.git/rebase-merge`, but Junio’s response suggests this would complicate the sequencer’s state management further. No concrete proposal or code is offered here; the focus remains on agreeing to the revert and deferring deeper architectural changes until after Git 2.56.

The email does not introduce new technical details or test modifications, but it solidifies the revert plan and underscores the tension between workflow flexibility and architectural consistency. Key references for follow-up discussion include the `rebase-merge/stopped-head` file (Elijah’s alternative design), the `MERGE_MSG` race condition, and the distinction between `git commit` and `git rebase --continue` behaviors. The thread is now converging on the revert as the immediate action, with broader design questions deferred.