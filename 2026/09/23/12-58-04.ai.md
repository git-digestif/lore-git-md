This is the first patch in the v2 series fixing the `git stash` autostash bug with staged index entries. It is a **contributor housekeeping** patch—removing an unused `#include "oid-array.h"` from `builtin/stash.c`. The change is trivial: the header is not referenced in the file, and the transitively included `hash.h` declarations are already covered by a direct include. The patch is already accepted by Junio C Hamano as a "no-brainer" and serves as a clean precursor to the more substantive refactoring and bugfix patches that follow.

The patch touches only `builtin/stash.c` (one line removed) and has no functional impact. It is motivated by a Clang warning and aligns with the project’s preference for minimal includes. This patch is effectively merged; the remaining three patches in the series (refactoring, test additions, and the functional fix) build on this cleanup. No further review is needed here.