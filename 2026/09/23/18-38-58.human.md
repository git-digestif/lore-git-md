This is a **regression fix discussion** for `git commit --amend` during interactive rebase, now converging on a full revert of the regressing commit (6257588252) for Git 2.56. Patrick Steinhardt’s reply to Junio C Hamano clarifies his earlier suggestion about modifying `git commit` itself to preserve authorship information during conflict resolution.

Patrick confirms that his idea would involve `git commit` reading and overriding authorship from a file in `.git/rebase-merge/`, then cleaning up that file afterward. He acknowledges this would complicate the sequencer’s state management but argues it might better match user expectations—amending a commit after resolving conflicts should not reset authorship. This remains a speculative, long-term idea; the thread’s immediate focus is still the agreed revert of commit 6257588252 to restore the broken functionality for the 2.56 release.

The exchange underscores the tension between preserving established workflows (like amending commits mid-rebase) and the architectural goal of steering users toward `git rebase --continue`. Junio’s skepticism about the `git commit` modification proposal signals that any such change would require a much broader discussion, likely deferred until after Git 2.56. No new code or test changes are introduced here; the thread remains in consensus-building mode.