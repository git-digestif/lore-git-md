**Summarizer brief mode**

This is a **follow-up email in the `[PATCH v6]` series adding `path.superproject-root` keys to `git repo info`**, specifically addressing Junio C Hamano’s request to split the second patch into two. The author acknowledges the feedback and agrees to the refactoring but raises a **substantive concern about test placement**: the `get_superproject_working_tree()` fix affects both `git rev-parse` and `git repo info`, so isolating its tests in `t1500-rev-parse.sh` (as Junio suggested) may not fully capture the regression risk. The author plans to find a more appropriate location for the tests, implying they may propose a new test script or a shared location (e.g., `t1500-rev-parse.sh` *and* `t1900-repo-info.sh`).

The email does not include a revised patch or test updates, so the **high-weight mechanical requirement remains unresolved**. The author’s hesitation about test placement suggests this may become a minor point of discussion in the next iteration, though the core refactoring (splitting the patch) is not in dispute. Reviewers should watch for the next version to confirm whether the tests are placed in a way that satisfies both the fix’s scope and Git’s testing conventions.

Key technical details carried forward:
- **Files touched**: `submodule.c`/`submodule.h` (fix to `get_superproject_working_tree()`), `builtin/repo.c` (new keys), and the test suite (location TBD).
- **New symbols**: None in this email, but the split will create two patches: one for the fix (with tests) and one for the feature (with tests).
- **Behavior**: The fix ensures `get_superproject_working_tree()` respects the `repo` parameter and uses the repository’s working directory, resolving the `--git-dir` edge case. The feature patch will reuse this fixed function to implement `path.superproject-root.absolute` and `path.superproject-root.relative`.
- **Test coverage**: The author’s concern implies the tests for the fix may need to cover both `git rev-parse --show-superproject-working-tree` *and* `git repo info path.superproject-root` to fully validate the regression fix. This could involve updating `t1500-rev-parse.sh` and/or `t1900-repo-info.sh`, or introducing a new test script (e.g., `t1501-submodule.sh` or a dedicated `t1502-superproject.sh`).

The email does not address the other open issues in the series (e.g., the `path.index` correctness issue or the `path.cdup` edge-case fix), which remain pending. The **patch 2/7 refactoring is now the most immediate blocker** for further review.