This is a substantive review reply in the ongoing documentation cross-reference modernization thread. Jeff King (Peff) engages with the technical details of Julia Evans's patch, focusing on the implications of AsciiDoc's auto-generated section IDs versus manually assigned ones.

Peff notes that Git's AsciiDoc toolchain now supports auto-generated section IDs, which would allow replacing manual `[[EDITING_PATCHES]]` anchors with the auto-generated `_editing_patches` syntax. However, this change would alter the fragment identifiers in the HTML output, breaking existing external links to the documentation (e.g., `https://git-scm.com/docs/git-add#_editing_patches` would become `https://git-scm.com/docs/git-add#EDITING_PATCHES`). While Peff personally prefers the all-caps style, he flags this as a conscious trade-off rather than an oversight, raising the question of how much the project should prioritize link stability versus readability.

The review also highlights minor typographic inconsistencies in the patch, such as the placement of quotation marks and backticks around cross-referenced text. Peff suggests these could be addressed in follow-up work, emphasizing that the current patch's mechanical focus is reasonable. The discussion touches on broader documentation maintenance challenges, including the fragility of auto-generated IDs for list items and the tension between toolchain conventions and user-facing link aesthetics.

This reply advances the thread by surfacing a design decision (manual vs. auto-generated IDs) that was not explicitly addressed in the original patch. While the patch is already approved and queued for `next`, Peff's feedback may prompt a v2 if Julia or others decide the link stability trade-off warrants revisiting the approach. The tone is collaborative, framing the issue as a matter of project priorities rather than a flaw in the patch.