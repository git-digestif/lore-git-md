This is a brief follow-up in the `imap-send` OpenSSL compatibility series, specifically about patch 1/3. Patrick Steinhardt acknowledges Beat Bolli’s clarification that the OpenSSL 4.1 compatibility macro is necessary because `ASN1_STRING_length()` will be hidden when `OPENSSL_API_COMPAT` is defined for OpenSSL 4.1 or later, and Git’s `DEVELOPER=1` build treats deprecation warnings as errors. He suggests updating the commit message to explicitly mention `OPENSSL_API_COMPAT` for clarity.

This reply resolves the immediate question about the macro’s necessity but does not address the other unresolved issues in the series: the memory-safety bug in patch 2 (with two competing proposed fixes), incomplete RFC 6125 compliance in patch 3 (missing IP SAN handling), or the architectural question about manual vs. OpenSSL-native verification. The series remains under review, with patch 1 now better justified but still contested in principle. Key technical details: the macro is compile-time only, affects only `imap-send.c`, and is a forward-compatibility measure for OpenSSL 4.1. No new symbols or behavior changes are introduced.