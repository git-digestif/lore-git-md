This is a **release-management discussion** in the ongoing thread about the `git commit --amend` regression during interactive rebase. Junio C Hamano and Elijah Newren now agree on a pragmatic short-term plan: revert the regressing commit (6257588252) entirely for the Git 2.56 release cycle, deferring both Patrick Steinhardt’s targeted fix and the broader architectural question until after the release.

Junio endorses Elijah’s suggestion to revert rather than ship a partial fix, calling it a "clean start" and explicitly rejecting the idea of rushing a new mechanism into -rc2. His tone is light ("castrating 'git commit'" is a joke about the long-term proposal to restrict plain `git commit` during conflict resolution), but the decision is firm: the revert will happen before Git 2.56 final. The rationale is release stability—neither the `MERGE_MSG`-based approach nor Elijah’s alternative (`rebase-merge/stopped-head`) has had enough scrutiny to be trusted this close to release.

The broader architectural question—whether plain `git commit` should be rejected outright during conflict resolution for sequencer-based commands—is now clearly deferred to post-3.0, not just post-2.56. Junio’s phrasing ("post-3.0, not post-2.56") signals that this is a major design change, not a quick follow-up.

No code has been written or merged yet; this email is purely about release planning. The next step is for Junio to prepare the revert commit, which will restore the pre-2.56 behavior and unblock users who need to amend commits during interactive rebase. The thread’s focus will likely shift to reviewing that revert patch once it is posted.