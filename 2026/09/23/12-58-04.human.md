This is the first patch in a four-part bugfix series for `git stash`. It’s a simple cleanup: removes an unused `#include "oid-array.h"` from `builtin/stash.c`. The header isn’t needed—none of its functions or types are used—and the transitively included `hash.h` declarations are already covered by a direct include. The change is trivial (one line deleted) and was already accepted by Junio as a “no-brainer” in the v1 discussion. No functional impact; just housekeeping.