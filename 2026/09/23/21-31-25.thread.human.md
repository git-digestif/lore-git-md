This thread is a minor documentation fix to the `git-rm` man page. A first-time contributor, parovozik, submitted a one-line patch removing an unnecessary comma in the description of the `--` option. The change is purely stylistic, with no functional impact, and touches only the `Documentation/git-rm.adoc` file. The patch is correctly formatted, applies cleanly, and includes the required Signed-off-by trailer. Given its trivial nature, it’s unlikely to spark discussion and should be merged without controversy once reviewed.