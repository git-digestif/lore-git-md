This is a surface-level review of a documentation patch. Junio C Hamano replies to parovozik’s trivial comma-removal patch in `Documentation/git-rm.adoc` with two observations: (1) the Signed-off-by trailer should use the contributor’s real name, not a pseudonym, per `Documentation/SubmittingPatches`, and (2) the same phrasing appears elsewhere in the documentation (`git grep` reveals one more instance), so the comma should be removed there as well to keep the text consistent. No technical objections are raised; the feedback is purely about project conventions and copy-editing hygiene. The patch remains uncontroversial and is likely to be accepted once the author resends with the requested adjustments.