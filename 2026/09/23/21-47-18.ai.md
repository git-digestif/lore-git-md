This is a **post-merge test-infrastructure fixup discussion** for the `http.sslVerifyStatus` security feature. Junio C Hamano replies to SZEDER Gábor’s test failure report, acknowledging the two issues Gábor identified: the overly permissive `SSL_VERIFYSTATUS` prerequisite and the test’s invalid expectation that a revoked certificate should be accepted without `http.sslVerifyStatus`.

Junio’s response is procedural rather than technical. He apologizes for the premature merge to `next` and announces a revert of the topic from `next` to give the test suite a "clean slate" for a follow-up fix. The revert is purely integration hygiene—no objection to the feature itself—and the patch remains in `master`. Junio explicitly praises Gábor’s suggestion to replace the negative-string-match prerequisite with a positive test, calling it "constructive and useful."

The thread is now waiting for a follow-up patch that (1) tightens the `SSL_VERIFYSTATUS` prerequisite to only pass when staple verification is actually available, and (2) isolates OCSP stapling from CRL checks in the test environment so the test’s expectation (revoked certificate accepted without `http.sslVerifyStatus`) holds. Gábor’s trace shows libcurl 7.81.0 rejects revoked certificates via CRLs even when stapling is disabled, so the test setup must either disable CRL checks or adjust its expectation. The two minor code-quality nits (exact error message check and `export` usage) are also still open.