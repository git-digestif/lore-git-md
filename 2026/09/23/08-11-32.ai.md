This is a **thread update** in the ongoing `parse-options` early-scanning API series. Christian Couder replies to Junio C Hamano’s skepticism about the practical value of fixing `--` detection in `git bisect` when a term name is `--`. In response, Christian **drops the `git bisect` conversion patches entirely** from v2, narrowing the series to focus on the more compelling `git fast-import` bugfix and the core API. This resolves Junio’s concern without further debate, simplifying the series and removing a potential blocker. The decision to defer the `git bisect` conversion to a future effort (when the command adopts the full `parse-options` API) keeps the door open for later adoption while avoiding controversy now. The thread’s trajectory is now clearer: the series is **leaner, more focused, and more likely to advance**, with the remaining patches addressing a real bug and introducing a minimal, reusable API.