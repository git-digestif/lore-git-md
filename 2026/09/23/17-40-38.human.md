This is the author’s reply to Junio’s request to split the second patch in the `git repo info` path-keys series. The patch currently adds `path.superproject-root` keys but also fixes a latent bug in `get_superproject_working_tree()`—a function shared with `git rev-parse --show-superproject-working-tree`. Junio suggested splitting the patch into two: one to fix the function (with tests in `t1500-rev-parse.sh`) and another to introduce the new keys (with tests in `t1900-repo-info.sh`). The author agrees in principle but pushes back on the test location, arguing that since the bug affects `git repo info` as well, `t1500` (which tests `git rev-parse`) is not the best place for the fix’s tests.

The response is brief and pragmatic. The author does not dispute the need for the split—acknowledging it as a reasonable request—but focuses on the mechanical detail of where to place the tests. This is a **high-weight mechanical issue** that blocks further review of the patch, but it is not a design or correctness dispute. The author’s concern about test location is valid: the bug affects both commands, and isolating the fix’s tests in `t1500` could obscure its relevance to `git repo info`. However, the pushback is mild and leaves room for compromise (e.g., adding tests in both locations or choosing a more neutral test file).

No progress has been made on the other high-weight issues in the series—the unresolved `path.index` correctness issue (where `repo_get_index_file()` invokes `BUG()`) and the pending implementation of the `path.cdup` edge-case fix. The series remains **feature-complete and mechanically sound**, but this patch is now blocked on the refactoring. Reviewers should expect the next iteration to include the split, with tests likely placed in `t1900-repo-info.sh` or a new shared test file. The author’s willingness to comply suggests this will be resolved quickly.