This is the first patch in Souma’s v3 series teaching `git history` to sign rewritten commits. It refactors the replay machinery (`replay.c` and `replay.h`) so that callers can pass a signing key through the `replay_revisions_options` struct. The change is purely plumbing: no user-visible behavior is altered yet, but the API is now ready for the next patch to wire up the command-line and config options. The diff is small (14 lines added, 5 removed) and mechanical, touching only the internal commit-creation path. All substantive review feedback from earlier rounds has been addressed, leaving only surface-level cleanups in the queue.