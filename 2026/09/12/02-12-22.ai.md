This is a brief follow-up in the refs/files-backend bugfix thread. Ariel Keselman acknowledges sending v3 of the patch as a new thread by mistake and provides a link to it. The email is a direct reply to Patrick Steinhardt’s review of v2, but contains no new technical content—it only clarifies the location of the updated patch.

The core fix (skipping packed-refs lock for root ref deletions) remains unchanged. Patrick’s feedback on v2 was purely about test robustness: adding an explicit check that `AUTO_MERGE` is not packed after `git pack-refs --all`, using `git refs list --include-root-refs` to capture full ref state in the mixed-transaction test, and minor test-script cleanups (dropping redundant `start/prepare/commit` lines, inlining heredocs). Ariel’s v3, now posted separately, incorporates these suggestions.

No substantive discussion or objections are in play; the thread is effectively resolved, with the final version of the patch available in the linked v3. The fix addresses a real regression in linked worktrees with read-only shared metadata, where post-commit cleanup (e.g., deleting `AUTO_MERGE`) spuriously fails due to an unnecessary `packed-refs.lock`. The patch is minimal, well-tested, and uncontroversial.