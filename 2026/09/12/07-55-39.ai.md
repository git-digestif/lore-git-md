This is a CI/build system patch. Harald Nordgren removes a redundant `brew link --force gettext` command from the macOS CI dependency installation script. The commit message explains that gettext is already linked on the macOS CI runner image, so the command was a no-op that only produced a spurious "Already linked" warning in every macOS job log. The change is minimal—one line deleted from `ci/install-dependencies.sh`—and is clearly motivated by reducing log noise rather than fixing a functional issue.

The patch is self-contained and unlikely to be controversial. It touches only the CI infrastructure, not the core Git codebase, and the rationale is straightforward. The author is not a frequent contributor, but the change is simple enough that it does not require deep domain expertise. No test failures or edge cases are expected, as the command being removed was already inert.

This is a single-patch series with no cover letter or prior discussion in the provided thread context. The change is specific to GitHub Actions CI and does not affect other platforms or build systems.