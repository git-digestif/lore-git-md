Thread identity
Subject: [PATCH] ci: remove redundant `brew link --force gettext` on macOS
Series version: single-patch, no prior iterations
Originating author: Harald Nordgren
Thread type: patch (CI/build system)

Core problem or goal
Remove a redundant `brew link --force gettext` command from the macOS CI dependency installation script (`ci/install-dependencies.sh`). The command was a no-op (gettext is already linked on the macOS CI runner image) and only produced a spurious “Already linked” warning in every macOS job log. The goal is to reduce log noise.

Current status
Under review; no prior versions or discussion in this thread.

Key technical details
- File touched: `ci/install-dependencies.sh` (one line deleted)
- Subsystem: GitHub Actions CI (macOS runner only)
- No new symbols, CLI options, or on-disk format changes
- No test coverage implications

Open questions and loose ends
None identified; the change is self-contained and uncontroversial.

Participant positions
None recorded yet; the patch has not been reviewed on-list.

Related work
None.