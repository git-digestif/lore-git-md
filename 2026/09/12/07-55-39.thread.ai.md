Thread identity
Subject: [PATCH v2] ci: remove redundant `brew link --force gettext` on macOS
Series version: v2 (single-patch series, second iteration)
Originating author: Harald Nordgren
Thread type: patch (CI/build system)

Core problem or goal
Remove a redundant `brew link --force gettext` command from the macOS CI dependency installation script (`ci/install-dependencies.sh`). The command was added to ensure gettext (a keg-only Homebrew formula) is symlinked into the system path, but a Homebrew policy change in 2020 made gettext no longer keg-only, so a fresh install now automatically links it. The goal is to reduce log noise (spurious “Already linked” warnings in every macOS job log) while ensuring gettext remains correctly exposed in the system path.

Current status
Under review; v2 addressed all open feedback from v1. The patch is now unblocked: both the historical rationale (Homebrew policy change in 2020) and empirical validation (a passing macOS CI pipeline) have been provided. No further technical questions or objections are open. The patch is ready for final review and likely to proceed unless new concerns arise.

Key technical details
- File touched: `ci/install-dependencies.sh` (one line deleted)
- Subsystem: GitHub Actions CI (macOS runner only)
- No new symbols, CLI options, or on-disk format changes
- No test coverage implications
- Homebrew context: gettext was keg-only prior to 2020, requiring `--force` to symlink into `/usr/local`; it is now linked by default on fresh installs, making the command redundant.

Open questions and loose ends
None. The sole open question (empirical validation) has been resolved by Harald’s new CI pipeline link showing the macOS job passing with the patch applied.

Participant positions
- **Patrick Steinhardt**: Substantive review in v1; requested empirical evidence (CI pipeline links) and historical rationale for the command’s original addition and current redundancy. Both requests have now been satisfied: the historical rationale was addressed in v2 (Homebrew policy change in 2020), and the empirical evidence was provided in the latest reply (passing macOS CI pipeline). Position is now closed; no further objections.
- **Harald Nordgren**: Provided empirical evidence in v1 (GitHub Actions CI pipeline link, later invalidated due to skipped macOS job) and addressed the historical rationale in v2 (Homebrew policy change in 2020). In the latest reply, Harald provided new empirical evidence (a passing macOS CI pipeline) that confirms the patch’s safety, unblocking the thread. Position is now closed.