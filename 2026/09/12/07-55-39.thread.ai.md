Thread identity
Subject: [PATCH v2] ci: remove redundant `brew link --force gettext` on macOS
Series version: v2 (single-patch series, second iteration)
Originating author: Harald Nordgren
Thread type: patch (CI/build system)

Core problem or goal
Remove a redundant `brew link --force gettext` command from the macOS CI dependency installation script (`ci/install-dependencies.sh`). The command was added to ensure gettext (a keg-only Homebrew formula) is symlinked into the system path, but a Homebrew policy change in 2020 made gettext no longer keg-only, so a fresh install now automatically links it. The goal is to reduce log noise (spurious “Already linked” warnings in every macOS job log) while ensuring gettext remains correctly exposed in the system path.

Current status
Under review; v2 addresses all open feedback from v1. The patch itself is unchanged (one line deletion in `ci/install-dependencies.sh`), but the commit message now includes the historical rationale (Homebrew policy change in 2020) and links to the relevant Homebrew PR, satisfying Patrick Steinhardt’s requests for empirical evidence (already provided in v1 via a passing GitHub Actions CI pipeline link) and historical context. No further technical questions remain; the ball is back in the reviewer’s court.

Key technical details
- File touched: `ci/install-dependencies.sh` (one line deleted)
- Subsystem: GitHub Actions CI (macOS runner only)
- No new symbols, CLI options, or on-disk format changes
- No test coverage implications
- Homebrew context: gettext was keg-only prior to 2020, requiring `--force` to symlink into `/usr/local`; it is now linked by default on fresh installs, making the command redundant.

Open questions and loose ends
- None; all feedback from v1 has been addressed. The patch is ready for final review and potential approval.

Participant positions
- **Patrick Steinhardt**: Substantive review in v1; requested empirical evidence (CI pipeline links) and historical rationale for the command’s original addition and current redundancy. Both requests have now been satisfied in v2. Position is now closed pending final assessment of the updated commit message.
- **Harald Nordgren**: Provided empirical evidence in v1 (passing GitHub Actions CI pipeline link) and addressed the historical rationale in v2 (Homebrew policy change in 2020, linked PR). The v2 commit message is thorough and self-contained, making the patch’s motivation clear. No further action required from Harald.