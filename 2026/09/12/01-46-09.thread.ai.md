Thread identity
Subject: [PATCH v3] refs/files-backend: skip packed transaction for root ref deletion
Series version: v3 (third iteration)
Originating author: Ariel Keselman
Thread type: bugfix patch series (single patch)

Core problem or goal
Fix an inefficiency in the refs files backend where deleting root refs (e.g., `AUTO_MERGE`, `CHERRY_PICK_HEAD`) unnecessarily attempts to lock the packed-refs file. Root refs are never packed, so the lock acquisition is redundant and causes `git update-ref --no-deref -d AUTO_MERGE` to fail when `.git/packed-refs.lock` is held. This also breaks post-commit cleanup in linked worktrees with read-only shared metadata.

Current status
**Queued in Junio’s `next` branch** for integration testing; **approved by Patrick Steinhardt**. v3 posted, incorporating substantive feedback from Patrick. Junio C Hamano explicitly stated he will queue the patch to `next` post-`rc1`; graduation to `master` is deferred until after Git 2.56 final unless deemed an emergency critical fix.

Key technical details
- Files touched: `refs/files-backend.c` (core logic), `t/t0600-reffiles-backend.sh` (tests)
- New/renamed symbols: none; uses existing `is_root_ref()` helper
- Behavior change: skips packed-ref transaction for root ref deletions only; preserves loose-ref locking and packed-ref deletion for non-root refs
- Flags involved: `REF_DELETING`, `REF_LOG_ONLY`, `REF_IS_PRUNING`
- Test coverage: adds tests for root ref deletion with `packed-refs.lock` held, mixed transactions (root + packed ref), and post-`pack-refs` loose ref verification
- On-disk format: unchanged

Open questions and loose ends
None. The patch is self-contained, addresses the inefficiency, and carries no unresolved edge cases. No new concerns were raised in Junio’s queuing notice.

Participant positions
- Ariel Keselman (author): identified the inefficiency, implemented the fix, and incorporated feedback across v1–v3.
- Patrick Steinhardt (reviewer): provided substantive feedback on v2, leading to v3; **explicitly approved v3** with “this version looks good to me.”
- Junio C Hamano (maintainer): **queued the patch to `next`** in response to Patrick’s approval; no additional technical feedback.

Related work
None mentioned in the thread. The patch is standalone.