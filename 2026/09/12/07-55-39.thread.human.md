This thread is a small but practical improvement to Git's macOS CI workflow. Harald Nordgren submitted a patch to remove a redundant `brew link --force gettext` command from the GitHub Actions dependency installation script. The command was already a no-op—gettext is pre-linked on the macOS CI runner image—so its only effect was to clutter job logs with a harmless but unnecessary "Already linked" warning. The change is a one-line deletion in `ci/install-dependencies.sh` and doesn’t touch any core Git code.

The patch is straightforward and unlikely to draw controversy. It’s a self-contained fix targeting log noise rather than functionality, and since the command was inert, there’s no risk of breaking the build. Harald isn’t a frequent contributor, but the simplicity of the change means it doesn’t need deep review scrutiny. No follow-up discussion or version updates are expected—this should land as-is once a maintainer picks it up.