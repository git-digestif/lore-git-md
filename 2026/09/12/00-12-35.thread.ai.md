Thread identity
Subject: [PATCH] completion: add support for `git worktree repair`
Series version: standalone patch (v2)
Originating author: Yoichi NAKAYAMA
Thread type: patch (completion script update)

Core problem or goal
Add shell completion support for the `git worktree repair` subcommand in the completion scripts (`contrib/completion/git-completion.bash` and related), following the established pattern for other `git worktree` subcommands.

Current status
Approved and ready for integration. v2 posted with an adjusted commit message (imperative mood, generic reference to "completion scripts" covering bash, zsh, and tcsh). Code unchanged from v1 and uncontroversial. Patrick Steinhardt’s review explicitly approves the patch for merging ("Thanks, this looks good to me!") and confirms the commit message update resolves prior framing concerns ("Ah, fair. We know to reuse the Bash completion scripts for both of these."). No further technical discussion or objections expected.

Key technical details
- File touched: `contrib/completion/git-completion.bash`
- New subcommand added: `repair`
- Completion behavior: extends the existing path-completion case pattern to cover `repair` alongside `lock`, `remove`, and `unlock`
- No new symbols, CLI options, or on-disk format changes
- No test or documentation changes (typical for completion script updates)

Open questions and loose ends
None; the patch is approved and ready for integration.

Participant positions
Patrick Steinhardt: explicit approval for merging, unchanged from prior review. Acknowledges commit message update as sufficient.

Related work
None.