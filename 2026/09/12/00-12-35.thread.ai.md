Thread identity
Subject: [PATCH] completion: add support for `git worktree repair`
Series version: standalone patch (no series)
Originating author: Yoichi NAKAYAMA
Thread type: patch (completion script update)

Core problem or goal
Add shell completion support for the `git worktree repair` subcommand in `contrib/completion/git-completion.bash`, following the established pattern for other `git worktree` subcommands.

Current status
Under review; no prior versions. Patch is a single commit with no dependencies. The code itself is uncontroversial and described by Patrick Steinhardt as "looking good." Awaiting a v2 with an adjusted commit message (imperative mood, optionally including a brief note that completion was previously missing).

Key technical details
- File touched: `contrib/completion/git-completion.bash`
- New subcommand added: `repair`
- Completion behavior: extends the existing path-completion case pattern to cover `repair` alongside `lock`, `remove`, and `unlock`
- No new symbols, CLI options, or on-disk format changes
- No test or documentation changes (typical for completion script updates)

Open questions and loose ends
None technical; the only remaining action is a commit message tweak.

Participant positions
Patrick Steinhardt: surface-level review (commit message style only); no technical objections; code "looks good." Position unchanged.

Related work
None.