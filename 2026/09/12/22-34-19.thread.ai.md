Thread identity
Subject: [PATCH] pull: prevent segfault on invalid merge head
Series version: standalone patch (no series)
Originating author: Jiri Kuncar
Thread type: bugfix patch

Core problem or goal
`git pull` segfaults when `lookup_commit_reference()` returns NULL because a merge head reference is corrupted (e.g., by concurrent operations like parallel fetches or garbage collection). The patch adds NULL guards in `builtin/pull.c` so that the command fails gracefully instead of crashing, treating the failed lookup as “not up to date” and falling back to a normal merge that reports the broken object.

Current status
Under review; no prior versions.

Key technical details
- Files touched: `builtin/pull.c`, `t/t5520-pull.sh`
- New symbols: none
- Behavior change: `git pull` no longer segfaults on invalid merge heads; instead it reports the broken object via the normal merge machinery
- On-disk format: unchanged
- Test coverage: new test in `t/t5520-pull.sh` that (1) creates a two-branch repo, (2) clones it, (3) truncates an object file to simulate corruption, (4) sets `fetch.unpackLimit=1000` to force loose object storage, (5) runs `git commit-graph write` to ensure reachability, and (6) verifies graceful failure

Open questions and loose ends
None yet; the patch is self-contained and includes both fix and test.

Participant positions
None recorded yet; first email in thread.