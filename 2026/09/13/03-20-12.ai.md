This is the first patch in a two-part bugfix series for `git worktree repair`. It refactors the code in `worktree.c` to reduce redundant `.git` file reads and extract the worktree ID, setting up the validation logic that will be added in patch 2/2.

The patch replaces `read_gitfile_gently()` with `read_gitfile_raw()` in `repair_worktree_at_path()`, which simplifies error handling and avoids reading the `.git` file multiple times. It also extracts the worktree ID parsing logic into a new helper function, `get_worktree_id()`, which will be used in the next patch to validate the repair target. The refactoring consolidates path construction and error checking, making the code more maintainable and paving the way for the actual fix.

The changes are confined to `worktree.c` and are purely internal—no new symbols are introduced, and the behavior of `git worktree repair` remains unchanged for now. The commit message is clear and explains the motivation for the refactoring, though the real payoff (the validation logic) comes in the next patch. This looks like a well-structured preparatory step for the bugfix.