This is a **CI/build system** follow-up in the thread enabling Rust compilation in Git’s GitHub Actions Windows CI jobs. The email captures a brief but decisive exchange between Johannes Schindelin and James Le Cuirot that resolves the last open technical question in the series.

Johannes confirms that James’s recommendation—**omitting the `--target` flag (and `CARGO_BUILD_TARGET`) entirely for native builds**—works as intended once the variable is explicitly exported in the Makefile. He also accepts Junio’s earlier suggestion to rename `CARGO_TARGET` to `CARGO_BUILD_TARGET` for consistency with the recently merged `jc/rust-cargo-build-target` topic. The change is mechanical: the conditional in `CARGO_ARGS` will now read `$(if $(CARGO_BUILD_TARGET),--target $(CARGO_BUILD_TARGET))`, and the variable will only be set for cross-compilation cases (e.g., `aarch64` builds on `x86_64` runners).

No new objections or edge cases are raised; the discussion is purely about aligning the patch with existing build-system conventions. The series is now ready for a v3 revision that adopts these adjustments. Once posted, the two-patch series will complete the Windows CI Rust toolchain configuration, unblocking the broader Rustification effort by ensuring Cargo produces a static library (`libgitcore.a`) compatible with MinGW’s `ld.exe`. Testing on GitLab’s Windows runners remains an open request but is not a blocker for merging.