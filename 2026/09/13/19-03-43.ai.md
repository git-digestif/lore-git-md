This is a gentle ping in the `git fetch --prune-tags` bugfix thread. The author, Orgad Shaneh, follows up on Junio C Hamano's earlier review comment about test coverage. Junio had noted that the new test in `t5516-fetch-push.sh` only verifies that `git fetch` exits successfully when `--prune-tags` is used, not whether the tag is actually updated. Orgad clarifies that the test is checking tag *replacement* (not deletion) and points to existing pruning tests in `t/t5510-fetch.sh`. He offers to expand the test to verify tag content if Junio thinks it necessary.

The thread remains stalled on the core question: whether the current behavior (no force-update of tags) is a bug or an intentional design choice. Orgad has provided historical context suggesting it was likely an oversight, but Junio has not yet weighed in on the substance. The patch itself is minimal -- a one-line change in `builtin/fetch.c` to pass `TAG_FETCH_FORCE` when `--prune-tags` is active -- and the author is responsive to feedback. No new technical details emerge here; this email is purely a procedural nudge to keep the discussion moving. The next step likely depends on Junio's response or input from Ævar Arnfjörð Bjarmason, who implemented the original `--prune-tags` feature.