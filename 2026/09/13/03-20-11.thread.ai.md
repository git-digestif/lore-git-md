Thread identity
Subject: [PATCH 0/2] worktree repair: fix incorrect repair of unrelated worktrees
Series version: v1 (initial submission)
Originating author: Yoichi NAKAYAMA (via GitGitGadget)
Thread type: patch series (bugfix)

Core problem or goal
The `git worktree repair` command can incorrectly modify `.git` files or gitdirs belonging to unrelated worktrees when cross-references between a linked worktree and its administrative data are not validated before repair. The goal is to add validation that ensures the repair target is correct before making changes, preventing accidental corruption of unrelated worktrees. The issue manifests in edge cases such as manually swapped worktree directories or copied linked worktrees, where `git worktree repair` would previously operate on the wrong administrative files without checks.

Current status
Under review (no prior versions). The series is complete: patch 1/2 refactors code to reduce redundant `.git` file reads and extract the worktree ID; patch 2/2 adds validation logic using that ID to prevent repairs on unrelated worktrees. No merge status reported; no "What's cooking" citation.

Key technical details
- Files touched: `worktree.c`, `t/t2406-worktree-repair.sh`
- New/renamed symbols: `get_worktree_id()` (new helper function, internal use only)
- Behavior change: `git worktree repair` now validates the worktree ID and gitdir path before making changes, skipping repairs if the target is unrelated. The command will no longer corrupt administrative files of unrelated worktrees.
- On-disk format: No changes
- Test coverage: Updated `t/t2406-worktree-repair.sh` with two new test cases: (1) skipping unrelated `.git` files, and (2) handling `.git` files that do not belong to a linked worktree.

Open questions and loose ends
None identified. The series is narrowly scoped, addresses the stated problem, and includes test coverage for the new behavior. No review feedback has been received yet.

Participant positions
- Yoichi NAKAYAMA: Author. Submitted the series with clear motivation, a two-patch structure (refactoring + validation), and thorough test coverage. No prior reviews or objections recorded.

Related work
None mentioned. This is a standalone bugfix for the worktree subsystem. No dependencies or interactions with other in-flight topics.