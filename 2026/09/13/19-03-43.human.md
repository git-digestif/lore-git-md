This is a gentle ping in the ongoing bugfix thread about `git fetch --prune-tags` behavior. The author, Orgad Shaneh, is following up on Junio C Hamano's feedback from earlier in the thread, where Junio asked for clearer test coverage to verify that `--prune-tags` actually clobbers (force-updates) existing tags when they differ from the remote.

Orgad had previously explained that his tests focus on tag *replacement* rather than deletion, and that existing pruning tests (in `t/t5510-fetch.sh`) already cover tag deletion. He now offers to expand the test coverage to include explicit content verification for tag updates, which would address Junio's concern about whether the patch achieves its stated goal. The tone is collaborative, and Orgad is clearly waiting for guidance on whether this additional test work is needed before the patch can move forward.

The thread remains unresolved on the core question: whether the current behavior (no force-update) is a bug or an intentional design choice. The historical context Orgad provided—showing that `--prune-tags` predates tag clobbering protection—still suggests this was likely an oversight, but Junio's hesitation indicates the maintainer wants to be sure before changing long-standing behavior. No new technical details or objections have emerged in this email; it's purely a procedural nudge to keep the discussion moving. The ball is now in Junio's court to clarify whether he wants expanded tests or if the current patch is sufficient.