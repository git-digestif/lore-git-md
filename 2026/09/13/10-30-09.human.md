This is a **CI/build system** follow-up in the ongoing effort to enable Rust compilation in Git’s GitHub Actions Windows CI jobs. The email captures a brief but decisive exchange between Johannes Schindelin and James Le Cuirot, confirming how to adjust the patch series to align with Cargo’s expected behavior.

Johannes acknowledges James’s feedback: the series should **not** set `--target` (or `CARGO_BUILD_TARGET`) for native builds, and the variable name should be `CARGO_BUILD_TARGET` (not `CARGO_TARGET`) to match the recently merged `jc/rust-cargo-build-target` topic. He reports a quick experiment that confirms the change works once `CARGO_BUILD_TARGET` is explicitly exported in the Makefile. The adjustment is mechanical and does not alter the series’ core goal—configuring Rust to produce a GCC-compatible static library for MinGW in Windows CI.

The patch series is now expected to be revised accordingly: drop the explicit `--target` flag for native builds, adopt `CARGO_BUILD_TARGET` for cross-compilation cases, and ensure the variable is exported. This keeps the build system consistent with upstream Cargo conventions and avoids subtle behavioral differences that could break assumptions (e.g., `RUSTFLAGS` not applying to build-time tools). The change is narrow and should not affect the series’ readiness for review.