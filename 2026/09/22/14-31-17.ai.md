This is the first patch in a test modernization series (v5, 1/3) targeting the `t40*` test scripts. The author, Mark C. Chu-Carroll, updates `t4001-diff-rename.sh` and `t4009-diff-rename-4.sh` to align with current Git test conventions. Changes are purely stylistic and structural: replacing setup functions with `test_expect_success 'setup'` blocks, standardizing indentation and here-doc syntax (`<<-`), improving test naming for clarity, and consolidating commands into single test blocks. The patch removes redundant `echo` commands and renames output files from `expected`/`current` to the more conventional `expect`/`actual`.

The diffstat shows a net reduction in lines (66 added, 79 removed), reflecting streamlined test structure rather than new coverage. No test logic or behavior is altered—this is a mechanical cleanup. The changes address prior feedback about readability and error-message clarity, and the patch appears ready for integration. This is part of the broader community effort to modernize Git’s test suite, with no dependencies on other in-flight topics. Key files touched: `t/t4001-diff-rename.sh` and `t/t4009-diff-rename-4.sh`.