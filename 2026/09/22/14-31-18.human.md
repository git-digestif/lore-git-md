This is the second patch in Mark C. Chu-Carroll’s three-patch series modernizing the `t40*` test scripts. It updates `t4009-diff-rename-4.sh` to align with current Git test conventions without altering the test logic or coverage.

The changes are purely stylistic and structural: the patch replaces standalone setup commands with `test_expect_success 'setup'` blocks, renames output files from `expected`/`current` to `expect`/`actual`, switches here-documents to use `<<-` for consistent indentation, and improves test naming for clarity. The diffstat shows a net reduction in lines (66 removed, 60 added), reflecting the cleanup.

This patch is part of a well-scoped, uncontroversial effort to reduce technical debt in the test suite. The author has incorporated feedback across multiple iterations (this is v5), and the changes are mechanical rather than behavioral. The series appears ready for `next`, pending any final nitpicks. No new test cases or functionality are introduced—only the presentation layer is updated.