This is the third and final patch in Mark C. Chu-Carroll’s v5 test modernization series, targeting `t4010-diff-pathspec.sh`. The patch mechanically updates the test script to align with current Git test conventions—no behavioral changes, only structural and stylistic improvements.

The changes mirror those in the earlier patches: setup logic is folded into the first `test_expect_success` block, here-documents use `<<-` for consistent indentation, output files are renamed from `expected`/`current` to `expect`/`actual`, and test names are made more descriptive. The diffstat shows a slight net reduction in lines (59 added, 61 removed), reflecting the cleanup.

This patch completes the series, which has iterated through five versions incorporating reviewer feedback on style and structure. The entire effort is uncontroversial and appears ready for `next`; no substantive objections remain. If you’ve been following the series, this is the last piece—nothing new in scope or motivation, just the same modernization applied to the final test file.