Thread identity
Subject: Feature request: fast, low-disruption removal of personal information from Git history
Series version: none (standalone feature request)
Originating author: JESUS rose to life <christrosetolife@gmail.com>
Thread type: feature request / user-experience wish

Core problem or goal
Allow users to purge specific sensitive data (passwords, PII) from a public Git repository’s history without rewriting commit hashes, so that existing clones remain functional and development can continue uninterrupted.

Current status
Under discussion; no implementation proposed. The request is framed as a user-experience wish rather than a technical design. No prior art (filter-repo, git replace, git notes) is referenced, and no trade-offs (recoverability from reflog or alternate object stores) are acknowledged.

Key technical details
None provided in the initial email. Likely follow-up topics:
- How purged data is identified (path, regex, object ID)
- Whether objects are physically deleted or merely hidden
- How clients discover the purge (new ref, server-side hook, client-side fetch filter)
- Server-side vs. client-side support

Open questions and loose ends
- Identification mechanism for sensitive data
- Physical vs. logical deletion
- Client discovery mechanism
- Recoverability from reflog or alternate object stores
- Compatibility with existing clones and workflows

Participant positions
None yet; this is the thread root.

Related work
None mentioned. Existing tools (git filter-repo, git filter-branch, BFG) are disruptive by design (rewrite history), which the author explicitly wants to avoid.