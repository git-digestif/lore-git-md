Thread identity
Subject: [PATCH] git-p4: prevent shell injection from user-supplied commit ID
Series version: standalone patch (no series)
Originating author: Anupam Mediratta (via GitGitGadget)
Thread type: security bugfix (patch)

Core problem or goal
A shell injection vulnerability exists in `git-p4.py`'s `applyCommit()` function. When a user supplies a commit ID via the `--commit` option, the value is interpolated into a shell pipeline (`git diff-tree ... | git apply ...`) without validation. An attacker can craft a commit ID containing shell metacharacters (e.g., `$(touch evil)`) that will be executed during command substitution, leading to arbitrary command execution.

Current status
Under review. The patch is a minimal, surgical fix that replaces the vulnerable shell pipeline with two direct subprocess calls (`read_pipe`, `p4_system`) connected by a pipe, eliminating the shell entirely. A regression test in `t9830-git-p4-shell-injection.sh` verifies that `git p4 submit --commit` with a shell metacharacter payload does not execute it.

Key technical details
- **File touched**: `git-p4.py` (specifically the `applyCommit()` function)
- **Subsystem**: `git-p4` (bidirectional Git-Perforce bridge)
- **Vulnerability**: Shell injection via unvalidated `--commit` argument
- **Fix**: Replace `os.system(shell=True)` pipeline with direct subprocess calls (`read_pipe`, `p4_system`)
- **Test coverage**: New test `t9830-git-p4-shell-injection.sh` exercises the attack vector
- **Symbols**: No new or renamed symbols; the fix modifies existing control flow
- **Behavior change**: The `--commit` option is now safe from shell injection; no functional change for benign inputs
- **On-disk format**: No changes

Open questions and loose ends
None identified. The patch is self-contained, addresses the vulnerability, and includes test coverage. No objections or requests for changes have been raised in the thread.

Participant positions
- **Anupam Mediratta**: Author. Provided a thorough commit message explaining the problem, fix, and test. No prior positions recorded in this thread.

Related work
None mentioned. This is a standalone security fix with no dependencies or prior versions.