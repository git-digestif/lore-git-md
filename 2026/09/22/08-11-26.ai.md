This is a status update from Kristoffer Haugsbakk on the `kh/format-patch-range-diff-notes` topic, which is currently stalled awaiting a reroll. The series teaches `` `git format-patch` `` a new `--[no-]range-diff-notes` option so that the notes shown in the range-diff section can be configured separately from the notes displayed on the patches themselves. The three-patch series also includes preparatory clean-ups: renaming a struct member in `revision.h` to clarify its role and simplifying the `get_notes_arg()` helper’s parameter list.

The topic was last marked “Expecting a reroll” in Junio’s “What’s cooking” report on 2026-09-22, with a pointer to an earlier review thread. Kristoffer acknowledges the delay and commits to producing the reroll by the coming weekend. No substantive technical feedback is addressed in this message; it is purely a scheduling note. The series touches the `format-patch` command, the revision-walking internals, and the notes API, but does not change on-disk formats or introduce new config keys.