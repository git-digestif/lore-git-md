Thread identity: Two distinct but related documentation improvement efforts in Git's `Documentation/` directory. The first is the **cross-reference modernization** (v2 patch series, originating author Julia Evans), a systematic replacement of informal plain-text section references with formal AsciiDoc link syntax (`<<target,text>>`) across 38 man pages to improve HTML navigation. The second is a **proposed rewrite of the Git tutorial** (`gittutorial.adoc`), also by Julia Evans, aiming to simplify the tutorial by focusing on a smaller set of commands (`git init`, `git add`, `git commit`, `git status`, `git diff`, `git push`, `git config`, `git remote add`) and omitting branches entirely, with the intention of covering branches in a separate follow-up tutorial (`gittutorial-2.adoc`). Both threads are documentation improvements but address different aspects: markup/toolchain behavior (cross-reference modernization) and pedagogy (tutorial rewrite).

Core problem or goal:
1. **Cross-reference modernization**: Replace informal references (e.g., "see EXAMPLES below") with formal AsciiDoc cross-references to improve navigation in HTML-rendered man pages. The effort explicitly addresses toolchain inconsistencies (AsciiDoc vs. Asciidoctor) and the trade-off between link stability (manual anchors) and readability (auto-generated anchors). The necessity of the two-part link form (`<<target,text>>`) is confirmed to avoid rendering edge cases (e.g., `[REMOTES]` in HTML or duplicated text in man pages).
2. **Tutorial rewrite**: Reduce cognitive load for Git beginners by simplifying the tutorial to cover only the most essential commands, deferring branches to a follow-up tutorial. The goal is to make the initial learning experience less overwhelming, with early feedback from test readers (including Jeff King) indicating positive reception. A key structural concern is ensuring that topics omitted from the new tutorial are either discarded, relocated to other Git documentation, or deferred to external resources, rather than left in limbo.

Current status:
1. **Cross-reference modernization**: Patch v2 approved by Junio C Hamano and queued for integration in `next` since 2024-07-18. No technical changes were made between v1 and v2; only the commit message was revised for editorial clarification. No merge to `master` yet. The patch remains unchanged, and Jeff King's latest review confirms the updated commit message "looks good" without new technical feedback.
2. **Tutorial rewrite**: Early design proposal stage. Julia Evans has shared a draft in her GitHub fork and is soliciting high-level objections to the structure, not detailed feedback. The patch series is not yet sent to the mailing list, and no review or integration status applies. Junio C Hamano has endorsed the ground-up rewrite approach but flagged a gap in the proposal: the omitted topics from the current tutorial must be triaged into (1) obsolete and discardable, (2) still relevant and needing a home in Git’s documentation, or (3) beyond Git’s scope and better left to external resources. Input is prioritized from actual Git beginners.

Key technical details:
1. **Cross-reference modernization**:
   - Files: 38 files in `Documentation/`, including `git-rebase.adoc`, `git-grep.adoc`, `git-notes.adoc`, and `git-push.adoc`.
   - Change: Systematic replacement of informal references with formal AsciiDoc cross-references using the two-part form (`<<target,text>>`). Manual anchors (e.g., `[[EDITING_PATCHES]]`) are used for section targets, which may break existing external links.
   - Behavior: No functional change to man page content; only HTML navigation is improved. The two-part form ensures consistent rendering across AsciiDoc and Asciidoctor backends, avoiding edge cases where the single form fails (e.g., producing `the section called "EXAMPLES"` or `[EXAMPLES]` in HTML). The rendering inconsistencies stem from the docbook layer, where single-form links are expanded into verbose or inconsistent text.
   - Testing: Author built all man pages before and after the patch and verified no functional differences. Junio spot-checked specific examples. Jeff King independently tested both backends, confirming the two-part form’s reliability and documenting edge cases (e.g., `[REMOTES]` rendering, duplicated text in man pages).
   - Related effort: Part of the ongoing documentation synopsis-style conversion led by Jean-Noël Avila.
2. **Tutorial rewrite**:
   - File: `Documentation/gittutorial.adoc`.
   - Change: Complete replacement of the existing tutorial with a new version covering a smaller set of commands (`git init`, `git add`, `git commit`, `git status`, `git diff`, `git push`, `git config`, `git remote add`). Branches are omitted and deferred to `gittutorial-2.adoc`.
   - Behavior: Pedagogical simplification; no technical implementation details or edge cases discussed yet. The rewrite aligns with the 2026 Contributor’s Summit consensus favoring ground-up rewrites over incremental polishing of tutorial materials.
   - Testing: Early feedback from test readers (including Jeff King) has been positive, but no formal testing or validation has occurred.

Open questions and loose ends:
1. **Cross-reference modernization**:
   - **Anchor style trade-off**: The patch uses manual anchors (e.g., `[[EDITING_PATCHES]]`), preserving existing HTML fragment identifiers but diverging from AsciiDoc toolchain conventions. Auto-generated anchors (e.g., `_editing_patches`) would align with toolchain conventions but break external links. Julia Evans outlines three options: (1) accept breaking some external links (current approach), (2) manually align anchors with auto-generated IDs (error-prone and confusing), or (3) find a way to use the single-form (`<<PRUNING>>`) reliably (ideal but unclear how to implement). The project has not yet decided whether to prioritize link stability or readability, though the current patch is approved and queued. This question remains open for follow-up discussion.
   - Minor typographic inconsistencies (e.g., quotation mark placement around cross-referenced text) were noted by Jeff King but deemed suitable for follow-up work.
2. **Tutorial rewrite**:
   - **Omitted topics triage**: The rewrite omits branches and other topics from the current tutorial. Junio C Hamano has flagged that these omitted topics must be sorted into three buckets: (1) obsolete and discardable, (2) still relevant and needing a home in Git’s documentation, or (3) beyond Git’s scope and better left to external resources. Julia Evans has not yet clarified her plan for these topics, leaving this question open.
   - High-level objections to the proposed structure (e.g., omitting branches from the initial tutorial) are still solicited. No specific technical or pedagogical concerns have been raised beyond the triage question.

Participant positions:
1. **Cross-reference modernization**:
   - Julia Evans (author): Proposed the change, provided rationale, validated the patch by building all man pages, and documented edge cases in AsciiDoc’s behavior. Revised the commit message in v2 to clarify the necessity of the two-part form and the potential for broken links due to anchor changes. Position: Proposes adoption of the two-part form with manual anchors; explicitly frames the anchor style trade-off as a design decision and leans toward accepting link breakage (option 1) due to lower effort, but leaves the question open for discussion. Weight: High (author, thorough testing, clarification, and design framing).
   - Junio C Hamano (maintainer): Reviewed and approved the patch, confirming the design choices and queuing it for integration. Requested and received a reroll of the commit message for editorial clarification but no technical changes. Position: Approves and has queued the current patch; no explicit stance on the anchor style trade-off yet. Weight: High (maintainer decision).
   - Jeff King (Peff): Substantive technical review engaging with AsciiDoc/Asciidoctor behavior, testing both backends, and documenting edge cases. Confirms the two-part form’s necessity for consistent rendering, clarifies that the quoted rendering issue occurs only in man pages, and raises the anchor style trade-off as a conscious design decision. Provides a detailed technical explanation of the underlying toolchain behavior. Latest review confirms the updated commit message "looks good" but adds no new technical feedback. Suggests minor typographic improvements could be addressed in follow-up work. Position: Approves the current patch; flags the anchor style trade-off as a project priority question rather than a flaw. Weight: High (substantive review, testing, domain expertise, and toolchain analysis).
   - Kristoffer Haugsbakk: Surface-level review; endorses the concept as "an obvious idea in hindsight" but does not engage with implementation, testing, or edge cases. Position: Positive but non-substantive. Weight: Low.
2. **Tutorial rewrite**:
   - Julia Evans (author): Proposed the rewrite, shared a draft in her GitHub fork, and solicited high-level objections to the structure. Position: Advocates for simplifying the tutorial to reduce cognitive load for beginners, with early positive feedback from test readers. Has not yet addressed the triage of omitted topics. Weight: High (author, design proposal).
   - Junio C Hamano (maintainer): Endorses the ground-up rewrite approach but raises a structural concern: the omitted topics from the current tutorial must be triaged into discardable, relocatable, or external buckets. References the 2026 Contributor’s Summit consensus favoring ground-up rewrites. Position: Supportive of the rewrite but flags the triage of omitted topics as a prerequisite for integration. Weight: High (maintainer, design framing).
   - Jeff King (Peff): Provided early feedback on the draft, indicating positive reception. Position: Supportive of the rewrite’s goals but no detailed technical review yet. Weight: Medium (early feedback, domain expertise).

Related work:
- The cross-reference modernization effort is part of the ongoing **documentation synopsis-style conversion** led by Jean-Noël Avila.
- The tutorial rewrite aligns with the **2026 Contributor’s Summit consensus** favoring ground-up rewrites of tutorial materials over incremental polishing. No direct dependencies or conflicts with other in-flight topics.