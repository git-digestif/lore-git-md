Here’s the human-mode summary for a developer catching up on the thread:

This thread is Junio’s "What’s cooking" report for September 2026, summarizing the state of in-flight topics as Git 2.56-rc1 is tagged, and the follow-up discussion about the Git 3.0 release timeline, scope, and outcomes from the Contributor’s Summit. The big picture is that Git 2.56-rc1 is out, no new features will land before the final release, and the community is aligning on what Git 3.0 will look like—though the timeline is now explicitly flexible.

Junio proposed a three-phase plan for Git 3.0: Git 2.98 (final 2.x feature release) in December 2026, Git 2.99 (transitional release with breaking changes disabled) in March 2027, and Git 3.0 (same code as 2.99 but with breaking changes enabled) in April 2027 or later. The key update is that the stabilization period for 2.99 may require additional bugfix releases (e.g., 2.99.2 or 2.99.3) before 3.0 is finalized, so downstream projects should plan for potential delays. Rust support remains a goal for 3.0 but isn’t guaranteed due to portability concerns, and SHA-256 interoperability with JGit is technically feasible but lacks active development.

Johannes Schindelin confirmed that the timeline works for Git for Windows’ roadmap, tying it to Rust-based builds and Git Credential Manager v3.0 integration. However, JGit’s readiness for pluggable backends and SHA-256 is now clarified as a resource issue, not a technical one—its infrastructure is viable but under-resourced. Toon Claes and Schindelin also made progress on security process improvements, with Toon committing to document Git’s security model and collaborate on the next security release. Documentation improvements are split between incremental updates (e.g., man pages) and complete rewrites (e.g., tutorials), with Julia Evans’ Sovereign Tech Fund grant providing some funding.

The latest update is Ramsay Jones’s report on building Git with Rust support on Cygwin. Using an unofficial Rust toolchain, he successfully compiled Git on `master` (v2.56.0-rc2 plus one commit), with no new test failures compared to the non-Rust build. While the Rust code is minimal and the toolchain is experimental, this is a positive data point for Rust portability. Ramsay’s absence for surgery means follow-up testing will be delayed, but the report gives the community concrete evidence that Rust can work on Cygwin, even if it’s not yet officially supported.

Open questions include the final scope of Git 3.0 (e.g., which breaking changes will land), whether Rust support will make the cut, and how downstream projects like JGit and distributions will coordinate. The security process and documentation tooling are also works in progress, with Toon and Schindelin leading operational improvements. For now, the focus is on stabilizing Git 2.56, while the Git 3.0 timeline remains a moving target.