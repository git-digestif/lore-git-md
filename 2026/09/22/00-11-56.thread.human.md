Here’s the human-mode narrative summary for a developer catching up on the thread:

This thread is Junio’s regular “What’s cooking in git.git” report for September 2026, issued just after Git 2.56-rc1 was tagged, plus the follow-up discussion about the Git 3.0 release. The report lists which patch series are in `master`, `next`, and `seen`, and flags stalled topics that need review or a reroll. Two small fixes—one for `git pack-refs`/`git refs` documentation and another for pathspec matching—have already graduated to `master`; half a dozen new topics (protocol v2 fetch, OpenBSD runtime-prefix, Coverity fixes, WinANSI error reporting, `git diff --no-index -R`, and GitLab CI Rust setup) are queued for review.

The bigger story is the Git 3.0 timeline. At the Contributor’s Summit the plan was to release Git v2.99.1 and v3.0 simultaneously in early 2027, differing only in breaking-change defaults, with Rust support considered mandatory. Johannes Schindelin recapped that consensus, but Junio’s follow-up email tempers it: while the summit room had no objections to Rust, the mailing list still has unresolved portability concerns, especially for platforms like NonStop. So Rust support is a goal, not a hard requirement, and the final scope may shift depending on how those issues play out. Downstream projects should expect Rust to land, but not count on it until the summit notes are public and the community has weighed in. The rest of the 3.0 roadmap—SHA-256 interoperability, version numbering, and the focus on compatibility over features—remains on track, but the timeline is still fluid.