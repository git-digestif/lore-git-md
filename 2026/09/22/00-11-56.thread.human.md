Here’s a concise human-readable summary of the Git mailing list thread based on Junio’s "What’s cooking" report for September 2026:

The thread is Junio’s regular status update as Git 2.56-rc1 has been tagged, outlining what’s merged, cooking, or under review. Two topics graduated to `master`: documentation fixes for `git pack-refs` and `git refs` (converting to the new AsciiDoc style), and a bugfix for pathspec matching that prevented out-of-bounds memory access with negative pathspecs.

New topics under review include protocol v2 fetch fixes for `uploadpack.allowRefInWant`, OpenBSD `RUNTIME_PREFIX` support, Coverity-driven boundary fixes, WinANSI error reporting improvements, and a `git diff --no-index -R` bugfix. The GitLab CI Windows job now provisions Rust for MinGW builds.

In `next` (candidates for the next release), notable work includes faster compilation via precompiled headers, submodule merge fixes, Coccinelle updates, ref storage terminology unification, a new `receive-report` hook, nanosecond timestamp runtime toggling, and an experimental `git history squash` subcommand. Stalled topics include external diff hunk processing, midx incremental fixes, and high-precision timestamp conversion.

The report also flags Git 3.0 preparation (lowercase-only hex object names) and experimental features like `git format-rev` options and OCSP stapling checks. Git 2.98 will follow 2.56, reflecting the version numbering change from the Contributor’s Summit. No new features are expected to graduate before 2.56 final.