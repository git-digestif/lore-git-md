Here’s the human-mode narrative summary for a developer catching up on the thread:

This thread began as Junio’s September 2026 “What’s cooking” report, summarizing where all in-flight patch series stand as Git 2.56-rc1 was tagged. Since then, the focus has shifted to planning Git 3.0, with Johannes Schindelin’s detailed recap of the 2026 Contributor’s Summit adding crucial context.

Git 2.56-rc1 is now out, and only two small topics—documentation fixes for `git pack-refs`/`git refs` and a pathspec-matching bug—have graduated to `master` since the last report. Everything else is either cooking in `next` (e.g., ODB alternates at clone time, a new `receive-report` hook, and the `git history squash` command), stalled awaiting review or a reroll (notably Kristoffer Haugsbakk’s `git format-patch --range-diff-notes` series), or brand new (protocol v2 fetch fixes, OpenBSD runtime-prefix support, and Coverity-driven clean-ups).

The big story is Git 3.0. The summit proposed a spring 2027 release, with v2.98 (December 2026) and v2.99 (early 2027) as transitional milestones. Rust support was not formally objected to, but portability concerns—especially for the NonStop platform—remain unresolved, so its inclusion is not yet guaranteed. SHA-256 interoperability is technically complete but not upstreamed, and ecosystem adoption (e.g., JGit) is uneven. Johannes has asked Junio to publish a provisional release plan in the next “What’s cooking” report to give downstream projects a baseline for planning, even if the details are still fluid.

Other summit topics that may ripple into future threads include security-process bottlenecks, documentation funding constraints, and the push-protocol-v2 design for large repositories. The recap is Schindelin’s personal take, and he encourages other attendees to share their perspectives to round out the picture. For now, the Git 3.0 timeline and scope are the main open questions; everything else is business as usual.