This thread is Junio C Hamano’s "What’s cooking in git.git" report for September 2026, summarizing the state of all in-flight patch series as Git 2.56-rc1 has just been tagged, followed by discussion about the Git 3.0 release timeline and scope. The report itself lists which topics have graduated to `master`, which are cooking in `next`, and which are stalled or need review, while also recapping the Git 3.0 plans as discussed at the Contributor’s Summit.

Key developments since the last report include the graduation of documentation updates and two bug fixes to `master`, and the addition of several new topics—ranging from protocol v2 fetch fixes and OpenBSD portability improvements to CI updates and new features like the `receive-report` hook and `git history squash` subcommand. The Git 3.0 timeline remains the most active discussion point: Johannes Schindelin has now explicitly asked Junio to provide a provisional release plan in the next "What’s cooking" report, even if the Contributor’s Summit notes aren’t yet published. This request aims to give downstream projects a baseline for planning, acknowledging that the final scope (including Rust support and SHA-256 interoperability) may still shift based on unresolved portability concerns and community feedback.

The thread is still open on several fronts. Stalled topics like `mm/diff-process-hunks` and `kh/format-patch-range-diff-notes` need review or rerolls, and the `git history squash` feature is cooking in `next` but may face design scrutiny before graduation. The Git 3.0 roadmap itself remains fluid, with Junio yet to confirm a target date or finalize which breaking changes will be included. Downstream projects are now waiting for Junio’s next update to align their own schedules with Git’s major-version transition.