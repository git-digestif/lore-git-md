Here’s the human-mode narrative summary for a developer catching up on this thread:

---

This thread is Junio’s September 2026 “What’s cooking” report, tracking all in-flight patch series as Git 2.56-rc1 was tagged, but the discussion has since expanded into planning for Git 3.0—its timeline, scope, and ecosystem readiness. The key participants are Junio (maintainer), Johannes Schindelin (Git for Windows), Daniele Sassoli and Luca Milanesio (JGit), Toon Claes (security process), and Ramsay Jones (Rust portability).

Git 2.56-rc1 is out, and no new features will graduate before the final release. The provisional Git 3.0 timeline remains: Git 2.98 in December 2026, 2.99 in March 2027, and 3.0 in April 2027 or later, but Junio has emphasized that the stabilization period for 2.99 may require additional bugfix releases (e.g., 2.99.2 or 2.99.3) before 3.0 ships. The technical strategy is unchanged—breaking changes will be gated by a `WITH_BREAKING_CHANGES` Makefile knob, and 2.99.x and 3.0 will share the same codebase—but the timeline is now explicitly flexible to accommodate downstream needs.

The biggest updates since the last summary are on JGit and Rust portability. Luca Milanesio provided production-scale evidence that JGit’s pluggable backend architecture is not just viable but already deployed at scale: Google’s internal Git servers (including `gerrit.googlesource.com`) have used a proprietary JGit backend for over a decade, and GerritForge’s open-source global refdb backend powers multi-site Gerrit deployments. This confirms that JGit is technically ready for Git 3.0’s pluggable ODB goals, though SHA-256 support remains stalled due to resource constraints, not technical barriers. Meanwhile, Ramsay Jones reported that Git’s experimental Rust support compiles successfully on Cygwin using an unofficial toolchain, with no new test failures, though NonStop remains untested.

On the security front, Toon Claes and Johannes Schindelin have agreed to collaborate on the next security release, with GitLab increasing staffing for report triage. Toon has also committed to documenting Git’s security model and reviving the stalled security boundary discussion. Documentation improvements are moving forward with a two-pronged approach: incremental fixes for reference material (e.g., Jean-Noël Avila’s synopsis-style conversion) and complete rewrites for tutorials (e.g., Julia Evans’ work on `git reset`). The `git-scm.com` website, however, remains a maintenance bottleneck, with stagnation in the “UI list” and no clear plan to revive it.

Open questions include whether Rust support will make it into Git 3.0 (portability to NonStop is still unresolved), how downstream projects like JGit and distributions will coordinate around the 2.99.x/3.0 transition, and whether the proposed Git 2.x LTS branch will gain traction. The AI policy debate and push protocol v2 design are still unresolved, but the Contributor’s Summit recap and follow-up clarifications have narrowed the focus to actionable next steps. For now, the thread’s core takeaway is that Git 3.0’s scope is stabilizing, but the timeline—and downstream readiness—remain fluid.