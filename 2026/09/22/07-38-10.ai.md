This is the author's **substantive reply** to Patrick Steinhardt's review of the second patch in the v5 series (which defers auto maintenance until sequencer completion). The core technical dispute is now resolved: Thomas demonstrates that single-pick operations (`cherry-pick <commit>`, `revert <commit>`, and their `--continue`/`--skip` variants) **bypass `pick_commits()` entirely**, exiting early through `single_pick()`, `continue_single_pick()`, or `skip_single_pick()`. This confirms the patch's original design choice—delegating maintenance to the built-in commands (`builtin/rebase.c` and `builtin/revert.c`)—is necessary because the sequencer lacks a reliable single exit point.

The reply also clarifies the scope of changes needed if Patrick's alternative (triggering maintenance in `pick_commits()` and on abort) were adopted: the call would need to be added to **four locations** in `sequencer.c` (`pick_commits()`, `single_pick()`, `continue_single_pick()`, `skip_single_pick()`) plus abort handling in `git cherry-pick` and `git revert`. Thomas signals openness to either approach but notes Phillip Wood previously approved the current design.

Key technical details for follow-up:
- **Files/subsystems**: `sequencer.c` (exit paths: `pick_commits()`, `single_pick()`, `continue_single_pick()`, `skip_single_pick()`), `builtin/rebase.c` (`finish_rebase()`), `builtin/revert.c` (abort handling).
- **Behavior**: Single-pick operations **do not** execute `pick_commits()`, invalidating Patrick's proposed consolidation.
- **Test impact**: The existing `GIT_TRACE2_EVENT` tests (verifying maintenance runs once at sequence end) remain valid regardless of implementation.
- **Controversy status**: **Settled**—the design justification is now explicitly documented in the v5 commit message, and no further objections are expected. The series is likely to proceed to `next` unless new feedback emerges.

This reply effectively closes the architectural debate, leaving only minor polish (e.g., commit message tweaks) as potential follow-up. The thread is now in a "waiting for Junio to queue" state.