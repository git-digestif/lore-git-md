This is the second patch in Mark C. Chu-Carroll’s three-patch test modernization series (v5). It updates `t/t4009-diff-rename-4.sh` to align with current Git test conventions—no behavioral changes, only structural and stylistic improvements.

The patch mechanically rewrites the test script to use modern patterns: setup logic is folded into a single `test_expect_success 'setup'` block, here-documents switch to `<<-` syntax for consistent indentation, expected/actual output files are renamed from `expected`/`current` to `expect`/`actual`, and test names are made more descriptive. The diffstat shows a net reduction of six lines (66 removed, 60 added), reflecting the cleanup.

This is a straightforward, uncontroversial modernization that matches the broader community effort to standardize the test suite. The changes are purely presentational, so test coverage and logic remain unchanged. The patch is ready for `next` once any final nits are addressed.