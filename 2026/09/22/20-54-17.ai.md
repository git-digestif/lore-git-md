This is a follow-up clarification in the documentation cross-reference modernization thread. Julia Evans responds to Junio C Hamano’s technical confirmation about the necessity of the two-part AsciiDoc link syntax (`<<target,text>>`). She acknowledges the explanation but notes uncertainty about the exact rules governing when the double form is required versus the single form (`<<target>>`). Her testing reveals that while `<<PRUNING>>` works in some cases (e.g., cross-file references), `<<REMOTES>>` fails in others, rendering as `[REMOTES]` in HTML output instead of the expected section name. This suggests the patch’s current approach—using the two-part form universally—is a pragmatic choice to avoid broken links, even if the single form might suffice in some contexts. The clarification does not challenge the patch’s design or readiness; it simply documents an edge case in AsciiDoc’s behavior that the author encountered during development. No action is implied, and the patch remains queued for integration.