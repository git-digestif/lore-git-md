Julia Evans has updated the commit message for her patch modernizing cross-references in Git's man pages, addressing Junio C Hamano's request for editorial clarification. The patch itself—replacing informal plain-text references with AsciiDoc link syntax (`<<target,text>>`) across 38 files—remains unchanged and is still queued in `next`. The revised message now explicitly explains why the two-part form is necessary: the short form (`<<EXAMPLES>>`) can produce inconsistent output like `the section called "EXAMPLES"` or `[EXAMPLES]` in HTML, while the two-part form ensures predictable rendering. Jeff King (Peff) previously validated this behavior through testing with both AsciiDoc and Asciidoctor backends.

The patch is part of the broader documentation modernization effort led by Jean-Noël Avila, though it stands alone as a systematic improvement. The unresolved question about anchor style trade-offs (manual vs. auto-generated) remains open but is not revisited here. With this commit message update, the patch is now finalized for integration, pending no further changes. Junio has already approved the technical content, and the contribution is uncontroversial, with no objections or pending feedback.