This is a review comment on the first patch of Mark C. Chu-Carroll’s test modernization series (v5, 1/3). Junio C Hamano points out a discrepancy in the commit message: it claims the patch updates only `t4001-diff-rename.sh`, but the diffstat shows changes to both `t4001-diff-rename.sh` and `t4009-diff-rename-4.sh`. This appears to be a rebase error or mislabeling in the patch description.

The series itself remains a straightforward test modernization effort—no logic changes, just structural and stylistic improvements to align with current Git test conventions. The error here is purely about patch organization: the commit message should accurately reflect which files are modified. This is a surface-level issue and does not affect the substance of the changes, but it does need correction before the patch can be merged. The rest of the series (patches 2/3 and 3/3) are unaffected by this specific comment.