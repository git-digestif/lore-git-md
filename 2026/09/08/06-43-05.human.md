This is a tiny platform-compatibility fix for a development helper script. Tuomas Ahola replies to Junio C Hamano’s curiosity about how the issue was discovered: he simply ran `make -C Documentation lint-docs` on his system and saw the older Perl version complain about the `{,8}` regex quantifier. The fix itself—replacing `{,8}` with `{0,8}` in `Documentation/lint-gitlink.perl`—was already accepted and will be queued by Junio. No further discussion or changes are needed; the thread is effectively closed.