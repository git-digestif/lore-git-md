This is patch 5/9 in Patrick Steinhardt’s ODB alternates refactoring series, which consolidates alternates handling during repository creation. The patch moves the setup of alternates for shared local clones (`git clone --shared`) from `clone_local()` into `collect_alternates()`, unifying all alternates logic into a single code path. This is a preparatory step for later patches that will write alternates during ODB creation rather than as a separate step.

Justin Tobler’s review raises a surface-level question about the conditional logic in `collect_alternates()`. He suggests tightening the `is_local` check to also require `option_shared`, arguing that the current code might unnecessarily resolve the common directory even for non-shared local clones. Patrick Steinhardt’s diff snippet shows the current implementation, which resolves the common directory unconditionally for local clones but only pushes the objects directory to the alternates `strvec` if `option_shared` is set. The review does not engage with the patch’s core goal or technical correctness but instead focuses on a minor optimization opportunity.

The feedback is unlikely to block the patch, as the current logic is functionally correct and the optimization is trivial. However, it highlights a small inefficiency: resolving the common directory for non-shared local clones serves no purpose in this patch, though it may become relevant in the next patch (6/9), which handles the `--no-shared` case. The review does not address whether the inefficiency is worth fixing or whether the current structure better prepares for the upcoming changes.

This is a surface-level review with no substantive engagement with the patch’s design or edge cases. The series remains on track, with the consolidation of alternates logic proceeding as planned. Reviewers focused on the ODB abstraction effort may still scrutinize the patch’s alignment with the broader goal of simplifying the ODB interface, but no major concerns are evident.