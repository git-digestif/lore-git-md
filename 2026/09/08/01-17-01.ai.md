This is a review reply in the `imap-send` OpenSSL compatibility series. Junio C Hamano identifies a memory-safety bug in Beat Bolli’s second patch: the code increments the `pattern` pointer after allocating it with `xmemdupz()`, then later attempts to `free()` the modified pointer, which would corrupt the heap. The review is substantive — it catches a real defect that would likely cause crashes or security issues in edge cases. Junio also suggests the established Git idiom for handling such cases: keep a separate `pattern_to_free` variable solely for the `free()` call. The fix is small and mechanical, but the issue is serious enough that the patch cannot be merged as-is. No other concerns are raised; the rest of the patch logic is not questioned.