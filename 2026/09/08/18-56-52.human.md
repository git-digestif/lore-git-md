This is a small but user-facing documentation fix in the advice subsystem. The patch updates the on-screen instructions for disabling the `defaultBranchName` advice message to recommend using `--global` instead of `set`, ensuring the setting applies globally rather than just to the current repository. The author, Vsevolod Myalitsin, has revised the design in response to earlier feedback by adding a `global_hint` field to the `advice_setting` struct, which makes the code more maintainable and extensible for future advice keys that might also need global-level recommendations.

The change is minimal and targeted: it touches only `advice.c`, extends the `advice_setting` struct with a `global_hint` boolean, and updates the `vadvise()` helper to conditionally substitute `--global` for `set` when the hint is set. The core goal -- improving the clarity and correctness of the advice message -- remains uncontroversial, and the revised design addresses Ben Knoble’s earlier concern about hardcoding logic for specific advice keys.

The discussion is now focused on architectural details. Junio C Hamano has suggested replacing the boolean `global_hint` with a more general `config_scope` field (e.g., `CONFIG_SCOPE_GLOBAL`, `CONFIG_SCOPE_SYSTEM`) to future-proof the design, and passing the entire `advice_setting` struct to `vadvise()` to narrow the interface. The author has not yet responded to these suggestions, and the unresolved syntax preference (`--global` vs `set --global`) from Ben Knoble also remains open. Neither issue blocks the patch’s intent, but they may influence the final implementation.

This is a low-risk, self-contained change with no test coverage added, but it improves the user experience by ensuring the advice message recommends the correct command for global configuration. The patch is well-motivated and clearly explained, and the ongoing discussion reflects a healthy focus on long-term maintainability. For readers tracking the advice subsystem or interested in small but impactful UX improvements, this thread is worth following.