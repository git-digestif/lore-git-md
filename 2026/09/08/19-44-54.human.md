This is the second patch in a three-part test modernization series. Mark C. Chu-Carroll updates `t/t4009-diff-rename-4.sh` to use modern Git test style conventions, replacing multi-line `test_expect_success` blocks with backslash continuations with cleaner single-quoted strings. The patch touches 48 lines, converting 24 legacy patterns to 24 modern equivalents, with no functional changes to test behavior.

The changes are purely stylistic and align with the ongoing community-wide effort to modernize Git's test suite. The patch is mechanical and low-risk, focusing on readability improvements rather than new functionality. The commit message is minimal but sufficient for this type of refactoring.

This patch is part of a larger series that also updates `t4001-diff-rename.sh` and `t4010-diff-pathspec.sh`. The series is self-contained and does not depend on other in-flight topics. Reviewers may want to check for edge cases in shell quoting, particularly in the `sed` command used in the test setup, but the changes otherwise look routine.

The patch appears well-crafted and uncontroversial. It follows the established pattern of test modernization contributions, which are generally welcomed by the community. No substantive review feedback has been posted yet, but this looks like a straightforward cleanup that should move forward smoothly.