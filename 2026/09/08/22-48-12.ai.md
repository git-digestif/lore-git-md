This is a **surface-level follow-up** in the ODB alternates refactoring thread. Justin Tobler retracts his earlier suggestion to tighten the `is_local` conditional in `collect_alternates()` after noticing that the next patch (6/9) already handles the `--no-shared` case. The retraction is brief and confirms that the series’ incremental approach (first consolidating shared clones, then non-shared ones) is correct as written.

No code changes are needed, and the thread’s technical trajectory remains unchanged. The exchange serves as a minor clarification for reviewers tracking the alternates logic across patches 5–6, but it does not introduce new concerns or alter the series’ design. The focus now shifts to patch 6/9, which completes the consolidation of alternates handling for non-shared local clones.