This is a minor follow-up in an ongoing documentation-adjacent patch thread about the advice subsystem. The author (Vsevolod Myalitsin) acknowledges Ben Knoble’s review feedback and confirms he will send a v2 patch incorporating the `global_hint` field design. He also asks whether his reply formatting now meets the mailing list’s bottom-posting conventions.

The thread itself remains focused on a small but user-visible improvement: changing the on-screen instructions for disabling the `defaultBranchName` advice message to recommend `--global` instead of `set`, ensuring the setting applies globally rather than just to the current repository. The revised design uses a configurable field in the `advice_setting` struct to allow future advice keys to specify their preferred scope, making the machinery more extensible. Junio C Hamano has since suggested a more general `config_scope` field and passing the entire `advice_setting` struct to `vadvise()` for better encapsulation, but the author has not yet responded to those architectural points.

This particular email is purely procedural and adds no new technical content. It signals that a v2 patch is forthcoming, but the unresolved architectural questions and the syntax preference (`--global` vs `set --global`) remain open. The patch is still under active review and has not yet reached a consensus on the final design.