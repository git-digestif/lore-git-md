This is a surface-level review reply to the first patch in Beat Bolli’s three-patch series on `git imap-send` OpenSSL compatibility. Junio C Hamano flags a minor style nit: the comment line `// map to the pre-4.1 name` uses C++-style `//` comments instead of the Git project’s preferred C-style `/* ... */` comments. The rest of the patch—its technical content, version-gate logic, and macro definition—is not questioned, and the review does not engage with the patch’s correctness or motivation. The comment is purely cosmetic and does not affect functionality or maintainability.