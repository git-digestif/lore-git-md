This thread is about a bugfix for a regression in Git’s multi-pack-index (MIDX) subsystem, where incremental MIDX writes incorrectly publish empty layers without a reverse index, causing subsequent bitmap-enabled writes to fail. The patch, submitted by Pia Park, prevents empty layers from being published during non-compacting incremental writes and includes thorough test coverage for edge cases.

Taylor Blau’s review raises important questions about the bug’s scope. He demonstrates that the issue might not be limited to incremental writes—non-incremental MIDX writes with empty packs also succeed, suggesting the problem could be broader. He proposes a more aggressive fix: exiting early in `write_midx_internal()` for *any* empty MIDX attempt, not just incremental non-compacting writes. His alternative diff replaces the current warning with an error and early exit, arguing for consistency across all MIDX write scenarios. The discussion is substantive, focusing on correctness and edge-case handling rather than style.

The thread is still under review, with no prior versions or merge status reported. The key open question is whether the fix should be expanded to cover all empty MIDX writes, not just incremental ones. Pia’s original patch is well-tested, but Taylor’s feedback may lead to a revised approach. No other participants have weighed in yet.