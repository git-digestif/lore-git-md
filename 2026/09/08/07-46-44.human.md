This is the latest exchange in the ongoing patch series that defers auto maintenance until sequencer-driven operations (`git rebase`, `git cherry-pick`, `git revert`) complete. The series is technically complete and addresses all prior feedback, but Patrick Steinhardt has raised a final hurdle: the commit messages must be rewritten to clearly explain design choices and read as human-written, not LLM-generated.

Thomas Bachem acknowledges the feedback and commits to rewriting all three commit messages for the next revision. He also clarifies that moving the `run_auto_maintenance()` call out of the sequencer and into the built-in commands (`builtin/rebase.c` and `builtin/revert.c`) has no impact on other subsystems, as those are the only two entry points to the sequencer. This justification will be included in the revised commit message.

The series remains on track for merging once the commit messages are polished. The technical work is done, and the only remaining task is documentation hygiene. If the revised messages meet Patrick’s standards, this should be the final iteration before the patches are queued.