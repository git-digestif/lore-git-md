This is the first patch in a three-part series that refactors Git’s config parameter handling to support the broader goal of deferring auto maintenance during sequencer operations (`git rebase`, `git cherry-pick`, `git revert`). The patch introduces `git_config_append_parameter()`, a helper function that safely appends a single `-c key=value` setting to a `GIT_CONFIG_PARAMETERS` string with proper shell quoting. This replaces the inline formatting in `git_config_push_split_parameter()` and makes the logic reusable for other callers.

The change is a clean refactoring with no behavior change: the existing `git_config_push_split_parameter()` now delegates to the new helper. The function is documented in `config.h` with a clear explanation of the `GIT_CONFIG_PARAMETERS` format (a space-separated list of `'key'='value'` pairs, both sides single-quoted). The patch touches only `config.c` and `config.h`, and the diff is straightforward and well-contained.

This is a well-crafted infrastructure patch that addresses a real need: the sequencer will use this helper in the next patch to disable auto maintenance for the commands it spawns. The commit message is clear, the code is clean, and the documentation is thorough. No controversy or open questions remain on this patch itself; the only remaining work for the series is commit message polish in the follow-up patches. If you care about sequencer behavior or config plumbing, this patch is worth a quick skim to understand the foundation being laid.