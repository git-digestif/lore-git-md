This is Junio’s "What’s cooking in git.git" report for September 2026, summarizing the state of integration branches and in-flight topics. It’s the canonical status update for Git development, showing what has graduated to `master`, what is cooking in `next`, and what is stalled or discarded.

The report highlights that the 23rd batch of topics has graduated to `master`, bringing the total to 616 non-merge commits since Git 2.55. The `next` branch holds 72 commits, while `seen` contains 219, including those already in `next`. The report is organized into sections: graduated topics, new topics, stalled topics, cooking topics, and discarded topics.

Notable graduations include optimizations to the reftable backend (`kn/reftable-optimize-reloading`), improvements to `git worktree add` and `git checkout` for ambiguous remote branch names (`yn/worktree-ambiguous-remote-advice`), and a fix for `git worktree repair` with relative paths (`yn/worktree-repair-relative`). The `gr/add-e-use-apply-api` topic refactored `git add -e` to use the internal apply API directly, avoiding a subprocess.

Among the new topics, `jk/ci-bump-debian-to-12` updates the CI environment to Debian 12, and `tc/push-force-if-includes-fixes` fixes a bug in `--force-if-includes` where the wrong reflog was consulted. The latter is expected to require a reroll. Another new topic, `as/cherry-pick-no-commit-doc`, clarifies the behavior of `git cherry-pick --no-commit` in the documentation and adds a test to ensure `CHERRY_PICK_HEAD` is not set.

Stalled topics include `ps/libgit-in-subdir`, which moves `libgit.a` sources into a `lib/` directory but has been ejected due to merge conflicts, and `tb/pack-with-duplicates`, which hardens packfile handling for duplicate objects but has not seen recent activity. Several other stalled topics, such as `hs/rebase-continue-edit` and `sn/rebase-update-refs-symrefs`, are marked for discard if no response is received.

Cooking topics include `ps/odb-stop-registering-in-memory-sources`, a significant refactoring to remove the ad-hoc alternate object source registration mechanism, and `ps/odb-pluggable-fsck`, which moves fsck checks into backend-specific layers. The `jc/checkout-refactor` topic restructures the front-end code for `git checkout`, `git switch`, and `git restore`, paving the way for libification. Another notable cooking topic is `en/no-amend-during-conflicts`, which prevents `git commit --amend` and partial commits during conflict resolution, aligning the behavior of `am`, `revert`, and `rebase` with `cherry-pick` and `merge`.

The report also includes several topics that are waiting for review or response, such as `yt/pathspec-negative-prefix`, which fixes out-of-bounds memory access in pathspec matching, and `kn/receive-report-hook`, which introduces a new hook to filter or modify the status report sent by `git receive-pack`. The `mm/diff-process-hunks` topic introduces a new `diff.<driver>.process` configuration to allow external tools to act as hunk providers, enabling more flexible diff behavior.

Overall, this report provides a comprehensive snapshot of Git’s development state, highlighting progress on major refactoring efforts, bug fixes, and new features while flagging stalled or problematic topics. It’s an essential read for anyone tracking Git’s evolution.