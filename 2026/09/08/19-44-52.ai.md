This is a test modernization patch series. The author, Mark C. Chu-Carroll, updates three test scripts (`t4001-diff-rename.sh`, `t4009-diff-rename-4.sh`, and `t4010-diff-pathspec.sh`) to use modern test style conventions. The changes replace older test patterns (e.g., `test -f`, `! test -f`) with newer helper functions like `test_path_is_file` and `test_path_is_missing`. The series touches 3 files, converting 81 lines of legacy syntax to 82 lines of modern equivalents, with no functional changes to test behavior.

The motivation is part of the ongoing community-wide effort to modernize Git's test suite. These updates align the `t40*` tests with current best practices, improving readability and maintainability. The commit messages are minimal but sufficient, and the changes appear mechanical and low-risk. No new test coverage is added—this is purely a refactoring of existing tests.

The series is self-contained and does not depend on other in-flight efforts. It targets the test infrastructure, specifically the shell-based test scripts in `t/`. The patches are small and focused, making them easy to review. There is no indication of controversy or edge cases; this looks like a routine cleanup contribution.

Key technical details for follow-up discussion: the files modified are `t/t4001-diff-rename.sh`, `t/t4009-diff-rename-4.sh`, and `t/t4010-diff-pathspec.sh`. The modernized functions (`test_path_is_file`, `test_path_is_missing`, etc.) are part of Git's standard test library. The base commit is `b8242b093d9e941a34460d715e3ce616a34ac3fe`.