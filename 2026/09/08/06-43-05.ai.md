This is a follow-up in a closed maintenance thread. Tuomas Ahola clarifies how the Perl regex compatibility issue was discovered: he ran `make -C Documentation lint-docs` on a system with a Perl version older than 5.34.0 (though not necessarily 5.26.0), triggering the syntax error. Junio acknowledges the explanation and reaffirms his intent to queue the patch for merging.

The patch itself remains unchanged and uncontroversial -- a one-line fix in `Documentation/lint-gitlink.perl` replacing `{,8}` with `{0,8}` to maintain compatibility with Git’s minimum Perl requirement. The thread is now fully resolved, with no further discussion or revisions expected. Junio’s merge is the only remaining action.