Thread identity: Bug report about `git send-email` failing to handle SMTP rate-limiting (4.7.1 "too many recipients") gracefully. Originating author: Juha-Matti Tilli. Thread type: bug report.

Core problem or goal: `git send-email` currently exits immediately when encountering SMTP rate-limiting errors (e.g., 4.7.1 "too many recipients"), leaving remaining patches unsent and causing incorrect threading if manually resumed. The goal is to improve error handling to either retry after a delay or prompt the user for continuation, preventing partial sends and threading issues.

Current status: No prior discussion or patch versions; this is the initial report. The issue is confirmed by the user’s experience with a 13-patch series to the Linux kernel mailing list (18 recipients per email). The user’s workaround (`--batch-size=4 --relogin-delay=60`) demonstrates a manual solution, but automatic handling is proposed.

Key technical details:
- **Subsystem**: `git send-email` (SMTP client logic).
- **Error code**: SMTP 4.7.1 ("too many recipients").
- **Impact**: Partial sends, incorrect threading on manual resume.
- **Existing options**: `--batch-size` and `--relogin-delay` (not used by default).
- **Proposed fix**: Automatic retry with delay or user prompt for rate-limiting errors.

Open questions and loose ends:
- Should `git send-email` automatically retry on rate-limiting errors, or prompt the user?
- What default delay (if any) should be used for retries?
- Should `--batch-size` and `--relogin-delay` be adjusted or made more prominent in documentation?
- Are there other SMTP error codes that should trigger similar handling?

Participant positions:
- **Juha-Matti Tilli** (reporter): Describes the bug, proposes automatic retry or user prompt, and notes the workaround. No prior review or feedback from others yet.

Related work: None mentioned in the thread. The issue is specific to `git send-email`’s SMTP error handling.