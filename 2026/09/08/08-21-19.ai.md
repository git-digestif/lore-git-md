This is a **follow-up to a critical Windows-specific bug report** in the `git worktree` subsystem. The original report described how `git worktree add "" HEAD` deletes the `.git` directory on Windows, causing data loss. This reply from Tuomas Ahola confirms the issue is acknowledged and provides links to the ongoing fix effort.

The fix is already queued for merging into `master`, meaning it will be part of the next Git release. The links point to Git for Windows issue #6346 and a patch series on the mailing list (Lars Schneider, 2026-08-25), indicating the root cause has been identified and addressed. No technical details about the fix itself are provided here, but the patch likely involves argument validation or Windows-specific path handling in `builtin/worktree.c` or `compat/`.

This reply advances the thread by confirming the bug is real, under active resolution, and on track for inclusion in `master`. The tone is matter-of-fact: the issue is treated as serious but already in hand. Future discussion may focus on testing the fix or verifying its correctness across Windows environments. The original reporter’s system details (Git for Windows 2.55.0.windows.5, OpenSSL 3.5.7, SHA-1/SHA-256) remain relevant for regression testing.