This is the first patch in the **v8 iteration of the `receive-report` hook series**, a documentation update that plugs a gap in `git-receive-pack(1)` by adding an eight-line `PROC-RECEIVE HOOK` section. The patch is **purely additive** and does not affect code or behavior; it explains the existing `proc-receive` hook’s purpose (invoked when `receive.procReceiveRefs` is set and commands match the configured refs) and its responsibility for updating references and reporting results. The section links to `git-receive-pack(1)` and is styled consistently with the surrounding hook documentation.

The change addresses a prior omission noted in review and includes a `Helped-by` credit to Patrick Steinhardt, reflecting his earlier surface-level feedback on the documentation structure. No substantive concerns remain; this patch is **ready for integration** and is already queued in `next` alongside the rest of the series. The addition is uncontroversial and aligns with the project’s documentation standards.