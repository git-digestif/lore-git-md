This is a test modernization patch series. Mark C. Chu-Carroll updates three test scripts in the `t40*` range (`t4001-diff-rename.sh`, `t4009-diff-rename-4.sh`, and `t4010-diff-pathspec.sh`) to use modern Git test style.

The changes replace older test patterns (like `! test -f` or `test -f`) with more explicit helper functions such as `test_path_is_missing` and `test_path_is_file`. This is part of the ongoing community effort to modernize Git’s test suite, making tests more readable and maintainable. The series touches only test files, so there is no risk of changing Git’s behavior—only how it is verified.

The commit messages are brief but clear, and the patches themselves are mechanical and well-scoped. No new functionality is introduced; this is purely cleanup. The series is small (three patches, ~80 lines changed in total) and looks uncontroversial. If the tests still pass, these patches are likely to be merged without much discussion.