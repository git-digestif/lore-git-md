This is the first patch in a three-part test modernization series. It updates two test scripts (`t4001-diff-rename.sh` and `t4009-diff-rename-4.sh`) to use Git's modern test helper syntax. The changes are purely stylistic: replacing older shell test patterns (like multi-line `test_expect_success` blocks with backslash continuations) with cleaner, more readable single-quoted forms. No functional behavior is altered—this is a straightforward refactoring to align these tests with current project conventions.

The patch touches 31 lines in `t4001` and 8 lines in `t4009`, converting them to the preferred style where `test_expect_success` bodies are written as single-quoted strings rather than multi-line shell scripts with explicit continuation markers. This matches the ongoing community-wide effort to modernize Git's test suite, improving maintainability without changing test coverage or outcomes. The changes are mechanical and low-risk, making them easy to review.

This patch is part of a larger series (1/3) targeting the `t40*` test scripts. The thread context confirms this is the initial version (v1) of the series, with no prior iterations or merge status. The author, Mark C. Chu-Carroll, is a first-time contributor to this effort, and the series is described as uncontroversial. The base commit is `b8242b093d9e941a34460d715e3ce616a34ac3fe`.

Key technical details for follow-up discussion: the files modified are `t/t4001-diff-rename.sh` and `t/t4009-diff-rename-4.sh`. The changes involve only test helper syntax (`test_expect_success` formatting) and do not introduce or modify any functions, options, or on-disk formats. The patch is self-contained and does not depend on other in-flight topics.

The patch appears well-crafted and aligns with the project's test modernization goals. It is unlikely to generate controversy, but reviewers may check for edge cases in the shell quoting (e.g., the switch from single to double quotes in the `sed` commands in `t4009`). No substantive issues are evident from the diff alone.