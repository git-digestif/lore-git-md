This is a brief follow-up in the ODB alternates refactoring thread. Justin Tobler retracts his earlier suggestion to tighten the `is_local` conditional in patch 5/9 after noticing that the next patch (6/9) already handles the non-shared case. His initial concern was that the current logic would unconditionally add alternates for all local clones, including those not intended to be shared (`git clone --local --no-shared`), but he now recognizes that patch 6/9 addresses this by moving the remaining alternates logic into `collect_alternates()`.

The exchange is purely procedural and resolves the only open question from Justin’s prior review. No code changes are needed for patch 5/9, and the series remains on track. The thread can now proceed to discussion of the remaining patches (6–9/9), which implement the core design changes for alternates handling during ODB creation.