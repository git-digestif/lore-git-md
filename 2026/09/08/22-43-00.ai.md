This is a **substantive review** of patch 5/9 in Patrick Steinhardt’s ODB alternates refactoring series. Justin Tobler’s feedback targets a potential edge case in the patch’s logic: the new `is_local` conditional in `collect_alternates()` currently pushes the source repository’s objects directory into the alternates `strvec` unconditionally for all local clones, regardless of whether `--shared` was specified. This could inadvertently add alternates for local clones that were not intended to be shared (e.g., `git clone --local --no-shared`), breaking the expected isolation of the new repository.

The review suggests tightening the condition to `if (is_local && option_shared)`, ensuring alternates are only added when `--shared` is explicitly set. This aligns with the patch’s stated goal of consolidating alternates logic for shared local clones while deferring the `--no-shared` case to the next commit. The change is small (a one-line adjustment) but critical for correctness, as the current logic would silently alter behavior for non-shared local clones.

Patrick Steinhardt’s diff snippet in the reply confirms the proposed fix: the `strvec_pushf()` call is now guarded by `option_shared`, preventing unintended alternates from being added. The review is narrowly scoped to this edge case and does not engage with broader design questions (e.g., the ODB abstraction’s long-term flexibility), focusing instead on immediate correctness. The feedback is likely to be uncontroversial, as it addresses a clear oversight in the patch’s current implementation. No test failures are expected if the fix is applied, but reviewers may scrutinize whether the next patch (6/9) adequately handles the `--no-shared` case to avoid regressions.