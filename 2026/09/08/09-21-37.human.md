This is a surface-level review reply to patch 2/11 in Patrick Steinhardt’s refactoring series that standardizes Git’s terminology for "ref storage format." The patch renames the `--ref-format=` option to `--ref-storage-format=` in `git clone`, retaining the old name as a hidden backward-compatible alias.

Kaartic Sivaraam’s feedback is purely editorial: he repeats his earlier suggestion (from the cover letter discussion) that the old `--ref-format=` option should either be marked as deprecated in its help text or implemented as an `OPT_ALIAS` to clarify its status. He also catches a typo in the test script (`--ref-storage` should be `--ref-storage-format`). The rest of the patch meets his approval.

No substantive technical concerns are raised here; the discussion remains focused on the backward-compatibility strategy for the old option name. The typo fix is trivial and will likely be incorporated in the next revision.