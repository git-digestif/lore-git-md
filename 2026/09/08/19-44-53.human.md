This is a test modernization patch, part of Mark C. Chu-Carroll’s three-patch series converting legacy test patterns in the `t40*` scripts to modern Git test helpers. Patch 1/3 updates `t4001-diff-rename.sh` and `t4009-diff-rename-4.sh`, replacing the old `test_expect_success` one-line syntax with the multi-line form that uses single quotes around the test body. The changes are purely stylistic—no test logic or coverage is altered—and follow the established community-wide effort to modernize the test suite.

The patch is small, focused, and mechanically sound. The commit message is brief but sufficient for a routine cleanup. There are no functional changes, so the risk of regression is negligible. This looks like a straightforward, uncontroversial contribution that aligns with ongoing test modernization goals. Reviewers are likely to approve it quickly unless they spot a subtle formatting issue.