This is a minor follow-up in the ongoing ODB refactoring series, specifically about the commit message clarity for patch 5/13. The patch itself removes a 2019 workaround that registered submodule object databases as in-memory sources of `the_repository` to allow `config_with_options()` to read `.gitmodules` blobs from submodules. Justin Tobler, who reviewed the patch, initially found the commit message confusing—particularly the ambiguous use of "the repository" to refer to a submodule rather than the global `the_repository`. He suggests the phrasing could be clearer but does not object to the technical content.

Patrick Steinhardt acknowledges the feedback and offers to rewrite the message if needed, but the exchange is low-stakes: no technical issues are raised, and the patch’s correctness remains uncontested. The discussion is purely about readability, not functionality. The series as a whole continues to progress smoothly, with no new objections or merge intent from Junio C Hamano. If you’re tracking the ODB abstraction effort, this is a trivial detail; if you’re not, it’s safe to ignore.