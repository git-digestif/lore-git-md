This is the first review reply to Beat Bolli’s OpenSSL compatibility patch for `git imap-send`. Patrick Steinhardt questions the necessity of the change: the OpenSSL 4.1 documentation still lists the old function (`ASN1_STRING_length()`) as available, with no deprecation notice, so the patch’s motivation—preparing for a rename that hasn’t actually happened yet—is unclear. The review is substantive but concise, focusing on the commit’s rationale rather than its implementation. No alternative approach is suggested; the reviewer simply asks for clarification. This leaves the series in limbo until Beat responds or OpenSSL’s plans become clearer.