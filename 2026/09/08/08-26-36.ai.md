This is a review comment on the first patch of Beat Bolli’s three-patch series addressing OpenSSL compatibility in `git imap-send`. Patrick Steinhardt questions the necessity of switching from `ASN1_STRING_length()` to `ASN1_STRING_get_length()`, noting that the OpenSSL documentation does not indicate the old function is deprecated or slated for removal. His concern is architectural: if the old function remains available and functional, introducing a compatibility macro may be premature and could add unnecessary complexity.

The review is substantive but narrow, focusing only on the motivation for the change rather than its implementation. It does not engage with the rest of the series or the broader TLS verification logic. For the AI summarizer brief, note that this introduces a new open question: whether the compatibility shim is justified given the current OpenSSL documentation, or if it should be deferred until the old function is officially deprecated. The thread’s existing memory-safety and RFC compliance concerns remain unresolved and are unaffected by this comment.