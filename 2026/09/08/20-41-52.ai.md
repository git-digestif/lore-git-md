This is the first patch in the **v15 reroll of the `git history squash` feature**, a refactoring that extracts a helper function to reduce code duplication. The patch introduces `first_parent_tree_oid()`, a helper that resolves the tree OID of a commit’s first parent (or the empty tree for root commits), replacing three nearly identical code blocks in `builtin/history.c`.

The change is purely mechanical: the new helper consolidates the repeated logic for parsing the parent commit, checking for root commits, and copying the tree OID. No behavior changes are introduced, and the patch is well-scoped, touching only 58 lines in a single file. The commit message clearly states the motivation (reducing duplication) and the outcome (no functional change).

This is a **clean, uncontroversial refactoring** that sets up the rest of the series. The helper will be used in later patches to implement the core squash logic, so its correctness is critical—but the patch itself is straightforward and unlikely to attract debate. The test suite (introduced later in the series) will validate the helper’s behavior indirectly.

For a future AI summarizer, note the key names: `first_parent_tree_oid()`, `builtin/history.c`, and the three call sites (`commit_tree_with_edited_message`, `commit_became_empty`, `split_commit`). These will likely be referenced in follow-up discussion. The patch is part of a **technically complete series** marked "Will replace" by Junio, so it is on track for integration.