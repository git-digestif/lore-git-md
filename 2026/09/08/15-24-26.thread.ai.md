Thread identity: Patch series addressing a cross-compilation issue in Git's Rust build infrastructure (`src/cargo-meson.sh` and `Makefile`), affecting both Meson and `make`-based builds. Originating author: James Le Cuirot. This is the third version of the patch (v3 pending).

Core problem or goal: Fix a bug where the build scripts fail to locate the compiled Rust library (`libgitcore.a`) when `CARGO_BUILD_TARGET` is set, even if it matches the native target. The issue arises because Cargo writes the library to `$BUILD_DIR/$CARGO_BUILD_TARGET/$BUILD_TYPE/`, while the scripts previously assumed `$BUILD_DIR/$BUILD_TYPE/`. The patch ensures both build systems respect `CARGO_BUILD_TARGET` in the output path using shell parameter expansion and Makefile variable substitution.

Current status: Under review. This is the second version of the patch, with a third version expected to address Junio's feedback on rebasing and path formatting. The technical fix remains uncontroversial, but two mechanical issues must be resolved: (1) rebasing onto commit `924dfced6b` ("Makefile: support universal macOS builds via RUST_TARGETS", 2026-07-08) to avoid conflicts with recent `RUST_TARGETS` logic, and (2) fixing a double-slash path issue in `src/cargo-meson.sh` when `CARGO_BUILD_TARGET` is unset. No objections to the technical correctness of the fix have been raised.

Key technical details:
- Files modified: `Makefile`, `src/cargo-meson.sh`
- Environment variable: `CARGO_BUILD_TARGET` (determines the output path for Rust artifacts)
- Artifact: `libgitcore.a` (the compiled Rust library)
- Build types: `debug`/`release` (controlled by `DEBUG` in the `Makefile`)
- The patch aligns the scripts' path resolution with Cargo's behavior by including `CARGO_BUILD_TARGET` in the path, with fallbacks to native paths when the variable is unset.
- New requirement: The patch must now coexist with `RUST_TARGETS` logic introduced in `924dfced6b`.
- Shell parameter expansion in `src/cargo-meson.sh` must avoid double slashes (e.g., `${CARGO_BUILD_TARGET+$CARGO_BUILD_TARGET/}`).

Open questions and loose ends:
- The commit title may still be refined for clarity (e.g., to explicitly reference "build system" or "cross-compilation"), though the expanded scope (Meson and `make`) reduces ambiguity.
- The patch must be rebased onto `924dfced6b` to ensure compatibility with `RUST_TARGETS`.
- The double-slash path issue in `src/cargo-meson.sh` must be fixed using Junio's proposed parameter expansion.

Participant positions:
- **James Le Cuirot**: Authored the patch, addressing Junio's questions in v2 by extending the fix to the `Makefile` and confirming the issue was not Meson-specific. Expected to send v3 addressing the rebasing and path formatting feedback.
- **Junio C Hamano**: Raised substantive questions in the first iteration about the patch's scope (Meson vs. `make`) and documentation (commit title clarity). In v2, identified two mechanical issues: (1) the need to rebase onto `924dfced6b` to avoid conflicts with `RUST_TARGETS`, and (2) a stylistic but undesirable double-slash path in `src/cargo-meson.sh`. No objections to the core technical approach.

Related work: This patch is part of the broader Rustification effort in Git and is particularly relevant for cross-compilation workflows (e.g., Gentoo, embedded systems, macOS universal builds). It now depends on the `RUST_TARGETS` logic introduced in `924dfced6b`. No other in-flight dependencies are mentioned. The patch is applicable to both Meson and `make`-based builds.