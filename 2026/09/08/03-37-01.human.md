This is a small platform-compatibility fix for a development helper script. Junio C Hamano has already queued the patch after a brief exchange with the author, Tuomas Ahola. The change replaces the Perl regex quantifier `{,8}` with `{0,8}` in `Documentation/lint-gitlink.perl` to maintain compatibility with Git’s minimum Perl version (5.26.0), since the shorthand form was only introduced in Perl 5.34.0.

The patch is trivial, touching just one line in a single file, and is uncontroversial. The commit message clearly explains the motivation, and Junio’s reply confirms the fix is appropriate. The script is a development tool, so no tests are needed. Junio’s curiosity about how the issue was discovered doesn’t affect the patch’s correctness—it was likely found through static analysis or a compatibility audit rather than a runtime failure.

This is a standalone fix, not part of any larger series, and aligns with Git’s existing Perl compatibility policy. It should merge smoothly without further discussion.