This is a documentation fix in the advice subsystem, part of the ongoing thread to correct the on-screen instructions for disabling the `defaultBranchName` advice. The patch itself is small and well-motivated, but Junio’s reply raises architectural questions about the design’s future-proofing and interface cleanliness.

Junio’s feedback centers on two points. First, he questions whether the new `global_hint` field should instead be a more general `config_scope` field, anticipating that future advice keys might need to suggest `--system` scope rather than just `--global`. Second, he suggests narrowing the interface by passing the entire `advice_setting` struct to `vadvise()` rather than individual fields, which would make the function more self-contained and easier to extend later. The current implementation hardcodes the scope logic inside `vadvise()`, but Junio’s alternative would move that logic into the struct definition, keeping the helper function unaware of specific advice keys.

The thread remains under active review, with no objections to the core goal but open questions about the best way to structure the code for long-term maintainability. The unresolved syntax preference (`--global` vs `set --global`) from Ben Knoble’s earlier review is still pending, but Junio’s comments shift the focus to the underlying design rather than the surface wording. This is now a substantive discussion about how to balance simplicity with extensibility in the advice machinery.