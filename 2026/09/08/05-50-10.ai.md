This is a **substantive review follow-up** in the ongoing thread about deferring auto maintenance until sequencer-driven operations (`rebase`, `cherry-pick`, `revert`) complete. The patch in question (`[PATCH v2 2/3]`) is part of Thomas Bachem’s v3 series, which is otherwise technically complete and addresses all prior feedback.

Patrick Steinhardt responds to Thomas’s proposal to move the `run_auto_maintenance()` call out of the sequencer and into the built-in commands (`run_specific_rebase()` in `builtin/rebase.c` and `run_sequencer()` in `builtin/revert.c`). This would consolidate maintenance into a single exit path per command, aligning with the apply backend’s design and simplifying the rule to "the command runs maintenance once when done." Patrick’s reply is not a technical objection but a **process-oriented critique** of the commit message’s clarity and authorship.

He emphasizes that the commit message must explain design decisions—such as why maintenance is delegated to callers rather than handled internally—so readers can evaluate the patch. More pointedly, he warns against **LLM-generated commit messages**, which he describes as "completely useless" for conveying meaningful context. While he acknowledges that LLMs can assist in drafting, he insists that the final message must be **human-reviewed** to ensure it is coherent, accurate, and demonstrates the author’s understanding of the change. His tone is firm but constructive, framing this as a requirement for effective communication rather than a personal criticism.

The exchange does not introduce new technical concerns or block the series, but it **raises the bar for the next revision**. Thomas will need to revise the commit message to explicitly justify the design choice (delegating maintenance to callers) and ensure it reads as human-written. The thread remains on track for merging once this polish is addressed.