This is a one-line documentation fix for the third patch in Karthik Nayak’s four-patch series introducing the `receive-report` hook. The patch itself refactors status-report generation into a reusable `generate_report()` helper, unifying the previously separate `report()` and `report_v2()` functions. Junio C Hamano’s review identified a stale comment in the helper’s docstring that still referenced the removed `detailed_report` boolean parameter; the comment now correctly describes the `version` parameter (an `enum report_status_version`) that determines whether to generate a v0 or v2 protocol report. Karthik acknowledges the issue and confirms the comment will be updated.

The fix is purely cosmetic and does not affect functionality or test coverage. The series is already queued in `next` and is otherwise feature-complete, with all prior feedback—including substantive reviews from Patrick Steinhardt and Junio’s earlier concerns about error-handling logic and enum naming—addressed in the v8 iteration. This is the last remaining surface-level nit before the series graduates to `master`. The files touched are limited to `builtin/receive-pack.c`; no other subsystems or documentation are affected. The stale comment was introduced in patch 3/4, which is a refactoring prerequisite for the hook implementation in patch 4/4.