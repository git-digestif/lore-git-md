This is a surface-level review reply in the ongoing ref-storage-format terminology cleanup series. Kaartic Sivaraam raises a procedural question about the backward-compatibility strategy for the old `--ref-format=` option: whether it should be deprecated with warnings (and eventually removed) or simply retained as a hidden alias. The review suggests two concrete implementation options—either mark the old option as deprecated in its help text (following the pattern in `builtin/name-rev.c`) or use `OPT_ALIAS` to clarify its status (as done for `--recursive` in `builtin/clone.c`). No technical objections or edge-case concerns are raised; the feedback is purely about long-term maintainability and user communication. The patch itself is a mechanical rename of `--ref-format=` to `--ref-storage-format=` in `git init`, retaining the old name as a hidden alias, and touches 15 files (documentation, built-in command, and test scripts). The series remains well-motivated and uncontroversial, but this review introduces a new discussion point about the lifecycle of deprecated options.