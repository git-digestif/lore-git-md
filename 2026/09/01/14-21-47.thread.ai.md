AI thread summary:

Thread identity: Patch series (v2) introducing Rust compilation support in Git’s GitHub Actions Windows CI jobs. Originating author: Johannes Schindelin (via GitGitGadget). Thread type: CI/build system adjustment.

Core problem or goal: Enable Rust compilation in Git’s GitHub Actions Windows CI workflows by configuring the Rust toolchain to target the GCC ABI (used by Git for Windows) instead of the default MSVC ABI. This unblocks the default Rust requirement introduced in Git 2.55.0 for Windows CI, ensuring Cargo produces a static library (`libgitcore.a`) compatible with MinGW’s `ld.exe`.

Current status: Series v2 posted, incorporating feedback from v1. Under review; no merge status recorded. The series is now complete with both patches: (1) introduced `CARGO_TARGET` mapping and ABI configuration, and (2) removed the `NO_RUST` opt-out and installed the precompiled Rust `std` library. Changes from v1: corrected UCRT64 target triple mapping to consistently use `x86_64-pc-windows-gnu` (GCC ABI) instead of `x86_64-pc-windows-gnullvm` (Clang ABI), aligning with the MinGW toolchain used by Git for Windows. The commit message now clarifies that `rustup target add` is sufficient for installing the required Rust components, as the linker is already provided by the SDK.

Key technical details:
- Files touched: `.github/workflows/main.yml`, `ci/lib.sh`, `config.mak.uname`, `Makefile`.
- New behavior: CI workflows use GitHub’s pre-installed Rust toolchain, configured via `CARGO_ARGS` to target `*-pc-windows-gnu` (MinGW) or `*-pc-windows-gnullvm` (Clang/LLVM) based on the `MSYSTEM` environment variable. The `CARGO_TARGET` variable maps `MSYSTEM` to the correct Rust target triple (e.g., `x86_64-pc-windows-gnu` for `UCRT64`, `aarch64-pc-windows-gnullvm` for `CLANGARM64`), and `HOST_CPU` is repurposed to derive the CPU architecture (`i686`, `x86_64`, or `aarch64`). The `RUST_LIB` path is adjusted to include the target triple, ensuring the correct `.a` file is linked. The `PATH` is adjusted to ensure `cargo.exe` is found in the minimal Git for Windows SDK environment. The second patch adds a step to install the precompiled Rust `std` library for the GCC-compatible target triple via `rustup target add`, and removes the `NO_RUST` opt-out in `ci/lib.sh`.
- No changes to core Git functionality, on-disk formats, or user-facing features. The patch is CI/build-system-only.
- Test coverage: CI-only; no new test files or test cases. The patch includes links to failed CI runs demonstrating the original problem (MSVC-targeted `.lib` file incompatible with MinGW).

Open questions and loose ends:
- Request for testing on GitLab’s Windows runners (`saas-windows-medium-amd64`) remains open; Rust availability and toolchain compatibility there are unconfirmed.

Participant positions:
- Junio C Hamano: Substantive review in v1 identifying a potential edge-case inconsistency in the Rust target triple mapping for UCRT64. Feedback addressed and resolved in v2; no further objections.
- Johannes Schindelin: Author; confirmed the UCRT64 mapping correction and aligned the build configuration logic. Explicitly requests testing on GitLab’s Windows runners to determine if additional changes are needed. Split the series into two patches for clarity and incremental review. The series is now complete and ready for merge pending GitLab runner testing.

Related work: This series is a prerequisite for the broader Rustification effort in Git, but does not itself introduce Rust code into the codebase. The removal of the `NO_RUST` opt-out directly unblocks the eventual removal of the Rust opt-out in Git’s CI. The series is narrowly scoped to Windows CI and does not interact with other in-flight efforts (e.g., ODB abstraction, reftable backend).