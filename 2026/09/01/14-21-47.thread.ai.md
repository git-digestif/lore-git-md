AI thread summary:

Thread identity: Patch series (v4) introducing Rust compilation support in Git’s GitHub Actions Windows CI jobs. Originating author: Johannes Schindelin (via GitGitGadget). Thread type: CI/build system adjustment.

Core problem or goal: Enable Rust compilation in Git’s GitHub Actions Windows CI workflows by configuring the Rust toolchain to target the GCC ABI (used by Git for Windows) instead of the default MSVC ABI. This ensures Cargo produces a static library (`libgitcore.a`) compatible with MinGW’s `ld.exe`, unblocking the default Rust requirement introduced in Git 2.55.0 for Windows CI. The series avoids including Rust in the minimal Git for Windows SDK, which would more than double its size.

Current status: Series v4 merged to `master` (date unknown, fast-tracked from `next` due to low risk and narrow scope). The v4 revision addresses all prior feedback, including removing the `--target` flag for native builds to align with Cargo’s expected behavior (per James Le Cuirot’s input and the merged `jc/rust-cargo-build-target` topic). The series is split into two patches: (1) maps the `MSYSTEM` environment variable to the correct Rust target triple (e.g., `x86_64-pc-windows-gnu` for `UCRT64`, `aarch64-pc-windows-gnullvm` for `CLANGARM64`) and adjusts the `RUST_LIB` path, and (2) removes the `NO_RUST` opt-out in `ci/lib.sh` and adds a step to install the precompiled Rust `std` library via `rustup target add`. The `CARGO_BUILD_TARGET` variable is now set only for cross-compilation cases (e.g., `aarch64` builds on `x86_64` runners), while native builds omit it entirely.

Key technical details:
- Files touched: `.github/workflows/main.yml`, `ci/lib.sh`, `config.mak.uname`, `Makefile`.
- New behavior: CI workflows use GitHub’s pre-installed Rust toolchain, configured via `CARGO_ARGS` to target `*-pc-windows-gnu` (MinGW) or `*-pc-windows-gnullvm` (Clang/LLVM) based on the `MSYSTEM` environment variable. The `CARGO_BUILD_TARGET` variable maps `MSYSTEM` to the correct Rust target triple **only for cross-compilation cases**; native builds omit the `--target` flag entirely to avoid breaking Cargo’s internal assumptions (e.g., `RUSTFLAGS` not applying to build-time tools). `HOST_CPU` is repurposed to derive the CPU architecture (`i686`, `x86_64`, or `aarch64`). The `RUST_LIB` path is adjusted to include the target triple, ensuring the correct `.a` file is linked. The `PATH` is adjusted to ensure `cargo.exe` is found in the minimal Git for Windows SDK environment. The second patch adds a step to install the precompiled Rust `std` library for the derived target triple via `rustup target add`, and removes the `NO_RUST` opt-out in `ci/lib.sh`.
- No changes to core Git functionality, on-disk formats, or user-facing features. The patch is CI/build-system-only.
- Test coverage: CI-only; no new test files or test cases. The patch includes links to failed CI runs in prior versions demonstrating the original problem (MSVC-targeted `.lib` file incompatible with MinGW).

Open questions and loose ends:
- Rust is **not** preinstalled on GitLab’s Windows runners (`saas-windows-medium-amd64`), as confirmed by Junio C Hamano’s recent CI job link. This runner will require additional setup (e.g., `rustup` installation) to build Rust-enabled Git, but this is not a blocker for the series, which is scoped to GitHub Actions.

Participant positions:
- Junio C Hamano: Substantive review in v1 identifying a potential edge-case inconsistency in the Rust target triple mapping for UCRT64. Feedback addressed in v2. In v2 review, raised a naming inconsistency (`CARGO_TARGET` vs. `CARGO_BUILD_TARGET`), referencing the recently merged `jc/rust-cargo-build-target` topic as precedent. Feedback resolved in v3 and v4. Approved the v4 series for merging to `next` and fast-tracking to `master`, citing low risk and narrow scope. Later provided a factual report confirming Rust is not preinstalled on GitLab’s Windows runners, directly answering Johannes’s open question.
- Johannes Schindelin: Author; confirmed the UCRT64 mapping correction in v2 and aligned the build configuration logic. Explicitly requested testing on GitLab’s Windows runners to confirm Rust availability and toolchain compatibility. Split the series into two patches for clarity and incremental review. The v4 revision incorporates all feedback (variable naming, Cargo’s native build behavior, and mechanical cleanups) and is now merged.
- James Le Cuirot: Substantive review reinforcing Junio’s naming feedback and clarifying Cargo’s expected behavior. Explained that `--target` (or `CARGO_BUILD_TARGET`) should **not** be set for native builds, as it changes Cargo’s internal behavior and can break assumptions (e.g., `RUSTFLAGS` not applying to build-time tools). His feedback aligns with Gentoo Linux’s Rust cross-compilation practices and directly references the `jc/rust-cargo-build-target` topic. Feedback now resolved in v3 and v4; no objections to the series’ core goal.

Related work: This series is a prerequisite for the broader Rustification effort in Git, as it removes the `NO_RUST` opt-out in CI and directly unblocks future patches introducing Rust code. The series is narrowly scoped to Windows CI and does not interact with other in-flight efforts (e.g., ODB abstraction, reftable backend). The `jc/rust-cargo-build-target` topic (merged) is directly relevant, as it establishes the `CARGO_BUILD_TARGET` convention adopted here.