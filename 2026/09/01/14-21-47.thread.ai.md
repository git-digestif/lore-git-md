AI thread summary:

Thread identity: Patch series (v1) introducing Rust compilation support in Git’s GitHub Actions Windows CI jobs. Originating author: Johannes Schindelin (via GitGitGadget). Thread type: CI/build system adjustment.

Core problem or goal: Enable Rust compilation in Git’s GitHub Actions Windows CI workflows by configuring the Rust toolchain to target the GCC ABI (used by Git for Windows) instead of the default MSVC ABI. This unblocks the default Rust requirement introduced in Git 2.55.0 for Windows CI, ensuring Cargo produces a static library (`libgit.a`) compatible with MinGW’s `ld.exe`.

Current status: Series v1 posted; under review with one open technical question resolved. No prior versions. No merge status recorded. A v2 incorporating the UCRT64 target triple correction is expected next.

Key technical details:
- Files touched: `.github/workflows/main.yml`, `ci/lib.sh`, `config.mak.uname`, `Makefile`.
- New behavior: CI workflows use GitHub’s pre-installed Rust toolchain, configured via `CARGO_ARGS` to target `*-pc-windows-gnu` (MinGW) or `*-pc-windows-gnullvm` (Clang/LLVM) based on the `MSYSTEM` environment variable. The `RUST_LIB` path is adjusted to include the target triple, ensuring the correct `.a` file is linked. The second patch removes the `NO_RUST=UnfortunatelyYes` opt-out and installs the precompiled Rust `std` library for the GCC-compatible target triple, with explicit mapping from `$MSYSTEM` to target triple (`x86_64-pc-windows-gnu`, `aarch64-pc-windows-gnullvm`, etc.). The `PATH` is adjusted to ensure `cargo.exe` is found in the minimal Git for Windows SDK environment.
- The UCRT64 target triple mapping inconsistency identified by Junio has been resolved: UCRT64 now consistently maps to `x86_64-pc-windows-gnu` in both the `Makefile` and the shell script, matching the GCC ABI used by MinGW.
- No changes to core Git functionality, on-disk formats, or user-facing features. The patch is CI/build-system-only.
- Test coverage: CI-only; no new test files or test cases. The patch includes links to failed CI runs demonstrating the original problem (MSVC-targeted `.lib` file incompatible with MinGW).

Open questions and loose ends:
- Request for testing on GitLab’s Windows runners (author unable to confirm Rust availability there) remains open.

Participant positions:
- Junio C Hamano: Substantive review identifying a potential edge-case inconsistency in the Rust target triple mapping for UCRT64. Feedback addressed and resolved by Johannes; no further objections.
- Johannes Schindelin: Author; confirmed the UCRT64 mapping correction and aligned the build configuration logic.

Related work: This series is a prerequisite for the broader Rustification effort in Git, but does not itself introduce Rust code into the codebase. The removal of the `NO_RUST` opt-out in the second patch directly unblocks the eventual removal of the Rust opt-out in Git’s CI.