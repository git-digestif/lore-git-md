Mode: ai

Thread identity
Subject: [PATCH 0/13] odb: remove ad-hoc source linking
Series version: v2 (revision of v1; patch 1/13 revised, patches 2-5/13, 8/13, and 12-13/13 new in this update, patch 6/13 unchanged from v1 except for context adjustments, patch 7/13 updated to remove now-unused submodule ODB registration infrastructure, patches 9-11/13 unchanged except for context, patch 13/13 final removal of `odb_add_to_alternates_memory()`)
Originating author: Patrick Steinhardt
Thread type: patch series

Core problem or goal
Refactor the object database (ODB) subsystem to remove the mechanism allowing registration of in-memory sources (primarily used for submodules and temporary object directories). This eliminates dual-use of the ODB sources list for both alternates and temporary sources, simplifying the codebase and preparing for future migration of alternate handling into the "files" backend. The series also continues the broader `the_repository` removal effort by eliminating implicit dependencies on global state in the `cache-tree`, `submodule-config`, and related subsystems.

Current status
Under review; v2 posted 2026-09-05. All 13 patches are now complete: patch 1/13 revised to address Junio C Hamano’s feedback from v1, patches 2-5/13, 8/13, and 12-13/13 added in v2 to encapsulate repository dependencies, remove implicit submodule ODB source registration, eliminate remaining `the_repository`/`the_hash_algo` uses, and update test helpers, patch 6/13 unchanged from v1 except for context, patch 7/13 updated to remove the now-unused submodule ODB registration infrastructure, patches 9-11/13 unchanged except for context, and patch 13/13 (new in this update) removing the final `odb_add_to_alternates_memory()` function from `odb.c`/`odb.h`. The series is now fully self-contained: all callers of the ad-hoc source registration API have been updated or removed, the last non-test caller (in `git multi-pack-index --object-dir=`) is now rejected with an explicit error (patch 10/13), and test helpers (`test-read-midx.c`, `test-ref-store.c`) manage ODB sources explicitly. Junio C Hamano provided **substantive reviews** of patches 1/13 and 6/13 in v1, signaling unqualified approval with *“Yay. Very nice.”* Karthik Nayak provided **substantive reviews** of patches 2/13 and 7/13 in v2, confirming the correctness of the repository dependency encapsulation and the removal of the now-unused submodule ODB registration infrastructure. In v2, Karthik Nayak additionally provided a **substantive review** of the full series, offering cautious approval ("looks good") while acknowledging limited familiarity with the ODB subsystem. Justin Tobler provided a **substantive review** of patch 5/13 in v2, confirming the safe removal of the last call to `odb_add_submodule_source_by_path()` in `submodule-config.c` and the elimination of the 2019 workaround (commit `e3e8bf046e`) that became unnecessary after earlier patches in the series removed implicit dependencies on `the_repository`. Patrick Steinhardt’s reply to Karthik Nayak in v2 confirms the diagnosis in patch 2/13: the `cache-tree` subsystem was incorrectly using `the_repository` when looking up `.gitmodules` from a submodule’s index, causing the lookup to fail unless the submodule’s ODB was registered as an alternate. The fix propagates the correct repository context through the call chain, eliminating the need for the alternate registration workaround. Patrick Steinhardt’s follow-up reply to Justin Tobler in v2 clarifies the commit message of patch 5/13, addressing Justin’s initial confusion about the historical context of the 2019 workaround. No new objections or merge intent from Junio; the series remains uncontroversial with no external dependencies.

Key technical details
- Files touched: `odb.c`/`odb.h` (removal of `odb_add_to_alternates_memory()`, `submodule_source_paths` field, `odb_add_submodule_source_by_path()`, `register_all_submodule_sources()`), `submodule-config.c`, `builtin/grep.c`, `tmp-objdir.c`/`tmp-objdir.h` (removal of `tmp_objdir_add_as_alternate()`), `t/helper/test-read-midx.c` (explicit ODB source management), `t/helper/test-ref-store.c` (explicit ODB source management), `cache-tree.c`/`cache-tree.h`, `read-cache-ll.h`, `read-cache.c`, `unpack-trees.c`, `builtin/checkout.c`, `builtin/commit.c`, `sequencer.c`, `sparse-index.c`, `odb/source-packed.c` (memory leak fix), `builtin/multi-pack-index.c` (enforce alternate-only `--object-dir=`), plus callers in `builtin/fetch.c`, `builtin/submodule--helper.c`, `submodule.c`, `t/helper/test-submodule.c`, `t/README`, and test scripts (`t/t5319-multi-pack-index.sh` updated to retain `repo64` for alternate validity; seven additional test scripts updated to remove `GIT_TEST_FATAL_REGISTER_SUBMODULE_ODB` exports).
- Symbols removed: ODB source registration functions (`odb_add_submodule_source_by_path()`, `register_all_submodule_sources()`, `odb_add_to_alternates_memory()`, `tmp_objdir_add_as_alternate()`), `submodule_source_paths` string list in `struct object_database`, `GIT_TEST_FATAL_REGISTER_SUBMODULE_ODB` test mode, `#define USE_THE_REPOSITORY_VARIABLE` in `submodule-config.c`.
- Symbols modified: `must_prefetch_predicate` callback type extended to accept `void *cb_data` for repository context; all functions in `submodule-config.c` updated to accept `struct repository *` parameter; `hash_oid_string` updated to use the hash algorithm embedded in the object ID (`hash_algos[oid->algo].rawsz`). Patch 1/13 encapsulates the repository dependency within `cache-tree.c` by renaming `cache_tree_fully_valid()` to `cache_tree_fully_valid_recursive()` (file-scoped static) and adding a wrapper that delegates to it using `istate->repo`. Patch 2/13 updates `must_check_existence()` and `update_one()` to accept a `struct repository *` parameter, propagates it through `cache_tree_write()`/`cache_tree_read()`, and adjusts `prefetch_cache_entries()` to accept `void *cb_data`. Patch 4/13 updates `gitmodules_cb` to use `repo->hash_algo` and `hash_oid_string` to use `hash_algos[oid->algo].rawsz`. Patch 5/13 removes the last call to `odb_add_submodule_source_by_path()` in `submodule-config.c` (a 2019 workaround for `config_with_options()`’s limitation, made obsolete by commit `e3e8bf046e` in 2021 and the removal of implicit `the_repository` dependencies in earlier patches). The commit message for patch 5/13 was clarified in response to Justin Tobler’s feedback, explaining that the 2019 workaround was necessary because `config_with_options()` could only read blobs from `the_repository`, even when the blob was resolved via `repo_get_oid()` in a submodule. Patch 7/13 removes the `submodule_source_paths` string list and deletes `odb_add_submodule_source_by_path()`/`register_all_submodule_sources()`. Patch 8/13 removes `tmp_objdir_add_as_alternate()`. Patch 9/13 adds a call to `odb_source_close(source)` in `odb/source-packed.c` to fix a memory leak. Patch 10/13 enforces the alternate-only constraint for `git multi-pack-index --object-dir=`, replacing ad-hoc source creation with an explicit error message. Patches 11/13 and 12/13 update test helpers to manage ODB sources explicitly, replacing `odb_add_to_alternates_memory()` with direct creation/freeing of standalone `odb_source_packed` instances. Patch 13/13 removes the final `odb_add_to_alternates_memory()` function (14 lines deleted).
- Subsystems affected: ODB (including packed source backend), submodule handling, grep plumbing, cache-tree, checkout, commit, sequencer, sparse-index, unpack-trees, fetch, submodule helpers, test infrastructure, temporary object directory handling, multi-pack-index.
- No on-disk format changes; no behavioral changes expected beyond the memory leak fix (patch 9/13) and the breaking change in `git multi-pack-index --object-dir=` (patch 10/13: now rejects non-alternate directories, aligning with documented behavior since 2021).
- Test coverage: updated test helpers (`test-read-midx.c`, `test-ref-store.c`, `test-submodule.c`) and test scripts to reflect API removal; the `GIT_TEST_FATAL_REGISTER_SUBMODULE_ODB` test mode is now a no-op and removed from documentation and test scripts. Patch 2/13 verifies the removal of implicit dependencies by running tests with `GIT_TEST_FATAL_REGISTER_SUBMODULE_ODB=true`. Patch 6/13 relies on prior work (commit 0693806bf8, 2021) to ensure submodule repositories are passed directly to worker threads. Patch 10/13 updates `t5319-multi-pack-index.sh` to retain `repo64` for alternate validity. Patches 11/13, 12/13, and 13/13 ensure test infrastructure remains functional after the removal of the ad-hoc source registration API.

Open questions and loose ends
- No substantive design objections or new questions raised in v2; the correctness issues identified by Junio in v1 are resolved, and Karthik Nayak’s reviews of patches 2/13, 7/13, and the full series, along with Justin Tobler’s review of patch 5/13, confirm the elimination of implicit dependencies and the safe removal of unused infrastructure. Patrick Steinhardt’s replies to Karthik Nayak and Justin Tobler confirm the diagnosis in patch 2/13 and clarify the historical context in patch 5/13, removing ambiguity about the root causes of the bugs fixed.
- Cover letter notes this paves the way for future performance fixes in alternate handling (not part of this series).
- The memory leak fix (patch 9/13) is a prerequisite for later patches, ensuring the refactoring does not introduce regressions.
- Patch 10/13 enforces an existing documented constraint, removing the last non-test caller of the ad-hoc source mechanism.
- The NEEDSWORK comment in `builtin/grep.c` (patch 6/13) notes a remaining thread-safety issue: initializing the subrepository still requires the object-read lock due to `get_main_ref_store()`. This is outside the scope of the series.
- No indication yet whether Junio intends to merge the series to `next`; his earlier substantive approvals of patches 1/13 and 6/13 ("Yay. Very nice."), Karthik Nayak’s approvals of patches 2/13, 7/13, and the full series, and Justin Tobler’s approval of patch 5/13 suggest the core refactoring is uncontroversial, but no explicit merge intent has been stated.

Participant positions
- Patrick Steinhardt (author): proposes the refactoring as a maintainability improvement and prerequisite for ODB abstraction work. Revised patch 1/13 and added patches 2-5/13, 8/13, and 12-13/13 to address Junio’s feedback, encapsulate repository dependencies, remove implicit submodule ODB source registration, eliminate remaining `the_repository`/`the_hash_algo` uses, and update test helpers to manage ODB sources explicitly. Patch 6/13 (unchanged from v1) removes the now-unnecessary registration of submodule ODBs in `git grep`. Patch 7/13 removes the now-unused submodule ODB registration infrastructure. Patch 9/13 fixes a memory leak in the packed ODB source backend. Patch 10/13 enforces the alternate-only constraint for `git multi-pack-index --object-dir=`. Patch 13/13 removes the final `odb_add_to_alternates_memory()` function, completing the cleanup. All 13 patches reviewed in detail by the author. Confirmed Karthik Nayak’s diagnosis of the bug fixed in patch 2/13, clarifying that the `cache-tree` subsystem was incorrectly using `the_repository` for `.gitmodules` lookups, necessitating the alternate registration workaround. Clarified the commit message of patch 5/13 in response to Justin Tobler’s feedback, explaining the historical context of the 2019 workaround.
- Junio C Hamano (maintainer): provided **substantive reviews** of patches 1/13 and 6/13 in v1, signaling unqualified approval with *“Yay. Very nice.”* No further engagement with v2.
- Karthik Nayak: provided **substantive reviews** of patches 2/13 and 7/13 in v2, confirming the correctness of the repository dependency encapsulation and the safe removal of the now-unused submodule ODB registration infrastructure. In v2, additionally provided a **substantive review** of the full series, offering cautious approval ("looks good") while acknowledging limited familiarity with the ODB subsystem. The review did not identify specific issues but confirmed understanding of the series’ goals and the correctness fixes in patch 1/13. Diagnosed the root cause of the bug fixed in patch 2/13: the `cache-tree` subsystem’s incorrect use of `the_repository` for `.gitmodules` lookups, which Patrick Steinhardt later confirmed.
- Justin Tobler: provided a **substantive review** of patch 5/13 in v2, confirming the safe removal of the last call to `odb_add_submodule_source_by_path()` in `submodule-config.c` and the elimination of the 2019 workaround (commit `e3e8bf046e`) that became unnecessary after earlier patches in the series removed implicit dependencies on `the_repository`. Initially found the commit message of patch 5/13 unclear ("I had to reread the commit message a couple of times") but eventually grasped the historical context ("makes sense now"). His review ("Nice to have one less workaround") signals approval and adds confidence to the patch’s correctness. Patrick Steinhardt deferred to Justin’s preference on whether to revise the commit message, indicating no urgency to change it.

Related work
- Part of Patrick Steinhardt’s ongoing ODB abstraction effort.
- Depends on prior refactoring (e.g., submodule sources no longer needing to link into the main ODB, and `builtin/receive-pack`’s switch to ODB transactions in commit bdee7b3013, July 2026).
- Continues the `the_repository` removal effort (René Scharfe as primary driver).
- No external dependencies or related in-flight topics mentioned.