This is a **substantive review** of Michal Koutný's bugfix patch for merge driver temporary file cleanup on interrupt. Jeff King (Peff) responds with an alternative approach that replaces the signal-handling workaround with Git's existing `tempfile` API, which already handles signal propagation and atomic cleanup.

Peff's review identifies a cleaner solution: instead of manually managing signal chains to ensure cleanup, the patch should register the temporary files with Git's `tempfile` subsystem (`register_tempfile()` or `xmks_tempfile()`). This subsystem is designed to handle exactly this scenario—ensuring files are deleted even if the process is interrupted. His proposed patch refactors `ll_ext_merge()` to use `struct tempfile` objects, eliminating the need for custom signal handling while maintaining the same cleanup guarantees.

The review includes a complete, lightly tested patch that:
- Replaces the fixed-size `char temp[3][50]` buffers with `struct tempfile *` pointers.
- Introduces a `create_temp()` helper that returns a `tempfile` object (using `xmks_tempfile()`).
- Adds a `get_temp_path()` helper to work around historical limitations with absolute paths and shell quoting (noting this as a potential future improvement).
- Uses `delete_tempfile()` for cleanup, which is signal-safe.

Peff also notes a minor nit: the code could use `strbuf_read_file()` instead of manual `open()`/`fstat()`/`xread()` calls, but this is left as a suggestion rather than a requirement.

This is a **tested review**—Peff explicitly states he has lightly tested the patch, though he does not provide test results or scenarios. The feedback is **substantive**, engaging with the core problem (resource cleanup on interrupt) and proposing a more idiomatic solution that leverages existing Git infrastructure. The alternative approach is likely to be less error-prone and more maintainable, as it avoids duplicating signal-handling logic already encapsulated in the `tempfile` API.

Key technical details for follow-up:
- **Files touched**: `merge-ll.c` (same as original patch).
- **New functions**: `create_temp()` (returns `struct tempfile *`), `get_temp_path()` (handles path quoting).
- **APIs used**: `xmks_tempfile()`, `delete_tempfile()`, `get_tempfile_path()`.
- **Behavior change**: No functional change in cleanup guarantees, but the implementation is now more robust and aligned with Git's established patterns.
- **Open question**: Whether to address shell quoting of absolute paths in the future (noted in `get_temp_path()`).

The review advances the thread by shifting the discussion from "how to handle signals" to "how to best use Git's existing cleanup mechanisms." No objections to the original problem statement are raised, and the alternative solution appears to address the same edge case more elegantly. The next step is likely for Michal to evaluate Peff's patch or propose adjustments.