Here is the updated AI-mode thread summary, preserving all prior state and incorporating the new email:

---

Thread identity: "What's cooking in git.git" integration report (recurring series), originating author Junio C Hamano, thread type integration status report.

Core problem or goal: Provide a snapshot of the current state of Git's integration branches (`next`, `seen`, `master`), listing topics under review, cooking, stalled, or recently graduated, to inform contributors of what is in flight and what needs attention.

Current status: As of September 2026, the following topics have graduated to `master` since the last report: `en/midx-missing-pack-fallback` (fixes object lookup and segfault in `git replay`), `hk/typofix` (routine typo fixes), `hn/ci-cancel-stale-pr-runs` (GitHub Actions CI improvement), `jk/rev-info-argv-to-free` (memory ownership fixes), `ns/ref-symref-additional-tests` (new ref-handling tests), `rs/worktree-add-basename-fixes` (path-handling fixes in `git worktree add`), and `tc/replay-linearize` (new `--linearize` option for `git replay`). The one-patch documentation fix `bc/maintenance-doc-markup-fix-for-asciidoc` (an AsciiDoc bullet formatting correction in `git-config(1)`) has been fast-tracked directly to `next` by Junio C Hamano, bypassing the usual one-week soak due to its trivial and uncontroversial nature. The `ps/libgit-in-subdir` series (proposed `lib/` subdirectory for `libgit.a` sources) has been definitively discarded following Patrick Steinhardt's withdrawal; it may be revisited at the upcoming Contributor’s Summit. The `ps/odb-stop-registering-in-memory-sources` series (13-patch ODB cleanup removing ad-hoc alternate registration) has been merged to `next` after its v3 update, which addressed a single-word commit-message tweak. New topics needing review remain `vm/advice-config-global-hint` (advice scoping), `jc/rust-cargo-build-target` (Rust cross-compilation fix), `mh/rust-crate-subdir` (Rust code organization), `ps/ref-storage-format` (unified ref-storage terminology), `ap/var-broken-down-idents` (extended `git var` functionality), `ks/history-commit-leakfix` (memory leak fix in `git history`), and `kn/receive-report-hook` (new hook for packet-line status reports). The duplicate CI commit issue noted by Tuomas Ahola (identical fixes in `ps/ci-depends-on-ruby` and `jk/ci-use-system-asciidoctor`, with the former containing a typo) has been resolved: Junio C Hamano confirmed the earlier, typo-containing commit (`ps/ci-depends-on-ruby`) will be dropped when merging to `master`, retaining only the corrected version (`jk/ci-use-system-asciidoctor`).

Key technical details: Subsystems touched include the object database (ODB abstraction in `ps/odb-*` topics), reference storage formats (`ps/ref-storage-format`), sequencer/autostash (`tb/rerere-lock-grace`, `hn/history-squash`), Rust integration (`jc/rust-cargo-build-target`, `mh/rust-crate-subdir`), hooks (`kn/receive-report-hook`), documentation markup (`bc/maintenance-doc-markup-fix-for-asciidoc`), and CI/build system (duplicate commit cleanup in `ps/ci-depends-on-ruby` and `jk/ci-use-system-asciidoctor`). New CLI options include `--linearize` for `git replay` and `--ref-storage-format` for configuration. On-disk format changes are not noted in this report. Test coverage is mentioned for ref handling (`ns/ref-symref-additional-tests`) and implied for other fixes.

Open questions and loose ends: Likely points of contention include Rust-related changes (e.g., `jc/rust-cargo-build-target`) due to platform compatibility concerns (notably NonStop), and Patrick Steinhardt’s ODB abstraction effort (`ps/odb-pluggable-fsck`, `ps/odb-stop-registering-in-memory-sources`), which has historically generated discussion. `hn/history-squash` introduces experimental functionality that may need refinement. The withdrawn `ps/libgit-in-subdir` series may resurface at the Contributor’s Summit. The duplicate CI commit issue has been resolved procedurally; no further action is required. No conditional approvals or unresolved design objections are explicitly noted beyond the procedural decisions already addressed.

Participant positions: Junio C Hamano (maintainer) fast-tracked `bc/maintenance-doc-markup-fix-for-asciidoc` to `next`, discarded `ps/libgit-in-subdir` following Patrick Steinhardt's withdrawal, merged `ps/odb-stop-registering-in-memory-sources` v3 to `next`, and confirmed the resolution to drop the typo-containing `ps/ci-depends-on-ruby` commit when merging to `master`. Patrick Steinhardt withdrew `ps/libgit-in-subdir` due to lack of consensus and provided the v3 update for `ps/odb-stop-registering-in-memory-sources`. Tuomas Ahola noted the duplicate CI commit issue, suggesting the earlier, typo-containing commit be dropped when merging to `master`. No explicit Reviewed-by/Acked-by trailers are present in the report itself. Low-weight messages (e.g., typo fixes in `hk/typofix`) are omitted from participant tracking. The report does not record individual reviewer positions on active topics; those would be captured in prior per-topic summaries.

Related work: The report references Patrick Steinhardt’s ODB abstraction effort (`ps/odb-*` topics), which is a multi-year project to enable pluggable object storage backends. Rust integration (`jc/rust-cargo-build-target`, `mh/rust-crate-subdir`) is part of the ongoing Rustification effort led by Ezekiel Newren. The `ps/ref-storage-format` topic unifies terminology for reference storage formats, relevant to the reftable backend effort (Patrick Steinhardt, Karthik Nayak). No external dependencies are mentioned.

---