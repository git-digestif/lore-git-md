This is the **v10 cover letter** for the `receive-report` hook series, now in `next`. Karthik Nayak confirms the final iteration (v10) is ready, incorporating all remaining feedback: the **memory leak fix** in `override_cmds_error()` (Junio’s last substantive review) and explicit acknowledgment of the **untestable `BUG()` flaw** (Jeff King’s and Junio’s clarification). The series is feature-complete, with no open technical or design questions; the only loose end is the test coverage gap for clients that omit the `report-status` capability, which is now documented as a practical limitation rather than a flaw. The four patches remain unchanged in scope:
1. Documentation (unchanged since v9).
2. Refactoring (enum conversion, `BUG()` fix applied in v9).
3. Refactoring (unified `report()` helper, unchanged since v9).
4. Core implementation (memory leak fixed in v10, 224-line test suite covering all other edge cases).

Key technical details carried forward: the hook filters the pkt-line encoded status report on stdin/stdout, forwards stderr via sideband, and runs after all ref updates are committed. The `error_string_owned` flag now tracks ownership of `cmd->error_string` to prevent leaks in the non-zero exit path. The commit message explicitly justifies the consistency trade-off (decoupling reported status from actual repository state) with GitLab’s MVCC use case. Junio’s manual conflict resolution with Justin Tobler’s "pluggable writes" series is confirmed correct. The series is **ready for final review and graduation to `master`**.