This is a brief follow-up in the ongoing Rust reorganization series. Mike Hommey acknowledges two mechanical errors in his v3 patch that Junio C Hamano flagged in review: duplicate `.gitignore` entries for `/target/` and `/Cargo.lock` (which should have been removed from the top level rather than duplicated in `rust/`), and duplicate `RUST_SOURCES` entries in the Makefile. Both are straightforward rebase artifacts that slipped through.

He also confirms that the `Cargo.lock` file *does* move to the `rust/` subdirectory, meaning the `clean` target in the Makefile needs to be updated to match the new location. This resolves the apparent contradiction Junio noted between the `.gitignore` changes and the `clean` target.

The fixes are purely mechanical and do not affect the patch’s core goal—relocating Rust files to improve project structure. No new technical concerns are raised; this is just cleanup before the series can be accepted. The thread remains focused on implementation details rather than the broader Rustification discussion.