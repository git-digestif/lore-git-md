This is patch 5/9 in Patrick Steinhardt’s ODB alternates refactoring series, now receiving a surface-level review from Karthik Nayak. The patch moves the setup of alternates for shared local clones (`git clone --shared`) from `clone_local()` into the newly extended `collect_alternates()` function, consolidating alternates logic into a single code path. The change is mechanical and preserves existing behavior, but Karthik’s review raises a minor observation about the conditional logic.

Karthik’s feedback is narrowly scoped: he asks whether the `is_local` check in `collect_alternates()` should also guard against `option_shared`, noting that the patch currently checks `option_shared` only after entering the `is_local` block. This is a surface-level readability suggestion rather than a technical critique. The patch’s logic is correct as written—the `option_shared` guard ensures alternates are only added for shared local clones, addressing Justin Tobler’s earlier feedback about incorrectly adding alternates for non-shared local clones (`git clone --local --no-shared`). Karthik’s comment does not engage with the patch’s core refactoring, correctness, or edge cases, and his observation is unlikely to require significant changes.

The patch remains uncontroversial and aligns with the series’ incremental approach. The consolidation of alternates logic is a necessary step toward eliminating the `write_alternates()` callback in patch 9/9. Reviewers may focus on the `option_shared` guard to ensure it correctly isolates shared clones, but the fix is straightforward and already addresses the specific issue raised in v3. No new symbols or user-visible changes are introduced. The patch’s minimal diff (14 insertions, 9 deletions) and targeted scope make it a low-risk refactoring.