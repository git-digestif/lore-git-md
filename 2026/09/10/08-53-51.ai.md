This is the first patch in Vsevolod Myalitsin’s v4 series, a refactoring that prepares the `advice.c` subsystem for the upcoming scope-hint feature. It changes the `vadvise()` function to accept the entire `advice_setting` struct instead of just the configuration key and level. The patch is purely internal: no user-visible behavior changes, no new fields, and no translatable strings are touched. The diff is confined to `advice.c` and mechanically replaces the old two-argument interface with a single struct pointer, adjusting the call sites in `advise()` and `advise_if_enabled()` accordingly.

The motivation is straightforward: the next patch in the series will add a `scope_hint` field to `advice_setting`, and passing the whole struct now avoids a second interface change later. The commit message is concise but clear, and the diff itself is clean—only 11 lines added, 9 removed, with no indentation or style issues. This is a textbook refactoring patch: small, focused, and safe. It should be uncontroversial and is ready for review. Key symbols to watch in follow-up discussion are `vadvise()`, `advice_setting`, and the call sites in `advise()` and `advise_if_enabled()`.