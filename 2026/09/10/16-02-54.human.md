This is a brief, friendly post-merge note from Jeff King (Peff) acknowledging the already-merged memory-leak fix in `git history reword`. The patch itself is unchanged and already in `master`; Peff’s email is purely editorial.

He compliments the improved commit message in v3 but offers an even more concise alternative that strips the explanation down to the essentials: the pairing rule for `repo_logmsg_reencode`/`repo_unuse_commit_buffer`, the leak’s conditional nature (commit-graph parsing), and a minimal reproduction recipe. He explicitly says he is not asking for another iteration—just sharing a style suggestion for future patches. The tone is collegial and low-stakes; there is nothing here that requires further action or review.