This is a **maintainer reply** in the ongoing advice-scope-hint thread, responding to Jeff King’s historical argument for defaulting all advice settings to `--global`.

Junio acknowledges that he did not contribute to the philosophical design of the `advice.*` config system when it was introduced in 2009, and that the lack of scope discussion in that thread (or later ones) aligns with the original intent: the "use `git config advice.foo false`" instruction was never meant to be a literal cut-and-paste command, but rather a general suggestion that users would adapt to their own needs. This stance implies that the absence of a scope indicator in the hint was intentional, as users were expected to know how to adjust the command for their context (e.g., adding `--global` or `--local` as needed).

The reply does not endorse or reject the boolean `is_global_hint` simplification proposed in Vsevolod’s v4 series, but it subtly reinforces the idea that the current design (no scope hint) was not an oversight, and that adding scope hints now is a deliberate enhancement rather than a bugfix. Junio’s tone is neutral, leaving the door open for either the simplified boolean approach or a more flexible mechanism, though his earlier counterpoint about per-project workflows suggests he remains open to `--local` support. The thread’s unresolved design question—whether to adopt the boolean simplification or retain flexibility for future scopes—remains unanswered, with no new technical details or build issues introduced.