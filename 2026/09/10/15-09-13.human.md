This is a mechanical refactoring patch in Patrick Steinhardt’s 9-patch series to refactor ODB alternates handling during repository creation. Part of the ongoing ODB abstraction effort, this patch moves the `setup_reference()` function in `builtin/clone.c` closer to `copy_alternates()` to eliminate a forward declaration, preparing for patch 4/9’s consolidation of alternates logic.

The change is purely structural: `setup_reference()` is relocated 20 lines downward in the file, immediately after `copy_alternates()`, with no modifications to its logic or behavior. This avoids the need for a forward declaration when `setup_reference()` starts calling `copy_alternates()` in the next patch. The patch touches only `builtin/clone.c` (10 insertions, 10 deletions) and introduces no new symbols or user-visible changes.

Karthik Nayak’s surface-level review of v4 noted that `setup_reference()` will be renamed in patch 4/9 but did not engage with the patch’s correctness or design. The change is uncontroversial and aligns with the series’ incremental approach, which has already addressed all substantive feedback from earlier iterations. This patch is a straightforward prerequisite for the more significant alternates refactoring in patches 4-6.