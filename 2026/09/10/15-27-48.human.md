This is a brief confirmation email in the ongoing memory-leak fix for `git history reword --dry-run`. Kristoffer Haugsbakk chimes in to acknowledge that the leak’s environment-dependent nature—specifically, its reliance on commit buffers not being cached in the commit slab—now makes sense, with the commit graph identified as the likely triggering factor. This aligns with earlier discussion that the leak only surfaces in "real repos" with packfiles or commit-graph configurations, not in minimal test setups.

The thread itself is effectively closed on the technical side; the fix (a single-line addition of `repo_unuse_commit_buffer()` in `builtin/history.c`) is settled, and the only remaining action is Kaartic Sivaraam’s reroll with a corrected commit message and possibly a test case. Kristoffer’s note doesn’t introduce new information but serves as a quiet sign-off, reinforcing that the root cause is understood. Watch for Kaartic’s reroll—it should be the final step before Junio picks up the patch for `next`.