This is the first patch in a v2 test modernization series targeting the `t40*` test scripts. The patch updates `t/t4001-diff-rename.sh` and `t/t4009-diff-rename-4.sh` to use modern Git test syntax and structural improvements, following Junio C Hamano’s substantive feedback on v1. The changes are purely stylistic, with no functional impact on test behavior.

The patch replaces the old `setup` test block with a helper function (`initial_setup`) and merges setup steps directly into the main test bodies, eliminating redundant `echo` commands and here-documents. It also standardizes variable naming (`actual`/`expect` instead of `current`/`expected`) and improves indentation and logical grouping of test steps. The diff shows a net reduction in lines (97 insertions, 109 deletions in `t4001`), reflecting a more concise and idiomatic style.

Key technical details include the use of modern test helpers (e.g., `test_path_is_file`) and the adoption of single-quoted strings for multi-line `test_expect_success` blocks, which improves readability. The patch also aligns with Git’s documented test best practices, particularly around consistent indentation and logical grouping of related test steps. The base commit is `5c40f33ab5825c90dd32cfb08198de2beedc6ed1`.

This patch is part of the broader community-wide test modernization effort and is under review. The v2 revisions address Junio’s critique that mechanical modernization alone was insufficient, demonstrating the author’s engagement with the project’s stylistic expectations. The changes are small and focused, making them easy to review, but the structural improvements may warrant closer scrutiny to ensure they fully meet the project’s standards. No new functional issues or edge cases are introduced.