This is patch 4/9 in Patrick Steinhardt’s ODB alternates refactoring series, a well-executed refactoring that prepares for the consolidation of alternates handling during repository creation. Karthik Nayak’s review here is surface-level, focusing on readability and minor stylistic choices rather than the patch’s technical substance or correctness.

The patch renames `setup_reference()` to `collect_alternates()` and refactors it to gather alternates paths into a `strvec` instead of immediately writing them to the object database. This is a mechanical intermediate step that preserves existing behavior while enabling later patches to unify alternates logic across all clone modes. The new `struct add_one_alternate_data` tracks whether each alternate is required or optional, and the alternates writes are deferred until after the `strvec` is populated. Karthik’s comments note that the struct name could be clearer (it holds data for `add_one_alternate()`, not a single alternate) and suggest a minor readability improvement by inverting the early-return condition in `collect_alternates()`. He also observes that the alternates writes are now handled outside the function, which could be worth mentioning in the commit message.

The patch itself is uncontroversial and aligns with the series’ goal of simplifying the ODB interface. Karthik’s feedback does not engage with the patch’s design, edge cases, or testing, and his suggestions are purely stylistic. The changes are minimal (28 insertions, 16 deletions) and touch only `builtin/clone.c`, making this a low-risk refactoring. Reviewers may focus on the clarity of the new `collect_alternates()` interface, but the patch is a straightforward step toward the series’ broader consolidation of alternates logic. No substantive objections or technical concerns are raised in this review.