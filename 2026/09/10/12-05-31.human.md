This is a Rust infrastructure discussion, part of Mike Hommey’s ongoing patch series to reorganize Git’s Rust code into a `rust/` subdirectory. The series aims to improve project structure and support downstream projects that vendor Git’s C code while excluding its Rust components.

In this reply, Mike clarifies his long-term plan for handling the forward-compatibility concern raised by brian m. carlson. Specifically, Mike proposes publishing the `gitcore` crate to crates.io under a namespaced identifier (or ideally, having the Git project itself publish it). This would allow downstream projects like git-cinnabar to depend on Git’s C code without being forced to adopt its Rust components, even after Git 3.0 makes Rust mandatory. The response does not address whether this approach would require changes to the current patch series, but it suggests that Mike sees the reorganization as a step toward a sustainable solution rather than a temporary workaround.

The exchange remains focused on the practical implications for downstream consumers, with no objections to the patch itself. The discussion now hinges on whether Mike’s proposed crate-publishing strategy is acceptable to the Git project and whether it resolves the forward-compatibility issue. This is a substantive clarification that may influence how the community views the patch series’ long-term viability.