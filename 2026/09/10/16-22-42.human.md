This is a **substantive review** of Michal Koutný’s bugfix patch for merge-driver temporary-file cleanup on interrupt. Jeff King (Peff) responds with an alternative approach that replaces the signal-handling workaround with Git’s existing `tempfile` API, which already handles signal propagation and atomic cleanup.

Peff’s counter-patch does three things:
1. Converts the manual `.merge_file_*` creation into `tempfile`-managed objects, eliminating the need for custom signal handling.
2. Introduces a helper (`get_temp_path`) to extract the relative path from the `tempfile` struct, preserving compatibility with the existing command-line format strings that expect relative paths.
3. Removes the manual `unlink_or_warn` calls in favor of `delete_tempfile`, which is signal-safe and atomic.

The review is **tested** (Peff notes “lightly tested”) and **substantive**: it identifies a cleaner architectural solution, avoids reinventing signal handling, and reduces code complexity. The discussion is now focused on which approach—signal-chain manipulation or `tempfile` API adoption—is the better fix. No objections have been raised yet, but the thread is likely to continue with either a v2 from Michal or an adoption of Peff’s version.