This is patch 8/9 in Patrick Steinhardt’s 9-patch series refactoring ODB alternates handling during repository creation, part of the ongoing ODB abstraction effort. The patch wires up the alternates `strvec` collected by `collect_alternates()` to the ODB creation process via the new `odb_create_on_disk_options` struct, eliminating the manual alternates loop in `cmd_clone()` and preparing for the removal of the `write_alternates()` callback in patch 9/9.

The change is mechanical and straightforward: `create_object_database()` now accepts a `strvec` of alternates paths (or `NULL` for no alternates), and `cmd_clone()` passes the alternates `strvec` directly to `create_object_database()` instead of manually writing each alternate via `odb_add_to_alternates_file()`. The patch touches four files: `builtin/clone.c` (removing the manual alternates loop), `builtin/init-db.c` (updating `cmd_init_db()` to pass `NULL` for alternates), `setup.c` (extending `create_object_database()` to accept the alternates `strvec` via `odb_create_on_disk_options`), and `setup.h` (updating the function signature and documentation). No new symbols are introduced, and the patch preserves existing behavior while simplifying the code.

This patch is a direct follow-up to patch 7/9, which implemented the core design change of writing alternates during ODB creation using Git’s standard lockfile API. The alternates `strvec` is now passed through the `odb_create_on_disk_options` struct, ensuring atomic writes and resolving TOCTOU race and concurrent access concerns. The patch is uncontroversial and aligns with the series’ incremental approach, as all alternates logic has already been consolidated into `collect_alternates()` in earlier patches (4-6/9). The removal of the manual alternates loop in `cmd_clone()` is a natural progression, and the updated function signature in `setup.h` clearly documents the new `alternates` parameter.

The patch’s impact is limited to wiring up the alternates `strvec` to the ODB creation process, with no changes to the alternates collection logic or the lockfile-based writing mechanism introduced in patch 7/9. The `NULL` default in `cmd_init_db()` ensures backward compatibility for callers that do not use alternates, and the patch does not affect the latent bug fix for worktree alternates handling (addressed in patch 6/9). Reviewers may focus on the updated function signature in `setup.h` to ensure clarity, but the patch’s core logic is unlikely to attract substantive feedback given its mechanical nature.

This patch is a key step toward completing the series’ goal of making alternates an ODB backend implementation detail, as it eliminates the need for manual alternates handling in `cmd_clone()` and prepares for the removal of the `write_alternates()` infrastructure in patch 9/9. The series remains on track for integration, with all substantive review feedback addressed in earlier patches. The only open technical concern from the thread—Karthik Nayak’s feedback on error handling robustness in patch 7/9—does not affect this patch, as it is purely about wiring up the alternates `strvec` to the existing ODB creation mechanism.