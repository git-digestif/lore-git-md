This is a surface-level review in the ongoing documentation formatting thread. Jeff King (Peff) replies to Kristoffer Haugsbakk’s compatibility question about the patch’s effect on AsciiDoc versus Asciidoctor rendering. He confirms the discrepancy Kristoffer observed: Asciidoctor (used by git-scm.com) already renders the bullet list correctly without the patch, while classic AsciiDoc requires the fix. Peff provides practical guidance for documentation contributors, suggesting the use of `./doc-diff` with both `--asciidoc` and `--asciidoctor` flags to compare rendering outputs before submitting patches. He notes that direct comparisons between the two processors (`--from-asciidoctor --to-asciidoc`) generate significant noise due to stylistic differences, which may obscure the specific formatting issue at hand.

The review does not challenge the patch itself or suggest changes; it simply clarifies the processor-specific behavior and offers a workflow tip for future documentation work. This reinforces the patch’s scope (targeting classic AsciiDoc) while acknowledging that Asciidoctor’s resilience to the issue makes the fix non-universal. The thread remains uncontroversial, with no new objections or requests for rerolls. The original patch (v2) is still a strong candidate for integration into `next`, pending Junio’s next merge cycle. Key technical details to note for follow-up discussion: the `doc-diff` tool, the `--asciidoctor` flag, and the historical context of processor divergence in Git’s documentation pipeline.