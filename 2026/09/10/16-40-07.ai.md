This is the **v10 cover letter** for the `receive-report` hook series, now queued in `next`. Karthik Nayak acknowledges Jeff King’s demonstration of how to trigger the `BUG()` flaw in patch 2/4 using an interop test with an older Git client (v1.0.0). The test confirms the flaw was real but untestable with the existing test infrastructure because older clients do not request status reports, making the crash invisible to them. Karthik explicitly declines to add an interop test, citing the practical limitations Peff noted: the interop suite is rarely run, has hidden gotchas (e.g., building old Git versions), and is not a realistic addition to the series. This email does not introduce new code or design changes; it closes the loop on the test coverage gap by documenting the limitation and the rationale for not addressing it. The series remains **feature-complete and ready for final review**, with all prior feedback incorporated. Key files: `builtin/receive-pack.c`, `Documentation/githooks.adoc`, `t/t5412-receive-report-hook.sh`.