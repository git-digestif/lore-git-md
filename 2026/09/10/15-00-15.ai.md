This is a v3 resend of a bugfix patch that was already merged to `master`. The patch addresses a memory leak in `git history reword` when the commit buffer is dynamically allocated (e.g., from commit-graph loading) rather than cached in Git’s commit slab. The fix is a one-line addition in `builtin/history.c` (`commit_tree_ext`) to call `repo_unuse_commit_buffer` on the buffer obtained via `repo_logmsg_reencode`.

The v3 iteration improves the commit message’s readability but makes no functional changes to the code. The leak’s root cause—commit-graph parsing bypassing raw object reads and thus never caching the buffer in the slab—is now clearly explained, along with a minimal reproduction recipe and confirmation that the fix resolves related test failures (`t345x`). The patch is narrowly scoped, well-motivated, and already integrated; this resend appears to be a courtesy to the list or a documentation update rather than a substantive revision.

Key technical details remain unchanged: the leak surfaces under `SANITIZE=leak GIT_TEST_COMMIT_GRAPH=1`, the fix targets `builtin/history.c` (`commit_tree_ext`), and the buffer management involves `repo_logmsg_reencode`, `repo_get_commit_buffer`, and `repo_unuse_commit_buffer`. The broader thread context (systemic slab management gaps, topological walk slab leaks) is unrelated to this patch’s correctness but may inform future work. No further review or action is needed for this specific patch.