This is a surface-level review of patch 5/9 in Patrick Steinhardt’s ODB alternates refactoring series. Karthik Nayak’s feedback is narrowly focused on the `option_shared` guard in `collect_alternates()`: he asks whether the conditional should also check `option_shared` before adding the source repository’s `objects` directory to the alternates `strvec`. The question is valid but mechanical, as the patch already includes the `option_shared` guard in the `is_local` block (line `if (option_shared)`). Karthik’s comment does not engage with the patch’s core refactoring, correctness, or edge cases, nor does it address the critical fix for non-shared local clones (`git clone --local --no-shared`) that Justin Tobler identified in v3.

The patch itself is uncontroversial and aligns with the series’ incremental approach. It moves the setup of alternates for shared local clones into `collect_alternates()`, consolidating logic and preparing for the eventual removal of the `write_alternates()` callback. The `option_shared` guard is a key correctness improvement, ensuring alternates are only added when explicitly requested. Karthik’s feedback is unlikely to require significant changes, as the guard is already present in the patch as written. The review does not raise substantive concerns about the patch’s design or implementation.