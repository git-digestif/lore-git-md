Here’s the human-mode summary for a developer catching up:

This is the latest update in Junio C Hamano’s recurring “What’s cooking in git.git” report, which tracks what’s in flight across Git’s integration branches. Since the last report, a handful of small fixes and improvements have graduated to `master`, including a segfault fix in `git replay`, typo corrections, CI tweaks, and a new `--linearize` option for `git replay`. One trivial AsciiDoc formatting patch was fast-tracked straight to `next` because it was uncontroversial. Patrick Steinhardt’s proposal to move `libgit.a` sources into a `lib/` subdirectory has been dropped for now, but may resurface at the upcoming Contributor’s Summit. His 13-patch ODB cleanup series, which removes ad-hoc alternate registration, has landed in `next` after a minor commit-message tweak.

The only loose end that needed attention was a duplicate CI commit: two nearly identical patches (`ps/ci-depends-on-ruby` and `jk/ci-use-system-asciidoctor`) both fixed the same Ruby dependency issue, but the earlier one contained a typo. Junio has now confirmed that the typo-ridden version will be dropped when the topics graduate to `master`, so the history will stay clean. Everything else—Rust cross-compilation fixes, ref-storage terminology unification, new `git var` features, and a receive-packet hook—remains under review and is waiting for feedback.