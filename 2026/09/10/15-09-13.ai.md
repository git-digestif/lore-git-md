This is patch 3/9 of Patrick Steinhardt’s 9-patch series refactoring ODB alternates handling during repository creation, part of the ongoing ODB abstraction effort. The patch is a mechanical refactoring that moves the `setup_reference()` function in `builtin/clone.c` closer to `copy_alternates()` to eliminate a forward declaration, preparing for patch 4/9’s consolidation of alternates logic.

The change is minimal (10 insertions, 10 deletions) and introduces no new behavior, symbols, or edge cases. It simply relocates the existing `setup_reference()` function definition to appear after `copy_alternates()`, avoiding the need for a forward declaration when `setup_reference()` starts calling `copy_alternates()` in patch 4/9. The patch is uncontroversial and aligns with the series’ incremental approach, with Karthik Nayak’s surface-level review of v4 noting that `setup_reference()` will be renamed in patch 4/9 but not engaging with the patch’s correctness or design.

No new technical details or loose ends are introduced. The patch preserves existing behavior and does not affect the series’ core goals: consolidating alternates logic into ODB creation, resolving TOCTOU race concerns via the lockfile API, or simplifying the ODB interface. Reviewers are unlikely to focus on this patch, as it is purely preparatory for the substantive changes in patches 4-9. The patch is ready for integration and does not require further feedback.