This is a **substantive review reply** in the ongoing advice-scope-hint thread, specifically addressing the design question of whether advice settings should default to `--global` or allow `--local` (or other) scopes.

Jeff King (Peff) reinforces his position that *all* advice settings should default to `--global`, arguing that the per-project use case (where a user might want to squelch advice differently across repositories) is effectively nonexistent. He acknowledges Junio’s counterpoint about workflow-dependent advice but contends that no one has likely considered this distinction in practice. Peff cites the historical context: when he introduced the `advice.*` config system in 2009, the assumption was that users would set it in their global config, and later discussions about `git config` advice (e.g., in 2020) did not debate scope at all. His skepticism about the per-project use case is now backed by this historical precedent, suggesting the boolean `is_global_hint` simplification (which defaults all advice to `--global` except where explicitly overridden) aligns with the original intent and practical reality.

The email does not introduce new technical details but strengthens the case for the simplified design proposed in Vsevolod’s v4 series. Key symbols (`is_global_hint`, `advice_setting`) and the unresolved design question (boolean vs. flexible scope mechanism) remain central to the thread. The discussion is now framed by both maintainer nuance (Junio’s openness to `--local`) and historical context (Peff’s argument that `--global` was always the implicit default). No new build failures or mechanical issues are raised; the focus is purely on the design rationale.