This is a **substantive review reply** in the ongoing advice-scope-hint series. Jeff King (Peff) responds to Junio’s nuanced counterpoint about whether advice settings should default to `--global` (user-specific) or `--local` (project-specific).

Peff doubles down on his position that *all* advice settings should default to `--global`, arguing that the per-project use case Junio describes ("different workflows across projects") is effectively nonexistent in practice. He acknowledges he hasn’t audited every advice setting for counterexamples but expresses skepticism that anyone has ever carefully considered per-project advice configuration. As supporting evidence, he cites the original 2009 introduction of `advice.*` (which he authored) and notes that even the later addition of `git config` advice in 2020 lacked any discussion of global vs. local scope. The reply includes a link to the 2020 thread for historical context, though Peff admits he had no memory of it despite participating.

The email doesn’t directly address the boolean `is_global_hint` simplification proposed by Vsevolod, but it reinforces the technical rationale behind it: if all advice settings are inherently user-specific, then a boolean flag (true for `--global`, false for `--local`) is sufficient and avoids over-engineering for hypothetical use cases. The thread remains unresolved, with Junio’s counterpoint still on the table, but Peff’s reply strengthens the case for the simpler design.