This is the first patch in Vsevolod Myalitsin’s three-part v4 series refining Git’s advice system. It refactors the internal `vadvise()` function to accept the entire `advice_setting` struct instead of just the advice level and key. The change is purely mechanical—no user-visible behavior is altered—but it paves the way for the next patch, which will add a `scope_hint` field to the struct so advice messages can suggest the correct config scope (`--global`, `--local`, etc.) when telling users how to disable them.

The patch touches only `advice.c`, renaming the anonymous struct to `advice_setting` and updating the function signature and callsites to match. The commit message is concise but clear about the motivation: keeping the advice configuration together simplifies future extensions. This is a well-scoped refactoring that directly addresses Junio’s feedback from v3 about interface design. The diff is clean and focused, and the change is unlikely to be controversial. If the rest of the series holds up, this patch should be straightforward to review and merge.