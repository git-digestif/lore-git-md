This is a **maintainer reply** in the advice-scope-hint thread, responding to Jeff King’s argument that all advice settings should default to `--global` because they are about silencing chatter for a *user* rather than a specific repository.

Junio acknowledges the merit of Peff’s position but introduces a nuanced counterpoint: while advice messages are often user-specific (supporting `--global`), some users work across multiple projects with different workflows, and may want to squelch advice *per project*. This suggests that `--local` (or even `--worktree`) could still be useful in practice, even if such cases are rare. He notes that the original design of Git’s advice system leaned toward the per-project use case, though he now believes the per-user argument (favoring `--global`) should have carried more weight.

The email does not endorse or reject the boolean `is_global_hint` simplification proposed by Vsevolod, but it clarifies the maintainer’s perspective on the underlying design question. Junio’s framing leaves the door open for either approach: the boolean simplification (which would default `defaultBranchName` to `--global` and others to `--local`) or a more flexible mechanism that could later support `--worktree` or `--system` if a use case emerges. The thread remains unresolved, with no indication that Junio has picked up the series for integration. Key symbols (`is_global_hint`, `advice_setting`, `vadvise()`) and the build-failure context (unhandled `CONFIG_SCOPE` values) remain relevant for future discussion.