This is a Rust infrastructure discussion, continuing the thread about Mike Hommey’s patch series to reorganize Git’s Rust code into a `rust/` subdirectory. The focus here is on the forward-compatibility of Mike’s use case—vendoring Git’s C code into a Rust project while excluding its Rust components—once Git 3.0 makes Rust mandatory.

Mike confirms brian m. carlson’s understanding: his project (git-cinnabar) vendors Git’s C code via submodule but does not use Git’s Rust components. The current patch series solves this by moving `Cargo.toml` out of the root, preventing Cargo from treating the entire Git repository as a single crate. However, brian’s concern is that this workaround may not survive Git 3.0, which will require Rust components. Mike’s proposed solution is to publish the `gitcore` crate to crates.io under a namespaced identifier (or ideally, under the Git project’s own namespace), allowing downstream projects to depend on Git’s C code without pulling in its Rust code. This would decouple the two components, preserving the ability to vendor Git’s C code independently even after Rust becomes mandatory.

The exchange does not challenge the patch series itself but clarifies how the reorganization fits into a longer-term strategy. Mike’s response suggests he has considered the 3.0 transition and sees crate publishing as a viable path forward. This may reassure reviewers that the current patch is not a short-term hack but part of a coherent plan for handling mixed C/Rust dependencies. The discussion remains open-ended—no alternative approaches are proposed, and the thread does not yet indicate whether this solution will satisfy brian’s concerns or prompt further debate.