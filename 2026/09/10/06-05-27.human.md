This is the final patch in Johannes Schindelin’s 13-part series updating Git’s Windows/MinGW build and runtime support. It is a **pure style adjustment** to the test script `t/t0060-path-utils.sh`, reinstating a closing single quote that was accidentally left on the same line as the `test_cmp` command in an earlier iteration. The change is mechanical, with no functional impact—just a cleanup to match the project’s current test-case formatting conventions. If you’ve followed the series up to now, this is a trivial polish commit; if you’re joining here, the rest of the series (already merged to `next`) addresses Windows build hygiene, MSYS2 integration, and runtime fixes.