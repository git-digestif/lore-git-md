This is a **substantive review exchange** in the ongoing thread about Vsevolod Myalitsin’s advice-scope-hint series. Jeff King (Peff) and Junio C Hamano are discussing a tangential but related documentation patch that Junio posted to clarify `<file-option>` in the `git config` man page.

Peff begins by admitting he was unaware `--worktree` existed until reviewing the code for this series, reinforcing his skepticism about its relevance for advice settings. He reiterates his view that `--global` is the only scope that makes sense for advice messages, since they are about user preferences rather than repository-specific behavior. His comment that *all* advice settings might default to `--global` is a notable shift—it suggests the scope-hint mechanism could be simplified even further, possibly aligning with Vsevolod’s later proposal to replace the `scope_hint` enum with a boolean `is_global_hint`.

The bulk of the email is Peff’s feedback on Junio’s documentation patch. He agrees with the goal but suggests a more explicit rewrite of the reading/writing paragraphs to consistently use the term `<file-option>` and avoid duplicating the list of scope flags. His proposed diff tightens the language and makes the connection between the placeholder and the actual options (`--system`, `--global`, etc.) clearer. He also notes that `--blob` is a special case (a "location" option, not a file option) and should not be grouped with the others, though he does not propose a change for it here.

Peff’s final comment addresses Junio’s `NEEDSWORK` note about `<display-option>`, explaining its origin (the `list` subcommand) and suggesting a larger rewrite of the man page to better organize the modes and options. This is a forward-looking observation rather than an immediate blocker for the current patch.

The exchange is constructive and focused on documentation clarity, but it does not directly advance the advice-scope-hint series. The key takeaway for the main thread is Peff’s implicit endorsement of a simpler scope-hint design (e.g., defaulting all advice to `--global`), which could make Vsevolod’s boolean proposal more appealing to Junio. The documentation patch itself is a small, uncontroversial improvement that would likely be merged independently.