This is a surface-level review comment on patch 3 of Patrick Steinhardt’s 9-patch ODB abstraction series. Karthik Nayak notes that the function `setup_reference()` might be renamed in a future patch to better reflect its evolving purpose—now that it will handle alternates collection rather than just reference setup. The comment is brief and speculative, observing that the current name is ambiguous given the function’s expanded role in the series.

The remark is purely about naming and does not engage with the patch’s technical content, which is a mechanical reordering of functions in `builtin/clone.c` to prepare for later refactoring. Karthik’s observation aligns with the series’ broader goal of consolidating alternates logic, but it does not critique or block the current change. The patch itself remains uncontroversial and is a small, well-motivated step toward simplifying the ODB interface. No action is required on this feedback for the current patch.