Thread identity: Bugfix patch addressing resource cleanup in low-level merge driver infrastructure when interrupted by SIGINT/SIGQUIT. Originating author: Michal Koutný. Thread type: standalone patch.

Core problem or goal: Ensure temporary files (`.merge_file_*`) created during external merge driver execution are removed even when the merge process is abruptly terminated by the user (e.g., via Ctrl+C). The issue arises because SIGINT/SIGQUIT sent to the external merge driver propagates to the parent `git merge` process, preventing cleanup handlers from running.

Current status: Patch under active review. Original solution (signal chain manipulation) has been superseded by Jeff King's alternative approach using Git's `tempfile` API. No merge status reported (not in `pu`, `next`, or `master`).

Key technical details:
- **File modified**: `merge-ll.c`
- **Function**: `ll_ext_merge()`
- **Temporary files**: `.merge_file_*` (created during external merge driver execution)
- **Signal handling**: Original patch used `sigchain_push`/`sigchain_pop` to temporarily ignore signals during child termination; Peff's alternative replaces this with `struct tempfile` objects, which handle signal-safe cleanup atomically via `register_tempfile()`/`xmks_tempfile()`.
- **New functions**: `create_temp()` (returns `struct tempfile *`), `get_temp_path()` (handles path quoting for shell commands).
- **APIs used**: `xmks_tempfile()`, `delete_tempfile()`, `get_tempfile_path()`.
- **Behavior**: No functional change in cleanup guarantees, but the implementation is now more robust and aligned with Git's established patterns.
- **Test coverage**: Still unaddressed; Peff's patch is "lightly tested" but no automated test scenarios for interrupt handling are mentioned.

Open questions and loose ends:
- Whether Michal will adopt Peff's alternative or propose adjustments.
- Need for test coverage: existing tests may not cover interrupt scenarios; new tests may be required to verify signal-safe cleanup.
- Minor nit: Peff suggests `strbuf_read_file()` could replace manual file reading, but this is not a blocker.
- Future improvement: `get_temp_path()` notes shell quoting limitations for absolute paths, but this is not urgent.

Participant positions:
- **Michal Koutný**: Author of original patch. Described the problem (resource cleanup on interrupt) and proposed a signal-handling workaround. Credited Jean Delvare via `Reported-by`.
- **Jeff King (Peff)**: Provided **substantive review** with an alternative solution using Git's `tempfile` API. His patch refactors `ll_ext_merge()` to use `struct tempfile` objects, eliminating custom signal handling. Lightly tested; no objections to the original problem statement.
- **Jean Delvare**: Reported the issue (no direct participation in this thread yet).

Related work:
- Prior art in `editor.c` (referenced in original commit message) for signal handling.
- Git's `tempfile` API (used in Peff's alternative) is the established pattern for signal-safe resource cleanup.