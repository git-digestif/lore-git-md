Thread identity: Bugfix patch addressing resource cleanup in low-level merge driver infrastructure when interrupted by SIGINT/SIGQUIT. Originating author: Michal Koutný. Thread type: standalone patch (not part of a series).

Core problem or goal: Ensure temporary files (`.merge_file_*`) created during external merge driver execution are removed even when the merge process is abruptly terminated by the user (e.g., via Ctrl+C). The issue arises because SIGINT/SIGQUIT sent to the external merge driver propagates to the parent `git merge` process, preventing cleanup handlers from running.

Current status: Patch posted as a standalone fix. No prior versions or iterations mentioned. Under review; no merge status reported (not in `pu`, `next`, or `master`).

Key technical details:
- **File modified**: `merge-ll.c`
- **Function**: `ll_ext_merge()`
- **Temporary files**: `.merge_file_*` (created during external merge driver execution)
- **Signal handling**: SIGINT/SIGQUIT propagation between child and parent processes. Patch adopts signal-handling idiom from `editor.c` (using `sigchain_push`/`sigchain_pop` to temporarily ignore signals during child termination, then conditionally re-raise if the child died from SIGINT/SIGQUIT).
- **New symbols/functions**: None (minimal changes to existing signal-handling logic).
- **Test coverage**: Not mentioned; likely requires manual testing of interrupt scenarios.

Open questions and loose ends:
- No review feedback yet; patch is newly posted.
- No discussion of test coverage for the edge case (e.g., whether existing tests cover interrupt scenarios or if new tests are needed).
- No objections or requests for changes raised so far.

Participant positions:
- **Michal Koutný**: Author. Describes the problem (resource cleanup on interrupt) and solution (signal chain manipulation to ensure cleanup while preserving interrupt behavior). Credits Jean Delvare via `Reported-by` trailer, indicating this was a real-world issue.
- **Jean Delvare**: Reported the issue (no direct participation in this thread yet).

Related work:
- Prior art in `editor.c` for signal handling idiom (referenced in commit message).
- No dependencies or conflicts with other in-flight topics mentioned.