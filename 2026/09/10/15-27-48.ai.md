**Summarizer brief mode**

This is a **bugfix follow-up** in the `git history reword --dry-run` memory-leak thread. Kristoffer Haugsbakk confirms the root cause identified earlier: the leak surfaces only when the commit buffer is **not cached in the commit slab**, a condition that depends on repository state (e.g., `i18n.commitencoding` settings or packfile configuration). The commit graph is now explicitly named as one of the triggering factors. No new code changes are proposed; the thread remains in a holding state awaiting Kaartic Sivaraam’s reroll, which will include a corrected commit message and possibly a test case. The fix itself (a single-line `repo_unuse_commit_buffer()` call in `commit_tree_ext()`) is unchanged and uncontested.

Key technical details carried forward:
- **File touched**: `builtin/history.c`, function `commit_tree_ext()`.
- **Leak trigger**: `git history reword --dry-run` in repositories where commit buffers bypass the slab cache (commit graph presence is now confirmed as one such condition).
- **Stack trace anchor**: `repo_get_commit_buffer()` → `unpack_compressed_entry()` in `packfile.c`.
- **Leak size**: 283 bytes per invocation.
- **Environment dependency**: Leak does not reproduce in minimal test repos or when commit buffers are slab-cached.
- **Related prior work**: Kaartic’s June 2026 patch (same fix, stalled due to commit-message inaccuracies).

The thread’s next expected event is Kaartic’s reroll, which will address the last procedural hurdle. No substantive objections or new technical concerns have been raised in this email.