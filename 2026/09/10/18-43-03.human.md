This is a procedural note from Junio C Hamano about the email threading of Tyler Cipriani’s `[PATCH v2 1/2]` submission. The patch itself—a bugfix for `--force-if-includes` in `git push`—is unaffected and remains ready for merge (as previously reviewed by D. Ben Knoble). The issue is purely mechanical: the v2 patches were incorrectly threaded as replies to the v1 cover letter instead of the v2 cover letter, which disrupts automated tooling that tracks patch series. Junio flags this so the author can correct the threading in future submissions. No technical feedback on the patch content is provided.