This is a surface-level review of patch 4/9 in Patrick Steinhardt’s ODB alternates refactoring series. The patch refactors `--reference` and `--reference-if-able` handling in `git clone` to collect alternates paths into a `strvec` instead of immediately writing them to the ODB, preparing for later consolidation of alternates logic.

Karthik Nayak’s feedback focuses on readability and clarity rather than technical correctness or edge cases. He suggests two minor improvements: inverting the conditional in `collect_alternates()` to reduce nesting, and noting in the commit message that alternates are now added outside the function. He also questions the use of `int` for boolean-like fields in `struct add_one_alternate_data` but acknowledges it is inconsequential. His observation about the struct’s naming ("`add_one_alternate_data` holds `alternates`") is a nit about potential confusion, not a functional issue.

The review does not engage with the patch’s core refactoring (e.g., the deferred alternates writes, the new `strvec` interface, or the preparation for ODB creation consolidation). It is purely surface-level, addressing style and documentation rather than design or behavior. The feedback is unlikely to require significant changes to the patch.