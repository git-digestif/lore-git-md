This is a **lightweight follow-up** in the ongoing stash/merge autostash bug thread. Eli Barzilay, the original bug reporter, chimes in as a non-developer observer to express enthusiasm for the proposed fixes and offer to test patches.

Eli reiterates that the bug—where `stash.index=true` combined with `--autostash` during a fast-forward merge leaves behind a redundant stash entry and an uncleaned `MERGE_AUTOSTASH` ref—is the only remaining pain point in his workflow with `stash.index` and `rebase.autoStash`. While he lacks the time to dive into the code, he is eager to help by testing patches or other proposed solutions. His message serves as a gentle nudge to the active participants (D. Ben Knoble and Phillip Wood) that end users are waiting for a fix and are ready to validate it.

The thread remains in the design phase, with two proposed fixes on the table: Phillip Wood’s surgical fix (`reset_tree(&c_tree, 0, 1)`) and the more ambitious refactoring (using `merge_incore_nonrecursive()` for index merges). Ben Knoble has committed to testing both approaches and may contribute implementation work. Eli’s offer to test patches adds another layer of validation, though no patches have been posted yet. The broader layering violation—`git reset` performing undocumented branch-state cleanups via `remove_merge_branch_state()`—remains noted but unaddressed in this thread. Key files and symbols (`builtin/merge.c`, `sequencer.c`, `reset_tree()`, `merge_incore_nonrecursive()`) continue to anchor the discussion.