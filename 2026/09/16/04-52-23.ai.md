This is a small feature patch for the `contrib/contacts/git-contacts` Perl script, extending it to read patch input from stdin when no arguments are provided and stdin is not a terminal. The v2 revision addresses all of Junio C Hamano’s stylistic feedback from the initial submission: the argument-handling logic is refactored for clarity, the usage comment is updated to document the new stdin capability, and the commit message trailers are cleaned up.

The patch remains narrowly scoped—it touches only the one script, introduces no new CLI options or on-disk formats, and reuses the existing `scan_patches()` helper. The motivation is explicitly cross-project: the author is also patching `b4` to automatically determine patch recipients, and this change makes `git-contacts` interoperable with that workflow. The commit message links to the corresponding `b4` thread and notes that the `b4` integration will have to wait for a bug-fix release, but this patch is useful on its own.

No functional objections were raised in the first round; the feedback was purely about code style and documentation. With those adjustments now incorporated, the patch looks ready for merging unless new concerns surface.