This "What's cooking" thread is Junio’s September 2026 integration report, tracking the state of all in-flight topics in Git’s development branches. The big picture: Git 2.56-rc1 has been tagged, with the final release just around the corner, and several topics have recently graduated to `master`, including Rust build fixes, ODB pluggability improvements, a new `receive-report` hook, and the `git history squash` command.

The most immediate update is that `sg/precompile-git-compat-util`—a build optimization that precompiles `git-compat-util.h` for faster builds—has cleared its last hurdle. SZEDER Gábor confirmed it works with Clang and Intel’s oneAPI compiler, not just GCC, removing the portability concern that was holding it back. This series is now unblocked and likely to move to `next` soon, then graduate to `master` in the current cycle. Other topics cooking in `next` include Patrick Steinhardt’s ref-storage-format standardization and Taylor Blau’s rerere lock-fix, while a few stalled efforts (like the Rust crate subdirectory move and a new diff hunk processor) are waiting on author revisions or reviewer feedback. No major controversies are brewing, but several topics are stuck on the usual "needs review" or "expecting reroll" cycle.