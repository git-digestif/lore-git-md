This is a platform compatibility bugfix for the Windows ANSI emulation layer, following up on the earlier discussion about how best to fix a long-standing formatting bug in `die_lasterr()`. René Scharfe now proposes a simpler alternative: replacing the variadic function with a macro that directly forwards its arguments to `die_errno()` after converting the Windows error code. This avoids the complexity of the original patch (which used a temporary `strbuf` and careful error-code preservation) while achieving the same goal.

The macro approach is minimal, robust, and aligns with how `die_errno()` itself is implemented (using a fixed-size buffer). It also sidesteps the need for heap allocation in a fatal-error helper, which is a defensible design choice. René’s suggestion is substantive—it engages with the trade-offs of the original solution and offers a cleaner path forward. The discussion is now focused on which approach is preferable: the original patch’s explicit handling of variadic arguments or the macro’s simplicity.

No resolution yet; the thread awaits feedback from Windows platform maintainers (Patrick Steinhardt and Johannes Schindelin) or other reviewers. The patch remains well-motivated and self-contained, but the macro alternative may ultimately be the more elegant fix.