This is a **substantive review** of the ongoing stash/merge autostash bug. Phillip Wood, a seasoned contributor with deep knowledge of Git's sequencer and merge machinery, joins the discussion with a concrete technical proposal.

He suggests the simplest fix is to replace the problematic `reset_head()` call in `stash apply --index` with `reset_tree(&c_tree, 0, 1)`, which would avoid the premature cleanup of the `MERGE_AUTOSTASH` ref. More ambitiously, he proposes refactoring the index-merge logic to use `merge_incore_nonrecursive()` instead of the current subprocess-heavy approach (`git diff-tree`, `git apply --cached`, `git reset`). This would leverage the existing merge machinery (`merge_ort_nonrecursive()`) already used for working tree changes, eliminating subprocess spawning and enabling a proper three-way merge for index changes.

Phillip also weighs in on the broader design question: he agrees with D. Ben Knoble that `git reset` performing undocumented branch-state cleanups via `remove_merge_branch_state()` is surprising and unintuitive. The discussion remains focused on the technical trade-offs between the two proposed fixes (shielding `MERGE_AUTOSTASH` or avoiding branch-state cleanup in `stash`'s `reset_head()`), with no clear consensus yet. No patch is attached, but the review advances the thread with actionable implementation ideas.