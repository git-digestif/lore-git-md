This is Junio C Hamano's final confirmation that the combined `http.sslVerifyStatus` patch (v7) is ready for merge. He explicitly accepts the updated test structure—keeping the non-OCSP cases in `t5551-http-fetch-smart.sh` and moving the OCSP-specific tests to the new `t5585-http-ssl-ocsp.sh`—and will replace the previous version in `next` with this one. The patch is now technically complete, with all review feedback addressed and test coverage confirmed by both Junio and Patrick Steinhardt. Graduation to `master` is imminent, pending Junio’s merge action. No further changes or discussion are expected.