Thread identity
Subject: [PATCH] compat/winansi: fix die_lasterr() formatting bug
Series version: standalone patch
Originating author: Yongqiang Tian
Thread type: bugfix

Core problem or goal
Fix a long-standing formatting bug in `compat/winansi.c:die_lasterr()` that causes Windows API failure messages to display the address of a `va_list` instead of the intended integer handle when `DuplicateHandle()` fails. The bug has no effect on other callers, which pass fixed strings.

Current status
Under review; no prior versions. Patch is minimal (7 insertions, 2 deletions), self-contained, and targets only `compat/winansi.c`. Junio C Hamano (maintainer) has redirected the patch to Patrick Steinhardt and Johannes Schindelin for review, as he lacks Windows expertise.

Key technical details
- File: `compat/winansi.c`
- Function: `die_lasterr()`
- New symbols: none (uses a temporary `strbuf` and `strbuf_vaddf()`)
- Behavior change: Windows API failure messages now show the correct integer handle instead of a `va_list` address
- On-disk format: unchanged
- Test coverage: none added; the bug is in a fatal-error helper that is difficult to trigger deterministically

Open questions and loose ends
None identified; the patch is well-motivated and the solution is straightforward. Awaiting feedback from Patrick Steinhardt and Johannes Schindelin.

Participant positions
- **Junio C Hamano**: Declined review due to lack of Windows expertise; added Patrick Steinhardt and Johannes Schindelin as reviewers.
- **Patrick Steinhardt**: Not yet reviewed.
- **Johannes Schindelin**: Not yet reviewed.

Related work
None.