Thread identity
Subject: "What's cooking in git.git" (September 2026 report)
Series version: N/A (maintainer report, not a patch series)
Originating author: Junio C Hamano
Thread type: integration status report

Core problem or goal
Junio’s periodic "What’s cooking" report summarizes the state of all in-flight topics across the integration branches (`seen`, `next`, `master`) and highlights which topics have graduated, which are cooking, which are stalled, and which have been discarded. The September 2026 edition covers 87 commits in `next`, 166 in `seen`, and notes that Git 2.56-rc1 has been tagged with the final release imminent.

Current status
Graduated to `master` (since the last report):
- jc/rust-cargo-build-target – cross-compilation fix for Rust builds respecting `CARGO_BUILD_TARGET`.
- ps/odb-pluggable-fsck – moves fsck checks into object-source backends, making them pluggable.
- ps/odb-stop-registering-in-memory-sources – removes ad-hoc alternate registration, simplifying ODB management.
- kn/receive-report-hook – adds a `receive-report` hook to filter/modify packet-line status reports.
- hn/history-squash – introduces `git history squash` to fold commit ranges into a single commit.

New topics (not yet in `next`):
- jc/advice-config-set-global – suggests `--global` for advice-squelching commands; under review.
- sg/precompile-git-compat-util – precompiles `git-compat-util.h` for faster builds; **confirmed to work with Clang and Intel oneAPI compilers by SZEDER Gábor**, removing a portability blocker and increasing likelihood of graduation.
- of/commit-reach-repo-awareness – fixes submodule merging by parsing commits in the correct repository; likely to merge.

Cooking in `next` (with recent activity):
- ps/ref-storage-format – standardizes terminology around "ref storage format" (e.g., `--ref-storage-format`).
- tb/rerere-wait-for-merge-rr-lock – makes rerere wait for locks instead of failing; needs review.
- tc/push-force-if-includes-fixes – fixes `--force-if-includes` to check the correct local reflog; expecting reroll.

Stalled or contentious:
- mh/rust-crate-subdir – moves Rust code to a `rust/` subdirectory; broken, needs review.
- mm/diff-process-hunks – introduces `diff.<driver>.process` for external hunk providers; expecting reroll.
- tb/midx-incremental-custom-base – fixes `--base` handling in incremental midx writes; expecting reroll.

Discarded topics:
- vm/advice-config-global-hint – superseded by jc/advice-config-set-global.

Key technical details
- Files/subsystems: Rust build system (`Cargo.toml`, `Makefile`), ODB backends (`object-file.c`, `fsck.c`), ref storage (`refs.c`, `builtin/reflog.c`), hook subsystem (`builtin/receive-pack.c`), commit reachability (`commit-reach.c`, submodule handling), diff drivers (`diff.c`, `diffcore.h`), multi-pack-index (`midx.c`), build system (`git-compat-util.h` precompilation).
- New/renamed symbols: `git history squash`, `receive-report` hook, `--ref-storage-format` option, `diff.<driver>.process` config key.
- On-disk changes: none reported in this update.
- Test coverage: not detailed in the report; assumed to be part of the series’ CI. Portability of `sg/precompile-git-compat-util` confirmed across GCC, Clang, and Intel oneAPI compilers.

Open questions and loose ends
- jc/advice-config-set-global: awaiting reviewer feedback.
- tb/rerere-wait-for-merge-rr-lock: needs review.
- mh/rust-crate-subdir: broken; requires attention.
- mm/diff-process-hunks and tb/midx-incremental-custom-base: both expecting rerolls.
- No major controversies noted, but several topics are blocked on reviewer input or author revisions.

Participant positions
- Junio C Hamano (maintainer): reports integration status; no explicit approvals or objections in this email.
- SZEDER Gábor: confirms portability of `sg/precompile-git-compat-util` across compilers (GCC, Clang, Intel oneAPI), removing a potential blocker. Review is substantive (build system correctness).
- Reviewers: not named in this summary; positions on individual topics are implied by "needs review" or "expecting reroll" labels.

Related work
- Git 2.56-rc1 tagged; final release imminent.
- Discarded topic vm/advice-config-global-hint superseded by jc/advice-config-set-global.