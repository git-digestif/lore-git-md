This is a small but useful feature patch for `git-contacts`, a contrib script that extracts likely reviewers from patch metadata. The v2 iteration implements Junio C Hamano’s requested stylistic adjustments from the initial review: the argument-handling logic is now cleaner (using an early `$read_from_stdin` variable instead of a late `@ARGV` check), the usage comment at the top of the file has been updated to document the new stdin capability, and the commit message trailers have been tidied.

The patch teaches `git-contacts` to read patch content from stdin when stdin is not a terminal and no arguments are supplied. This is a one-line behavioral change (`-t STDIN` check) plus a call to the existing `scan_patches()` helper with the stdin filehandle. The motivation is clear: better interoperability with external tools, specifically `b4`, which the author is also patching in a separate effort. The commit message links to the corresponding `b4` thread, making the cross-project context explicit.

The change is narrowly scoped—it touches only the Perl script in `contrib/contacts/`—and does not alter any core Git behavior. No new CLI options or on-disk formats are introduced, and the risk of regression appears low given the simplicity of the change. The author notes that this patch is useful on its own even if the follow-up `b4` integration is delayed, which strengthens the case for merging it independently.

Junio’s feedback on v1 was purely stylistic; no functional objections were raised. With those adjustments now incorporated, the patch looks ready for `next`. If you use `git-contacts` in tooling pipelines, this is a small but welcome quality-of-life improvement.