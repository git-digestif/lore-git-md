This is a brief but enthusiastic follow-up in the ongoing bug thread about `stash.index=true` leaving redundant stash entries after an autostash fast-forward merge. Eli Barzilay, the original reporter, chimes in from the sidelines to say he can’t dive into the code right now but is eager to test any patches that emerge. He calls the bug “the only thorn in my joy” of using `stash.index` with `rebase.autoStash`, signaling that this is a real pain point for users who rely on the feature.

The exchange he quotes—between D. Ben Knoble and Phillip Wood—has already crystallized the technical path forward: either a surgical fix (replace `reset_head()` with `reset_tree(&c_tree, 0, 1)`) or a deeper refactoring (use `merge_incore_nonrecursive()` for the index merge). Both approaches are now on the table, and Ben has committed to testing them. Eli’s offer to try patches means the fix will get real-world exposure quickly once it lands.

No code has been posted yet, but the thread is clearly converging. The bug is confirmed, the root cause is understood, and the proposed solutions are concrete. If you care about stash or autostash behavior, this is a thread to watch—patches are likely imminent.