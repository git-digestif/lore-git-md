This is a brief follow-up in the advice-scope-hint thread. Vsevolod Myalitsyn replies to Junio C Hamano’s question about whether the single-patch v5 implementation (which Junio already posted) fully resolves the series. Vsevolod agrees that the uniform `--global` approach is the right conclusion and confirms the thread’s design debate is settled.

He then suggests a small interface cleanup: passing a pointer to the `advice_setting` struct to `vadvise()` instead of its individual fields. This is a tangential refactoring unrelated to the scope-hint feature itself. Vsevolod proposes splitting it into a separate, follow-up patch. The suggestion is low-stakes and does not challenge the current implementation; it merely tidies up the internal API. No objections or further discussion are present in this email. The thread remains on track for the v5 patch to land as-is, with this cleanup deferred to a later series.