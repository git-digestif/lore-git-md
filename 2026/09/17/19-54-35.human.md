This is the second patch in a three-part **test modernization** series updating `t/t4009-diff-rename-4.sh` to use modern Git test style. The patch is purely stylistic, replacing older test patterns with current best practices to improve readability and error messages.

The changes are mechanical and confined to the test script, with no functional impact on Git's behavior. The diffstat shows a slight reduction in lines (54 insertions, 59 deletions), reflecting the shift from multi-line `test_expect_success` blocks with quoted commands to more concise single-quoted test bodies with inline shell functions. Key updates include standardizing variable names (`expect` instead of `expected`, `actual` instead of `current`), using `compare_diff_raw_z` consistently, and improving test descriptions.

This patch is part of the ongoing **test modernization** effort, a recurring community-wide initiative to update legacy test patterns. The work is well-scoped and unlikely to be controversial, though it may attract surface-level review feedback about specific style choices. The series as a whole appears to be a straightforward cleanup, with this patch focusing on a single test script that exercises rename/copy detection in `git diff-index -z`.