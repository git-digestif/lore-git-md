This is a brief follow-up in the "What's cooking" thread. Junio acknowledges SZEDER Gábor’s confirmation that the `sg/precompile-git-compat-util` topic — which precompiles `git-compat-util.h` to speed up builds — works not only with GCC but also with Clang and the Intel oneAPI compiler. The patch series was already marked as likely to merge in the September 2026 report, and this confirmation removes the last portability concern, making it even more certain to graduate soon. There is nothing controversial here; it is a straightforward, positive test result that clears the way for the topic to move forward.