This is a brief human-readable update in the ongoing stash/merge autostash bug thread. D. Ben Knoble acknowledges Eli Barzilay’s offer to test patches, confirming he will keep Eli in the loop as the fix progresses. The thread remains in the design and implementation phase, with no patches posted yet, but Eli’s offer adds a real-world tester to the effort. The bug itself—where `stash.index=true` combined with `--autostash` during a fast-forward merge leaves a redundant stash entry and an uncleaned `MERGE_AUTOSTASH` ref—is still the focus, and the proposed solutions (a surgical fix or a broader refactoring using `merge_incore_nonrecursive()`) are unchanged. This exchange is a small but meaningful signal that the fix is moving forward and that end-user validation is lined up.