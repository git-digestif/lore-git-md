This is a **bugfix implementation update** in the ongoing `stash.index=true` + `--autostash` thread. Phillip Wood reviews D. Ben Knoble’s patch that replaces the subprocess-heavy index-merge logic with a direct call to `merge_incore_nonrecursive()`, confirming it resolves the bug (redundant stash entry and uncleaned `MERGE_AUTOSTASH` ref) while improving performance.

The patch is now in final polish: Wood suggests minor cleanups (silencing merge output, using `oidcpy` for tree OID assignment, and dropping unnecessary conflict labels) but calls the approach "a nice improvement." Knoble reports the patch passes all tests and plans to submit a formal series soon. The fix targets `sequencer.c` and avoids touching the index until the final merge, eliminating the premature `MERGE_AUTOSTASH` ref deletion that caused the original issue.

This looks like a well-scoped, low-risk fix for a real user-facing bug. The collaboration between Knoble and Wood has been substantive, addressing both correctness and code quality. If the final patch lands as described, it should be uncontroversial and a candidate for backporting.