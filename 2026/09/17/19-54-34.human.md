This is the first patch in a **test modernization** series updating `t/t4001-diff-rename.sh` to use modern Git test style. The changes are purely stylistic, replacing older test patterns with current best practices to improve readability and test error messages. The patch touches only the test script, with no functional impact on Git's behavior.

The modernization involves converting the test script from a procedural setup style (using `test_expect_success` for setup steps) to a more declarative approach where setup is encapsulated in a helper function (`initial_setup`). This aligns the test with the project's current conventions, where setup is typically grouped and reused across test cases. The patch also renames temporary files (e.g., `expected` to `expect`, `no-rename` to `expect-no-rename`) to match modern naming conventions, and consolidates test steps into fewer, more focused `test_expect_success` blocks.

The diffstat shows a net reduction in lines (97 insertions, 108 deletions), reflecting the shift toward more concise test constructs. The changes are mechanical and unlikely to be controversial, though they may attract surface-level review feedback about specific style choices. This patch is part of the broader **test modernization** effort, a recurring community-wide initiative to update legacy test patterns in the `t/` directory.

The commit message is minimal but clear about the intent: updating the test to modern style for better readability and error messages. The patch appears well-scoped and motivated, targeting a single test script for cleanup. No prior discussion context is provided, so it’s unclear if this is part of a larger planned effort or if similar updates to other test scripts are expected. The series as a whole (3 patches) was previously summarized, and this patch is the first of the set.