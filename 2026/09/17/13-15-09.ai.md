This is a **substantive implementation update** in the ongoing `stash.index=true` + `--autostash` bugfix thread. D. Ben Knoble reports success implementing Phillip Wood’s proposed refactoring: replacing the subprocess-heavy index-merge logic (`git diff-tree`, `git apply --cached`, `git reset`) with a direct call to `merge_incore_nonrecursive()`. The patch passes all tests and resolves the original bug (redundant stash entry and uncleaned `MERGE_AUTOSTASH` ref).

Key technical details: the fix targets `sequencer.c` and swaps `merge_ort_nonrecursive()` for `merge_incore_nonrecursive()` when merging the *index*, pointing it at `info->i_tree` instead of `info->w_tree`. This preserves the `MERGE_AUTOSTASH` ref until autostash apply completes and eliminates three subprocess spawns. Knoble’s patch is still a work-in-progress (needs commit-message polish and removal of unused functions) but is confirmed to work on top of an earlier experimental `reset_tree()` version. The test failures from that earlier attempt are now resolved.

The tone is collaborative and forward-looking: Knoble plans to send a formal patch series soon, and the discussion remains substantive, engaging with both correctness (conflict detection) and code quality (reducing subprocess overhead). No new edge cases or open questions are introduced; the focus is on landing the agreed-upon fix.