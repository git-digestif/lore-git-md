This is the third and final patch in Mark C. Chu-Carroll’s **test modernization** series (v3), updating `t/t4010-diff-pathspec.sh` to modern Git test style. The patch mechanically replaces the old multi-line `test_expect_success` blocks with single-quoted test bodies, inlines setup steps, and standardizes variable naming and indentation. The changes are purely stylistic, with no functional impact on Git’s behavior, and reduce the file by one line (60 insertions, 61 deletions).

The modernization follows the same patterns seen in the earlier patches of this series: collapsing setup into the test body, using `<<-EOF` heredocs for expected output, and replacing `current`/`expected` with inline comparisons. The patch is well-scoped, touching only test infrastructure, and aligns with the ongoing **test modernization** effort. Given the mechanical nature of the changes and the lack of prior controversy in the thread, this patch is unlikely to attract substantive review feedback beyond minor style nitpicks.

Key details for follow-up discussion: the file modified is `t/t4010-diff-pathspec.sh`, and the changes are confined to test readability improvements. No new test cases, symbols, or behaviors are introduced. The series as a whole remains under review, with this patch completing the v3 submission.