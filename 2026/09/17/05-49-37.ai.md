This is a **build system** follow-up to Junio’s "What’s cooking" report. SZEDER Gábor confirms that the `sg/precompile-git-compat-util` topic, which precompiles `git-compat-util.h` to speed up builds, works not only with GCC but also with Clang and other LLVM-based compilers (he tested the Intel oneAPI compiler). The patch series is already marked as likely to merge in the current report, and this confirmation removes a potential portability blocker, making it even more likely to graduate soon. No new technical details or objections are raised; the email is purely a positive test report.