This is a **brief follow-up** in the ongoing stash/merge autostash bug thread. D. Ben Knoble acknowledges Eli Barzilay’s offer to test patches and confirms he will keep Eli in the loop as implementation work progresses. The thread remains in the design phase, with no patches posted yet, but this exchange signals forward momentum and a commitment to user validation.

The core technical direction—replacing the subprocess-heavy index-merge logic with a direct call to `merge_incore_nonrecursive()`—remains the agreed-upon fix. Key files (`builtin/merge.c`, `sequencer.c`) and symbols (`merge_incore_nonrecursive()`, `MERGE_AUTOSTASH`) continue to anchor the discussion. The next expected step is a patch from either Ben Knoble or Phillip Wood, with Eli Barzilay standing by to test. No new technical details or open questions are introduced in this message.