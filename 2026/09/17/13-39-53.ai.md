This is a contributor housekeeping patch. The author, Yoichi NAKAYAMA, adds a `.mailmap` entry to normalize their name formatting, mapping older variants (not shown in the diff but presumably present in the commit history) to the canonical "Yoichi NAKAYAMA <yoichi.nakayama@gmail.com>". The change is minimal—one line added to `.mailmap`—and follows the project's established pattern for name normalization.

The patch is self-contained and uncontroversial. `.mailmap` updates are routine maintenance that improve the consistency of `git shortlog` and other contributor listings without affecting code behavior. No test coverage is needed or expected for this type of change.

This is a single-patch submission (not part of a series) and appears to be the thread root. The commit message is brief but sufficient for its purpose. The patch is sent via GitGitGadget, a common submission path for contributors. There is no indication of prior discussion or review in the provided context.