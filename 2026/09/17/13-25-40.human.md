This is a brief follow-up in the ongoing `advice.*` scope-hint series. The thread has already converged on a minimal, consensus design: always recommending `--global` in the hint for every `advice.*` setting, implemented in Junio’s v5 patch. This email is a short exchange between Jeff King (Peff) and Vsevolod Myalitsin about a tangential cleanup.

Peff confirms that Vsevolod’s v4 patch looks good to him, matching the now-settled direction. Vsevolod then floats a small interface refactoring: passing a pointer to the `advice_setting` struct into `vadvise()` instead of unpacking its fields. Peff agrees this is a reasonable cleanup and suggests submitting it as a separate patch. The exchange is purely mechanical and does not affect the core design or the imminent v5 patch; it is a post-approval polish item. Busy readers can safely skip this message unless they are tracking every micro-refinement.