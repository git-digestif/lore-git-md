This is a **build system** follow-up in the "What's cooking" thread. Junio acknowledges SZEDER Gábor’s confirmation that the `sg/precompile-git-compat-util` topic—precompiling `git-compat-util.h` to speed up builds—works not only with GCC but also with Clang and the Intel oneAPI compiler. This removes the last known portability blocker for the series, which was already marked as likely to merge in the September 2026 report. No new technical details or objections are introduced; the exchange simply clears the way for the topic to graduate to `next` and eventually `master`. The patch touches the `Makefile` and CMake build system, with no on-disk or behavioral changes.