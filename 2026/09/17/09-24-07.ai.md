This is a **substantive follow-up** in the ongoing `stash.index=true` + `--autostash` bug thread, focused on refining the proposed fix. Phillip Wood and D. Ben Knoble are collaborating to implement a more ambitious refactoring: replacing the current subprocess-heavy index-merge logic (`git diff-tree`, `git apply --cached`, `git reset`) with a direct call to `merge_incore_nonrecursive()`. This would eliminate the root cause of the bug (premature `MERGE_AUTOSTASH` ref deletion) while also improving performance and code clarity.

Phillip clarifies that the current code path—updating the index, writing it to a tree, then resetting it—is necessary to detect conflicts before committing to the merge. However, he confirms that `merge_incore_nonrecursive()` can achieve the same conflict detection without spawning subprocesses, using the infrastructure already in place for working-tree merges (`merge_ort_nonrecursive()`). The key change would be swapping `merge_ort_nonrecursive()` for `merge_incore_nonrecursive()` when merging the *index* and pointing it at `info->i_tree` instead of `info->w_tree`.

Ben Knoble, who had been struggling to understand the existing flow, expresses enthusiasm for the refactoring and plans to align his notes with Phillip’s analysis. The tone suggests active collaboration: Phillip offers to answer questions off-list, and Ben may contribute implementation work. No patches have been posted yet, but the technical direction is now clear. The discussion remains substantive, engaging with both correctness (conflict detection) and code quality (reducing subprocess overhead). Key files and symbols: `sequencer.c`, `merge_incore_nonrecursive()`, `info->i_tree`, `info->w_tree`.