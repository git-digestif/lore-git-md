This is a contributor housekeeping patch. Yoichi NAKAYAMA adds a `.mailmap` entry to normalize their name across Git's commit history. The patch maps older or variant name formats to the canonical "Yoichi NAKAYAMA <yoichi.nakayama@gmail.com>".

The change is trivial: one line added to `.mailmap`. There is no behavior change, no new functionality, and no risk of regression. This is purely about making `git shortlog` and similar tools display a consistent name for the author's contributions.

The patch is well-formed: a clear commit message, a single logical change, and a Signed-off-by trailer. It follows the established pattern of other `.mailmap` updates. No controversy is expected.

This is a standalone patch, not part of a series. It targets the `master` branch and is ready for merging once reviewed.