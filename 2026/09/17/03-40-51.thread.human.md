This thread is about enabling `RUNTIME_PREFIX` support on OpenBSD by using the system’s `getexecpath()` function to locate Git’s binary at runtime. The patch, submitted by Brad Smith, replaces a legacy "pile of hacks" with a more consistent and accurate API available in OpenBSD 8.0 and later. Junio C Hamano has reviewed the patch and found the technical implementation sound but requested improvements to the commit message to clarify the motivation for preferring `getexecpath()` over the existing BSD sysctl-based method, the deprecation status of the old method, and the timeline for OpenBSD 8.0’s release.

Brad has provided additional context in the thread explaining why `getexecpath()` is preferable, but this rationale has not yet been incorporated into the commit message. Junio has also noted that OpenBSD 8.0 is not yet released, which may affect the availability of the new API. The patch follows the established pattern for `RUNTIME_PREFIX` support on other platforms and introduces no new CLI options, configuration keys, or on-disk format changes. No test coverage is included, consistent with other platform-specific runtime detection code.

The patch remains under review, with no other participants weighing in. A revised version (v2) is expected to address Junio’s feedback, particularly the missing context in the commit message. If these gaps are filled, the patch appears uncontroversial and ready for merging. No merge status has been reported yet.