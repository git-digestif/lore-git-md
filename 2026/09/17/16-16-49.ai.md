This is a surface-level review of the Git v2.56.0-rc1 release notes. Tuomas Ahola suggests a minor wording improvement to the entry about `git history` segfaulting in corrupt repositories. The current phrasing ("can segfault when a necessary tree object is not found") is accurate but could be slightly clearer by adding that the issue has been fixed. The proposed change ("...which has been corrected") would make it explicit that the segfault no longer occurs, avoiding any ambiguity for readers skimming the release notes.

The suggestion is purely editorial and does not address a technical flaw. It targets the final release notes rather than the code itself, so it carries no risk of regression or behavioral change. The fix is already queued for the `maint` branch (merge 76621488e8), meaning it will be included in the upcoming release regardless of whether the phrasing is updated. This is a routine polish request for a release candidate announcement.