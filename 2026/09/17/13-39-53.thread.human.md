This thread is a simple housekeeping update from Yoichi NAKAYAMA to normalize their name in Git's `.mailmap` file. The patch adds a single line to map older name variants to the canonical "Yoichi NAKAYAMA <yoichi.nakayama@gmail.com>", following the project's standard practice for contributor name consistency. This type of change is routine and uncontroversial—it affects only how contributor names appear in tools like `git shortlog` and has no impact on code behavior or functionality.

No review or discussion has appeared yet, but none is strictly needed for a `.mailmap` update. The patch is self-contained, sent via GitGitGadget, and follows the established pattern for such maintenance work. It’s a single-patch submission and the thread root.