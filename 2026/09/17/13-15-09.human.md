This is a **bugfix** targeting the stash and merge subsystems, part of the ongoing effort to resolve a bug where `stash.index=true` combined with `--autostash` during a fast-forward merge leaves behind a redundant stash entry and fails to clean up the `MERGE_AUTOSTASH` ref.

D. Ben Knoble reports progress on implementing the refactoring fix proposed earlier in the thread: replacing the current subprocess-heavy index-merge logic (`git diff-tree`, `git apply --cached`, `git reset`) with a direct call to `merge_incore_nonrecursive()`. This approach avoids premature deletion of the `MERGE_AUTOSTASH` ref, which was the root cause of the bug, while also improving performance by eliminating subprocess overhead. Knoble’s initial attempt to use `reset_tree(&c_tree, 0, 1)` as a simpler fix failed to resolve the bug and caused test failures across six test files, confirming that the more ambitious refactoring is necessary.

The attached work-in-progress patch successfully passes tests and fixes the bug, though it still needs polishing (commit message cleanup, removal of unused functions, and conflict resolution with an experimental branch). Knoble plans to send a formal patch series soon. The collaboration between Knoble and Phillip Wood continues, with Wood’s earlier analysis providing the technical foundation for the fix. The discussion remains substantive, engaging with both correctness (conflict detection during index merging) and code quality (reducing subprocess overhead and improving clarity). This patch looks well-motivated and likely to land once polished.