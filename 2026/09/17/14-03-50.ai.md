This is a brief follow-up in the advice-scope-hint thread. Vsevolod Myalitsyn replies to Junio C Hamano’s v5 patch, confirming that the single-patch approach implementing the uniform `--global` hint for all `advice.*` settings aligns with the thread’s consensus. He endorses the minimal design but suggests a follow-up refactor: passing a pointer to the `advice_setting` struct to `vadvise()` instead of its individual fields, arguing this would be a cleaner interface. The suggestion is narrowly scoped to interface hygiene and is explicitly decoupled from the core advice-scope work. No objections or new design questions are raised; the thread remains converged on the uniform `--global` approach. Key symbols to track in future discussion: `vadvise()` and `advice_setting`.