This is the second patch in Mark C. Chu-Carroll’s **test modernization** series (v3, 3/3). It updates `t/t4009-diff-rename-4.sh` to use modern Git test style, replacing older patterns with current best practices for readability and improved test error messages.

The patch is purely mechanical: it reorganizes the test script’s structure without changing any test logic or Git behavior. Key changes include consolidating setup steps into inline shell blocks within `test_expect_success`, standardizing variable names (`expect`/`actual` instead of `expected`/`current`), and using consistent indentation and quoting. The diffstat shows a net reduction of 5 lines (54 insertions, 59 deletions), reflecting the cleanup’s goal of conciseness.

This patch is part of the ongoing **test modernization** effort, which targets legacy test scripts for stylistic updates. The changes are confined to the test suite and are unlikely to be controversial, though they may attract surface-level review feedback about specific formatting choices. No new symbols, options, or behaviors are introduced.

The modernization here follows the same pattern as the rest of the series: encapsulating setup steps, improving test descriptions, and using modern helper functions like `compare_diff_raw_z`. The patch is well-scoped and aligns with the project’s current test-writing conventions.

Files touched: `t/t4009-diff-rename-4.sh`. No core Git code or user-facing functionality is affected. The series remains under review, with this patch positioned as a straightforward cleanup in a larger effort.