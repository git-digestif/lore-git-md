This is the first patch in SZEDER Gábor’s 4-patch **build system** series introducing precompiled headers for faster Git builds. It removes a now-empty `XDIFF_OBJS` initialization in the `Makefile`, a leftover from the 2025 removal of the static `XDIFF_LIB` (cf680cdb95). The change is purely cleanup—no functional impact, just removing a line that no longer serves any purpose. This is a straightforward, uncontroversial housekeeping patch that paves the way for the precompiled-header work in patch 4.