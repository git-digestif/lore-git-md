**Summarizer brief mode**

Patrick Steinhardt replies to Kaartic Sivaraam’s feedback on patch 7/11 (v2) of the ref-storage terminology unification series. Kaartic suggested rephrasing the error message to clarify that the input is a *configuration* (e.g., `fatal: invalid ref storage format configuration: 'garbage://hello-world'`), but Patrick declines, preferring to retain the existing wording (`unknown ref storage format: 'garbage://hello-world'`) for precision. He argues that printing the full URI (rather than just the format name) is acceptable and may help users identify the source of the error. No changes will be made to the patch in response to this feedback.

Key details:
- **Error message wording**: Patrick keeps `unknown` over `invalid` and retains the full URI in diagnostics.
- **Symbols**: `ref_storage_format_by_uri()`, `GIT_REFERENCE_BACKEND_ENVIRONMENT`, `discovery.format.ref_storage_format`, `discovery.format.ref_storage_payload`.
- **Files touched**: `setup.c` (error message remains unchanged).
- **No functional changes**: The patch’s refactoring (extraction of URI-parsing logic) is unaffected; this is purely an editorial discussion.