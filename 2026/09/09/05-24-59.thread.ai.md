Thread identity
Subject: [PATCH 0/2] doc: sync command-list.txt with non-command manual pages
Series version: v2 (addresses Junio’s quoting feedback)
Originating author: Tuomas Ahola
Thread type: patch series (contributor housekeeping)

Core problem or goal
Ensure the `command-list.txt` file stays in sync with the set of non-command manual pages (concept guides and interface manuals such as `gitformat-loose(5)` and `gitpacking(7)`). The immediate goal is to add two missing entries (`gitformat-loose(5)` as "developerinterfaces" and `gitpacking(7)` as "guide") and extend the `lint-manpages.sh` script so that future omissions are caught automatically. `gitweb.conf(5)` is intentionally omitted to avoid a confusing display name ("web.conf") in `git help -a`.

Current status
Under review; v2 posted. The series is two patches:
1. Adds the missing entries to `command-list.txt`; commit message adjusted to use `command-list.txt` as the area identifier (stylistic tweak).
2. Updates `lint-manpages.sh` to verify that all non-command manual pages are listed, using a new `MAN_GUIDES` variable derived from `MAN5_TXT` and `MAN7_TXT` in the Makefile. The linter now fails if `command-list.txt` is incomplete. Junio’s requested quoting fixes (`"$file"` and `$(dirname "$file")`) are incorporated. The patch depends on `c655855559` (which adds `gitdatamodel(7)`) to pass the new test.

Key technical details
- Files touched: `Documentation/` directory, `command-list.txt`, `lint-manpages.sh`.
- New or renamed symbols: `MAN_GUIDES` variable in `lint-manpages.sh`.
- Old-vs-new behavior: no runtime change; the linter now acts as a regression test for documentation completeness.
- On-disk format changes: none.
- Test coverage: the linter itself provides coverage.

Open questions and loose ends
None. Junio’s review feedback (quoting fixes) has been addressed. The series is ready for acceptance pending maintainer re-review.

Participant positions
- Tuomas Ahola (author): posted v1 and v2; incorporated Junio’s mechanical feedback (quoting fixes and commit message area identifier); provided clear justification for the exclusion of `gitweb.conf(5)`.
- Junio C Hamano (maintainer): surface-level review of v1’s first patch ("Good eyes. Thanks.") and mechanical review of the second patch (quoting fixes). No substantive engagement with design or motivation.

Related work
- `kh/doc-datamodel`: the broader documentation effort that motivated this housekeeping.
- `c655855559`: prior commit adding `gitdatamodel(7)`, a dependency for the linter check.