This is a short, focused exchange in the ongoing `defaultBranchName` advice documentation fix thread, resolving the last major technical obstacle before the patch can proceed. Junio C Hamano and Jeff King (Peff) agree on a backward-compatibility mechanism for the existing `advise()` function: instead of creating an ad-hoc `advice_setting` struct on the stack (Junio’s earlier proposal), they will pass `NULL` to the new `vadvise()` helper to indicate that the message is not tied to any specific advice config. This approach is cleaner, more idiomatic in C, and avoids the need for a dummy struct.

The exchange also touches briefly on the broader advice subsystem design. Junio acknowledges Peff’s observation that most `advise()` calls are wrapped in `if (advice_enabled(ADVICE_FOO))` and thus do not show turn-off instructions, while only `advise_if_enabled()` calls do. They agree that this inconsistency is not a blocker for the current patch but could be revisited in the future. Junio clarifies that the existing pattern (checking `advice_enabled()` before doing expensive work and then calling `advise()`) is intentional and should not be converted to `advise_if_enabled()` unless the turn-off instructions are explicitly desired.

The unresolved syntax debate (`--global` vs `set --global`) and the core architectural changes (using `scope_hint` and passing the `advice_setting` struct to `vadvise()`) remain settled. The author, Vsevolod Myalitsin, can now proceed with a v3 patch incorporating the agreed-upon `NULL` pointer approach for backward compatibility. The patch is now effectively ready for merging once the final implementation details are polished.