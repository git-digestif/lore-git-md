This is patch 8/11 in Patrick Steinhardt’s v2 series to unify Git’s terminology for "ref storage format." The email is a **substantive review** from Kaartic Sivaraam, focusing on a specific edge case in the immutability check for ref storage payloads.

The review identifies a real-world scenario where reinitializing a repository with semantically identical but syntactically different payload paths (e.g., `files:///path/to/store` vs. `files:///path/to/./store`) would incorrectly trigger a fatal error. Kaartic suggests using `strbuf_realpath()` to normalize paths before comparison, aligning with how the payload is resolved at runtime in `refs_compute_filesystem_location()`. This would prevent false positives while preserving the immutability guarantee.

The rest of the patch (precedence rules, error messages, and the core refactoring) is uncontroversial and receives no further feedback. The suggestion is narrowly scoped to the payload comparison logic and does not challenge the patch’s overall design or motivation. If adopted, it would improve robustness without changing the patch’s behavior for well-formed inputs. No other reviewers have weighed in on this point yet.