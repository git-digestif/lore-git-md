This is the first patch in SZEDER Gábor’s four-part **build system** series introducing precompiled headers for faster Git builds. This patch is a cleanup: it removes an obsolete `XDIFF_OBJS` initialization in the `Makefile` that has been empty since the `xdiff` static library was removed in October 2025. The change is trivial (one line deleted) and purely mechanical, with no functional impact on the build or runtime. It sets the stage for the later patches by tidying up unused variables, making the build system slightly cleaner before the precompiled header work begins. No controversy or review feedback is expected here—this is straightforward housekeeping.