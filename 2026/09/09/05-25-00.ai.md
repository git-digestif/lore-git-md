This is the first patch in a two-part contributor housekeeping series aimed at keeping `command-list.txt` in sync with Git’s non-command manual pages. The patch adds two missing entries: `gitformat-loose(5)` (classified as a "developerinterfaces" guide) and `gitpacking(7)` (classified as a "guide"). The author notes that `gitweb.conf(5)` is intentionally left out because its inclusion would result in a confusing display name ("web.conf") in `git help -a`.

The change is mechanical and confined to `command-list.txt`, with no runtime or on-disk format impact. It directly addresses the series’ goal of preventing documentation drift by ensuring the linter (updated in the next patch) can catch future omissions. The commit message is clear and explains the rationale for excluding `gitweb.conf(5)`, which avoids ambiguity for reviewers. This patch is uncontroversial and aligns with the broader `kh/doc-datamodel` documentation effort. No substantive feedback has been recorded yet, but the patch appears well-scoped and ready for review.