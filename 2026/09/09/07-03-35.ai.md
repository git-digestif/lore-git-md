This is a brief reply to patch 2/11 in Patrick Steinhardt’s v2 series standardizing Git’s ref-storage terminology. The patch renames `--ref-format=` to `--ref-storage-format=` in `git clone` while retaining the old option as a hidden backward-compatible alias.

Patrick acknowledges Kaartic Sivaraam’s feedback: a trivial typo fix in the test script (`t/t7424-submodule-mixed-ref-formats.sh`), where `--ref-storage` should be `--ref-storage-format`. The correction is mechanical and will be included in the next revision. No other changes are proposed or discussed in this reply.

The series remains focused on unifying terminology across CLI options, environment variables, config keys, and documentation, with patch 2 specifically addressing `git clone`. The backward-compatibility strategy (retaining old option names as hidden aliases) is unchanged, and the typo fix does not affect the patch’s functional scope.