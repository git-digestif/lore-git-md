This is Junio’s reply to Vsevolod Myalitsyn’s question about how to structure the upcoming v4 reroll of the `advice: add scope hint` series. The email clarifies the expected patch-series organization: each of the three patches should have its own distinct subject line and commit message, explaining its specific purpose within the overall goal (adding a `scope_hint` field to advice settings). Junio provides a concrete example using a recent 4-patch hook-series thread, illustrating how each patch addresses a separate logical step while contributing to the series’ shared objective. He also outlines what the commit messages for the three patches might look like, emphasizing that the cover letter should describe the motivation and overall structure, while each individual patch focuses on its narrower scope.

Junio additionally gives practical advice on threading: the v4 cover letter should reply to the v3 patch email, and each v4 patch should reply to the v4 cover letter, ensuring a clean, automation-friendly thread structure in the mailing-list archive. No new technical questions are introduced; the unresolved enum-reuse and translation-clarity issues from Peff’s earlier review remain open but are not discussed here. The series is still under active revision, with v4 expected to address Junio’s mechanical feedback and adopt the three-part split. Key symbols and files mentioned: `advice_setting`, `vadvise()`, `advice.c`, and the three-part split (API change, scope mechanism, default-branch-name scope assignment).