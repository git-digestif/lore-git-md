This thread is a small but useful housekeeping series from Tuomas Ahola to keep Git's `command-list.txt` in sync with its non-command manual pages. The goal is to add two missing entries (`gitformat-loose(5)` and `gitpacking(7)`) and extend the `lint-manpages.sh` script to catch future omissions automatically. The series is uncontroversial and under review, with no prior versions.

Junio C Hamano has reviewed the first patch and given it a brief "Good eyes. Thanks," signaling no issues with its content or design. The second patch, which adds the linter check, still needs two mechanical shell quoting fixes before it can be accepted. The author has already justified excluding `gitweb.conf(5)` to avoid a confusing display name in `git help -a`. Once the quoting fixes are made, this series should be ready to merge.