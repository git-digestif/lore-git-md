This thread is a small but useful housekeeping patch series from Tuomas Ahola, part of the broader `kh/doc-datamodel` documentation effort. The goal is to keep Git’s `command-list.txt` file in sync with its non-command manual pages—things like `gitformat-loose(5)` and `gitpacking(7)`—by adding two missing entries and extending the `lint-manpages.sh` script to catch future omissions automatically.

Junio C Hamano has reviewed the second patch and flagged two minor shell scripting issues: unquoted variables in the linter script that could misbehave with paths containing whitespace. These are straightforward fixes and don’t affect the series’ core purpose. The patches are otherwise uncontroversial, touching only documentation infrastructure with no runtime impact. The linter now acts as a regression test for documentation completeness, and the exclusion of `gitweb.conf(5)` (to avoid a confusing display name in `git help -a`) is clearly justified in the commit message.

No other reviews have appeared yet, but the changes are mechanical and align with ongoing documentation work. A v2 addressing Junio’s feedback is likely, after which the series should be ready for merging.