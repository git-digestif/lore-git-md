This thread is a small but useful housekeeping series from Tuomas Ahola to keep Git’s `command-list.txt` in sync with its non-command manual pages. The goal is to add two missing entries (`gitformat-loose(5)` and `gitpacking(7)`) and extend the `lint-manpages.sh` script to catch future omissions automatically. The series is now at v2, addressing Junio Hamano’s feedback on shell quoting in the linter script.

The changes are straightforward: the first patch adds the missing entries to `command-list.txt`, while the second updates the linter to verify completeness using a new `MAN_GUIDES` variable. The linter now fails if any non-command manual page is missing, acting as a regression test. `gitweb.conf(5)` is intentionally excluded to avoid a confusing display name in `git help -a`. Junio’s review was surface-level, focusing on mechanical fixes (quoting) rather than design, and those have been incorporated.

The series is uncontroversial and ready for acceptance. It touches only the `Documentation/` directory and `lint-manpages.sh`, with no runtime behavior changes or on-disk format modifications. The broader context is the `kh/doc-datamodel` effort to improve Git’s data model documentation, and the linter enhancement aligns with the project’s emphasis on documentation quality. No open questions remain.