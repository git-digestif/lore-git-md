This is the twelfth patch in Patrick Steinhardt’s v3 series unifying Git’s terminology for "ref storage format." It renames the configuration key `init.defaultRefFormat` to `init.defaultRefStorageFormat` while retaining the old name as a backward-compatible alias. The change aligns with the series’ goal of standardizing on `--ref-storage-format=` and related names across CLI options, environment variables, and now configuration keys.

The patch is mechanical and uncontroversial: it updates the config key in `setup.c`, adjusts documentation in `BreakingChanges.adoc`, `config/feature.adoc`, and `config/init.adoc` to reflect the new name, and adds a deprecation note for the old key. The test script `t/t0001-init.sh` is updated to use the new config key in all test cases, ensuring consistency with the rest of the series. The backward-compatible alias ensures scripts or tools using the old key will continue to work without warnings.

This patch completes the series’ coverage of all user-facing interfaces (CLI, environment variables, and config keys). The only remaining patch (13/13) is expected to be a final cleanup or documentation update. The series as a whole is now functionally complete, with all prior feedback addressed. The config key change is well-motivated and unlikely to cause disruption, given the backward-compatible alias and the series’ thorough test coverage.