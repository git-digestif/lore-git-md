This is a refactoring patch in Patrick Steinhardt’s 9-patch ODB abstraction series. It is the third patch in v4 (`[PATCH v4 3/9]`), part of the ongoing effort to simplify alternates handling by moving it into the ODB backend.

The patch moves the `setup_reference()` function in `builtin/clone.c` closer to `copy_alternates()`, eliminating the need for a forward declaration. This is a purely mechanical change that prepares for the next patch (4/9), where `setup_reference()` will be refactored to call `copy_alternates()` directly. The diff shows a straightforward reordering of the two functions, with no logic changes.

The patch is uncontroversial and aligns with the series’ goal of consolidating alternates logic into a single code path. It touches only `builtin/clone.c` and introduces no new symbols or behavior changes. The motivation is clear: avoid a forward declaration by placing `setup_reference()` after `copy_alternates()`, which it will soon call. This is a small but necessary step in the larger refactoring.

Reviewers are unlikely to focus on this patch, as it is a simple reordering with no functional impact. The patch is well-motivated and directly supports the series’ technical direction. No substantive feedback is expected, and the change is safe to integrate as part of the broader ODB abstraction effort.