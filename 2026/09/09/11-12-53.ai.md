This is patch 7/13 in Patrick Steinhardt’s v3 series to unify Git’s terminology around "ref storage format." The patch is a mechanical rename in the build-info output: it changes the field name from `default-ref-format` to `default-ref-storage-format` in `help.c` and updates the corresponding test in `t/t0001-init.sh`. The change aligns the build-info output with the new `--ref-storage-format=` CLI option and related environment/config keys introduced earlier in the series.

The patch is trivial and uncontroversial. It touches only two lines (one in `help.c`, one in `t/t0001-init.sh`) and is part of the broader effort to eliminate inconsistent terminology before Git 3.0, where the default ref storage format will switch from "files" to "reftable." The test update ensures the build-info output is correctly parsed when verifying the default format. No functional changes or edge cases are introduced; this is purely a naming consistency fix.