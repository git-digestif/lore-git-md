This thread is about a patch series from SZEDER Gábor that introduces precompiled headers to speed up Git builds by roughly 35%. The series targets `git-compat-util.h`, a frequently included header, and is purely a build-system optimization with no runtime changes. The initial v1 patches are under review, with no maintainer decisions yet.

The key technical work involves modifying the Makefile and CMake build scripts to generate and use a precompiled header (`.git-compat-util.h.gch`), while carefully excluding files that don’t include `git-compat-util.h`—such as reftable objects and third-party sources. The series also cleans up some legacy build variables and generalizes CMake’s parsing logic to avoid future issues.

In this latest email, Gábor responds to a reviewer’s suggestion to refine the exclusion logic by splitting object lists into `_NOPCH` variants. He demonstrates that this approach yields only a negligible 1% speedup (0.2 seconds) while adding significant complexity to the Makefile. His position is that the trade-off isn’t worth it, and the main series remains unchanged. The discussion stays focused on build-system mechanics, with no new cross-platform or runtime concerns raised. The thread is still in the review phase, with no clear path to merge yet.