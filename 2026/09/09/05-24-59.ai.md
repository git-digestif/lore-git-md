This is a two-patch contributor housekeeping series that keeps the documentation build system in sync with the list of concept guides and interface manuals.

The series expands the existing `lint-manpages.sh` script to verify that `command-list.txt` includes all non-command manual pages (guides like `gitformat-loose(5)` and `gitpacking(7)`). Patch 1 adds the two missing entries to `command-list.txt`, and patch 2 updates the linter to check for them. The changes are mechanical and confined to the `Documentation/` directory and the top-level `command-list.txt`.

The commit message references `kh/doc-datamodel`, indicating this work is part of the broader effort to document Git’s data model (led by Julia Evans and later picked up by others). No new user-facing behavior is introduced; the goal is to prevent future documentation drift.

The patches look clean and well-scoped. The linter change is a sensible addition that will catch similar omissions early. No controversy or follow-up work is expected.