This is a surface-level review reply in the ongoing thread about Patrick Steinhardt’s 11-patch v2 series to standardize Git’s terminology for "ref storage format." The email is a follow-up to patch 1/11, which renames `--ref-format=` to `--ref-storage-format=` in `git init` while retaining the old name as a hidden backward-compatible alias.

Kaartic Sivaraam acknowledges Patrick’s preference for a "quiet deprecation" of the old `--ref-format=` option—keeping it functional but hidden from help output without warnings or documentation updates. The discussion remains focused on implementation details rather than technical objections: Kaartic suggests two minor improvements to make the deprecation more transparent to future code readers. First, he proposes updating the description of the old option (even though it is hidden) to explicitly mark it as deprecated, following the pattern in `builtin/name-rev.c`. Second, he notes that `OPT_ALIAS` (which could clarify the alias relationship) currently lacks support for `PARSE_OPT_HIDDEN`, and agrees with Patrick that a preparatory patch to add this support would be worthwhile. Neither suggestion is a blocker; both are editorial refinements to improve maintainability. The thread’s broader questions—such as the lifecycle of the old option and the consistency of backward-compatibility aliases—remain unresolved but uncontroversial. No new technical concerns or edge cases are introduced in this reply.