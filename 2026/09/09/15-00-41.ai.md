This is a **substantive review comment** on the `receive-report` hook series, now at v9. Patrick Steinhardt questions why the critical design flaw in patch 2/4—where a `BUG()` was triggered when clients omitted the `report-status` or `report-status-v2` capability—went unnoticed in testing. The flaw was fixed in v9 by replacing the `default: BUG(...)` case with a silent `break` for `REPORT_STATUS_UNKNOWN`, but the absence of test failures suggests a gap in coverage: the test suite does not verify behavior when the client does *not* request a status report. The 224-line `t/t5412-receive-report-hook.sh` test script focuses on scenarios where the hook is invoked (i.e., when the client *does* request a report), leaving the no-report case untested. This is a **plausible blind spot** given the hook’s design as a filter for the status report, which is only generated when the client advertises the capability. The comment does not propose a specific fix but flags the issue as a potential oversight that could affect edge-case robustness. No other feedback is provided; the series remains otherwise approved and queued in `next`.