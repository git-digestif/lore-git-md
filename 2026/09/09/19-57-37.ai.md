This is a follow-up in SZEDER Gábor’s **build system** series introducing precompiled headers for faster Git builds. The email responds to a suggestion (not shown in the thread context) to refine the exclusion logic for files that don’t include `git-compat-util.h`, proposing a more granular approach that splits `COMPAT_OBJS`, `REFTABLE_OBJS`, and `THIRD_PARTY_SOURCES` into `_NOPCH` variants.

The patch demonstrates that the performance gain from finer-grained filtering is minimal—only a **1% speedup (0.2s)** on the author’s Linux box—while adding significant churn to the Makefile. The diff introduces three new variables (`COMPAT_NOPCH_OBJS`, `REFTABLE_NOPCH_OBJS`, `THIRD_PARTY_NOPCH_SOURCES`) and updates all conditional blocks that populate `COMPAT_OBJS` to use the `_NOPCH` variant instead. The CMake build system is also adjusted to parse both `REFTABLE_OBJS` and `REFTABLE_NOPCH_OBJS` for completeness.

The author’s position is clear: the complexity isn’t justified by the marginal improvement. The benchmark data (21.7s → 21.5s) reinforces this, and the patch is presented as a reference rather than a serious proposal. No changes to the main series are implied; this is purely an illustrative counterpoint to a reviewer’s suggestion. The discussion remains focused on build system mechanics, with no runtime behavior or cross-platform concerns raised in this reply.