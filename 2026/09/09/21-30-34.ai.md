This is a follow-up in the ongoing `advice: add scope hint` series, now preparing a v4 reroll. The email is a direct reply to Junio’s review of v3, confirming the author’s intent to address all three mechanical issues Junio raised: (1) adding explicit motivation to the commit message about why `advice.defaultBranchName` belongs in per-user (global) configuration, (2) documenting the interface narrowing in `vadvise()`, and (3) fixing the `switch` statement indentation. The author also agrees to Junio’s suggestion to split the patch into a three-part series (API change, scope mechanism, default-branch-name scope assignment) and asks for clarification on whether each patch should have its own subject line or share a common theme.

No new technical questions are introduced; the two open questions from Peff’s earlier review (enum reuse and translation clarity) remain unresolved but are not discussed here. The series is still under active revision and not yet ready for Junio’s queue.