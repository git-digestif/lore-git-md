This is the first patch in the v4 series that defers auto maintenance until the end of sequencer-driven operations (`git rebase`, `git cherry-pick`, `git revert`). It introduces a reusable helper, `git_config_append_parameter()`, to build `GIT_CONFIG_PARAMETERS` strings—a space-separated list of `'key'='value'` pairs with proper shell quoting. The helper is extracted from the existing `git_config_push_split_parameter()` function and documented in `config.h` with clear formatting rules (e.g., `NULL` values are treated as boolean `true`).

The patch is a straightforward refactoring that splits out the formatting logic for reuse in the sequencer. It touches only `config.c` and `config.h`, adding 26 lines and removing 7. The commit message is now concise and explicitly states the helper’s purpose: enabling the sequencer to build config strings for child processes. The "Assisted-by: Claude Fable 5.1" credit reflects the author’s use of an LLM for drafting, but the message itself has been tightened to avoid verbosity and ensure human authorship, addressing prior feedback from Junio and Patrick Steinhardt.

This patch is uncontroversial and well-motivated. It lays the groundwork for the next two patches, which use the helper to disable auto maintenance during sequencer operations. The series as a whole is technically complete, with Junio signaling intent to queue it ("Will queue"). No open issues remain for this patch.