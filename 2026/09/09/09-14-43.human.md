This is a surface-level review reply in the ongoing thread about Patrick Steinhardt’s 11-patch series to standardize Git’s terminology for "ref storage format." The series renames `--ref-format=` to `--ref-storage-format=` across multiple commands while retaining backward compatibility.

Kaartic Sivaraam acknowledges Patrick’s preference for a "quiet deprecation" of the old `--ref-format=` option—keeping it functional but hidden from help output—and agrees it’s a reasonable approach. The discussion focuses on two minor implementation details: whether to update the old option’s description in the code to explicitly mark it as deprecated (for clarity to future readers), and whether to use `OPT_ALIAS` (which currently lacks `PARSE_OPT_HIDDEN` support) to make the alias relationship more obvious. Patrick has already noted he might add support for hidden aliases in a preparatory patch, which Kaartic endorses as worthwhile. No technical objections or edge cases are raised; the feedback remains procedural and aimed at long-term code clarity. The series is still under review, with no merge status reported.