This is a small but necessary part of Patrick Steinhardt’s ongoing effort to unify Git’s terminology around "ref storage format." The patch renames the build-info field `default-ref-format` to `default-ref-storage-format` in `help.c` and updates the corresponding test in `t/t0001-init.sh`. This change aligns the build output with the new `--ref-storage-format=` CLI option and related environment variables introduced in earlier patches of the series.

The motivation is clear: Git 3.0 will change the default ref storage backend from "files" to "reftable," and the build info should reflect the new terminology consistently. The patch is mechanical and well-scoped, touching only two lines of code. The test update ensures the renamed field is correctly parsed and compared against the output of `git rev-parse --show-ref-storage-format`.

This is a straightforward refactoring with no functional impact. The change is uncontroversial and addresses the last remaining instance of the old "ref-format" terminology in Git’s build output. If the rest of the series lands, this patch will be a necessary follow-through. For readers tracking the larger effort, this is a minor but complete step toward consistency.