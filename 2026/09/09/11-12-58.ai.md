This is the twelfth patch in Patrick Steinhardt’s v3 series unifying Git’s terminology for "ref storage format." It renames the configuration key `init.defaultRefFormat` to `init.defaultRefStorageFormat` while retaining the old name as a backward-compatible alias, mirroring the changes already applied to command-line options and environment variables in earlier patches.

The patch updates five files: `Documentation/BreakingChanges.adoc` and `Documentation/config/feature.adoc` (mechanical string replacements), `Documentation/config/init.adoc` (adds the new key and marks the old one as deprecated), `setup.c` (teaches the config parser to recognize both keys and updates a comment to reflect the new name), and `t/t0001-init.sh` (renames all test references from `init.defaultRefFormat` to `init.defaultRefStorageFormat`). The test updates are purely mechanical and cover the precedence rules, error messages, and feature-experimental interactions.

No new functionality is introduced; the patch is a straightforward rename with backward compatibility. The only edge case is the comment in `setup.c` that now lists both the new and old config keys, ensuring the precedence logic remains clear. The series is now one patch away from completion, with only the final patch (13/13) left to summarize.