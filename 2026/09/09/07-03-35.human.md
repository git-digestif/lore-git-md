This is a brief, surface-level reply in Patrick Steinhardt’s v2 series standardizing Git’s terminology for "ref storage format." The series renames `--ref-format=` to `--ref-storage-format=` across Git commands while retaining backward-compatible aliases.

Patrick acknowledges Kaartic Sivaraam’s feedback on patch 2/11, which caught a typo in the test script (`--ref-storage` should be `--ref-storage-format`). The fix is trivial and already incorporated. No other changes are discussed; the rest of Kaartic’s feedback (about deprecation strategy and `OPT_ALIAS`) remains unresolved but is not addressed here. The series continues to progress with minor editorial fixes.