Thread identity
Subject: [PATCH v3 01/13] parse-options: support hidden option aliases
Series version: v3
Originating author: Patrick Steinhardt
Thread type: patch series (ref storage unification)

Core problem or goal
Extend the parse-options API to allow option aliases to be marked as hidden (`PARSE_OPT_HIDDEN`). This enables future patches to deprecate old option names while keeping them functional but out of help text, a requirement for the larger ref-storage unification effort.

Current status
Under review; this is the first patch in the v3 series. No prior versions of this specific patch exist; the series as a whole is at v3.

Key technical details
- Files touched: `parse-options.c`, `parse-options.h`
- New macro: `OPT_ALIAS_F()` (accepts flags)
- One-line change in option preprocessing to propagate alias flags
- No behavior changes in this patch; purely plumbing for later patches

Open questions and loose ends
None yet; the patch is uncontroversial and appears ready for integration.

Participant positions
None recorded yet; this is the thread root.

Related work
Part of Patrick Steinhardt’s ref-storage unification series (v3 01/13). The cover letter or later patches will demonstrate how hidden aliases are used to deprecate ref-storage options.