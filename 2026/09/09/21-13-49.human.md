This is a reply in the ongoing Rust infrastructure reorganization thread, specifically addressing Mike Hommey’s `[PATCH v3]` to move Git’s Rust code into a `rust/` subdirectory.

The email from brian m. carlson raises a forward-compatibility concern about the patch’s implications for projects that vendor Git as a submodule. Brian interprets Mike’s motivation as enabling a workflow where Git’s C code is vendored into a Rust project while excluding Git’s own Rust components. He questions whether this approach will remain viable after Git 3.0, when Rust components are expected to become mandatory, and suggests that a more future-proof strategy might be preferable now rather than a short-term workaround.

This is not a review of the patch’s code or mechanics, but a strategic objection to its underlying rationale. The concern is narrowly scoped: it does not challenge the directory reorganization itself, but rather the use case that motivated it. The thread’s earlier consensus on project hygiene (cleaning up the misleading top-level `Cargo.toml`) remains intact; this reply introduces a new dimension about how the change might interact with downstream consumers of Git’s source. The discussion is now split between immediate implementation feedback (Junio’s surface-level review) and longer-term architectural considerations (Brian’s question).