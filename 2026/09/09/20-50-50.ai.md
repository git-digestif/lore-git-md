This is a small feature patch targeting the advice subsystem, part of the ongoing v3 series to add scope hints for disabling advice messages. The thread AI summary already covered the series' goal and open questions; this email is Junio’s review of the v3 patch.

Junio’s feedback is surface-level but constructive. He points out three mechanical issues: (1) the commit message should explicitly state that `advice.defaultBranchName` should be set in per-user (global) configuration, not per-repository, (2) the `switch` statement indentation is non-standard, and (3) the narrowing of the `vadvise()` interface (from passing individual fields to passing the whole `advice_setting` struct) should be documented in the commit message. He also suggests splitting the patch into a three-part series: API change, scope mechanism, and default-branch-name scope assignment.

The patch itself remains unchanged in substance; Junio’s comments are about presentation and structure. No new technical questions are raised, and the two open questions from Peff’s earlier review (translation clarity and enum reuse) are not revisited here. The series is still under review and not yet queued.