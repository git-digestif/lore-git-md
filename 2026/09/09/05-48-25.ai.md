This is patch 2/9 in Patrick Steinhardt’s 9-patch series refactoring ODB alternates handling during repository creation. The patch defers object database (ODB) initialization in `git clone` until after the clone source URI is resolved, addressing forward compatibility with alternate ODB backends and the need to resolve alternates paths before ODB setup. This is a mechanical reordering with no user-visible behavior change, but it is a necessary preparatory step for later patches that consolidate alternates handling into ODB creation.

Justin Tobler’s reply misstates the patch’s scope, confusing the reference database (`refdb`) with the alternates setup performed by `setup_reference()`. Patrick’s correction clarifies that `setup_reference()` is unrelated to the refdb and is solely responsible for alternates. This minor miscommunication does not affect the patch’s technical substance but highlights the importance of precise terminology in the ODB abstraction effort.

The patch itself is uncontroversial and aligns with the series’ broader goal of simplifying repository initialization. By deferring ODB creation, it avoids adding more skip flags to `init_db()` and ensures alternates are written with full context (e.g., resolved URIs and object hash information). The change is limited to `builtin/clone.c` and does not introduce new symbols or edge cases. Justin’s surface-level review acknowledges the patch’s role as a preparatory step, and his feedback does not raise technical concerns.

This patch sets the stage for patch 7/9, which implements the core design change: writing alternates during ODB creation via `odb_create_on_disk_options` using Git’s lockfile API. The reordering here is critical for that patch’s correctness, as it ensures the URI is fully resolved before alternates are written. Reviewers may later verify that the deferral does not introduce regressions in edge cases (e.g., template config rewriting URIs), but the patch itself appears low-risk.

The thread’s focus remains on the later patches that implement the lockfile-based alternates writing and remove the `write_alternates()` infrastructure. This patch is a straightforward stepping stone with no loose ends or open questions.