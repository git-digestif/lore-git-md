This is the closing exchange in the abandoned `--[no-]range-diff-notes` feature thread. Junio C Hamano and Kristoffer Haugsbakk clarify the intended semantics of the proposed `--no-range-diff-notes` option, confirming that the original implementation followed interpretation #2: `--no-range-diff-notes` means "no notes for the range-diff section at all," not "revert to the notes used for the patches."

The discussion centers on a documentation snippet that suggested a toggle sequence (`--no-range-diff-notes --range-diff-notes`) to revert to the default behavior. Junio points out that under interpretation #2, this sequence does *not* revert the override—it merely ensures no notes are used for the range-diff, while patch notes remain unaffected. The documentation’s claim that the sequence "turns off the notes override behavior" is therefore misleading. Junio had previously suggested removing this part of the documentation, and this email reinforces that recommendation.

The exchange also touches on a minor UI inconsistency: `git log --notes=` (with an empty argument) currently emits a warning about an invalid notes ref, whereas `--range-diff-notes=` (empty) would behave differently. Junio calls this a "UI bug" that could be fixed without backward-compatibility concerns, though it is unrelated to the main feature.

No new implementation or redesign is proposed here. The thread remains closed, and the feature is abandoned. If revived, the documentation would need to accurately reflect interpretation #2, and the toggle sequence would either need to be removed or reworked to avoid misleading users. The key takeaway is that the original design’s fallback semantics were clearer than the documentation suggested, but the toggle mechanism’s behavior was still unintuitive and would require simplification.