This is a **substantive review follow-up** in the ongoing advice-scope-hint series. Jeff King (Peff) replies to Vsevolod Myalitsin’s concern about reusing Git’s `CONFIG_SCOPE` enum for the new `scope_hint` field in `advice.c`. Peff argues that `CONFIG_SCOPE_UNKNOWN` (value `0`) is not a problem: since the `scope_hint` is only a recommendation for where users *should* set the config (not a restriction on where they *can*), `UNKNOWN` can simply mean "use the default location for writing config," which matches the current behavior. He also notes that he hasn’t followed the earlier discussion closely enough to know why some advice settings should default to `--global` while others default to `--local`, but trusts that the author’s judgment is sound.

The email resolves the last open technical question from Peff’s earlier review, clearing the way for Vsevolod to reroll the series as v4. No new issues are raised; the discussion is now purely about whether `UNKNOWN` is an acceptable semantic default, and Peff’s answer is "yes." The patch remains a small, focused change to `advice.c` that adds scope hints to advice messages without altering any existing behavior.