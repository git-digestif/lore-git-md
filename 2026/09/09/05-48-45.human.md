This is a mechanical refactoring patch in Patrick Steinhardt’s 9-patch series to simplify ODB alternates handling. It moves the `setup_reference()` function in `builtin/clone.c` closer to `copy_alternates()` to avoid a forward declaration, preparing for the next patch where `setup_reference()` will call `copy_alternates()`.

The change is purely structural: the function’s body is unchanged, only its position in the file moves. This is a preparatory step for patch 4/9, which will refactor `setup_reference()` to gather alternates paths into a `strvec` instead of immediately writing them. No behavior changes or new symbols are introduced here.

The patch is uncontroversial and aligns with the series’ goal of consolidating alternates logic. It has no test coverage of its own, as it doesn’t alter functionality. Reviewers are unlikely to focus on this patch, given its limited scope.