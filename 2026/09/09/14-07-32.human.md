This is a documentation patch from Kristoffer Haugsbakk, a small but useful fix to the `git maintenance` man page. The patch corrects a formatting issue in the bullet list describing the `--auto` option, ensuring consistent indentation and readability. The commit message is clear and links to the original email where the patch was submitted.

The patch itself is trivial: it adjusts whitespace in `Documentation/git-maintenance.txt` to align bullet points under the `--auto` option description. There’s no behavior change, just a visual improvement for users reading the manual. Given that it’s a one-line fix with no risk of regression, it’s likely to be uncontroversial and could graduate quickly if Junio picks it up. It wasn’t mentioned in the "What’s cooking" report, so it may have arrived too late for inclusion or was simply overlooked. Either way, this is a straightforward documentation cleanup that busy readers can safely ignore unless they’re particularly attentive to man page formatting.