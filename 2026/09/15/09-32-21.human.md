This is a surface-level review in the ongoing ref-storage-format terminology unification series. Kaartic Sivaraam replies to Karthik Nayak’s earlier feedback about whether `PARSE_OPT_HIDDEN` is necessary for option aliases. He notes that Git already uses public (non-hidden) aliases for deprecated options in other commands—specifically `--recursive` (deprecated alias for `--recurse-submodules` in `git clone`) and `--negotiation-tip` (deprecated alias for `--negotiation-restrict` in `git fetch`). Neither of these aliases is hidden, yet they remain in the help output without causing confusion. Kaartic suggests that exposing the deprecated `--ref-format` alias publicly might be acceptable, aligning with existing practice rather than requiring the new `OPT_ALIAS_F()` machinery. The observation is narrowly focused on consistency with Git’s established deprecation patterns and does not challenge the series’ core goal. No merge status is reported; the thread remains under discussion.