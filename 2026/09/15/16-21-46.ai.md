This is a brief maintainer acknowledgment in the "What's cooking" thread. Junio C Hamano confirms he will pick up v4 of the `ks/history-commit-leakfix` patch, which fixes a memory leak in `git history` by unusing the commit buffer after it is processed. The patch is now ready for integration into the `next` branch, having incorporated feedback from Jeff King on the commit message. No further discussion or objections are present; the change is uncontroversial and touches only `builtin/history.c`. This patch is part of the ongoing stabilization of the experimental `git history` command.