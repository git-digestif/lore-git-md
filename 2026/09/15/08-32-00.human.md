This is a brief follow-up in the "What's cooking" thread. Kaartic Sivaraam confirms that his patch `ks/history-commit-leakfix` (a memory leak fix in `git history`) has received a thumbs-up from Jeff King (Peff) and has been resent as v4 with an improved commit message. The patch is ready for Junio to pick up into `next` if he deems it suitable.

The fix itself is straightforward: it ensures the commit buffer is properly released after use in `builtin/history.c`, addressing a leak identified during review. The change is small, well-scoped, and uncontroversial, with no objections raised in the thread. Given the prior review approval and the patch's low-risk nature, it is likely to graduate to `next` in the next integration cycle.