This is a **substantive review** in the ongoing thread about inconsistent behavior between the `+` refspec modifier and `--force` in `git fetch`.

Ben Knoble points out that the observed behavior might be documented in the `PRUNING` section of `git-fetch(1)`. He does not take a position on whether the current behavior is correct or a bug, but his comment suggests that the difference between `+` and `--force` could be intentional rather than an oversight. The implication is that the documentation in `git-push(1)` (which claims `+` "does the same thing as --force") may not fully apply to `git fetch`, or that the pruning behavior of `+` in fetch refspecs is a deliberate exception.

This review advances the thread by shifting focus from "is this a bug?" to "is this behavior documented and intentional?" If the `PRUNING` section does explain the discrepancy, the next step may be to clarify the documentation rather than change the code. No patch or test case is provided; the discussion remains at the conceptual level.