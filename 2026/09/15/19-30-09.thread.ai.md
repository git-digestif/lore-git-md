Thread identity: standalone bugfix patch for Git protocol v2 `upload-pack` regression in shallow fetch ordering with `uploadpack.allowRefInWanted=true`. Author: Royce Remer.

Core problem or goal: fix a client-side crash (`fatal: expected 'packfile', received 'shallow-info'`) during shallow clones or incremental shallow fetches when `uploadpack.allowRefInWanted` is enabled. The regression is caused by swapped function call order in `upload_pack_v2()`: `send_wanted_ref_info()` was called before `send_shallow_info()`, causing the client to skip the "wanted-refs" section while searching for "shallow-info" (due to the `peek` bit in `process_section_header()`). The patch moves `send_wanted_ref_info()` after `send_shallow_info()` to ensure the client receives sections in the expected order.

Current status: patch under review; v1 posted, v2 pending to address Junio’s commit-message requirements. The fix (swapping the order of `send_shallow_info()` and `send_wanted_ref_info()`) is technically sound and validated by both test cases and production-like testing. Files touched: `upload-pack.c` (one line moved), `t/t5703-upload-pack-ref-in-want.sh` (two new test cases: single-commit shallow clone and two-commit incremental fetch). New symbols: none. Old-vs-new behavior: server now sends "shallow-info" before "wanted-refs", restoring correct ref resolution and preventing client crashes. On-disk format: unchanged. Test coverage: confirms remote ref resolution and commit history in shallow fetch scenarios.

Open questions and loose ends:
- **(1) Commit message**: Must include:
  - (a) Precise failure mode (client crash with error message `fatal: expected 'packfile', received 'shallow-info'`),
  - (b) Technical rationale for ordering requirement (client-side `peek` logic in `process_section_header()` skips unexpected sections),
  - (c) Backward compatibility assessment (no existing clients are broken; the feature combination never worked in production).

Participant positions:
- **Royce Remer (author)**: Confirms the failure is a hard client-side crash (not silent misbehavior) and that the feature combination (`uploadpack.allowRefInWanted=true` with shallow fetches) never worked in practice, ensuring backward compatibility. Position: patch is correct and ready for v2 with updated commit message.
- **Junio C Hamano (maintainer)**: Substantive review. Identified the root cause (client-side ambiguity due to `peek` logic) and requested commit-message improvements. Position: patch is technically sound but not ready for merging until the commit message addresses the failure mode, technical rationale, and backward compatibility. No change in stance.

Related work: none mentioned.