Thread identity: standalone bugfix patch for Git protocol v2 `upload-pack` regression in shallow fetch ordering with `uploadpack.allowRefInWanted=true`. Author: Royce Remer.

Core problem or goal: fix a client-side crash (`fatal: expected 'packfile', received 'shallow-info'`) during shallow clones or incremental shallow fetches when `uploadpack.allowRefInWanted` is enabled. The regression is caused by swapped function call order in `upload_pack_v2()`: `send_wanted_ref_info()` was called before `send_shallow_info()`, causing the client to skip the "wanted-refs" section while searching for "shallow-info" (due to the `peek` bit in `process_section_header()`). The patch moves `send_wanted_ref_info()` after `send_shallow_info()` to ensure the client receives sections in the expected order.

Current status: patch **ready for integration**; v2 posted addressing Junio’s commit-message requirements and now includes:
- Precise failure mode (client crash with error message),
- Technical rationale for ordering requirement (client-side `peek` logic in `process_section_header()`),
- Backward compatibility assessment (no existing clients are broken; the feature combination never worked in production).
The fix (swapping the order of `send_shallow_info()` and `send_wanted_ref_info()`) is technically sound and validated by both test cases and production-like testing. Junio confirms the bug has existed since the ref-in-want feature was introduced in 2018 (commits `733020517a` and `516e2676bd`) but went unnoticed due to the feature's niche use case.

Files touched: `upload-pack.c` (one line moved), `t/t5703-upload-pack-ref-in-want.sh` (two new test cases: single-commit shallow clone and two-commit incremental fetch). New symbols: none. Old-vs-new behavior: server now sends "shallow-info" before "wanted-refs", restoring correct ref resolution and preventing client crashes. On-disk format: unchanged. Test coverage: confirms remote ref resolution and commit history in shallow fetch scenarios.

Open questions and loose ends: none. Junio’s confirmation of the latent regression and backward compatibility resolves the last technical concern.

Participant positions:
- **Royce Remer (author)**: Confirms the failure is a hard client-side crash (not silent misbehavior) and that the feature combination (`uploadpack.allowRefInWanted=true` with shallow fetches) never worked in practice, ensuring backward compatibility. Position: patch is correct and ready for review with v2 commit message.
- **Junio C Hamano (maintainer)**: Substantive review. Identified the root cause (client-side ambiguity due to `peek` logic), requested commit-message improvements, and confirmed the bug’s origin (2018, latent due to niche use). Position: patch is technically sound and ready for integration after v2 commit message updates.

Related work: none mentioned. Key technical details for future reference: the `peek` bit in `process_section_header()`, the `UPLOAD_SEND_PACK` state in `upload_pack_v2()`, and the test file `t/t5703-upload-pack-ref-in-want.sh` (lines 298–315). The patch does not affect protocol v0/v1 or non-shallow fetches.