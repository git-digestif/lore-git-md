Thread identity: standalone bugfix patch for Git protocol v2 `upload-pack` regression in shallow fetch ordering with `uploadpack.allowRefInWanted=true`. Author: Royce Remer.

Core problem or goal: fix incorrect or incomplete ref resolution during shallow clones or incremental shallow fetches when `uploadpack.allowRefInWanted` is enabled, caused by swapped function call order in `upload_pack_v2()`. The patch moves `send_wanted_ref_info()` after `send_shallow_info()` to ensure the client receives "shallow-info" before "wanted-refs", matching the client-side (`fetch-pack.c`) logic where `process_section_header()` checks for these sections in sequence with the `peek` bit set. Sending "wanted-refs" first risks the client skipping it while searching for "shallow-info", potentially causing the "shallow-info" section to be lost or misinterpreted, leading to crashes or incorrect shallow fetch behavior.

Current status: patch under review; v1 posted, awaiting revision. Files touched: `upload-pack.c` (one line moved), `t/t5703-upload-pack-ref-in-want.sh` (two new test cases). New symbols: none. Old-vs-new behavior: server now sends shallow info before wanted-ref info, restoring correct ref resolution in shallow fetches. On-disk format: unchanged. Test coverage: added for single-commit shallow clone and two-commit incremental fetch, confirming remote ref resolution and commit history.

Open questions and loose ends: **(1)** Backward compatibility concern: could swapping the order break existing clients? Junio’s analysis suggests the client can tolerate out-of-order sections, but this needs explicit confirmation. **(2)** Commit message must now include: (a) precise description of the failure mode (client crash or silent misbehavior), (b) technical rationale for the ordering requirement, and (c) backward compatibility assessment. **(3)** Junio’s question about client robustness (does it crash or silently ignore unexpected sections?) remains unanswered.

Participant positions:
- **Junio C Hamano (maintainer)**: Substantive review. Provides detailed technical rationale for the ordering requirement and identifies the client-side ambiguity as the root cause. Requests the commit message address the failure mode, technical rationale, and backward compatibility. Position: patch is not ready for merging until these points are addressed. No change in stance from prior review, but now with deeper technical justification.

Related work: none mentioned.