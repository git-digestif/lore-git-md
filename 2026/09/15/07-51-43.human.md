This is the final version of Harald Nordgren’s feature patch adding `--matched-only` to `git range-diff`. The patch is now ready for merging after addressing the last architectural concern: the error-handling layering has been restored so that the reusable library layer (`range-diff.c`) returns an `error()` and the CLI front-end (`builtin/range-diff.c`) calls `die()` if needed.

The implementation is clean and minimal. It adds the new `--matched-only` option, extends the mutual-exclusion check to cover all three filtering flags (`--left-only`, `--right-only`, `--matched-only`), and reuses the existing suppression logic by internally setting both `--left-only` and `--right-only` when `--matched-only` is given. The documentation and test coverage are thorough, demonstrating the new behavior and its interaction with the existing flags.

This is a well-crafted, uncontroversial addition that addresses a clear workflow pain point: when reviewing a rebase or range-diff, users often want to focus on how the commits that survived the operation changed, rather than seeing commits that were simply added or dropped. The patch is part of a resolved thread; no further changes are expected unless new objections arise. It should graduate to `next` shortly.