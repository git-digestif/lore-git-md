This is a review reply from Junio C Hamano to Brigham Campbell’s patch that teaches `git-contacts` to read patch content from stdin. The patch is part of the `contrib/` scripts and aims to improve interoperability with tools like `b4`.

Junio’s feedback is substantive but narrowly focused. He identifies two issues: the placement of `Cc:` trailers in the commit message (unusual for Git patches) and a stylistic concern about how the stdin logic integrates with the existing code. His proposed alternative patch restructures the argument-handling logic to use a dedicated variable (`$read_from_stdin`) instead of re-checking `@ARGV` later, aligning with the script’s pattern of sifting `@ARGV` into meaningful variables early. He also notes that the script’s usage comment at the top of the file should be updated to reflect the new stdin capability.

The review is constructive and specific, suggesting a minor refactor rather than rejecting the feature. No functional problems are raised, and the feedback is framed as a preference for consistency with the script’s existing style. The thread is now waiting for Brigham to respond or resubmit with the suggested adjustments.