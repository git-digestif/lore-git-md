This is the first patch in SZEDER Gábor’s v2 series introducing precompiled headers for faster Git builds. It performs a small cleanup in the `Makefile`, removing an obsolete initialization of the `XDIFF_OBJS` variable. The variable was emptied in cf680cdb95 (2025) when the static `XDIFF_LIB` was removed, but the initialization itself was left behind. The change is purely mechanical and has no functional impact—it simply tidies up the `Makefile` as a prerequisite for the later patches in the series.

The patch touches only one line in the `Makefile` and is uncontroversial. It aligns with the series’ broader goal of streamlining the build system before introducing precompiled headers. No runtime behavior, dependencies, or test coverage are affected. This is a straightforward housekeeping change that should not draw objections.