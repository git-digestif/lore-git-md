This is a reply in the ongoing discussion about inconsistent behavior between the `+` refspec modifier and `--force` in `git fetch`.

Ben Knoble points André Kießling to the **PRUNING** section of the `git-fetch` man page, suggesting that the observed behavior might be documented there. He describes the documentation as "a bit opaque," implying it may not be immediately clear whether the current behavior is intentional or a bug. This reply does not take a position on whether the behavior should be changed, nor does it propose a fix—it merely directs attention to existing documentation that might explain the discrepancy. The tone is neutral and factual, leaving the question of intent or correctness unresolved for now.