This is a **follow-up cleanup patch** for the `receive-report` hook series, now cooking in `next`. Karthik Nayak acknowledges Junio C Hamano’s proposed mechanical improvement to the `override_cmds_error()` helper in `builtin/receive-pack.c`.

The patch removes a redundant null-check before `FREE_AND_NULL(cmd->error_string_owned)`. The macro is already safe to call on `NULL`, so the explicit check is unnecessary. This change is purely stylistic and does not affect the hook’s behavior or memory management. It aligns the code with the established pattern elsewhere in Git and serves as a model for updating `tools/coccinelle/free.cocci` to enforce this idiom, though the Coccinelle update itself is not included here.

Karthik’s reply confirms the patch is correct and ready to be queued on top of the series. The cleanup is uncontroversial and resolves a minor technical debt introduced during the memory-leak fix in the main series. The series remains on track for graduation to `master`.