Thread identity
Subject: [PATCH] git-contacts: accept patch input from stdin
Series version: standalone patch (no series)
Originating author: Brigham Campbell
Thread type: feature patch for a contrib script

Core problem or goal
Extend `git-contacts` (a contrib Perl script that extracts likely reviewers from patch metadata) to read patch content from stdin when stdin is not a terminal and no arguments are supplied, improving interoperability with external tools such as `b4`.

Current status
Under review; no prior versions posted. Junio C Hamano provided feedback requesting minor stylistic adjustments (commit message trailer placement, argument-handling refactor, and usage comment update) but endorsed the feature concept.

Key technical details
- File touched: `contrib/contacts/git-contacts`
- New behavior: when invoked with no arguments and stdin is not a tty (`-t STDIN` false), the script calls the existing `scan_patches()` helper on the stdin filehandle.
- No new symbols, CLI options, or on-disk format changes.
- Usage comment at the top of the file must be updated to document the stdin capability.
- Junio’s suggested refactor replaces the late `@ARGV` check with an early `$read_from_stdin` variable to align with the script’s existing style.

Open questions and loose ends
- Awaiting Brigham Campbell’s response: either a resubmission with the requested adjustments or a counterargument.
- No functional objections raised; the feedback is purely stylistic.

Participant positions
- Brigham Campbell (author): implemented the feature, provided cross-project context (link to `b4` patch thread).
- Junio C Hamano (maintainer): reviewed the patch, requested minor stylistic changes (trailer placement, argument-handling refactor, usage comment update), and endorsed the feature concept.

Related work
- Corresponding `b4` patch thread (linked in commit message).