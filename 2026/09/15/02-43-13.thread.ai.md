Thread identity
Subject: [PATCH] git-contacts: accept patch input from stdin
Series version: standalone patch (v3, with v4 anticipated)
Originating author: Brigham Campbell
Thread type: feature patch for a contrib script

Core problem or goal
Extend `git-contacts` (a contrib Perl script in `contrib/contacts/`) to read patch content from stdin using the conventional UNIX `-` idiom, enabling mixed file/stdin usage (e.g., `git contacts patch1 - patch3 <patch2`) and improving interoperability with external tools such as `b4`. This replaces the v2 behavior, which implicitly read from stdin when no arguments were given and stdin was not a terminal.

Current status
Under review; v3 posted implementing the `-` idiom as suggested by Junio C Hamano. Junio has now proposed an alternative design that avoids a separate `$read_from_stdin` flag by treating `-` as a special filename throughout the code path, pushing the stdin logic into `scan_patch_file`. The feature concept remains endorsed; no functional objections have been raised. The author has not yet responded to Junio’s structural suggestion.

Key technical details
- File touched: `contrib/contacts/git-contacts`
- v3 behavior: treats `-` as a filename argument meaning "read from stdin," enabling mixed file/stdin usage without requiring a terminal check.
- v3 implementation: removes implicit stdin detection (`-t STDIN` check) and replaces it with an explicit `$read_from_stdin` flag set when `-` is encountered in `@ARGV`.
- Junio’s alternative: eliminate the `$read_from_stdin` flag and instead treat `-` as a special filename in `scan_patch_file`, making the behavior more localized.
- Usage comment: reverted to pre-v2 text in v3, as the `-` idiom is self-documenting and does not require explicit stdin documentation.
- Argument-handling logic: refactored in v2 to use an early `$read_from_stdin` variable, retained in v3 with the new semantics.
- No new symbols, CLI options, or on-disk format changes.

Open questions and loose ends
The design question of how to best integrate the `-` idiom into the codebase is now open: whether to keep the v3 `$read_from_stdin` flag or adopt Junio’s alternative of treating `-` as a special filename in `scan_patch_file`. The author has not yet responded to Junio’s suggestion.

Participant positions
- Brigham Campbell (author): implemented the feature across v1, v2, and v3, provided cross-project context (link to `b4` patch thread), incorporated Junio’s stylistic feedback in v2, and adopted the `-` idiom in v3 as suggested. Stated the patch is useful independently of the `b4` integration. Has not yet responded to Junio’s structural suggestion.
- Junio C Hamano (maintainer): reviewed v1, requested minor stylistic changes (trailer placement, argument-handling refactor, usage comment update) incorporated in v2, endorsed the feature concept, suggested the `-` idiom as a refinement without requiring a v3, and now proposes an alternative design that eliminates the `$read_from_stdin` flag in favor of localized `-` handling in `scan_patch_file`. No functional objections; the review is a structural refinement.

Related work
- Corresponding `b4` patch thread (linked in commit message). The `git-contacts` patch is part of a cross-project effort but is actionable on its own.