Thread identity
Subject: [PATCH] git-contacts: accept patch input from stdin
Series version: standalone patch (v2)
Originating author: Brigham Campbell
Thread type: feature patch for a contrib script

Core problem or goal
Extend `git-contacts` (a contrib Perl script in `contrib/`) to read patch content from stdin when stdin is not a terminal and no arguments are supplied, improving interoperability with external tools such as `b4`.

Current status
Under review; v2 posted incorporating Junio C Hamano’s stylistic feedback (commit message trailer cleanup, argument-handling refactor, usage comment update). The feature concept remains endorsed; no functional objections have been raised.

Key technical details
- File touched: `contrib/contacts/git-contacts`
- New behavior: when invoked with no arguments and stdin is not a tty (`-t STDIN` false), the script calls the existing `scan_patches()` helper on the stdin filehandle.
- No new symbols, CLI options, or on-disk format changes.
- Usage comment at the top of the file updated to document stdin capability.
- Argument-handling logic refactored to use an early `$read_from_stdin` variable, aligning with the script’s existing style.

Open questions and loose ends
- None currently; all prior feedback has been addressed. Next step is maintainer pickup unless new concerns surface.

Participant positions
- Brigham Campbell (author): implemented the feature, provided cross-project context (link to `b4` patch thread), and incorporated Junio’s stylistic feedback in v2.
- Junio C Hamano (maintainer): reviewed v1, requested minor stylistic changes (trailer placement, argument-handling refactor, usage comment update), and endorsed the feature concept.

Related work
- Corresponding `b4` patch thread (linked in commit message).