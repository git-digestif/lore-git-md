This is the first patch in SZEDER Gábor’s v2 series introducing precompiled headers to speed up Git builds. It is a **build system cleanup** with no functional impact.

The patch removes an obsolete `XDIFF_OBJS` initialization from the `Makefile`. The variable was emptied in cf680cdb95 (2025) when the static `XDIFF_LIB` was removed, but the initialization itself was left behind. This is a straightforward, mechanical change that tidies up the build system as a prerequisite for the precompiled-header work in later patches. The diff is minimal—one line deleted—and the commit message clearly explains the historical context and rationale.

No controversy or open questions here; this is a routine cleanup. The patch is unlikely to draw review comments beyond a cursory acknowledgment. It sets the stage for the more substantive build-system adjustments that follow in the series.