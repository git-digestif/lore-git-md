This is a maintainer response in an ongoing bug report about the inconsistency between the `+` refspec modifier and the `--force` flag in `git fetch`.

Junio C Hamano disputes the original claim that the behavior is inconsistent, arguing that the `+` modifier and `--force` are indeed equivalent in their core purpose (allowing forced updates) and that pruning is a separate concept. He provides a detailed reproduction attempt on a Unix system that fails to demonstrate the reported behavior: when he appends `+refs/tags/*:refs/tags/*` to the fetch refspec, local tags are *not* pruned, contradicting André Kießling's report. Junio suggests the discrepancy might stem from an undisclosed configuration setting or a Windows-specific issue, though he expresses skepticism about the latter.

The email does not resolve the thread but shifts its focus: instead of questioning whether the behavior is correct, it now asks why the reporter's environment produces different results. Junio's reproduction steps are thorough and include full command output, making it easy for others to verify or challenge his findings. The key technical detail to watch is whether the pruning behavior is tied to the `+` modifier itself or to some other configuration (e.g., `fetch.pruneTags` or `fetch.prune`). No patch or documentation change is proposed yet; this is a diagnostic reply.