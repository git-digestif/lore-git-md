This thread is about a small but useful enhancement to `git-contacts`, a contrib script that identifies likely reviewers from patch metadata. Brigham Campbell proposed allowing the script to read patch content from stdin when no arguments are given, making it easier to integrate with tools like `b4`. The patch is narrowly scoped, touching only the argument-handling logic in `contrib/contacts/git-contacts`.

Junio C Hamano reviewed the patch and suggested two adjustments: moving the `Cc:` trailers in the commit message to follow Git conventions, and restructuring the stdin logic to use a dedicated variable (`$read_from_stdin`) for clarity and consistency with the script’s existing style. His feedback is constructive and doesn’t raise any functional concerns, so the next step is for Brigham to address these suggestions and resubmit. No other reviews have appeared yet, and the patch remains under review with no prior versions posted.