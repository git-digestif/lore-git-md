This is a bugfix patch targeting Git's protocol v2 `upload-pack` implementation, specifically addressing a regression in shallow fetch ordering when `uploadpack.allowRefInWanted=true` is enabled. The author, Royce Remer, clarifies the real-world impact: on his Gitea servers with this config enabled, clients attempting shallow clones (`--depth`) would fail with `fatal: expected 'packfile', received 'shallow-info'`. The patch itself is minimal—swapping two function calls in `upload-pack.c` to ensure "shallow-info" is sent before "wanted-refs"—but the stakes are high: without this fix, shallow fetches are effectively broken when the server advertises `allowRefInWanted`.

Royce’s response adds critical context that was missing from the original patch. He confirms the issue is not theoretical: it surfaces only when both conditions are met (server-side config enabled *and* client uses `--depth`), explaining why it might have gone unnoticed in the wild. His test cases in `t/t5703-upload-pack-ref-in-want.sh` now cover both single-commit shallow clones and two-commit incremental fetches, providing concrete evidence of the failure mode. The patch has been running in his dev environment for a day without issues, suggesting it’s stable for the scenarios tested.

The backward-compatibility question raised by Junio is addressed directly: Royce argues this is a *fix* for a feature that never worked, not a breaking change. Clients that previously failed on shallow clones would now succeed, while servers without `allowRefInWanted` enabled remain unaffected. This framing is important—it positions the patch as restoring expected behavior rather than introducing a new requirement.

Junio’s technical deep-dive in the parent email remains the most substantive part of the thread. His analysis of the client-side `process_section_header()` logic (with the `peek` bit set) explains why the original ordering could cause the client to skip or misinterpret sections, potentially leading to crashes or silent failures. Royce doesn’t directly answer Junio’s question about whether the client crashes or silently ignores unexpected sections, but his real-world testing suggests the former (the `fatal` error message). This is a gap the next patch revision should address explicitly in the commit message.

The patch looks well-motivated and narrowly scoped, but it’s not yet ready for merging. The commit message needs to incorporate Junio’s technical rationale—why the ordering matters, what failure mode it prevents, and how the client behaves when it encounters unexpected sections. With those additions, this should be a straightforward fix for a real (if niche) problem. For now, it’s under active review and worth watching for anyone using shallow fetches with `allowRefInWanted`.