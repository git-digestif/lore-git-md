This is a **documentation patch proposal** in the ongoing discussion about Git’s automatic `LESS=FRX` environment injection. Todd Zullinger suggests a small update to the `core.pager` documentation to explicitly mention how users can override the default `LESS` options when using pager wrappers like `delta`.

The patch adds a few sentences to `Documentation/config/core.adoc`, clarifying that setting `core.pager` to `LESS="" less` (or similar) can suppress Git’s automatic `FRX` flags. This is framed as a low-cost way to reduce user surprise without changing Git’s behavior, which **brian m. carlson has already firmly rejected altering**. The suggestion is modest—just a documentation tweak—but it directly addresses the thread’s core frustration: the unintuitive interaction between Git’s environment injection and third-party pagers.

The patch is well-motivated and uncontroversial. It doesn’t challenge the existing design but instead makes the workarounds (like `LESS="" delta`) more discoverable. The change is minimal, so it’s unlikely to face resistance, though the thread’s tone suggests the discussion may be winding down. If accepted, this would be a practical improvement for users who stumble into the same issue.