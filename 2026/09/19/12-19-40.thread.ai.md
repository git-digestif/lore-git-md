Thread identity
Subject: Git’s automatic LESS=FRX injection affects custom pagers (delta, etc.)
Series version: N/A (bug report / RFC discussion, now with a documentation patch proposal)
Originating author: Francisco Boni
Thread type: platform-compatibility / user-experience bug report

Core problem or goal
Git unconditionally injects `LESS=FRX` into the environment whenever `LESS` is unset, even when the user’s `core.pager` is a third-party wrapper (e.g., delta) that internally chains to less. This causes unintended side effects—broken alternate-screen handling, disrupted keybindings, and inconsistent behavior between unset `LESS` (Git injects FRX) and `LESS=""` (Git leaves the environment alone). The report argues that this behavior, while documented and historically justified for direct less usage, feels like an implementation leak when applied to tools Git does not directly control.

Current status
The thread is effectively **closed to code changes**. Francisco Boni has accepted **brian m. carlson**'s compatibility argument and withdrawn his proposal to reconsider the behavior. The discussion is now focused solely on **Todd Zullinger's proposed documentation patch** to clarify the `LESS=""` workaround in `Documentation/config/core.adoc`. The patch remains uncontroversial in principle but awaits review and potential expansion to other documentation venues (e.g., release notes, `git-config` man page).

Key technical details
- Configuration key: `core.pager`
- Environment variable: `LESS` (defaults to `FRX` when *unset*, not when empty)
- Affected tools: any pager wrapper that internally invokes `less` (e.g., delta, bat)
- Cross-platform edge cases: FreeBSD’s `more` (which is actually `less`), Debian’s `sensible-pager`, and arbitrary shell commands in `core.pager` that may dynamically resolve to `less`
- Workarounds: users can override the default by setting `GIT_PAGER` or `core.pager` to explicitly clear `LESS` (e.g., `LESS="" delta` or `LESS="RX" delta`) or use `env -i` to sanitize the environment. The proposed documentation patch highlights these workarounds.
- On-disk or CLI changes: none; purely environment-handling logic and documentation
- Test coverage: none yet; reproduction steps provided in the original report

Open questions and loose ends
- Will Todd Zullinger's documentation patch be accepted? It is narrowly scoped and uncontroversial but adds verbiage to an already lengthy document.
- Should the `LESS` behavior and workarounds be documented in additional venues (e.g., release notes, `git-config` man page)?
- Is the thread now fully resolved, or could a future configuration knob (e.g., `core.pagerLessDefaults`) still be explored despite **brian m. carlson**'s resistance?

Participant positions
- **Francisco Boni (author)**: initially argued the current behavior is unintuitive and creates sharp edges for users of pager wrappers. **Now concedes the compatibility argument** and accepts the `LESS=""` workaround, closing his proposal for a code change.
- **brian m. carlson**: **firmly rejects changing the behavior**, citing cross-platform consistency, historical design, and the impracticality of statically detecting `less` usage. Frames the issue as a known limitation rather than a bug and points to existing workarounds. This position **effectively closed the code-change discussion**.
- **Todd Zullinger**: proposes a **documentation patch** to clarify the `LESS=""` workaround, aiming to reduce user surprise without altering the underlying logic. Acknowledges the trade-off of adding verbiage but argues the patch is well-targeted.

Related work
- **Documentation patch**: Todd Zullinger’s proposed update to `Documentation/config/core.adoc` to document the `LESS=""` workaround.
- Potential follow-ups: additional documentation patches (e.g., release notes, `git-config` man page) to further emphasize the `LESS` behavior and workarounds. A new configuration knob remains unlikely given Carlson’s stance.