This is the first patch in a two-patch bugfix series for `git stash`. It’s a straightforward cleanup: the `#include "oid-array.h"` line in `builtin/stash.c` is unused (Clang flags it, and the author confirms no `oid_array*` symbols are referenced), so it’s removed. The change is trivial—one line deleted—and serves as a clean precursor to the functional fix in the next patch. No behavior change, no controversy, and no loose ends; it’s exactly the kind of low-risk tidy-up that reviewers appreciate as a warm-up.