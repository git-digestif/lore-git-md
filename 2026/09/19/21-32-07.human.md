This is the **formal patch submission** for the `stash.index=true` + `--autostash` bugfix. D. Ben Knoble has sent a patch series (cover letter ID `<cover.1789853192.git.ben.knoble@gmail.com>`) that replaces the subprocess-heavy index-merge logic with a direct call to `merge_incore_nonrecursive()`, resolving the redundant stash entry and uncleaned `MERGE_AUTOSTASH` ref left behind during fast-forward merges with `--autostash`.

The series targets `sequencer.c` and addresses the root cause: premature deletion of the `MERGE_AUTOSTASH` ref during `stash apply --index`. The fix is well-motivated, with clear technical improvements—eliminating three subprocess spawns, preserving index state, and silencing unnecessary merge verbosity. Phillip Wood’s substantive review feedback has been incorporated, including conflict-label simplification and the use of `oidcpy()` for robustness. The patch is now ready for review and likely to be uncontroversial, given the prior discussion and local validation by both Ben Knoble and Phillip Wood.

This is a **bugfix** with potential backport consideration for `maint`, as the issue affects Git 2.55.0 and current `master`. The series is self-contained and touches only the stash, merge, and ref-management subsystems. If the patch holds up under review, it should land smoothly.