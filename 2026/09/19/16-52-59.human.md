This is a **platform compatibility / user experience discussion** wrapping up the thread about Git’s automatic `LESS=FRX` injection. Francisco Boni acknowledges brian m. carlson’s technical rationale for the current behavior, particularly the cross-platform edge cases (FreeBSD’s `more` being `less`, Debian’s `sensible-pager`, and arbitrary shell commands in `core.pager`). He concedes that statically detecting whether a custom pager will ultimately invoke `less` is impractical, which strengthens the case for maintaining the status quo.

The thread now appears to be winding down without a code change. Boni’s reply signals acceptance of the existing workarounds (e.g., `LESS="" delta`) and the documented limitations. While no new proposals emerge here, the exchange reinforces the earlier documentation patch from Todd Zullinger as the most likely path forward—clarifying the `LESS=""` workaround in `Documentation/config/core.adoc` to reduce user surprise. The tone is collaborative and pragmatic, with no lingering disagreement.