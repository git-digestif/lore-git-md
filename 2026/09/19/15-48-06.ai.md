This is a **platform compatibility / user experience discussion** continuing the thread about Git's automatic `LESS=FRX` environment injection. Brian M. Carlson responds to Francisco Boni's proposal to reconsider the scope of this behavior, offering a detailed technical rationale for why the current approach is unlikely to change.

Carlson explains that Git's logic—setting `LESS=FRX` only when the variable is *unset* (not when it is empty)—is intentional and necessary for cross-platform consistency. He highlights edge cases like FreeBSD's `more` (which is actually `less`) and Debian's `sensible-pager`, where the pager command may resolve to `less` dynamically. Since `core.pager` can contain arbitrary shell commands, Git cannot statically determine whether the pager will ultimately invoke `less`, making a scoped default (e.g., only for Git's default pager) impractical. He also notes that environment-based configuration (like `LESS`) is now considered outdated design, though it remains widely used in Git for historical reasons.

The response firmly rejects the idea of changing the behavior, calling it a "notable and unwelcome" compatibility break. Carlson instead points to existing workarounds: users can override the default by setting `GIT_PAGER` or `core.pager` to explicitly clear `LESS` (e.g., `LESS="" delta`) or use `env -i` to sanitize the environment. While acknowledging the surprising interaction with pager wrappers like `delta`, he frames this as a trade-off for broader compatibility and predictability across platforms.

This reply **shifts the thread toward closure** by dismissing the core proposal and reframing the issue as a known limitation rather than a bug. The discussion may now pivot to documenting the workarounds more prominently or exploring alternative designs (e.g., a new config knob), but the tone suggests resistance to further changes. Key technical details for follow-ups include:
- The distinction between `unset LESS` and `LESS=""` in Git's logic.
- The role of `GIT_PAGER` and `core.pager` in overriding defaults.
- The cross-platform challenges of statically detecting `less` usage.
- The `env -i` workaround for environment sanitization.