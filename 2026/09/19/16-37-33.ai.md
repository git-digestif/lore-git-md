This is a **documentation patch proposal** in the ongoing discussion about Git’s automatic `LESS=FRX` environment injection. Todd Zullinger suggests a small update to the `core.pager` documentation to explicitly mention the `LESS=""` workaround for users of third-party pager wrappers like `delta`.

The patch adds a few sentences to `Documentation/config/core.adoc`, clarifying that users can override Git’s default `LESS` settings by embedding `LESS=""` or `LESS="RX"` directly in their `core.pager` configuration. This addresses the sharp edge Francisco Boni raised—where Git’s behavior disrupts tools that internally chain to `less`—without changing the underlying logic. The proposal is narrowly scoped: it doesn’t challenge **brian m. carlson**’s rejection of behavioral changes but instead aims to reduce user surprise by making the existing workaround more discoverable.

The change is uncontroversial in principle, though Zullinger acknowledges the trade-off of adding verbiage to an already lengthy document. The patch itself is minimal and well-targeted, focusing on the specific pain point (pager wrappers) while avoiding broader design debates. If accepted, it would help users diagnose and resolve the issue without requiring code changes or new configuration knobs. The tone of the thread suggests this is the most viable path forward, given Carlson’s firm stance against altering the current behavior. Key technical details for follow-up include the distinction between `LESS=""` (clears all options) and `LESS="RX"` (selectively overrides), and the cross-platform implications of environment-based configuration.