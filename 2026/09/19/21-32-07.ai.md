This is the **formal patch series submission** for the `stash.index=true` + `--autostash` bugfix. D. Ben Knoble announces that the polished series (cover letter + patches) is now on the list under message ID `<cover.1789853192.git.ben.knoble@gmail.com>`. The series replaces the earlier subprocess-heavy index-merge logic with a direct call to `merge_incore_nonrecursive()`, resolving the root cause (premature `MERGE_AUTOSTASH` ref deletion) while improving performance and code clarity. Phillip Wood’s substantive review feedback has been incorporated: merge verbosity is silenced, conflict-label setup is simplified, and `oidcpy()` is used for robustness.

The patch targets `sequencer.c` and affects the stash, merge, and ref-management subsystems. It eliminates three subprocess spawns (`git diff-tree`, `git apply --cached`, `git reset`) and preserves the `MERGE_AUTOSTASH` ref until autostash apply completes, preventing the redundant stash entry and "ref not found" error. The fix is confirmed to work locally by both Knoble and Phillip Wood, and the reproduction script provided by Eli Barzilay validates the resolution. No formal test has been added yet, but the series is ready for review and likely targets `master` (with potential backport to `maint`). The thread’s substantive collaboration between Knoble and Wood suggests this is a well-vetted, low-risk fix for a real-world annoyance.