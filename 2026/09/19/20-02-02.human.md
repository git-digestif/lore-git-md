This is a **bugfix** for the `stash.index=true` + `--autostash` interaction during fast-forward merges. The thread is in its final stages: D. Ben Knoble and Phillip Wood are polishing a patch that replaces the buggy subprocess-based index merge with a direct call to `merge_incore_nonrecursive()`.

The fix is confirmed to work and addresses the root cause—premature deletion of the `MERGE_AUTOSTASH` ref—while also improving performance by eliminating three subprocess spawns (`git diff-tree`, `git apply --cached`, `git reset`). Phillip Wood’s latest feedback suggests minor but meaningful refinements: silencing merge verbosity (since conflicts already trigger an abort), simplifying conflict-label setup (passing `""` instead of full labels), and using `oidcpy()` for safer OID assignment. Knoble agrees with these suggestions and plans to submit the patch formally after incorporating them.

The discussion remains narrowly focused on correctness and code quality, with no new edge cases or open questions introduced. Both participants are aligned on the solution, and the patch is now one cleanup pass away from submission. If you care about stash reliability or the merge machinery, this fix is worth tracking—it’s small, well-motivated, and likely to land soon.