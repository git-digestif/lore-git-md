Thread identity
Subject: regression in reference-transaction hook: all-zero OIDs on branch/tag deletion
Series version: v3 (three-patch bugfix series, final iteration planned)
Originating author: Maciej Ciemborowicz
Thread type: bug report and fix

Core problem or goal
The reference-transaction hook, relied on by external tools (Gerrit, GitLab, CI systems) to monitor ref changes, regressed in Git 2.31: when a branch or tag is deleted via high-level commands (`git branch -d`, `git tag -d`, `git remote prune`, `git fetch --prune`), the hook receives all-zero object IDs for *both* the old and new OID fields, losing the previous object ID that was correctly reported in Git 2.28–2.30. The regression was introduced by commit `8198907795`, which optimized bulk tag deletions by replacing `delete_ref()` with `delete_refs()` but inadvertently omitted the old OID for safety-checked deletions. Direct deletion via `git update-ref -d` remains unaffected, providing the old OID followed by all-zeros. The regression persists through Git 2.55 and affects both the files and reftable backends. The v3 series will restore the pre-regression behavior—passing the resolved old OID to the hook—while preserving the performance gains of the original optimization and addressing the backward-compatibility concern raised by Karthik Nayak. The series now adopts `REF_TRANSACTION_ALLOW_FAILURE` to allow partial failures (preserving unconditional deletion for `git fetch --prune` and `git remote prune`) while still passing old OIDs to the hook, resolving the core design conflict.

Current status
Patch v3 is planned and will incorporate all feedback from v2. The **core design conflict** has been resolved in favor of Karthik Nayak’s proposed middle ground: using `REF_TRANSACTION_ALLOW_FAILURE` to preserve unconditional deletion behavior for `git fetch --prune` and `git remote prune` while still passing old OIDs to the hook. The series remains a high-priority bugfix for a regression affecting external tools. Performance measurements confirm no regression: deleting 10,000 packed tags remains at ~0.85 seconds. The series includes comprehensive test coverage for both files and reftable backends, SHA-1/SHA-256, broken/symbolic refs, atomic fetch pruning, and concurrent updates, with regression tests added to `t/t1416-ref-transaction-hooks.sh`. A rebase onto `master` (post-commit `d38352cd43`) is required to resolve merge conflicts.

Key technical details
- **Subsystems touched**: refs, reference-transaction hook
- **Commands affected**: `git branch -d`, `git tag -d`, `git remote prune`, `git fetch --prune` (all fixed in v3)
- **Commands unaffected**: `git update-ref -d`
- **Backends affected**: files, reftable
- **Introducing commit**: `8198907795` (Git 2.31)
- **Functions involved**: `refs_delete_refs()`, `ref_transaction_delete()`, `REF_HAVE_OLD` flag, `get_stale_heads()`, `REF_TRANSACTION_ALLOW_FAILURE`
- **New symbols**: `refs_delete_refs()` now accepts an optional `old_oid` parameter (as an `oid_array`); new `stale_ref` struct in `builtin/fetch.c` and `builtin/remote.c` to carry ref name and old OID; `refs_delete_refs()` validates that the `oid_array` size matches the refname count and skips null OIDs (treating them as no verification)
- **Old behavior (Git 2.28–2.30)**: hook received the resolved old OID followed by all-zeros on deletion; deletions were conditional (compare-and-delete) to prevent overwriting concurrent updates
- **New behavior (Git 2.31–2.55)**: hook received all-zeros for both old and new OIDs on deletion; deletions were unconditional (no safety check)
- **Restored behavior (v3 fix)**: hook receives the resolved old OID followed by all-zeros on deletion; deletions are unconditional (no safety check) but use `REF_TRANSACTION_ALLOW_FAILURE` to allow partial failures, preserving backward compatibility for `git fetch --prune` and `git remote prune`
- **On-disk format**: unchanged
- **Test coverage**: reproducer provided; regression tests added to `t/t1416-ref-transaction-hooks.sh`; covers files/reftable backends, SHA-1/SHA-256, broken/symbolic refs, atomic fetch pruning, concurrent updates, and all affected commands (`git branch -d`, `git tag -d`, `git remote prune`, `git fetch --prune`, `git fetch --atomic --prune`); v3 will add tests for partial failures and UI consistency (suppressing "[pruned]" on failure)
- **Performance context**: optimization reduced bulk tag deletion time from 30 minutes to 5 seconds; v3 fix preserves this gain (~0.85 seconds for 10,000 packed tags)
- **UI change**: v2 fixed an inconsistency where `git remote prune` printed "[pruned]" even after a deletion failure; v3 will preserve this fix
- **Code cleanup (v3)**: refactoring to eliminate redundant loops (e.g., moving `refnames` construction into the non-atomic branch, reusing the existing loop in `builtin/remote.c` to populate `old_oids`), fixing formatting nits, removing deprecated `test_create_repo` in favor of `git init`, and removing unnecessary `pack-refs` from tests
- **Input validation**: `refs_delete_refs()` validates that the `oid_array` size matches the refname count and skips null OIDs (treating them as no verification); **resolved concern**: implicit conversion of all-zero OIDs to `NULL` will be either removed or explicitly documented in v3

Open questions and loose ends
- Should the documentation for the reference-transaction hook be updated to explicitly state that the old OID is provided for deletions, or is the restored behavior sufficiently self-documenting?
- Are there other high-level commands or workflows (e.g., `git push --delete`, `git worktree remove`) that might be affected by the same regression and should be audited? The series addresses all known affected commands, but a broader audit may be warranted.
- The v3 series will need to handle the edge case of partial failures in `git fetch --prune` and `git remote prune` gracefully, ensuring the UI remains consistent (e.g., suppressing "[pruned]" for failed deletions).

Participant positions
- **Maciej Ciemborowicz (author)**: reports the regression, identifies the root cause (`8198907795`), and posts a three-patch v1 series. The v2 series explicitly addresses Karthik Nayak’s race condition concern by clarifying the intentional trade-off: conditional deletion restores the pre-regression safety check (from Git 2.28–2.30) to prevent deleting refs that were never examined by the caller. After further discussion, Maciej accepts Karthik’s proposed middle ground—using `REF_TRANSACTION_ALLOW_FAILURE` to preserve unconditional deletion while still passing old OIDs to the hook—and plans to implement it in v3. The v3 series will also incorporate all of Karthik’s code cleanup suggestions (redundant loops, formatting nits, test hygiene) and resolve merge conflicts via rebase. The author’s position has evolved to prioritize backward compatibility while still fixing the regression.
- **D. Ben Knoble**: suggested using `git bisect` to identify the introducing commit, which was instrumental in uncovering the regression. The suggestion was substantive and actionable, focusing on the next diagnostic step.
- **Karthik Nayak**: raises four **substantive technical concerns** in the v2 review, all of which have been accepted by the author for v3:
  1. **Core design conflict**: The v2 series restored conditional deletion (safety check), but Karthik argued this breaks user expectations for `git fetch --prune` and `git remote prune`, which previously succeeded even when refs were concurrently updated. Karthik proposed using `REF_TRANSACTION_ALLOW_FAILURE` to preserve unconditional deletion while still passing old OIDs to the hook. This middle ground has been adopted for v3, resolving the primary blocker.
  2. **Implicit conversion of all-zero OIDs to `NULL`**: Karthik argued that `refs_delete_refs()` should not treat all-zero OIDs as equivalent to `NULL`, as this could mask bugs in callers. The v3 series will either remove this implicit conversion or explicitly document it.
  3. **Code cleanup**: Karthik identified redundant loops (e.g., `refnames` construction in `builtin/fetch.c`, `old_oids` population in `builtin/remote.c`), formatting nits, and test hygiene issues (deprecated `test_create_repo`, unnecessary `pack-refs`). All of these will be addressed in v3.
  4. **Merge conflicts**: v2 conflicts with `master` (commit `d38352cd43`); a rebase is planned for v3.
  Karthik’s feedback is **high-weight**, addressing correctness, maintainability, and user-facing behavior. The procedural note about LLM usage is low-weight and does not affect the technical discussion. Karthik’s proposed middle ground (`REF_TRANSACTION_ALLOW_FAILURE`) has been accepted as the path forward.