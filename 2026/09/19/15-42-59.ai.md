This is a **bugfix** targeting the refs subsystem, specifically the `reference-transaction` hook. The email identifies the exact commit (`8198907795`) that introduced the regression in Git 2.31, where an optimization to speed up bulk tag deletions inadvertently broke the hook's contract by omitting the old OID for safety-checked deletions (e.g., `git branch -d`, `git tag -d`). The change replaced `delete_ref()` with `delete_refs()`, which preserved only ref names and passed `NULL` for the old OID, causing the hook to receive all-zero OIDs for both old and new fields.

The author clarifies that the regression was an unintended side effect of a performance optimization (reducing deletion time for 24,000 tags from 30 minutes to 5 seconds). The fix should aim to restore the old behavior—passing the resolved old OID to the hook—while preserving the performance gains. The commit message for `8198907795` did not mention this behavioral change, reinforcing the view that it was an oversight. No alternative approaches or trade-offs are discussed yet, but the author implies the fix is feasible without sacrificing performance.

Key technical details include the affected functions (`delete_refs()`, `ref_transaction_delete()`), the missing `REF_HAVE_OLD` flag, and the distinction between high-level commands (affected) and `git update-ref -d` (unaffected). The regression spans both the files and reftable backends and persists through Git 2.55. The thread now has a clear path forward: a patch to modify `delete_refs()` to pass the old OID when available, ensuring the hook receives the correct data for safety-checked deletions.

The email advances the thread significantly by pinpointing the root cause and framing the fix as a targeted correction rather than a broader design debate. No other participants have weighed in on the proposed solution, but the bisect suggestion from D. Ben Knoble was instrumental in uncovering the regression's origin. The next step is likely a patch to restore the old OID while maintaining the performance improvement.