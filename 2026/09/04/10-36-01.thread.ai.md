Mode: ai

Thread identity
Subject: [PATCH v1 00/11] refs: unify terminology for "ref storage"
Series version: v3 (v2 and v1 previously posted)
Originating author: Patrick Steinhardt
Thread type: patch series (refactoring/cleanup with functional fixes)

Core problem or goal
Git currently uses inconsistent terminology for the "ref storage format" across CLI options (`--ref-format=`), repository extensions (`refStorage`), environment variables (`GIT_REFERENCE_BACKEND`), config keys (`init.defaultRefFormat`), build-info output, and documentation. This series standardizes on `--ref-storage-format=` (and related names) to avoid confusion, particularly in preparation for an analogous `--object-storage-format=` extension. The series also centralizes URI parsing for reference storage backends, enabling `--ref-storage-format=` to accept URIs like `files://path/to/repo` with payloads (e.g., alternative directories for the "files" backend), and fixes a bug where reinitializing a repository with a different ref storage format silently corrupts the repository.

Current status
Under review (v3 posted; v2 and v1 previously posted). All substantive feedback from v2 has been addressed:
- Backward-compatible option aliases (e.g., `--ref-format=`) now use `OPT_ALIAS()` with `PARSE_OPT_HIDDEN` for clarity and consistency.
- Error messages in `setup.c` now correctly report whether a value came from the new or old environment variable.
- The test helper variable `GIT_TEST_DEFAULT_REF_FORMAT` has been renamed to `GIT_TEST_DEFAULT_REF_STORAGE_FORMAT`.
- Documentation breadcrumbs mark old names as deprecated (e.g., "use `--ref-storage-format=` instead").
- The `ref-storage-format.adoc` update has been reverted from `git-refs.adoc` since the `git refs` command does not yet support URIs.
- The `PARSE_OPT_NONEG` flag inconsistency in `git refs migrate` remains unresolved but is not a blocker.

No merge status reported; the series remains in discussion pending final review. The immutability check for payloads (patch 9/13) still uses direct string comparison, which may reject semantically identical but syntactically different paths (e.g., `files:///path/to/store` vs. `files:///path/to/./store`), but this edge case remains unresolved as Patrick considers normalization logic backend-specific.

Key technical details
- CLI options:
  - `--ref-format=` → `--ref-storage-format=` (implemented; old name retained as a hidden backward-compatible alias via `OPT_ALIAS()` with `PARSE_OPT_HIDDEN`).
  - `--show-ref-format` → `--show-ref-storage-format` (implemented; old name retained as a hidden alias).
  - Files touched: `builtin/init-db.c`, `builtin/clone.c`, `builtin/refs.c`, `builtin/submodule--helper.c`, `builtin/rev-parse.c`, `git-submodule.sh`, `Documentation/git-*.adoc`, shell completions (`contrib/completion/git-prompt.sh`), and 53 test scripts (distribution unchanged from v2).
  - Patch 1/13 (new in v3): adds `OPT_ALIAS_F()` support to `parse-options.c` for hidden aliases, used throughout the series to implement backward-compatible option names.
  - Patch 1/13 (v3): updates `builtin/init-db.c` to use `OPT_ALIAS()` for `--ref-format=`, marking it as deprecated in the help text (e.g., "use `--ref-storage-format=` instead"). Kaartic Sivaraam’s feedback on deprecation breadcrumbs is now addressed.
  - Patch 2/13 (v3): updates `builtin/clone.c` to use `OPT_ALIAS()` for `--ref-format=`, with deprecation breadcrumbs in the help text.
  - Patch 3/13 (v3): updates `builtin/refs.c` to use `OPT_ALIAS()` for `--ref-format=` in `git refs migrate`, retaining the `PARSE_OPT_NONEG` flag (inconsistency with other commands unresolved).
- Environment variables:
  - `GIT_REFERENCE_BACKEND` → `GIT_REF_STORAGE_FORMAT` (implemented; old name retained as backward-compatible alias).
  - `GIT_DEFAULT_REF_FORMAT` → `GIT_DEFAULT_REF_STORAGE_FORMAT` (implemented; old name retained as backward-compatible alias).
  - `GIT_TEST_REFERENCE_BACKEND` → `GIT_TEST_REF_STORAGE_FORMAT` (implemented; old name retained as backward-compatible alias).
  - `GIT_TEST_DEFAULT_REF_FORMAT` → `GIT_TEST_DEFAULT_REF_STORAGE_FORMAT` (implemented in v3; previously overlooked in v2).
  - Files touched: `setup.c`, `environment.h`, `Documentation/git.txt`, and 7 test scripts.
  - Patch 9/13 (v3): updates error messages in `setup.c` to correctly report whether a value came from the new or old environment variable (e.g., "unknown ref storage format specified via GIT_REFERENCE_BACKEND: 'garbage'").
- Config keys:
  - `init.defaultRefFormat` → `init.defaultRefStorageFormat` (implemented; old name retained as backward-compatible alias; patch 10/13).
  - Files touched: `BreakingChanges.adoc`, `config/feature.adoc`, `config/init.adoc`, `setup.c`, `t/t0001-init.sh`.
- Build-info output: `default-ref-format` → `default-ref-storage-format` (implemented; patch 6/13, `help.c` and `t/t0001-init.sh`).
- New functions:
  - `parse_ref_storage_uri()` in `refs.c`/`refs.h` (patch 7/13, unchanged from v2).
  - `ref_storage_format_by_uri()` in `refs.c`/`refs.h` (patch 7/13, replaces `ref_storage_format_by_name()` in patch 11/13 to support URIs with payloads).
  - `validate_ref_storage_format()` in `setup.c` (patch 8/13, enforces immutability of existing repository format and reorders precedence).
- On-disk format: no changes, but patch 8/13 enforces immutability of the existing repository format once set (preventing silent corruption on reinitialization).
- URI syntax: `--ref-storage-format=` accepts URIs like `files://path/to/alt-refs` or `reftable://path/to/repo` (patch 11/13), with payloads stored in the repository config and used to locate the ref storage directory. The "files" and "reftable" backends support payloads; other backends (e.g., "packed") ignore them. URI parsing is strict: malformed URIs or unsupported schemes are rejected with clear error messages.
- Test coverage: updated across all affected areas (53 test scripts total; distribution unchanged from v2). Patch 9/13 (v3) updates test scripts to use the renamed `GIT_TEST_DEFAULT_REF_STORAGE_FORMAT` variable.
- BreakingChanges.adoc: updated to document intentional breaking changes (e.g., immutability of existing repository format).
- Symbols: `ref_format` → `ref_storage_format` (implemented; internal variables in `builtin/init-db.c`, `builtin/clone.c`, `builtin/refs.c`, `builtin/submodule--helper.c`, and `builtin/rev-parse.c`).
- Error messages: updated in `builtin/fetch.c`, `setup.c`, and test scripts to use `--ref-storage-format=` terminology and include full URIs in diagnostics. Patch 9/13 (v3) fixes error messages to report the correct environment variable source.
- Submodule-specific: updates to `git-submodule.sh` and `builtin/submodule--helper.c` propagate the new terminology and URI support to submodule operations (patch 4/13, unchanged from v2).
- Shell completions: updated in `contrib/completion/git-prompt.sh` to reflect the new option name (patch 5/13, unchanged from v2).
- Precedence rules: patch 8/13 reorders precedence so that `--ref-storage-format=` overrides environment variables; patch 9/13 ensures backward-compatible environment variable handling by checking both old and new names; patch 10/13 extends precedence rules to the config key; patch 11/13 extends precedence to URI payloads.
- Bug fix: patch 8/13 prevents silent corruption when reinitializing a repository with a different ref storage format by treating the existing format as immutable; patch 11/13 extends this immutability to URI payloads.
- Documentation: breadcrumbs added to mark old names as deprecated (e.g., "use `--ref-storage-format=` instead"). The `ref-storage-format.adoc` update is now limited to `git-init.adoc` and `git-clone.adoc` (patch 11/13, v3).

Open questions and loose ends
- URI handling edge cases:
  - Behavior for backends other than "files" and "reftable" when a payload is provided (currently ignored, but should this be documented or rejected?).
  - Handling of relative paths in payloads (e.g., `files://./alt-refs`; currently accepted but may cause confusion).
  - Security implications of allowing arbitrary paths in payloads (e.g., symlink attacks, path traversal).
  - **Edge case in patch 8/13**: the immutability check for payloads uses a direct string comparison, which can incorrectly reject semantically identical but syntactically different paths (e.g., `files:///path/to/store` vs. `files:///path/to/./store`). Kaartic Sivaraam suggested using `strbuf_realpath()` to normalize paths before comparison, but Patrick has rejected this, arguing that payloads are not guaranteed to be filesystem paths and that normalization logic is backend-specific. The immutability check remains as written.
- Backward compatibility concerns:
  - Lifecycle of the old option names: whether they should eventually emit deprecation warnings or be retained indefinitely as hidden aliases (policy decision, not a technical blocker).
  - Consistency of the `PARSE_OPT_NONEG` flag for `--ref-storage-format=` across commands: present in `git refs migrate` but absent in `git init` and `git clone`. Kaartic Sivaraam suggested dropping the flag for consistency, but Patrick has declined to address this, leaving the inconsistency unresolved.
  - Impact of immutability enforcement on existing workflows (e.g., scripts or tools that reinitialize repositories with different formats or payloads).
- Documentation gaps:
  - URI syntax is documented for "files" and "reftable" backends, but other backends (e.g., "packed") may need similar documentation if they support payloads in the future.

Participant positions
- Patrick Steinhardt (author):
  - Proposes the cleanup as necessary for consistency ahead of the object storage extension. All feedback from v2 has been addressed in v3:
    - Backward-compatible option aliases now use `OPT_ALIAS()` with `PARSE_OPT_HIDDEN`.
    - Error messages in `setup.c` now correctly report the source environment variable.
    - The test helper variable `GIT_TEST_DEFAULT_REF_FORMAT` has been renamed to `GIT_TEST_DEFAULT_REF_STORAGE_FORMAT`.
    - Documentation breadcrumbs mark old names as deprecated.
    - The `ref-storage-format.adoc` update has been reverted from `git-refs.adoc`.
  - **Rejected** Kaartic Sivaraam’s suggestion to use `strbuf_realpath()` for normalizing payload paths in the immutability check, arguing that payloads are not guaranteed to be filesystem paths and that normalization logic is backend-specific.
  - **Declined** to address the `PARSE_OPT_NONEG` flag inconsistency, leaving it unresolved.
  - Prefers a quiet deprecation for old option names (retained as hidden aliases without warnings) but has added deprecation breadcrumbs in documentation.
- Karthik Nayak (reviewer):
  - **Substantive review** of v1: called the series "relatively quite an easy read" despite its length and said "Looks good otherwise," signaling the refactoring is clean and the motivation is clear.
  - **Surface-level review** of v1 patches: identified inconsistencies in help text placeholders and the missing `GIT_DEFAULT_REF_STORAGE_ENVIRONMENT` macro (both addressed in v2/v3).
- Junio C Hamano (maintainer):
  - **Design-level concern (resolved in v2)**: Initially questioned the practical value of renaming "ref-format" to "ref-storage," arguing that both terms are ambiguous. The concern was resolved by adopting `--ref-storage-format=` (and `--object-storage-format=`), which explicitly distinguishes storage format from object hash.
- Kaartic Sivaraam (reviewer):
  - **Surface-level review** of v2: raised procedural questions about the backward-compatibility strategy for old option names, the `PARSE_OPT_NONEG` flag inconsistency, error message clarity, and documentation consistency. All feedback has been addressed in v3 except:
    - The `PARSE_OPT_NONEG` flag inconsistency (declined by Patrick).
    - The immutability check edge case (rejected by Patrick).
  - **Substantive review** of patch 8/13 (v2): identified the edge case where the immutability check for payloads uses direct string comparison, which can incorrectly reject semantically identical paths. Suggested using `strbuf_realpath()` for normalization, but Patrick rejected this.
  - **Surface-level review** of v3: feedback on deprecation breadcrumbs and error message accuracy has been addressed.

Related work
- Upcoming object storage extension (mentioned as motivation; will use `--object-storage-format=`).
- Reftable backend effort (Patrick is primary contributor).
- ODB abstraction (Patrick is primary contributor).