Mode: ai

Thread identity
Subject: [PATCH v1 00/11] refs: unify terminology for "ref storage"
Series version: v3 (v2 and v1 previously posted)
Originating author: Patrick Steinhardt
Thread type: patch series (refactoring/cleanup with functional fixes)

Core problem or goal
Git currently uses inconsistent terminology for the "ref storage format" across CLI options (`--ref-format=`), repository extensions (`refStorage`), environment variables (`GIT_REFERENCE_BACKEND`), config keys (`init.defaultRefFormat`), build-info output, and documentation. This series standardizes on `--ref-storage-format=` (and related names) to avoid confusion, particularly in preparation for an analogous `--object-storage-format=` extension. The series also centralizes URI parsing for reference storage backends, enabling `--ref-storage-format=` to accept URIs like `files://path/to/repo` with payloads (e.g., alternative directories for the "files" backend), and fixes a bug where reinitializing a repository with a different ref storage format silently corrupts the repository.

Current status
Under review (v3 posted; v2 and v1 previously posted). All substantive feedback from v2 has been addressed, and the series is nearly ready for merging. Junio C Hamano’s latest reply confirms that the `OPT_ALIAS_F()` implementation with `PARSE_OPT_HIDDEN` is the correct deprecation mechanism, resolving the design question raised by Karthik Nayak and Kaartic Sivaraam. The only remaining open question is whether to leave a `#leftoverbit` to track the future cleanup of `OPT_ALIAS()`’s inability to support `OPT_HIDDEN`, but this is not a blocker. No merge status reported; the series remains in discussion pending final maintainer approval.

Key technical details
- CLI options:
  - `--ref-format=` → `--ref-storage-format=` (implemented; old name retained as a backward-compatible alias via `OPT_ALIAS_F()` with `PARSE_OPT_HIDDEN`). Junio C Hamano’s reply confirms this is the correct deprecation strategy, aligning with Git’s lifecycle for deprecated options (public → hidden → removed/errors).
  - `--show-ref-format` → `--show-ref-storage-format` (implemented in patch 6/13; old name retained as a hidden alias).
  - Files touched: `builtin/init-db.c`, `builtin/clone.c`, `builtin/refs.c`, `builtin/submodule--helper.c`, `git-submodule.sh`, `builtin/rev-parse.c`, `help.c`, `Documentation/git-*.adoc`, shell completions (`contrib/completion/git-prompt.sh`), and 54 test scripts.
  - Patch 1/13 (new in v3): adds `OPT_ALIAS_F()` support to `parse-options.c` for hidden aliases, used throughout the series to implement backward-compatible option names. Junio’s reply confirms this is the intended mechanism for deprecation, despite Git’s historical tolerance of public aliases for deprecated options (e.g., `--recursive` in `git clone`).
  - Patch 2/13 (v3): updates `builtin/init-db.c` to use `OPT_ALIAS_F()` for `--ref-format=`, marking it as deprecated in the help text.
  - Patch 3/13 (v3): updates `builtin/clone.c` to use `OPT_ALIAS_F()` for `--ref-format=`, with deprecation breadcrumbs in the help text.
  - Patch 4/13 (v3): updates `builtin/refs.c` to use `OPT_ALIAS_F()` for `--ref-format=` in `git refs migrate`, retaining the `PARSE_OPT_NONEG` flag (inconsistency with other commands unresolved).
  - Patch 5/13 (v3): updates `git-submodule.sh` and `builtin/submodule--helper.c` to use `OPT_ALIAS_F()` for `--ref-format=`, marking it as deprecated in the help text and documentation.
  - Patch 6/13 (v3): updates `builtin/rev-parse.c` to rename `--show-ref-format` to `--show-ref-storage-format`, retaining the old name as a hidden alias.
  - Patch 7/13 (v3): updates the build-info output in `help.c` to rename `default-ref-format` to `default-ref-storage-format`.
  - Patch 13/13 (v3): updates `builtin/init-db.c` and `builtin/clone.c` to accept URIs in `--ref-storage-format=`, passing the full URI string to `init_db()`. The `setup.c` layer uses `ref_storage_format_by_uri()` to extract the format and payload.
- Environment variables:
  - `GIT_REFERENCE_BACKEND` → `GIT_REF_STORAGE_FORMAT` (implemented; old name retained as backward-compatible alias).
  - `GIT_DEFAULT_REF_FORMAT` → `GIT_DEFAULT_REF_STORAGE_FORMAT` (implemented; old name retained as backward-compatible alias).
  - `GIT_TEST_DEFAULT_REF_FORMAT` → `GIT_TEST_DEFAULT_REF_STORAGE_FORMAT` (implemented in v3; patch 11/13 updates all test scripts and CI to use the new name; no backward-compatible alias retained for test-only variable).
  - Files touched: `setup.c`, `environment.h`, `Documentation/git.txt`, `Documentation/config/init.adoc`, and 13 test scripts.
  - Patch 9/13 (v3): updates error messages in `setup.c` to correctly report the source of the value (e.g., "unknown ref storage format specified via GIT_REFERENCE_BACKEND: 'garbage'"). Introduces a two-phase approach to resolve the desired format and validate it against the existing repository format, enforcing immutability for both the format and URI payload.
  - Patch 10/13 (v3): renames `GIT_REFERENCE_BACKEND` and `GIT_DEFAULT_REF_FORMAT` to `GIT_REF_STORAGE_FORMAT` and `GIT_DEFAULT_REF_STORAGE_FORMAT`, respectively, while retaining the old names as backward-compatible aliases. Updates precedence logic in `setup.c` to check new variables first, then fall back to old ones.
- Config keys:
  - `init.defaultRefFormat` → `init.defaultRefStorageFormat` (implemented; old name retained as backward-compatible alias; patch 12/13, v3).
  - Files touched: `BreakingChanges.adoc`, `config/feature.adoc`, `config/init.adoc`, `setup.c`, `t/t0001-init.sh`.
- Build-info output: `default-ref-format` → `default-ref-storage-format` (implemented; patch 7/13, `help.c` and `t/t0001-init.sh`).
- New functions:
  - `parse_ref_storage_uri()` in `refs.c`/`refs.h` (patch 8/13, replaces `ref_storage_format_by_name()` to support URIs with payloads).
  - `ref_storage_format_by_uri()` in `refs.c`/`refs.h` (patch 8/13, consolidates URI-parsing logic from `setup.c`).
  - `validate_ref_storage_format()` in `setup.c` (patch 9/13, enforces immutability of existing repository format and reorders precedence).
- On-disk format: no changes, but patch 9/13 enforces immutability of the existing repository format once set (preventing silent corruption on reinitialization). The immutability check now also covers URI payloads, using direct string comparison (edge case: may reject semantically identical but syntactically different paths).
- URI syntax: `--ref-storage-format=` accepts URIs like `files://path/to/alt-refs` or `reftable://path/to/repo` (patch 8/13 and patch 13/13), with payloads stored in the repository config and used to locate the ref storage directory. The "files" and "reftable" backends support payloads; other backends (e.g., "packed") ignore them. URI parsing is strict: malformed URIs or unsupported schemes are rejected with clear error messages. Patch 13/13 extends URI support to `git init` and `git clone`, enabling payloads to be specified at repository creation time.
- Test coverage: updated across all affected areas (54 test scripts total). Patch 6/13 (v3) updates 8 test scripts to use `--show-ref-storage-format` in place of `--show-ref-format`. Patch 9/13 (v3) updates test scripts to use the renamed `GIT_TEST_DEFAULT_REF_STORAGE_FORMAT` variable and adds a test case for the `GIT_REFERENCE_BACKEND` reinitialization edge case. Patch 11/13 (v3) updates 10 test scripts, `t/README`, `t/test-lib-functions.sh`, `t/test-lib.sh`, and `ci/run-build-and-tests.sh` to use the new `GIT_TEST_DEFAULT_REF_STORAGE_FORMAT` variable. Patch 12/13 (v3) updates `t/t0001-init.sh` to use `init.defaultRefStorageFormat` in tests. Patch 13/13 (v3) extends `t/t0001-init.sh` to verify URI handling and immutability of payloads, including error messages for reinitialization attempts.
- BreakingChanges.adoc: updated to document intentional breaking changes (e.g., immutability of existing repository format).
- Symbols: `ref_format` → `ref_storage_format` (implemented; internal variables in `builtin/init-db.c`, `builtin/clone.c`, `builtin/refs.c`, `builtin/submodule--helper.c`, and `builtin/rev-parse.c`).
- Error messages: updated in `builtin/fetch.c`, `setup.c`, and test scripts to use `--ref-storage-format=` terminology and include full URIs in diagnostics. Patch 9/13 (v3) fixes error messages to report the correct environment variable source and adds clarity to immutability violations.
- Submodule-specific: updates to `git-submodule.sh` and `builtin/submodule--helper.c` propagate the new terminology and URI support to submodule operations (patch 5/13, v3).
- Shell completions: updated in `contrib/completion/git-prompt.sh` to reflect the new option name (patch 5/13; patch 6/13 updates the same file for `--show-ref-storage-format`).
- Precedence rules: patch 8/13 reorders precedence so that `--ref-storage-format=` overrides environment variables; patch 9/13 ensures backward-compatible environment variable handling by checking both old and new names and makes the precedence order explicit (command-line option > environment variable > existing repository format > config key > compiled-in default).
- Bug fix: patch 9/13 prevents silent corruption when reinitializing a repository with a different ref storage format or URI payload by treating the existing format and payload as immutable. Patch 13/13 (v3) extends this fix to cover URI payloads in `git init` and `git clone`.
- Documentation: breadcrumbs added to mark old names as deprecated (e.g., "use `--ref-storage-format=` instead"). The `ref-storage-format.adoc` update is now limited to `git-init.adoc`, `git-clone.adoc`, and `git-submodule.adoc`. Patch 13/13 (v3) updates `Documentation/git-init.adoc` and `Documentation/git-clone.adoc` to describe the URI syntax and note that the "files" and "reftable" backends accept payloads.

Open questions and loose ends
- URI handling edge cases:
  - Behavior for backends other than "files" and "reftable" when a payload is provided (currently ignored, but should this be documented or rejected?).
  - Handling of relative paths in payloads (e.g., `files://./alt-refs`; currently accepted but may cause confusion).
  - Security implications of allowing arbitrary paths in payloads (e.g., symlink attacks, path traversal).
  - Edge case in patch 9/13: the immutability check for payloads uses direct string comparison, which can incorrectly reject semantically identical but syntactically different paths (e.g., `files:///path/to/store` vs. `files:///path/to/./store`). Kaartic Sivaraam suggested using `strbuf_realpath()` to normalize paths before comparison, but Patrick rejected this, arguing that payloads are not guaranteed to be filesystem paths and that normalization logic is backend-specific. The immutability check remains as written.
- Backward compatibility concerns:
  - Lifecycle of the old option/variable names: whether they should eventually emit deprecation warnings or be retained indefinitely as hidden aliases (policy decision, not a technical blocker). Junio’s reply confirms that the current approach (hidden aliases without warnings) aligns with Git’s deprecation lifecycle (public → hidden → removed/errors).
  - Consistency of the `PARSE_OPT_NONEG` flag for `--ref-storage-format=` across commands: present in `git refs migrate` but absent in `git init`, `git clone`, and `git submodule`. Kaartic Sivaraam suggested dropping the flag for consistency, but Patrick has declined to address this, leaving the inconsistency unresolved.
  - Impact of immutability enforcement on existing workflows (e.g., scripts or tools that reinitialize repositories with different formats or payloads).
- Documentation gaps:
  - URI syntax is documented for "files" and "reftable" backends, but other backends (e.g., "packed") may need similar documentation if they support payloads in the future.
- Test coverage:
  - **Test coverage gap**: Kaartic Sivaraam’s feedback highlights the absence of explicit tests for the deprecated backward-compatible aliases (`--ref-format=`, `GIT_REFERENCE_BACKEND`, etc.), despite their implementation via `OPT_ALIAS_F()` and environment-variable fallbacks. While the aliases are assumed to work by construction, Sivaraam argues that dedicated tests are needed to ensure the backward-compatibility guarantees are met in practice and to protect against regressions. Patrick has not yet responded to this concern.
  - Future cleanup: Junio suggested leaving a `#leftoverbit` to track the inability to apply `OPT_HIDDEN` to `OPT_ALIAS()` as a future infrastructure improvement, but this is not a blocker for the series.

Participant positions
- Patrick Steinhardt (author):
  - Proposes the cleanup as necessary for consistency ahead of the object storage extension. All feedback from v2 has been addressed in v3, including the `OPT_ALIAS_F()` implementation with `PARSE_OPT_HIDDEN` for backward-compatible option aliases. Junio’s reply confirms this is the correct deprecation mechanism.
  - **Rejected** Kaartic Sivaraam’s suggestion to use `strbuf_realpath()` for normalizing payload paths in the immutability check, arguing that payloads are not guaranteed to be filesystem paths and that normalization logic is backend-specific.
  - **Declined** to address the `PARSE_OPT_NONEG` flag inconsistency, leaving it unresolved.
  - Prefers a quiet deprecation for old option/variable names (retained as hidden aliases without warnings), aligning with Junio’s confirmation of Git’s deprecation lifecycle.
  - **Has not yet responded** to Kaartic Sivaraam’s feedback regarding the test coverage gap for deprecated backward-compatible aliases.
- Karthik Nayak (reviewer):
  - **Substantive review** of v1: called the series "relatively quite an easy read" despite its length and said "Looks good otherwise," signaling the refactoring is clean and the motivation is clear.
  - **Surface-level review** of v1 patches: identified inconsistencies in help text placeholders and the missing `GIT_DEFAULT_REF_STORAGE_ENVIRONMENT` macro (both addressed in v2/v3).
  - **Surface-level review** of v3 (patch 1/13): questioned the necessity of `PARSE_OPT_HIDDEN` on `OPT_ALIAS_F()` aliases, demonstrating that the alias is effectively hidden by the source option’s flags even without it. Explicitly stated he is "okay to keep it as is," indicating this is a nit rather than a blocker. **Latest acknowledgment signals the series is ready for maintainer attention.**
- Junio C Hamano (maintainer):
  - **Design-level concern (resolved in v2)**: Initially questioned the practical value of renaming "ref-format" to "ref-storage," arguing that both terms are ambiguous. The concern was resolved by adopting `--ref-storage-format=` (and `--object-storage-format=`), which explicitly distinguishes storage format from object hash.
  - **Latest reply (v3)**: confirms that `OPT_ALIAS_F()` with `PARSE_OPT_HIDDEN` is the correct deprecation mechanism for backward-compatible option aliases, resolving the design question raised by Karthik Nayak and Kaartic Sivaraam. Suggests leaving a `#leftoverbit` to track the future cleanup of `OPT_ALIAS()`’s inability to support `OPT_HIDDEN`, but this is not a blocker. **No substantive objections or open technical issues remain; the series is nearly ready for merging.**
- Kaartic Sivaraam (reviewer):
  - **Surface-level review** of v2: raised procedural questions about the backward-compatibility strategy for old option names, the `PARSE_OPT_NONEG` flag inconsistency, error message clarity, and documentation consistency. All feedback has been addressed in v3 except:
    - The `PARSE_OPT_NONEG` flag inconsistency (declined by Patrick).
    - The immutability check edge case (rejected by Patrick).
  - **Substantive review** of patch 8/13 (v2): identified the edge case where the immutability check for payloads uses direct string comparison, which can incorrectly reject semantically identical paths. Suggested using `strbuf_realpath()` for normalization, but Patrick rejected this.
  - **Latest feedback (v3)**: raises two concerns:
      1. **Design-level question**: argued that Git has tolerated public aliases for deprecated options (e.g., `--recursive` in `git clone`) for years, suggesting the added complexity of `OPT_ALIAS_F()` with `PARSE_OPT_HIDDEN` may not be justified. Junio’s reply confirms that the current approach (hidden aliases) is correct for Git’s deprecation lifecycle, though the historical tolerance of public aliases is acknowledged.
      2. **Test coverage gap**: highlights the absence of explicit tests for the deprecated backward-compatible aliases (`--ref-format=`, `GIT_REFERENCE_BACKEND`, etc.). Patrick has not yet responded to this concern.

Related work
- Upcoming object storage extension (mentioned as motivation; will use `--object-storage-format=`).
- Reftable backend effort (Patrick is primary contributor).
- ODB abstraction (Patrick is primary contributor).