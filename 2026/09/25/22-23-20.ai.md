This is a **bugfix** for the regression in `git shortlog` (and other revision commands) where unknown options are incorrectly reported as `(null)` instead of the actual option name. Junio C Hamano replies to Jeff King’s two-patch series with a brief but clear **tested review**, confirming that the fixes restore the correct behavior.

Junio’s reply is concise and factual: he states that both patches "make sense" and provides a simple reproduction test showing the regression in Git v2.52.0 (where `--no-such-option` is reported as `(null)`) and the correct behavior in v2.51.0 and with the applied patches (where the option name is shown). The test is minimal but effective, demonstrating the fix’s correctness without delving into implementation details.

The review is **substantive and tested**: Junio explicitly verifies the patches by building and running the code, and his reproduction steps are clear enough for others to replicate. His endorsement carries significant weight, as it signals that the series is ready for integration. No objections or requests for changes are raised, and the patches appear to be on track for merging into `next` (and eventually `master`) in the near term.

The thread remains focused on the regression and its fix, with no new technical questions or edge cases introduced. The series is small, well-motivated, and now has maintainer approval, making it a strong candidate for inclusion in the next release.