This is a **bugfix** for the regression in `git shortlog` (and other revision commands) where unknown options are reported as `(null)` instead of their actual name. Jeff King (Peff) follows up his earlier reply with a two-patch series that addresses both the immediate issue and a related, older bug in the revision option parsing logic.

The first patch is a minimal fix for the `(null)` regression, ensuring error messages correctly display the unknown option name. The second patch is more ambitious: it restores a pre-existing assumption in the codebase about how `argv` entries are managed during option parsing, while also fixing a latent bug that predates the regression. Peff acknowledges the second patch is complex but provides a detailed commit message to justify the approach.

The series touches only `revision.c` and adds a test case to `t/t4201-shortlog.sh`, confirming the fix is well-scoped and testable. The regression itself is user-visible but trivial to reproduce (`git shortlog --not-an-option`), and the patches appear to be low-risk mechanical changes. No objections or alternative proposals have emerged yet, and the thread remains in early review. If the patches hold up, this should resolve the issue cleanly.