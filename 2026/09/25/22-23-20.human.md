This is a **bugfix** for the regression in `git shortlog` (and other revision commands) where unknown options were incorrectly reported as `(null)` instead of the actual option name. Junio C Hamano has reviewed Jeff King’s (Peff) two-patch series and confirmed the fix works as intended.

The series addresses the immediate regression—introduced by commit cd439487 in September 2025—and also fixes a latent bug where known-but-invalid options (e.g., `--max-count=foo`) were misreported as unknown. Junio’s test confirms the fix: after applying the patches, `git shortlog --no-such-option` now correctly displays `error: unknown option '--no-such-option'`, matching the pre-regression behavior.

The patches are small, focused, and well-explained, with Peff’s detailed commit messages justifying the memory management changes in `revision.c`. Junio’s review is brief but decisive, and his local testing shows the fix restores the expected behavior. Given the regression’s low impact but clear user-facing nature, and the fact that both patches are from a trusted contributor with a strong track record, this series looks like a straightforward candidate for merging. No objections or alternative proposals have surfaced, and the fix is already confirmed to work across the affected integration branches (`master`, `next`, `seen`).