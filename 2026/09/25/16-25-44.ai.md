This is a **substantive review** of the `gitmergeconflicts(7)` documentation series, focusing on content and design choices rather than mechanical integration. D. Ben Knoble’s feedback engages deeply with the series’ goals, structure, and open questions, offering both validation and constructive input.

Knoble begins by praising the effort, framing it as a much-needed improvement to Git’s scattered conflict-resolution guidance. His review then systematically addresses Julia Evans’ explicit open questions, providing context and suggestions for each. On the equivalence of `git commit` and `git merge --continue`, he links to an earlier thread (likely discussing edge cases or corner behaviors), signaling that this may require further technical clarification. For `git apply`/`git am` conflicts, he endorses Evans’ decision to defer coverage, calling it a "good approach" given the complexity of patch application versus 3-way merges.

The review also clarifies a potential confusion about `git history --squash`: Knoble notes that the `git history` command (part of the experimental `git replay` series) currently rejects operations that create conflicts, so it may not need immediate inclusion in the conflict-resolution guide. This is a useful correction, as it avoids prematurely documenting behavior that might change.

Knoble’s most substantive contributions center on the design of the new guide. He strongly supports Evans’ choice to create a standalone page rather than using includes, agreeing that the ~300-line length would be unwieldy for embedding. His comment—"I have often wished some of our includes were actually links to separate documents"—suggests this approach aligns with broader documentation best practices. On the thorny issue of "ours" vs. "theirs" terminology, he validates Evans’ decision to de-emphasize the distinction, noting that most users rely on tools (`diff3`, mergetools) rather than memorizing the terms. However, he adds a practical caveat: the terms remain relevant for advanced use cases like `--ours`/`--theirs` strategies or restarting merges, implying the current placement (lower in the page) strikes a reasonable balance.

The review closes with a minor but concrete observation about `REBASE_HEAD`: Knoble shares his personal workflow (`git show REBASE_HEAD` as an alternative to `git rebase --show-current-patch`), subtly highlighting that these low-level references *are* used in practice, even if Evans’ guide omits them. This could inform future iterations of the documentation.

Overall, the feedback is **supportive and technically grounded**, with no major objections. It advances the discussion by:
1. Validating key design choices (standalone page, deferring `git apply` coverage).
2. Clarifying technical ambiguities (`git commit` vs. `git merge --continue`, `git history`’s conflict handling).
3. Providing real-world context for terminology and edge cases.
The review does not identify any blockers but surfaces areas where reviewer consensus (or further input) may be needed, particularly around the completeness of command-specific guidance. The tone is collaborative, treating the series as a work-in-progress rather than a finished product.