This is a brief maintainer note in the `git stash` autostash bugfix thread. Junio C Hamano explicitly endorses D. Ben Knoble’s decision to keep the broader argument-order standardization between `merge_ort_nonrecursive()` and `merge_incore_nonrecursive()` out of the current series, treating it as a separate future cleanup. The thread remains technically complete and ready for integration as v3.