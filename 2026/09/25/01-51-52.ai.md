This is a reply in the ongoing `git-contacts` stdin feature thread. Brigham Campbell responds to Junio C Hamano’s question about argument processing order by demonstrating that `git-contacts` already produces non-deterministic output order for the same input files and explicitly disregards argument order when mixing file paths and revision ranges. He argues that the commit message should not imply sequential processing of arguments (files, stdin, or revision ranges) since the script’s current behavior does not guarantee it. The reply is substantive: it clarifies the existing behavior and pushes back on the need for predictable argument processing order, suggesting instead that the commit message wording should be adjusted to avoid implying a guarantee that does not exist. The ball is now back in Junio’s court to either accept this clarification or press for a behavioral change.