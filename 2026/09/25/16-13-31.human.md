This is a brief follow-up in the `http.sslVerifyStatus` thread. Junio acknowledges Gábor’s confirmation that the test failure was caused by using the GnuTLS backend for libcurl, which lacks full OCSP stapling support. Switching to the OpenSSL backend resolves the issue, and the test suite now passes. Junio notes that this diagnosis both fixes Gábor’s local setup and highlights the need to improve the `SSL_VERIFYSTATUS` prerequisite. The core feature remains unchanged and fully functional; this is purely a test-infrastructure observation. The thread is now focused on tightening the prerequisite’s error-message check and renaming it to better reflect its scope.