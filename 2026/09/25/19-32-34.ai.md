**Summarizer brief mode**

This is a documentation review reply in the `gitmergeconflicts(7)` series, specifically addressing the technical accuracy of `git log --merge -p <path>` usage in the new guide. The email is a substantive follow-up from D. Ben Knoble to Junio C Hamano’s earlier clarification about the command’s behavior.

Knoble points out that Junio’s explanation, while correct, omits a key detail from the manual: the `--merge` option implicitly constructs a `HEAD...<other>` revision range, which is critical for understanding how the command works and why it is useful in merge conflict scenarios. This omission affects the proposed wording in the new `gitmergeconflicts(7)` guide, where the command’s behavior and rationale are currently underspecified. The missing context explains why the command shows commits from both sides of the merge (ours and theirs) and why it is more informative than a simple `git log -p <path>` for conflict resolution.

The email does not introduce new blockers but refines the technical foundation for the guide’s content. It surfaces a gap in the current discussion: while Junio’s reply corrected the misconception that `--merge` filters output to a single file, Knoble’s observation highlights that the *mechanism* of the command (the `HEAD...<other>` range) is still not fully explained in the thread. This detail is likely to be referenced in future discussion about the guide’s accuracy and completeness, particularly in sections covering tools for inspecting conflicts. The thread’s focus on `git log --merge` behavior is now split between two technical aspects: (1) what paths the command operates on (Junio’s clarification) and (2) how it selects commits to display (Knoble’s point).

Key technical details for follow-up:
- The `--merge` option in `git log` implies a `HEAD...<other>` revision range, where `<other>` is derived from the merge base or the conflicting side’s commit (e.g., `MERGE_HEAD`, `REBASE_HEAD`, or `CHERRY_PICK_HEAD` depending on the operation).
- This range is why the command shows commits from both sides of the merge, making it useful for conflict resolution.
- The omission affects the guide’s "Tools to get more context" section, where the command’s behavior and purpose are currently described without this foundational explanation.
- Knoble’s reference to "my alias" suggests this is a practical workflow tip that could inform the guide’s examples or recommendations.

The email advances the thread by identifying a missing piece of technical context that will need to be incorporated into the guide’s revised wording. It does not challenge Junio’s earlier clarification but supplements it with additional precision. The discussion remains focused on the new guide’s content rather than mechanical integration issues (e.g., `command-list.txt` registration or build system updates).