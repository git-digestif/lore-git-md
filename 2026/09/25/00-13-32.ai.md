This is a CI/build system patch discussion, specifically a review reply in the ongoing thread about reducing resource exhaustion in GitHub Actions CI jobs. Jeff King (Peff) chimes in on the first patch of Tamir Duberstein’s two-patch series, which replaces `test_cmp` with `test_cmp_bin` in `t/t4205-log-pretty-formats.sh` to avoid OOM kills when comparing enormous test outputs.

Peff endorses the change, calling the benchmark data (4 GiB RSS for `diff -u` vs. 1.2 MiB for `cmp`) a "big difference" and noting this is likely an outlier due to the unusually large files involved. He briefly muses about a hypothetical future improvement to `test_cmp`—using `cmp` as a fast pre-check before falling back to `diff` for human-readable output—but dismisses it as unnecessary for now, since most test comparisons are small and the process startup time dominates. His tone is matter-of-fact: the current patch is a reasonable, targeted fix for an edge case, and he is content to leave it at that. No new technical concerns or action items emerge; this reply simply reinforces the direction already taken.