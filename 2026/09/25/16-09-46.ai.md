This is a documentation update for the `git-contacts` contrib script, following up on the recently merged feature patch that added stdin support via the `-` idiom. Junio provides a proposed tweak to `contrib/contacts/git-contacts.adoc` to document the new behavior: the patch adds a sentence explaining that a single dash `'-'` in place of a `<patch>` argument tells the command to read a patch from stdin. The diff is minimal—just two lines added—and retains the existing placeholder style (`<rev>`, `<patch>`) for consistency.

The update is straightforward and uncontroversial. Junio notes a minor uncertainty about whether `scan_patches()` can handle multiple concatenated patches from stdin, suggesting the wording could be adjusted to "patch file(s)" if that is the case, but leaves that decision for later. The patch itself is ready to apply and does not introduce new technical questions. This is a routine documentation follow-up to a merged feature, not a substantive review or design discussion.