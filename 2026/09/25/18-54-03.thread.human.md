This thread is about a small but practical improvement to Git’s GitHub Actions CI: making leak sanitizer failures visible by stopping the test run at the first leak and emitting GitHub Actions annotations that point directly to the failing test file and line. The patch, sent by Harald Nordgren, is self-contained and touches the CI script and test infrastructure to achieve this.

So far, the patch is under review but hasn’t received any substantive feedback. Ben Knoble chimed in with a minor readability suggestion—moving helper function definitions before they’re used—without questioning the patch’s correctness or design. No other reviews have been posted, so the patch’s technical soundness remains untested by the list. If no further objections or requests for changes emerge, it’s likely to be picked up by Junio once the review period concludes.