This is a documentation patch thread, part of Julia Evans’ seven-patch series introducing `gitmergeconflicts(7)`, a new man page consolidating merge conflict resolution guidance. This specific email is a reply from D. Ben Knoble to Junio C Hamano’s clarification about `git log --merge -p <path>`, offering a small but substantive correction to the explanation.

Knoble points out that Junio’s description of `git log --merge` omits a key detail: the command implicitly uses `HEAD...<other>` as its revision range, where `<other>` is the commit being merged (e.g., `MERGE_HEAD` during a merge, `REBASE_HEAD` during a rebase, or `CHERRY_PICK_HEAD` during a cherry-pick). This detail is documented in the manual and is important for understanding why the command shows the history of changes that led to the conflict. Knoble also notes that this implicit range is the basis for his personal alias, suggesting it has practical utility for users.

The correction is minor but meaningful: it ensures the explanation aligns with the documented behavior and helps users grasp why `git log --merge` shows the specific commits it does. This exchange advances the series’ goal of providing accurate, user-friendly documentation by refining the technical details in the new guide. The broader context remains unchanged: the series is in `seen`, awaiting mechanical fixes (Meson build system, `command-list.txt` registration) and content refinements (cherry-pick-specific details, foundational explanations). Knoble’s feedback is substantive but does not introduce new blockers or alter the series’ trajectory.