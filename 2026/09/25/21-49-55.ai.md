Summarizer brief mode

This is a substantive review follow-up in the `gitmergeconflicts(7)` documentation series, focusing on the technical behavior of `git log --merge`. Junio C Hamano acknowledges D. Ben Knoble’s correction regarding the implicit `HEAD...<other>` revision range constructed by the `--merge` option, confirming that this detail is essential for understanding the command’s utility in merge conflict scenarios. The symmetric difference traversal (`HEAD...MERGE_HEAD`) is now explicitly identified as the mechanism enabling `--left-right` functionality and the command’s ability to show commits from both sides of the merge.

The email advances the thread by resolving a key technical ambiguity: the `--merge` option’s behavior is now fully described as combining a `HEAD...<other>` range (where `<other>` is context-dependent, e.g., `MERGE_HEAD` for merges, `REBASE_HEAD` for rebases) with conflicted-path filtering. This clarification directly impacts the proposed wording in the new `gitmergeconflicts(7)` guide, particularly in the "Tools to get more context" section. The missing detail—how the command selects commits—was previously underspecified, and Knoble’s observation ensures it will be incorporated into the guide’s revised content.

Key technical details for future reference:
- **Command behavior**: `git log --merge -p <path>` constructs a `HEAD...<other>` revision range (e.g., `HEAD...MERGE_HEAD` for merges) and filters output to conflicted paths, showing commits from both sides of the merge.
- **Implications for the guide**: The guide’s explanation of this command must now include the `HEAD...<other>` mechanism to fully justify its use in conflict resolution. The approved user-facing wording ("will print out all commits which caused the merge conflict for `<filename>`, and the diff of how they changed the file") remains valid but should be supplemented with this technical context if the guide aims to explain *why* the command works.
- **Related options**: The `--left-right` option’s functionality is tied to the symmetric difference traversal, reinforcing the command’s utility for inspecting merge conflicts.
- **Context dependency**: The `<other>` commit varies by operation (`MERGE_HEAD`, `REBASE_HEAD`, `CHERRY_PICK_HEAD`), which may warrant mention in the guide’s command-specific sections or examples.

The discussion remains centered on content accuracy rather than mechanical integration (e.g., `command-list.txt` registration or build system updates). No new blockers are introduced, but the thread’s focus on `git log --merge` is now complete, with all critical details clarified. Future revisions of the guide will need to reflect this consensus.