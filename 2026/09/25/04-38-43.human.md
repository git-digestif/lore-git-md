This is a maintainer review in the ongoing `git-contacts` stdin feature thread. Junio C Hamano acknowledges Brigham Campbell’s clarification that `git-contacts` does not guarantee deterministic argument processing order, and accepts that the commit message need not imply sequential handling. The review remains focused on structural refinement: Junio reiterates his preference for eliminating the `$read_from_stdin` flag by treating `-` as a special filename within `scan_patch_file`, making the stdin logic more localized. No functional objections are raised; the discussion is now centered on code organization rather than behavior. The ball is back to the author to either adopt this structural suggestion or explain why the current approach is preferable.