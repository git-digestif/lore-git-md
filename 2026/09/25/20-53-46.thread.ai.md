Thread identity
Subject: [PATCH 0/2] replay: add commit signing support via -S
Series version: v1 (initial submission)
Originating author: Patrick Monette
Thread type: patch series

Core problem or goal
Add GPG commit signing support to `git replay` via a new `-S` option, addressing a latent error-handling flaw in `pick_regular_commit()` and aligning the command with other Git commands that support signing. The series explicitly ignores `commit.gpgSign` to maintain plumbing command consistency (user configuration should not alter behavior).

Current status
Under review; no prior versions. Two patches:
1. Fixes error handling in `pick_regular_commit()` to propagate signing-related failures by ensuring `result->clean` is set to -1 (error) when commit creation fails (e.g., due to signing issues). The patch introduces a temporary variable `new_commit` to hold the result of `create_commit()` and checks for NULL before proceeding.
2. Adds the `-S`/`--gpg-sign` and `--no-gpg-sign` CLI options, updates documentation, and introduces comprehensive test coverage in `t/t3651-replay-gpg-sign.sh`. The test script verifies signing behavior (default and specified keys), `--no-gpg-sign` override, `commit.gpgSign` ignorance, and failure modes (e.g., invalid key, `--ref` rollback on failure).

Key technical details
- **Files touched**: `replay.c` (patch 1), `builtin/replay.c`, `replay.h`, `Documentation/git-replay.adoc`, `t/meson.build`, new test script `t/t3651-replay-gpg-sign.sh`.
- **New CLI options**: `-S`/`--gpg-sign` (mirrors `git commit -S`), `--no-gpg-sign` (overrides earlier `--gpg-sign`).
- **Behavior change**: Commit signing is opt-in via `-S`; `commit.gpgSign` is ignored (plumbing command consistency).
- **Error handling**: Patch 1 ensures failures during commit creation (including signing failures) propagate correctly by setting `result->clean = -1` when `create_commit()` returns NULL. Patch 2 relies on this fix to handle signing failures (e.g., missing GPG key).
- **Test coverage**: Dedicated test script covers signing with default/specified keys, `--no-gpg-sign` override, `commit.gpgSign` ignorance, and edge cases (e.g., invalid key, `--ref` rollback on failure).

Open questions and loose ends
None identified yet. The series is self-contained and uncontroversial in its design choices (e.g., ignoring `commit.gpgSign`). Reviewers may scrutinize whether the error propagation in patch 1 covers all edge cases (e.g., partial failures during signing) or if additional validation is needed for the `sign_commit` field in `replay_revisions_options`.

Participant positions
- **Patrick Monette (author)**: Submitted the series, including the bugfix in patch 1 and the feature implementation in patch 2. No feedback incorporated yet.

Related work
None mentioned. The series is a direct follow-up to the `replay` command introduced in Git 2.46.