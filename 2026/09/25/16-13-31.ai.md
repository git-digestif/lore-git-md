This is a **test-infrastructure follow-up** in the `http.sslVerifyStatus` security feature thread. Junio C Hamano acknowledges SZEDER Gábor’s confirmation that the test failure was caused by using the GnuTLS backend for libcurl, which lacks full OCSP stapling support. Switching to the OpenSSL backend resolves the issue, and the test suite now passes. The root cause is environmental (backend choice) rather than a flaw in the test logic or prerequisite. Junio’s reply signals agreement that the prerequisite’s error-message check and name should be improved as Gábor previously suggested, but no immediate action is taken in this email. The thread remains focused on post-merge test robustness, and this exchange closes the diagnostic phase without introducing new technical concerns. The core functionality of `http.sslVerifyStatus` is unaffected, and the revert from `next` remains procedural. A follow-up patch to tighten the prerequisite is still expected.