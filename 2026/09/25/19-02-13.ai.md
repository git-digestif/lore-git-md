This is a CI/build system patch that refines warning visibility in GitHub Actions logs. Junio’s reply surfaces a trade-off: the patch reduces log noise by scoping "missing tool" warnings to only the platforms that attempt to install Perforce, Git LFS, or JGit, but it may make future platform additions slightly harder to notice because the warnings will now be silenced on platforms that don’t currently install these tools. The risk is low—tests that require these tools will silently skip rather than fail—but the change does add a small maintenance burden: anyone adding a new platform that needs these tools must remember to update the `case` blocks in `ci/install-dependencies.sh`. Junio considers the trade-off reasonable and invites a second review.

The patch itself is a straightforward refactoring of `ci/install-dependencies.sh`: it wraps each tool check in a `case` statement that matches only the platforms where the tool is expected (ubuntu-* for all three, macos-* for Perforce). No behavior changes; only log verbosity is affected. The diff is clean and the commit message clearly explains the motivation. This is a small quality-of-life improvement for CI log readability, and Junio’s assessment suggests it is likely to be accepted once reviewed.