This is a **bugfix** targeting the revision option parsing subsystem in `revision.c`. Jeff King (Peff) follows up on his earlier reply to Kristoffer Haugsbakk’s regression report with a two-patch series addressing the `(null)` error message bug in `git shortlog` and related commands. The series also uncovers and fixes a second, older bug in the same code path, predating the regression introduced by commit cd439487.

The first patch (`revision: avoid reporting known options as unknown on error`) is the minimal fix for the immediate regression: it ensures that error messages for unknown options display the correct option name (e.g., `--not-an-option`) instead of `(null)`. The second patch (`revision: handle argv movement in parse_revision_opt()`) is more ambitious, restoring a pre-cd439487 assumption about memory ownership in `setup_revisions()` and fixing a latent bug where the code could misreport known-but-invalid options (e.g., `--max-count=foo`) as unknown. Peff’s commit message for the second patch is unusually detailed, reflecting the complexity of the memory management logic and the need to justify the broader change.

The series touches only `revision.c` and adds a test case to `t/t4201-shortlog.sh` to verify the fix. No new symbols or on-disk format changes are introduced. The patches are small and mechanical, but the second one’s implications are non-trivial: it revisits assumptions about how `argv` entries are managed during option parsing, which could affect other revision commands if the logic is shared. Peff’s direct involvement resolves any coordination concerns with the recently merged `jk/rev-info-argv-to-free` topic, which addressed related memory management issues.

The regression is user-visible but low-impact (error messages are misleading but not harmful), while the latent bug fixed by the second patch is more subtle and could lead to confusing error messages in edge cases. The test coverage is minimal but sufficient for the immediate issue. No objections or alternative proposals have surfaced yet, but the second patch’s complexity may prompt closer scrutiny during review. The series is under active review and appears to be a strong candidate for merging, given Peff’s track record and the clear problem statement.