This is a brief, substantive review reply in the ongoing CI/build system thread about reducing resource exhaustion in GitHub Actions. Junio C Hamano, the maintainer, chimes in on the first patch of Tamir Duberstein’s two-patch series, which replaces `test_cmp` with `test_cmp_bin` in `t/t4205-log-pretty-formats.sh` to avoid OOM kills when comparing enormous test outputs.

Junio agrees with Jeff King’s earlier endorsement of the change, confirming that the benchmark data (4 GiB RSS for `diff -u` vs. 1.2 MiB for `cmp`) aligns with his intuition: most test comparisons are small enough that process startup time dominates, making a hybrid `test_cmp` improvement unnecessary. His tone is matter-of-fact, and the reply simply reinforces the consensus that the current patch is a reasonable, targeted fix for an edge case. No new technical concerns or action items emerge; this is a straightforward acknowledgment of agreement.