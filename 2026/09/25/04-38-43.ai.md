This is a maintainer reply in the ongoing `git-contacts` stdin feature thread. Junio C Hamano acknowledges Brigham Campbell’s clarification about argument processing order: the script already produces non-deterministic output for the same input files and does not guarantee sequential processing, so the commit message should avoid implying such a guarantee. Junio’s response is brief and substantive, accepting the clarification in principle while noting that the order of processing would only matter if it affected the end result (which it does not, since the script sorts and emits gathered information). The structural design question—whether to keep the v3 `$read_from_stdin` flag or adopt Junio’s alternative of treating `-` as a special filename in `scan_patch_file`—remains the only open technical point. The thread is now waiting for Brigham to respond to that suggestion.