This is a surface-level review of Harald Nordgren’s CI/build system patch that improves leak sanitizer failure reporting in GitHub Actions.

Ben Knoble replies to the patch with a single, brief suggestion: the helper functions (`github_escape_message_`, `find_test_case_line_`, and `github_annotation_`) should be introduced before they are used in `finalize_test_leak_output`, rather than defining them afterward. This is a readability nit—no functional change is proposed, and the suggestion does not engage with the patch’s correctness, edge cases, or overall design. The review is purely about code organization and does not address the patch’s core value or technical soundness. No other feedback is provided.