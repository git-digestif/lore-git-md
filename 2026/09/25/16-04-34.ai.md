This is a **substantive review reply** in the `git stash` autostash bugfix thread. Phillip Wood endorses Junio C Hamano’s finding that the tree argument order in `merge_incore_nonrecursive()` is backwards, calling it a "nasty" oversight that slipped through because the existing tests used identical versions for "base" and "stage2", masking the error. He agrees the two closely related merge functions (`merge_ort_nonrecursive` and `merge_incore_nonrecursive`) should take their arguments in the same order to avoid confusion, and suggests either `"base", "stage1", "stage2"` or `"stage1", "stage2", "base"` as the standard. The review is narrowly focused on this one-line correctness issue; Phillip does not revisit the broader design or the memory-leak fix already agreed in earlier rounds. The thread is now technically complete, awaiting only the author’s v3 submission with the argument-swap and the previously agreed test robustness improvements.