This is the second patch in a two-patch series adding GPG commit signing support to `git replay` via a new `-S` option. The patch introduces the CLI plumbing for `-S` (and `--gpg-sign`/`--no-gpg-sign`), updates the documentation, and adds a comprehensive test script (`t/t3651-replay-gpg-sign.sh`) that verifies signing behavior, edge cases (e.g., missing GPG keys), and the explicit decision to ignore `commit.gpgSign`.

The implementation is straightforward: the `-S` option is wired into the command-line parser in `builtin/replay.c`, passed through the replay machinery in `replay.c`, and plumbed into `create_commit()` and `pick_regular_commit()`. The test coverage is thorough, checking not only successful signing but also failure modes (e.g., invalid key IDs) and the interaction with `--ref`. The decision to ignore `commit.gpgSign` is uncontroversial for a plumbing command and aligns with the series' stated goal of keeping behavior predictable.

This patch looks well-crafted and complete. The commit message is clear, the code changes are minimal and focused, and the test script covers the advertised functionality. No red flags or loose ends are apparent; the series appears ready for review from GPG-signing experts (e.g., Christian Couder) or `replay` subsystem maintainers (e.g., Phillip Wood).