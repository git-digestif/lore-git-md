This is a bugfix patch targeting the line-log subsystem (`git log -L`). The v2 patch addresses feedback from Junio C Hamano by expanding the fix to trim *all* trailing blank lines (lines containing only whitespace characters, including spaces and tabs) from function ranges, not just empty lines. This aligns `git log -L :funcname:`'s behavior with `git grep -W`, which already skips trailing whitespace-only lines per a prior fix (`8e5dd94e68`).

The implementation in `line-range.c` now scans each trailing line for whitespace using `isspace()`, adjusting the end pointer until it finds a line with non-whitespace content. The updated tests cover both empty lines and lines with spaces/tabs, and the expected output files for existing tests have been adjusted to reflect the new behavior (removing trailing blank lines from diff hunks). The patch also handles CRLF line endings implicitly by treating `\r` as whitespace.

The motivation remains clear: eliminate an inconsistency between two related commands. The v2 patch is more thorough than v1, addressing edge cases and platform compatibility. While the commit message does not explicitly discuss whether the commands *should* behave consistently, the patch assumes alignment is desirable. The change is small, focused, and well-tested, with no obvious regressions or controversial design choices. It looks ready for further review or merging.