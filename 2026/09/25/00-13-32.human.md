This is a CI/build system patch discussion. Jeff King (Peff) replies to Tamir Duberstein’s benchmark data comparing `diff` and `cmp` for large test outputs in `t4205-log-pretty-formats.sh`. Peff acknowledges the dramatic resource savings (`diff` peaks at 4 GiB RSS and takes 5 seconds, while `cmp` uses 1.2 MiB and finishes in 0.5 seconds) and endorses the `test_cmp_bin` approach for this specific test case. He briefly muses about a hypothetical future improvement to `test_cmp()` that would first use `cmp` for a quick check before falling back to `diff` for detailed output, but dismisses it as unnecessary for most cases. The reply is concise and supportive, removing any lingering doubt about the technical direction for Patch 1. The discussion can now focus on whether to retain the early file deletion or simplify the patch further.