This is a **translation (l10n)** patch follow-up, closing the loop on the Afrikaans (`af`) translation for `git-gui`. Johannes Sixt (Hannes) has queued the v2 patch after confirming it addresses the procedural requirements from the v1 review—registration in `po/meson.build` and stripping location metadata from `po/af.po`. The only change in this email is a minor editorial note: Hannes removed the new sentence in the commit message that described the procedural fixes, as it was redundant with the range-diff and better suited for a "changes since v1" section.

The patch is now procedurally complete and queued for integration, with no remaining technical or procedural objections. The translation itself (2,733 strings) remains un-reviewed for accuracy, but this is typical for l10n contributions, which often rely on post-merge community feedback. The patch touches only the `po/` directory and has no impact on Git’s core functionality or behavior. Future discussion, if any, will likely focus on translation quality rather than code or infrastructure.