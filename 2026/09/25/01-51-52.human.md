This is a small but useful feature patch for the `git-contacts` contrib script, now in its fourth round of discussion. The patch extends the script to accept patch input from stdin using the conventional UNIX `-` idiom, enabling mixed file/stdin usage like `git contacts patch1 - patch3 <patch2`. This is a follow-up to Junio C Hamano’s review of v3, where he proposed an alternative design to avoid a separate `$read_from_stdin` flag by treating `-` as a special filename throughout the code path.

Brigham Campbell’s reply clarifies a key implementation detail: the order in which arguments are processed does not matter for the script’s output. The script already processes file contents before revision ranges, regardless of their order in the command line, and the output order of email addresses is not guaranteed to be stable. This behavior is intentional, not accidental, and the commit message should reflect that the script does not promise ordered processing. Brigham argues that users should have no expectation of argument order affecting the result, which aligns with the script’s current design.

The discussion is now focused on whether the commit message needs adjustment to avoid implying sequential processing, rather than on the core functionality. The feature itself—supporting stdin via `-`—remains uncontroversial and well-motivated, with clear use cases for tool interoperability (e.g., `b4`). The patch is narrowly scoped to `contrib/contacts/git-contacts` and does not introduce new CLI options or on-disk changes.

This is a low-risk, high-utility improvement for a contrib script, and the remaining open question is purely about documentation clarity. The patch looks like it will land soon, assuming the commit message is tweaked to match the actual behavior.