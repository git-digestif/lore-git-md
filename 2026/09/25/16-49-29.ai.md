This is a brief maintainer reply in the `git stash` autostash bugfix thread. Junio C Hamano explicitly endorses D. Ben Knoble's decision to exclude the broader argument-order standardization between `merge_ort_nonrecursive()` and `merge_incore_nonrecursive()` from the current series, confirming that it is out of scope. The series remains technically complete and ready for integration as v3, with all critical correctness issues resolved (including the tree-argument-order fix in `merge_incore_nonrecursive()`) and all prior feedback incorporated. No further changes are anticipated unless Junio requests them. The broader standardization task is now formally acknowledged as a separate, future cleanup effort.