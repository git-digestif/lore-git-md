This is a reply in the RFC thread about improved error reporting for invalid `--git-dir` directories. The email is a follow-up to Junio C Hamano’s surface-level review of the second patch in Kaartic Sivaraam’s three-patch series. The discussion remains purely stylistic: Kaartic proposes adjusted indentation for the `strbuf_addf()` calls in `setup.c` to better match the surrounding codebase’s single-line `} else {` style. The examples he provides show two patterns—one for short messages (single-line `strbuf_addf()`) and one for longer messages (multi-line with aligned arguments). No technical concerns are raised or addressed; the conversation is still focused on code formatting before any substantive feedback on the RFC’s direction or implementation. The series remains in the RFC phase, awaiting broader input on the error-message design and edge-case coverage.