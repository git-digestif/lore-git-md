This is a bugfix patch update targeting the `git status --ignored` pathspec substring matching regression. René Scharfe acknowledges Junio C Hamano’s review feedback identifying a NULL-dereference crash risk in the new `dir_match()` helper when `pathspec` is NULL. The email confirms the safety issue and implies the v2 patch (already posted earlier in the thread) incorporates Junio’s squash-in fix—a defensive NULL check in `dir_match()` and a new test case in `t/t2021-checkout-overwrite.sh` to verify the fix prevents segfaults during `git checkout` with untracked nested repositories.

The patch remains focused on the `dir.c` subsystem, specifically the `treat_directory()` function, and restores exact-match pathspec behavior for ignored directories by removing an optimization that inadvertently caused substring matching. No new technical details are introduced here; this reply serves as a formal acceptance of the review feedback and confirms the fix is complete. The patch is now technically ready for integration, with no open questions or loose ends. Maintainer status is still pending.