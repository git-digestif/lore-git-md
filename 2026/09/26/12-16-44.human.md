This is a small cleanup patch in the `git stash` bugfix series. The patch removes an unused `#include "oid-array.h"` from `builtin/stash.c`. Clang flagged the header as unused, and the author confirms none of its functions or types are referenced in the file. The change is trivial—just one line deleted—and has no functional impact.

This is the first patch in the five-patch series, and it was already merged to `master` as a standalone cleanup. The rest of the series (patches 2–5) fixes a bug where autostashing fails to preserve staged index entries when `stash.index=true` is set, but this patch is purely preparatory. If you’re following the series for the bugfix, this patch can be safely ignored; it’s already in the codebase.