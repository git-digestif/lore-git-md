This is a CI/build system patch, now in its second iteration. Junio C Hamano asks whether the patch is still relevant and whether macOS contributors still need it, noting that review exchanges on the previous version overlapped with this update.

The thread context confirms that all open feedback has been addressed: the commit message now explains the historical rationale (Homebrew’s 2020 policy change making gettext no longer keg-only), and Harald provided empirical evidence (a passing macOS CI pipeline) showing the patch works as intended. Patrick Steinhardt, the sole substantive reviewer, has no further objections. The patch is ready for final review and is likely to proceed unless new concerns arise.

The change itself is minimal—removing a single redundant `brew link --force gettext` command from `ci/install-dependencies.sh`—and eliminates spurious "Already linked" warnings in macOS CI logs. No new symbols, options, or on-disk changes are introduced, and the patch has no test coverage implications. The macOS CI job passes with the patch applied, confirming its safety.