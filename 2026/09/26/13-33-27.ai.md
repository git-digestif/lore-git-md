This is a CI/build system patch, part of a single-patch series now in its second iteration. The email directly addresses Junio C Hamano’s open question about the patch’s continued relevance. Harald Nordgren clarifies that the change is not aimed at local macOS development but specifically targets GitHub CI builds, where the redundant `brew link --force gettext` command generates spurious "Already linked" warnings in every macOS job log. This confirms the patch’s purpose and resolves the last remaining uncertainty in the thread.

The patch itself removes a single line from `ci/install-dependencies.sh` that is no longer needed due to a Homebrew policy change in 2020, which made gettext no longer keg-only and thus automatically linked on fresh installs. The change is minimal, safe, and empirically validated by a passing macOS CI pipeline. No new symbols, options, or on-disk format changes are introduced, and there are no test coverage implications. All prior feedback—including requests for historical rationale and empirical evidence—has been addressed, and the sole substantive reviewer, Patrick Steinhardt, has no further objections.

The thread is now unblocked and appears ready for final review. Unless new concerns arise, this patch is likely to proceed. The clarification in this email removes the last ambiguity about the patch’s scope and necessity.