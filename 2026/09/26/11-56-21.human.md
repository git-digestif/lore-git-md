This is a documentation patch, the fifth iteration of an effort to clarify the limitations of `git log --follow`. The patch tightens the language around `--follow`'s single-file requirement, distinguishing between explicit command-line usage (which errors on zero or multiple paths) and the `log.follow` configuration setting (which silently does nothing in those cases). It also explicitly marks directory arguments and wildcard patterns as producing unspecified results, and cross-references `--no-follow` as the way to disable following.

This version is a significant expansion of scope compared to v4, which focused narrowly on documenting `--no-follow` and its override behavior. The new text now addresses edge cases that were previously left ambiguous: what happens when `--follow` is given with no paths, multiple paths, or directory pathspecs. The commit message explains that these cases were underspecified, leaving users uncertain whether unsupported inputs would be rejected or silently ignored. The patch resolves this by documenting the actual behavior: command-line `--follow` errors on zero or multiple paths, while `log.follow` has no effect in those cases.

The patch touches two files: `Documentation/git-log.adoc` (reordering `--follow` and `--no-follow` entries and expanding the `--follow` description) and `Documentation/config/log.adoc` (updating the `log.follow` entry to match). The changes are well-motivated and reflect real-world usage questions, but the expanded scope may attract more scrutiny than v4 did. The thread context shows that v4 was already merged to `next`, so this v5 represents a late-stage revision that could reopen discussion.

The patch is technically sound and addresses a genuine documentation gap, but the timing is unusual—typically, documentation clarifications like this would land in a follow-up patch rather than a new iteration of a merged series. The author notes that this version rebases onto current `master`, which includes the `mv/log-follow-mergy` topic, suggesting an intent to supersede the already-merged v4. Whether this will be accepted as a drop-in replacement or treated as a new topic remains to be seen.