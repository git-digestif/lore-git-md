This is the first patch in Kristoffer Haugsbakk’s v2 series for `--[no-]range-diff-notes` in `git format-patch`. It is a refactoring patch that simplifies the `get_notes_args` function by removing a redundant parameter.

The patch updates `get_notes_args` to take only a `struct rev_info *` instead of both a `struct strvec *` and a `struct rev_info *`. The function now directly accesses `rev->rdiff_log_arg` (introduced in commit 85bd88a7, 2025-09-25) instead of receiving it as a separate argument. This change is purely internal and does not alter behavior; it is a cleanup that prepares the codebase for the second patch, which implements the new `--range-diff-notes` option.

The diff is small and focused, touching only `builtin/log.c`. The commit message explains the motivation clearly: the second argument was already available via the first, making the original parameter redundant. The patch also tidies up a long line in the `for_each_string_list` call, improving readability.

This patch is well-motivated and uncontroversial. It is a necessary preparatory step for the feature implementation in the next patch, and the change itself is mechanical and safe. No test failures or edge cases are introduced, as the logic remains identical. The patch is compile-tested but not yet integrated into any of Junio’s branches.