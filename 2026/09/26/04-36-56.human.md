This is a CI/build system patch, part of a two-iteration series targeting the macOS GitHub Actions runner. Junio C Hamano checks in to confirm whether the patch is still relevant after the review exchanges on v1.

The patch removes a redundant `brew link --force gettext` command from the macOS CI dependency script. The command was originally added to ensure gettext (a keg-only Homebrew formula) was symlinked into the system path, but a 2020 Homebrew policy change made gettext no longer keg-only, so fresh installs now link it automatically. The patch’s goal is to eliminate spurious "Already linked" warnings in every macOS job log while ensuring gettext remains available.

The thread is now unblocked: all open feedback from v1 has been addressed. The historical rationale (Homebrew’s 2020 policy change) and empirical validation (a passing macOS CI pipeline) have been provided, and no further technical questions remain. The patch is minimal—deleting a single line—and platform-specific, so it is unlikely to be controversial. Unless new concerns arise, it appears ready for final review and likely to proceed.