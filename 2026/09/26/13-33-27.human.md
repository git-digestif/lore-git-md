This is a CI/build system patch, now in its second iteration. The author, Harald Nordgren, confirms the patch remains relevant—not for local macOS development, but to eliminate a spurious "Already linked" warning in GitHub Actions CI logs for macOS runners. The change removes a redundant `brew link --force gettext` command from `ci/install-dependencies.sh`, which was originally added when gettext was a keg-only Homebrew formula but became unnecessary after a 2020 Homebrew policy change.

All prior feedback has been addressed: the commit message now explains the historical rationale, and a passing macOS CI pipeline demonstrates the patch works as intended. Junio C Hamano’s question about continued relevance is the only remaining open item, and Harald’s reply clarifies the patch’s purpose. No technical objections are outstanding, and the patch is ready for final review. It’s a small, low-risk cleanup with no impact on functionality or test coverage.