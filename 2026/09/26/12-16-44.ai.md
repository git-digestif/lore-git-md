This is the first patch in a five-patch bugfix series for `git stash`. It is a **refactoring patch** that removes an unused `#include "oid-array.h"` from `builtin/stash.c`. The change is trivial: Clang flagged the header as unused, and the author confirms no functions or types from `oid-array.h` are referenced in the file. The transitively included `hash.h` declarations are already covered by a direct include.

This patch has already been merged to `master` and is included in the series for completeness. It has no functional impact and serves as a clean precursor to the later, more substantive changes. The diff is a single line deletion. No controversy or follow-up discussion is expected.