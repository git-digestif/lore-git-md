Existing AI thread summary: (none; this is the thread root)

---
Thread identity: Announcement of Git for Windows 2.56.0-rc0, a downstream release candidate tracking upstream Git v2.56.0-rc0. Originating author: Johannes Schindelin.

Core problem or goal: Inform Windows users and packagers about the availability of Git for Windows 2.56.0-rc0, its bundled components, Windows-specific changes (including dropped Windows 8.1 support and installer fixes), and download locations.

Current status: Released as a downstream distribution; no upstream Git integration status applies. The release is a packaging effort, not a patch series or RFC.

Key technical details:
- Bundled components: Git LFS v3.8.0, cURL v8.22.0, OpenSSL v3.5.8.
- Windows-specific changes: dropped Windows 8.1 support (aligning with MSYS2), path changes (`/ucrt64/bin/git.exe` replaces `/mingw64/bin/git.exe`), installer fixes for "Use external OpenSSH" option (broken in v2.55.0), stack-smashing fix for parallel checkouts, support for committing objects >4GB, vim integration fix for omitted first line.
- Variants provided: 64-bit, ARM64, 32-bit, portable, MinGit.

Open questions and loose ends: None; this is a finalized release announcement.

Participant positions: None; this is an announcement, not a discussion thread.

Related work: Tracks upstream Git v2.56.0-rc0 but is a separate downstream effort. No dependencies on or interactions with in-flight upstream topics.