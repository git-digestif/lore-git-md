This is a CI/build system patch addressing log noise in macOS CI jobs. The author, Harald Nordgren, replies to Patrick Steinhardt’s review by providing a link to a passing GitHub Actions pipeline (GitHub PR #2402) that demonstrates the change works without the `brew link --force gettext` command. The patch removes a redundant line from `ci/install-dependencies.sh` that was causing a spurious "Already linked" warning in every macOS CI run.

The thread remains focused on validating the patch’s safety. Patrick’s earlier feedback questioned whether the command was truly unnecessary and requested empirical evidence (CI pipeline links) to confirm gettext remains correctly exposed in the system path after removal. Harald’s response directly addresses this by pointing to a successful CI run, which suggests the change does not break dependency visibility. However, the historical rationale for the command’s original inclusion—and why it might no longer be needed—remains unanswered. The patch itself is minimal (one-line deletion) and does not introduce new behavior, but the discussion now hinges on whether the CI evidence is sufficient to justify the removal.

No further technical objections or alternative approaches have been raised yet. The ball is back in Patrick’s court to assess whether the GitHub pipeline link satisfies his concerns or if additional context (e.g., GitLab CI results or historical investigation) is still needed. The patch looks uncontroversial if the CI evidence holds, but the thread’s resolution depends on whether the provided link is deemed adequate proof.