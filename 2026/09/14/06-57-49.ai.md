This is a CI/build system patch in follow-up discussion. Harald Nordgren replies to Patrick Steinhardt’s substantive review, providing a link to a passing GitHub Actions CI pipeline that empirically validates the patch’s safety. The patch removes a redundant `brew link --force gettext` command from the macOS CI dependency script (`ci/install-dependencies.sh`), aiming to eliminate a spurious “Already linked” warning in job logs.

The new information here is the GitHub Actions pipeline link, which shows the change working without the command. This directly addresses Patrick’s request for empirical evidence that gettext remains correctly exposed in the system path after the removal. The link is a concrete step toward resolving the open question about the patch’s safety, though the thread remains open until Harald incorporates the rest of Patrick’s feedback (e.g., explaining the historical rationale for the command and why it is no longer necessary).

No additional technical details or edge cases are introduced in this reply. The focus is on demonstrating the change’s viability rather than revisiting its motivation or potential side effects. The ball is back in Harald’s court to update the commit message or patch based on Patrick’s remaining comments.