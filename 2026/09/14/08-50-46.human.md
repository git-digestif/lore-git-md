This is a CI/build system patch in follow-up discussion. Harald Nordgren replies to Patrick Steinhardt’s objection about the missing macOS CI validation by providing a link to an earlier GitHub Actions run where the macOS job *did* execute successfully. The patch removes a redundant `brew link --force gettext` command from the macOS dependency installation script, which was originally added to symlink gettext (a keg-only Homebrew formula) into the system path but became unnecessary after a 2020 Homebrew policy change.

The new evidence—an older CI pipeline where the macOS job ran and passed—addresses the immediate blocker Patrick raised. The earlier run confirms that gettext remains correctly exposed in the system path even without the redundant command, which was the core safety concern. No other technical questions remain open; the historical rationale (the 2020 Homebrew policy change) was already accepted in the v2 iteration.

This patch is small, well-motivated, and now empirically validated. It touches only one line in `ci/install-dependencies.sh` and has no test coverage or behavior implications beyond reducing log noise in macOS CI jobs. If no further objections arise, it looks ready to proceed.