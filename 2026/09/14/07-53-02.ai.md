This is a CI/build system patch in follow-up discussion. Patrick Steinhardt replies to Harald Nordgren’s claim that the patch works, pointing out that the linked GitHub Actions pipeline did not actually test the macOS job—almost all jobs were skipped, so the empirical evidence Harald provided is invalid. This undermines the key justification for the patch (that gettext remains correctly exposed in the system path after removing the redundant `brew link --force gettext` command).

The thread’s status shifts from "ready for final review" to "blocked on new evidence." Harald must now either provide a passing macOS CI pipeline that demonstrates the change works, or explain why the skipped jobs do not invalidate the patch’s safety. The historical rationale (Homebrew policy change in 2020) remains valid, but without empirical validation, the patch cannot proceed. No other technical questions are raised in this reply.