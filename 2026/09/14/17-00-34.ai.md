This is a substantive review reply from Jeff King (Peff) in the ongoing thread about adding scope hints to Git’s advice messages. Peff reinforces his earlier position that the hint should *always* recommend `--global` for all `advice.*` settings, eliminating the need for per-setting scope hints like the `is_global_hint` boolean proposed in Vsevolod Myalitsin’s patch series. He argues that while it is *possible* some advice settings might be better set per-repository, such cases are likely rare exceptions, and if one emerges later, a hint flag could be introduced for that specific setting. This aligns with Junio’s latest proposal to simplify the design by always recommending `--global` without marking individual settings, but Peff’s phrasing ("I think suggesting --global for all advice would be fine") is more explicitly supportive of Junio’s direction.

The reply does not introduce new technical details or objections, but it strengthens the case for a uniform `--global` hint by framing it as the default rather than an exception. This reduces the complexity of the patch series and resolves the open question about whether to adopt the boolean `is_global_hint` mechanism or Junio’s simpler alternative. The thread now appears to be converging on the uniform `--global` approach, though Vsevolod has not yet responded to confirm whether he will revise the series accordingly. No build failures or mechanical issues are raised here, and the discussion remains focused on design intent rather than implementation.