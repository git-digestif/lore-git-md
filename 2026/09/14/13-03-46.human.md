This is a **bugfix series follow-up** from D. Ben Knoble, responding to Tyler Cipriani’s v4 patch series that fixes `--force-if-includes` to check the correct reflog. The series itself is technically complete and ready for merge, but Knoble raises a **backwards-compatibility concern** about the new behavior of rejecting non-branch pushes (e.g., tags, object IDs, detached HEAD).

Knoble describes a real-world workflow where he force-pushes a tag to reset a hotfix branch (`git push --force-with-lease origin <TAG>:hotfix`). Under v4, this would now be rejected by `--force-if-includes` unless explicitly bypassed with `--no-force-if-includes`. He questions whether the documentation’s phrasing ("integrated locally") justifies this break in behavior, noting that the change could disrupt existing workflows without clear warning. While he acknowledges the fix’s technical correctness, he suggests the series might need to either preserve the old behavior for tag/object pushes or document the break more prominently.

The email is **substantive**—it identifies a concrete use case that the patch breaks and ties it to the series’ stated goal of tightening validation. It does not propose a solution but flags the issue as a potential point of contention before merge. The thread is now at a decision point: whether to accept the stricter validation as a necessary correctness trade-off or adjust the patch to preserve compatibility for non-branch pushes.