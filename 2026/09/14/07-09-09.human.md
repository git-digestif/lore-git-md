This is a CI/build system patch, now in its second iteration. Harald Nordgren removes a redundant `brew link --force gettext` command from the macOS CI dependency script, eliminating a spurious "Already linked" warning that clutters every macOS job log. The v2 commit message now provides the missing historical context: gettext was once a keg-only Homebrew formula (installed but not linked by default), requiring `--force` to expose it on the system path. Homebrew removed this restriction in 2020, making the forced relink unnecessary.

The change is minimal—one line deleted from `ci/install-dependencies.sh`—and well-justified. The updated commit message addresses Patrick Steinhardt’s earlier request for both empirical evidence (already provided in v1 via a passing CI pipeline link) and historical rationale. The patch remains self-contained, affecting only GitHub Actions CI on macOS, with no impact on core Git functionality or other platforms.

This looks like a straightforward improvement. The v2 revision resolves the open questions from the initial review, and the patch is unlikely to be controversial. If no further concerns arise, it should be ready for merging.