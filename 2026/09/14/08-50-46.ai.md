This is a CI/build system patch in follow-up discussion, now unblocked. Harald Nordgren provides new empirical evidence: a GitHub Actions pipeline link showing the macOS job passing with the patch applied. This addresses Patrick Steinhardt’s earlier objection that the previous pipeline did not actually test the macOS job (most jobs were skipped). The new link demonstrates that gettext remains correctly exposed in the system path after removing the redundant `brew link --force gettext` command, confirming the patch’s safety.

The patch itself is a one-line deletion in `ci/install-dependencies.sh`, removing a command that became redundant after a 2020 Homebrew policy change (gettext is no longer keg-only and is now linked by default on fresh installs). The goal is to reduce log noise (spurious “Already linked” warnings) in macOS CI job logs. No new symbols, CLI options, or on-disk format changes are introduced, and there are no test coverage implications.

The thread’s status shifts from "blocked on evidence" to "ready for final review." The historical rationale (Homebrew policy change) and empirical validation (passing macOS CI job) are now both satisfied. No further technical questions or objections are open. The patch is likely to proceed unless new concerns arise.