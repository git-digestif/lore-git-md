Thread identity: Bug report about `git status --ignored` with pathspec substring matching; originating author Sean Whitton (forwarding Lester Longley's Emacs issue #81625). Series version: single patch (v1).

Core problem or goal: `git status --ignored` with a pathspec (e.g., `ba`) incorrectly matches ignored directories whose names *contain* the pathspec as a substring (e.g., `bar/baz/` and `bar/quux/`), rather than requiring an exact name match. The regression was introduced in commit `95c11ecc73` ("Fix error-prone fill_directory() API; make it only return matches", 2020-04-01), which removed pathspec matching for excluded directories as an optimization.

Current status: Bugfix patch (v1) posted by René Scharfe. The patch removes the optimization in `dir.c` that skipped pathspec matching for excluded directories, restoring the expected exact-match behavior. The patch is minimal and directly addresses the reported bug, though René notes the optimization might be repairable in a future iteration. No maintainer response or integration status yet; the patch is under review.

Key technical details:
- **Subsystem**: `git status` (specifically `dir.c` and the `treat_directory()` function).
- **CLI options**: `--ignored`, `--untracked-files`, `--porcelain`.
- **Root cause**: Optimization in `95c11ecc73` that skipped pathspec matching for excluded directories, causing substring matching instead of exact matching.
- **Files touched**: `dir.c` (no new symbols or on-disk format changes).
- **Test coverage**: Reproduction case from the original report remains valid; no formal test case included in the patch. René confirmed the bug does not manifest identically in Git's own repository (e.g., `.depen` does not match `.depend`), suggesting context-dependent behavior.

Open questions and loose ends:
- Whether the removed optimization can be restored without reintroducing the bug (René suggests this as a potential follow-up).
- No maintainer or reviewer feedback yet; the patch has not been queued in any integration branch.
- No formal test case has been added to the test suite to prevent regression.

Participant positions:
- **Sean Whitton**: Original reporter (forwarding Lester Longley's issue).
- **Lester Longley**: Original reporter in Emacs issue #81625.
- **René Scharfe**: Posted the bugfix patch, identified the regression's origin, and confirmed the reproduction case. Position: the patch is a "quick fix" that trades correctness for a minor optimization; open to revisiting the optimization later.

Related work: None mentioned. The regression is tied to a 2020 commit, but no other in-flight topics are linked to this issue.