Thread identity: Bug report about `git status --ignored` with pathspec substring matching; originating author Sean Whitton (forwarding Lester Longley's Emacs issue #81625).

Core problem or goal: `git status --ignored` with a pathspec (e.g., `ba`) incorrectly matches ignored directories whose names *contain* the pathspec as a substring (e.g., `bar/baz/` and `bar/quux/`), rather than requiring an exact name match. The expected behavior is that the pathspec should only match exact directory or file names.

Current status: Bug report filed; no patch or proposed fix yet. Reproduction case provided: create `bar/`, add `bar/` to `.gitignore`, initialize Git repos in `bar/baz` and `bar/quux`, then run `git status --porcelain --ignored --untracked-files -- ba`. The command incorrectly reports `bar/baz/` and `bar/quux/` as ignored.

Key technical details: Subsystem: `git status`; CLI options: `--ignored`, `--untracked-files`, `--porcelain`; pathspec matching logic for ignored files. No new symbols or on-disk format changes. Test coverage: reproduction case provided, but no formal test case yet.

Open questions and loose ends: Root cause of the substring matching behavior (regression or edge case in pathspec logic). No maintainer or reviewer response yet; no design objections or requests for changes.

Participant positions: Sean Whitton (reporter, forwarding the issue); Lester Longley (original reporter in Emacs issue #81625). No reviews or positions from maintainers or established contributors yet.

Related work: None mentioned.