This is a brief reply in the ongoing thread about deferring auto maintenance until the end of sequencer-driven operations (`git rebase`, `git cherry-pick`, `git revert`). The author, Thomas Bachem, accepts Patrick Steinhardt’s **surface-level** suggestion to reword a header comment in `config.h` to avoid implying the `git_config_append_parameter()` helper is tied specifically to `-c` command-line arguments. The revised wording clarifies that the helper builds a `GIT_CONFIG_PARAMETERS` string for cross-process config propagation, with a note on how `NULL` values are handled (treated as boolean `true`).

This change is minor and uncontroversial; it addresses a nitpick rather than the substantive design debate Patrick raised in his review of the same patch. The core discussion—whether auto maintenance should be triggered in the sequencer’s `pick_commits()` function or delegated to built-in commands—remains unresolved. Thomas has not yet responded to Patrick’s architectural objection, so the thread is still waiting on the author’s next move. Expect a follow-up email addressing the design question, which will determine whether the series requires a v5 rewrite.