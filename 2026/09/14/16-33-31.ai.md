This is a **substantive review reply** from Junio C Hamano on the final version of Harald Nordgren’s `--matched-only` feature patch for `git range-diff`. The email revisits a mechanical detail that was previously agreed upon and now rejects it on architectural grounds.

Junio identifies a **layering violation**: the current patch moves error handling from the caller (the CLI front-end) into the reusable library layer (`range-diff.c`) by replacing an `error()` call with `die()`. This change would prevent alternative front-ends (e.g., GUI tools, scripting wrappers) from recovering gracefully from incompatible option combinations. He argues that the original design—letting the caller decide how to handle the error—was architecturally sounder and should be restored.

The email also hints at a possible future refactoring (`error_for_incompatible_optN()`) but explicitly sets it aside as a separate effort, emphasizing that this topic should not be held hostage to it. The tone is constructive and matter-of-fact, framing the reversal as a principled architectural decision rather than a critique of the author’s work.

For the **summarizer brief**, the key takeaway is that the patch’s error-handling mechanism is now in flux: the `die_for_incompatible_opt3()` call will need to revert to the original `error()`-based approach, and the CLI front-end (`builtin/range-diff.c`) will regain responsibility for calling `die()` if needed. This change is narrow but touches the same lines of code as the `--matched-only` logic, so follow-up discussion may reference "layering," "error handling," or "front-end vs. library separation." No other aspects of the patch (behavior, tests, documentation) are affected.