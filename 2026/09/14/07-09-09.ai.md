This is a CI/build system patch, part of a two-iteration series. Harald Nordgren revises the commit message in v2 to explain the historical rationale for the now-redundant `brew link --force gettext` command in the macOS CI dependency script. The patch itself remains unchanged: it still removes the single line that was producing a spurious "Already linked" warning in every macOS job log.

The new commit message now traces the Homebrew policy change in 2020 that made gettext no longer keg-only, so a fresh install automatically links into the prefix. This addresses Patrick Steinhardt’s request for both empirical evidence (already provided in v1 via a passing CI pipeline link) and historical context. The explanation is thorough and links to the relevant Homebrew PR, making the patch’s motivation clear and self-contained.

No further technical questions remain open; the ball is back in the reviewer’s court to assess whether the updated commit message fully satisfies the feedback. The change is minimal, platform-specific, and validated by CI, so it is unlikely to be controversial.