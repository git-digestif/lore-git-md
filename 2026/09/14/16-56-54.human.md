This is a brief follow-up in the ongoing bugfix series for low-level merge driver resource cleanup. Jeff King (Peff) responds to Elijah Newren’s **substantive review** of the second patch, which ensures `close()` errors after successful `write()` calls are treated as fatal. Peff acknowledges the subtlety Elijah pointed out—implicit `close()` via `die()` is safe but potentially confusing—and agrees to restructure the error handling for clarity. The change is purely stylistic: splitting the `write_in_full()` and `close()` checks into separate `die()` calls to avoid ambiguity and set a better example for future code. This adjustment does not alter the patch’s behavior or correctness, and the series remains uncontroversial. Peff will include this tweak in the upcoming v3 re-roll, which will also address the type-safety fix for the first patch. No new technical concerns are raised.