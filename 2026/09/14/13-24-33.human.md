This is the final, minor follow-up in a three-patch bugfix series that ensures Git reliably cleans up temporary files (`.merge_file_*`) when an external merge driver is interrupted by SIGINT or SIGQUIT. The series, now awaiting a v3 to address a latent type-safety bug in the first patch, replaces fragile custom signal handling with Git’s robust `tempfile` API. This specific email is a lighthearted reply from Michal Koutný (the original reporter) to Elijah Newren’s surface-level review of the third patch, where Elijah suggested tweaking the commit message attribution to better credit Michal’s earlier work.

Michal playfully proposes an alternative: adding him to the `Reported-by:` trailer instead of rewriting the commit message. This exchange is purely procedural and has no technical impact. The patch itself is uncontroversial, well-tested, and aligns with Git’s established patterns for signal-safe resource management. The only remaining blocker for the series is the type-safety fix in patch 1/3, which Junio and Elijah have already agreed on. No further changes are needed here—this is just a friendly clarification about attribution. The series remains ready for v3 and is likely to merge once the fix is incorporated.