Thread identity
Subject: [PATCH 0/6] repack: fix concurrent-push race leading to data loss
Series version: v1 (initial submission)
Originating author: qeesung (Qin ShiCheng via GitGitGadget)
Thread type: bugfix series

Core problem or goal
Fix a race condition in `git repack -d` where concurrent pushes can cause the repack machinery to delete packs whose objects were never copied elsewhere, resulting in data loss (dangling refs pointing to nonexistent commits). The series addresses the root cause by ensuring `.keep` files are only removed by the process that created them and by replacing the `--honor-pack-keep` mechanism with a consistent snapshot of kept packs observed at repack startup. Additionally, the series fixes a latent bug in the cruft-pack walk that would become critical once `--honor-pack-keep` is removed.

Current status
Under review; series remains at v1. All tests pass and the series merges cleanly into `next` and `seen`. No prior versions exist.

Key technical details
- **Files touched**: `builtin/pack-objects.c`, `builtin/repack.c`, `object-file.c`, `packfile.h`, `packfile.c`, `tempfile.h`, `tempfile.c`, `odb/source-packed.h`, `t/t5547-push-quarantine.sh`, `t/t5331-pack-objects-stdin.sh`, `t/t5329-pack-objects-cruft.sh`, `t7700-repack.sh`, `t7703-repack-geometric.sh`
- **New plumbing**:
  - `--stdin-packs=follow` now treats `--keep-pack` packs as traversal blockers only if they are explicitly marked as closed (`^`); otherwise, they are treated as "kept-open" (`!`), allowing traversal through their objects (patch 2/6).
  - `--stdin-packs=follow` with `--keep-pack` now correctly includes objects reachable through kept packs but stored in other packs (patch 2/6).
  - `--keep-pack-from-file=<file>` added to `git pack-objects` (patch 5/6): reads a file containing one pack name per line (empty lines ignored, relative paths resolved against working directory) and treats them as if passed via `--keep-pack`. The `keep_pack_list` now owns its strings (`strdup_strings = 1`). Documented in `git-pack-objects.adoc` and tested in `t/t5331-pack-objects-stdin.sh` (two new tests: behavior identical to `--keep-pack`, including ignoring empty lines and non-existent packs; fails cleanly on missing file).
  - `unregister_tempfile()` helper introduced in `tempfile.c`/`tempfile.h` to disown tempfiles without deleting them (patch 1/6).
  - `clear_kept_pack_cache()` helper exposed in `packfile.c`/`packfile.h` to invalidate the kept-pack cache for ODB sources (patch 3/6).
  - **Performance optimization in `add_extra_kept_packs`** (patch 4/6): sorts the `--keep-pack` list once and replaces linear scans with binary search (`string_list_has_string`), yielding an 11× speed-up (11 s → 1 s) when matching 20,000 kept packs against 20,000 repository packs. The change is internal-only; the comparison function (`fspathcmp`) and matching semantics remain unchanged.
- **Behavior changes**:
  - `receive-pack` no longer removes `.keep` files it didn’t create (patch 1/6).
  - `repack` now passes `pack-objects` the exact list of `.keep` packs observed at startup via `--keep-pack-from-file` (patch 6/6), replacing the `--honor-pack-keep` mechanism which rescanned the `.keep` directory mid-repack. The snapshot file is written next to the refs snapshot already used for `multi-pack-index write` and contains one `<name>.pack` per line, matching the exact set of kept packs `repack` observed when it began.
  - `pack-objects --stdin-packs=follow` no longer incorrectly treats `--keep-pack` packs as closed under reachability (patch 2/6).
  - The cruft-pack walk now correctly traverses packs that were initially unknown to `pack-objects` but later marked as kept, ensuring all reachable objects are included (patch 3/6).
  - Alternate ODB packs are no longer skipped; they are now packed (not deleted), trading disk space for safety (patch 6/6).
- **On-disk format**: no changes.
- **Test coverage**:
  - Patch 1/6 adds tests in `t/t5547-push-quarantine.sh` to verify:
    1. A rejected push doesn’t remove a foreign `.keep`.
    2. A `.keep` installed during a failed migration is still cleaned up.
  - Patch 2/6 adds a test in `t/t5331-pack-objects-stdin.sh` to reproduce the traversal bug: a kept pack (`B_ONLY`) containing only a commit, with its tree and blob in an excluded pack (`B`). The test confirms the fix includes the tree and blob in the output pack.
  - Patch 3/6 adds a test in `t/t5329-pack-objects-cruft.sh` to verify the cruft-pack walk includes all reachable objects, even when the middle commit of an unreachable chain sits in an untold pack.
  - Patch 5/6 adds two tests in `t/t5331-pack-objects-stdin.sh` to verify `--keep-pack-from-file` behaves identically to `--keep-pack` (including ignoring empty lines and non-existent packs) and fails cleanly when the file is missing.
  - Patch 6/6 adds three tests:
    1. `t7700-repack.sh` verifies `--keep-pack` with `--pack-kept-objects` still excludes the kept pack’s objects.
    2. `t7703-repack-geometric.sh` reproduces the race (a `.keep` appearing mid-repack) and confirms objects are not lost.
    3. A second test in `t7703-repack-geometric.sh` ensures kept packs do not block traversal of objects reachable through them.

Open questions and loose ends
The tempfile ownership logic and signal-safety guarantees in patch 1/6 may attract scrutiny from reviewers familiar with the ODB and quarantine subsystems. Patch 2/6’s traversal logic change is narrowly scoped but may prompt discussion about edge cases in `pack-objects` reachability. Patch 3/6’s cruft-pack walk fix is minimal but critical for the series’ correctness; reviewers may seek confirmation that the cache invalidation is sufficient and does not introduce performance regressions. Patch 4/6’s performance optimization is unlikely to draw objections but may prompt verification that the sorted list does not interact poorly with future callers expecting insertion order. Patch 5/6’s `--keep-pack-from-file` is a scalability enabler for patch 6/6 and is unlikely to be controversial, but reviewers may discuss whether the file format should be more structured (e.g., NUL-terminated); the one-per-line convention is consistent with other Git tools and sufficient for the use case. Patch 6/6’s removal of `--honor-pack-keep` and the snapshot-based approach may prompt discussion about the trade-off between safety and disk space (alternate ODB packs are now packed rather than skipped).

Justin Tobler’s review of patch 1/6 introduces a higher-level architectural alternative: **stop relying on `git index-pack` to create `.keep` files prematurely** and instead have the ODB transaction’s commit phase create them explicitly. This would eliminate the need for pre-migration tracking of `.keep` files and simplify the fix by allowing the process to abort early if the `.keep` file already exists. Justin notes that the `.keep` file’s message (written by `index-pack`) differs between concurrent pushes, enabling collision detection, and that the current behavior already rejects one of two identical concurrent pushes. He has prototyped this approach locally as part of a separate series, indicating it is feasible and tested. This proposal may prompt discussion about whether the incremental fix (patch 1/6) should land as-is or be replaced with a more fundamental change. Reviewers with ODB expertise (Patrick Steinhardt, Jeff King) may weigh in on the trade-offs between immediacy and long-term maintainability.

Participant positions
- **qeesung (Qin ShiCheng)**: author; describes the bug as production-tested and well-motivated, with clear commit messages and comprehensive tests. Patch 1/6 is framed as a necessary, low-risk first step; patch 2/6 as a correctness fix for `pack-objects` traversal; patch 3/6 as a critical fix for the cruft-pack walk that would become unmasked once `--honor-pack-keep` is removed; patch 4/6 as a performance optimization enabling the scalability of patch 6/6; patch 5/6 as a scalability enabler for patch 6/6, following established patterns and maintaining backward compatibility; and patch 6/6 as the capstone fix that eliminates the race by ensuring `repack` and `pack-objects` work from a single, consistent snapshot of kept packs.
- **Justin Tobler**: substantive review of patch 1/6; agrees with the diagnosis but proposes an alternative architectural direction (explicit `.keep` file creation during ODB transaction commit) that would simplify the fix and eliminate the need for pre-migration tracking. His proposal is tested (prototyped locally) and framed as a preference rather than a demand, leaving room for collaboration or incremental landing.

No Reviewed-by, Acked-by, or objections recorded yet. Likely reviewers include Patrick Steinhardt and Taylor Blau (repack/ODB maintainers), as well as Jeff King (Peff) for low-level traversal, signal-safety, and cruft-pack concerns.

Related work
- **Subsystems**: repack machinery, ODB (object database), quarantine mechanism, `pack-objects` traversal logic, cruft-pack handling.
- **Dependencies**: Patch 1/6 is a prerequisite for the full race fix in patch 6/6. Patch 2/6 is a correctness fix that enables patch 6/6’s approach. Patch 3/6 is a critical fix for the cruft-pack walk, unmasked by patch 6/6’s removal of `--honor-pack-keep`. Patch 4/6 is a performance optimization enabling the scalability of patch 6/6. Patch 5/6 is a scalability enabler for patch 6/6, addressing command-line length limitations for repositories with many kept packs. No external dependencies. Justin’s proposal, if adopted, would refactor the ODB transaction commit phase and could subsume patch 1/6 entirely.