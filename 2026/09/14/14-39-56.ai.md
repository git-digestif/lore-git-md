This is a **maintainer integration notice** for Ariel Keselman’s root-ref deletion efficiency patch. Junio C Hamano replies to Patrick Steinhardt’s approval of v3, stating he will queue the patch and mark it for the `next` integration branch. The message also notes that, because this is post-`rc1`, the patch will not graduate to `master` until after Git 2.56 final unless it is deemed an emergency critical fix.

The patch remains a focused bugfix for the refs files backend, addressing the redundant packed-refs lock acquisition when deleting root refs like `AUTO_MERGE`. With Patrick’s explicit approval and Junio’s queuing, the patch is now cooking in `next` and will undergo integration testing alongside other topics. No new technical concerns or open questions are introduced; the thread is effectively closed unless a late regression surfaces during cooking. Key context for follow-ups: the patch is in `refs/files-backend.c`, uses the existing `is_root_ref()` helper, and carries no on-disk format changes.