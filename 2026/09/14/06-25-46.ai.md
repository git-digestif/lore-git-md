This is a CI/build system patch under review. Patrick Steinhardt replies to Harald Nordgren’s single-patch series that removes a redundant `brew link --force gettext` command from the macOS CI dependency script. The patch itself is straightforward—deleting one line to eliminate a spurious log warning—but Patrick’s feedback raises a substantive question about the change’s safety and history.

Patrick points out that the commit message oversimplifies the situation by not explaining why the command was originally added or why it might no longer be necessary. He provides technical context: gettext is keg-only in Homebrew, meaning it is installed but not linked into the system path by default. The `--force` flag overrides this behavior, symlinking gettext into a central location, which could potentially override system-provided versions. While the current CI runner emits a warning that gettext is already linked, this does not confirm whether the library is correctly exposed in the system path. Patrick suggests that the commit message should clarify whether the command is truly unnecessary and, if so, why it stopped being required. He also hints that links to passing CI pipelines (GitHub Actions and GitLab CI) would help demonstrate the change’s safety.

The review is substantive, not surface-level. It does not question the patch’s intent—reducing log noise—but rather its technical justification and potential side effects. The feedback implies that the change, while small, could have unintended consequences if gettext’s visibility in the system path is affected. No test results or alternative implementations are provided, but the request for CI pipeline links suggests Patrick expects empirical validation before accepting the patch. The thread remains open, with the ball now in Harald’s court to address the historical and technical rationale.