This is a brief maintainer reply in the advice-scope-hint thread. Junio explicitly endorses Jeff King’s proposal to simplify the design by always recommending `--global` in the hint for *all* `advice.*` settings, eliminating the need for per-setting scope tracking (such as the `is_global_hint` boolean introduced in Vsevolod Myalitsyn’s v4 series). The thread now appears to have converged on this uniform `--global` approach, which resolves the central design debate and removes the build-failure concerns raised by earlier iterations. No new technical details or objections are introduced; the reply is a straightforward acceptance of the simplification. The next step is for Vsevolod to revise the series accordingly, though he has not yet responded to confirm his intent. Key symbols to track in follow-up discussion: `advice_setting`, `vadvise()`, `--global`, and the absence of `is_global_hint`.