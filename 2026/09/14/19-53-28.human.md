This is a brief maintainer reply in the ongoing thread about adding scope hints to Git’s advice messages. Junio explicitly endorses Jeff King’s proposal to simplify the design by always recommending `--global` in the hint for all `advice.*` settings, eliminating the need for per-setting scope tracking like the `is_global_hint` boolean introduced in Vsevolod Myalitsin’s patch series. The reply is short and decisive, signaling that the thread has now converged on this approach.

The key takeaway is that the series will likely need a final revision to adopt the uniform `--global` hint, removing the `is_global_hint` field and its associated logic. This resolves the central design debate and simplifies the implementation, though Vsevolod has not yet responded to confirm whether he will post a v5. No new technical details or objections are raised here, and the discussion is now focused on finalizing the design rather than revisiting alternatives. The series remains under review, with no indication that Junio has picked it up for integration.