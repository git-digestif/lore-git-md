This is a **substantive review** of the first patch in Maciej Ciemborowicz’s three-patch bugfix series, which restores the `reference-transaction` hook’s ability to receive the resolved old OID when branches or tags are deleted. The patch modifies `refs_delete_refs()` to accept an optional `oid_array` of old OIDs, enabling conditional deletions that prevent concurrent updates from being silently overwritten.

Karthik Nayak’s review focuses on two technical points. First, he questions the implicit conversion of all-zero OIDs to `NULL` in `refs_delete_refs()`, arguing that callers should explicitly pass `NULL` if they don’t care about the previous value. This is a **clarity and robustness concern**: the current implementation silently treats zero OIDs as "no verification," which could mask bugs in callers that mistakenly pass zero OIDs instead of `NULL`. Karthik suggests this should be treated as a bug rather than a feature, and the documentation should reflect that zero OIDs are not supported.

Second, Karthik notes that the commit message and documentation do not mention the handling of zero OIDs, which could lead to confusion. His feedback is **constructive and precise**, targeting both the code’s behavior and its documentation. The review is **substantive** because it addresses a potential correctness issue (how `refs_delete_refs()` interprets its inputs) and a maintainability issue (whether implicit conversions are desirable).

The review does not challenge the core design of the patch—restoring the pre-regression safety check via conditional deletion—but it does raise a **minor but important implementation detail** that could affect caller behavior. The discussion is now focused on whether the implicit conversion should be removed or explicitly documented, with no broader controversy apparent. The patch remains well-motivated and technically sound, with Karthik’s feedback likely to result in a small but meaningful improvement.