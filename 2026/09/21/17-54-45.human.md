This is Junio’s review of Maciej Ciemborowicz’s bugfix patch that ensures `reference-transaction` hooks see both sides of a branch rename or copy. The patch itself remains well-motivated and technically sound—Junio’s feedback is entirely surface-level, focusing on commit-message wording, a boolean parameter instead of an `int`, and whether the new transaction-type fields should live in the generic `ref_transaction` struct or in a backend-specific sub-struct. None of these points challenge the core design or correctness; they are stylistic nits that will be addressed in a v2. The patch is still on track to land in `next` once the minor tweaks are made.