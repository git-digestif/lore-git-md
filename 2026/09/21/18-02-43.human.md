This is a brief follow-up in the already-merged Rust Windows CI series. Junio C Hamano confirms that the last open question—whether GitLab’s Windows runners (`saas-windows-medium-amd64`) have Rust preinstalled—has been resolved: they do not, as evidenced by a recent CI failure ("cargo: command not found"). He acknowledges Johannes Schindelin’s separate thread where he is working on installing Rust via `rustup` on GitLab’s runners, and expresses confidence that the issue is now in good hands. Karthik Nayak’s response in that thread is noted as part of the resolution.

The email is purely informational and does not change the status of the original patch series, which remains merged to `master`. It simply closes the loop on the final loose end from the v4 cover letter, redirecting further discussion to the linked thread. No technical changes or new patches are introduced.