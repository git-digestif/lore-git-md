This is a brief follow-up in the now-merged Rust Windows CI series. Johannes Schindelin acknowledges Junio C Hamano’s confirmation that GitLab’s Windows runners (`saas-windows-medium-amd64`) do not have Rust preinstalled. To address this, Johannes points to a separate thread where he is working on adding Rust toolchain setup to GitLab’s CI configuration. The original series (v4) remains merged to `master`; this email simply closes the open question about GitLab compatibility without reopening the discussion.