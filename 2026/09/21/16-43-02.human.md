This is a **platform compatibility** follow-up in the ongoing discussion about enabling `RUNTIME_PREFIX` support on OpenBSD. Johannes Sixt (Hannes) replies to Chris Torek’s surface-level review, clarifying the technical behavior of the patch under discussion.

The patch in question adds OpenBSD support to Git’s runtime prefix detection by introducing a new `HAVE_GETEXECPATH` code path in `exec_cmd.c`. Chris Torek had noted that the patch’s structure—stacking multiple conditional blocks with `&&`—implies mutual exclusivity between `HAVE_GETEXECPATH` and `HAVE_BSD_KERN_PROC_SYSCTL`, but lacks documentation to that effect. Johannes’s reply explains that the `&&` chaining is intentional: the first successful method short-circuits the rest, so only one method is ever used, even if multiple are defined. This addresses Chris’s concern without requiring code changes, though it doesn’t resolve whether the behavior should be documented for future maintainers.

The exchange is brief and technical, focusing on a narrow implementation detail. Johannes’s clarification is matter-of-fact and confirms the patch’s design is sound, but it doesn’t engage with broader questions like testing on OpenBSD or whether this is part of a larger effort. The thread remains low-stakes—this is a routine portability fix with no signs of controversy, but the lack of validation reports or broader context leaves some loose ends.

For a busy reader, this is a minor update: the patch’s logic is correct as written, but the discussion about documentation is still open. If you care about OpenBSD support or `RUNTIME_PREFIX` plumbing, this thread is worth monitoring for a final resolution, but it’s not urgent. The patch itself looks well-crafted and uncontroversial.