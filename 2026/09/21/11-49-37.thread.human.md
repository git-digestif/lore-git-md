This thread is about a bugfix for `git log -L :funcname:`, which currently includes trailing empty lines in function ranges while `git grep -W` trims them. Kristofer Karlsson’s initial patch aimed to align the two commands by trimming literal newlines (`\n`), but Junio C Hamano pointed out that `git grep -W` actually trims *all* whitespace-only lines (spaces, tabs, `\n`), creating a new inconsistency.

The discussion has now shifted to a design question: should `git log -L` mirror `git grep -W`’s whitespace-aware trimming, or should it stick to the literal (and ambiguous) documentation, which doesn’t mention blank-line handling? Kristofer notes that neither command’s documentation explicitly promises this behavior, leaving the intended design unclear. The thread is at a crossroads—resolving this will likely require updating both the code (to handle whitespace-only lines and CRLF edge cases) and the documentation, along with additional tests. No revised patch has been posted yet, and the series remains under review. Junio’s feedback suggests the current implementation is insufficient, but the broader design question must be settled first.