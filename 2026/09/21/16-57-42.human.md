This is a platform compatibility patch addressing OpenBSD’s `RUNTIME_PREFIX` support. Junio C Hamano follows up on Brad Smith’s earlier reply, confirming that the missing context about `getexecpath()`—why it is preferable to the existing method, its accuracy advantages, and its availability starting with OpenBSD 8.0—should be incorporated into the commit message for the next version of the patch. Junio notes that OpenBSD 8.0 is not yet released, which may affect the timing or adoption of this change, but does not raise any technical objections to the implementation itself.

The patch adds OpenBSD support for resolving Git’s binary location at runtime using `getexecpath()`, a new system API that replaces a legacy set of inconsistent hacks. The technical approach follows the established pattern for `RUNTIME_PREFIX` on other platforms, introducing a new wrapper function (`git_get_exec_path_getexecpath()`) in `exec-cmd.c` and gating the feature to OpenBSD 8.0+ via `config.mak.uname`. No new user-facing options, configuration keys, or on-disk format changes are involved, and the patch does not include test coverage, consistent with other platform-specific runtime detection code.

Junio’s feedback remains focused on the commit message rather than the code. The patch appears uncontroversial and well-scoped, but the next version will need to address the remaining questions about the deprecation status of the old BSD sysctl method and the specific situations where `getexecpath()` provides a measurable improvement. If those gaps are filled, the patch looks ready for merging. No other participants have weighed in, and the topic is still under review with no indication of merge status.