This is a brief follow-up in the already-merged Rust Windows CI series. Junio C Hamano confirms that the last open question—whether GitLab’s Windows runners (`saas-windows-medium-amd64`) have Rust preinstalled—has been definitively answered: they do not, as evidenced by a recent CI failure ("cargo: command not found"). He acknowledges Johannes Schindelin’s separate thread where Rust is being installed via `rustup` on GitLab’s runners, and expresses confidence that the issue is now in good hands (Karthik Nayak has also responded there).

The email is purely informational and does not introduce new patches or alter the status of the original series, which remains merged to `master`. It simply closes the loop on the loose end documented in the v4 cover letter. No further action is needed on the original CI configuration; the GitLab-specific work is being handled elsewhere.