This is a **platform compatibility** patch discussion targeting OpenBSD systems. The thread revisits a previously submitted patch by Brad Smith that enables `RUNTIME_PREFIX` support on OpenBSD by adding a new code path (`HAVE_GETEXECPATH`) to `exec_cmd.c`. Chris Torek’s reply is a **surface-level review** pointing out a minor structural issue: the patch adds a new conditional block (`HAVE_GETEXECPATH`) alongside an existing one (`HAVE_BSD_KERN_PROC_SYSCTL`) without documenting that only one of these methods should be defined at a time. The concern is about maintainability and clarity, not a functional bug.

The patch itself is small and narrowly scoped—it extends Git’s runtime prefix detection to OpenBSD by leveraging the `getexecpath()` function, which is analogous to existing BSD-specific sysctl logic. The commit message (not shown in this reply) likely frames this as a portability fix, but the thread context does not clarify whether this is part of a broader effort (e.g., expanding `RUNTIME_PREFIX` coverage) or an isolated OpenBSD improvement. No prior cover letter is referenced, so the patch’s motivation and scope remain limited to the immediate code change.

Chris’s feedback is purely stylistic: the code structure implies mutual exclusivity between `HAVE_GETEXECPATH` and `HAVE_BSD_KERN_PROC_SYSCTL`, but this assumption is not documented. While the review does not question the technical correctness of the patch, it highlights a potential footgun for future contributors who might enable both methods simultaneously. The tone is constructive but low-stakes—this is not a blocker, just a suggestion for improved clarity.

The patch touches `exec_cmd.c`, a core file for Git’s command execution plumbing, specifically the `git_get_exec_path()` function. Key names introduced include the `HAVE_GETEXECPATH` macro and the `git_get_exec_path_getexecpath()` helper function. No behavior changes are proposed beyond enabling OpenBSD to use `RUNTIME_PREFIX`, which allows Git to locate its runtime files (like templates or system hooks) relative to the executable’s path rather than hardcoded installation directories.

Given the patch’s narrow focus and the surface-level nature of the feedback, this is unlikely to spark significant controversy. The discussion may resolve with a simple commit message tweak or a comment in the code, but it’s worth watching if other OpenBSD users chime in to validate the approach or suggest alternatives. For now, this looks like a routine portability fix with a minor documentation nit.