This is a v2 bugfix patch for `git pull` that prevents a segmentation fault when an invalid merge head is encountered. The patch adds NULL guards around `lookup_commit_reference()` calls in `builtin/pull.c` to handle cases where the function returns NULL due to corrupted or unparseable objects (e.g., from concurrent fetches or garbage collection). Instead of crashing, the code now treats the failed lookup as "not up to date," allowing the normal merge machinery to report the broken object gracefully.

The v2 iteration addresses Junio C Hamano's feedback from v1 by rewriting the commit message in imperative mood, simplifying the test setup (removing redundant `git fetch` and replacing object truncation with `rm -f` followed by writing garbage to the object file), and adding detailed comments explaining why the object must be corrupted rather than removed. The test now uses the idiom from `t1450` and documents the rationale for the corruption approach, ensuring the crash scenario is properly exercised.

The patch remains minimal and focused, touching only `builtin/pull.c` and adding a new test in `t/t5520-pull.sh`. The behavior change is narrow: `git pull` no longer segfaults on invalid merge heads, falling back to the existing merge error reporting instead. The on-disk format is unchanged, and no new symbols are introduced. The test coverage is thorough, simulating a realistic corruption scenario while avoiding false positives from commit-graph consistency checks.

Junio's review of v1 indicated the patch was ready in principle, pending the test simplification. With those changes now incorporated, this version appears poised for merging. The commit message clearly explains the problem, the solution, and the trade-offs (e.g., preferring minimal guards over per-lookup error reporting), making it easy for reviewers to assess the approach. The patch addresses a real-world edge case with a pragmatic fix, and the test ensures the regression cannot reoccur.