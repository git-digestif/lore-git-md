This is a CI/build system follow-up in the merged Rust Windows CI series. Johannes Schindelin acknowledges Junio C Hamano’s confirmation that GitLab’s Windows runners (`saas-windows-medium-amd64`) do not have Rust preinstalled, closing the open question from the v4 cover letter. He points to a separate thread where he is working on a solution to install Rust via `rustup` on GitLab’s runners, ensuring the CI configuration remains functional for both GitHub Actions and GitLab.

The email is purely informational and does not alter the status of the original series, which remains merged to `master`. It simply documents the resolution of the last loose end and redirects further discussion to the linked thread. No technical changes or new patches are introduced here.