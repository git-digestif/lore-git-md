This is a bugfix patch for the line-range functionality in `git log -L`. The patch addresses an inconsistency where `git log -L :funcname:` includes trailing empty lines in the function range, while `git grep -W` does not. The fix trims these trailing empty lines from the computed range, aligning the behavior of `git log -L` with `git grep -W`.

The commit message clearly explains the problem and references a prior fix (`8e5dd94e68`) that handled a similar issue for `git grep`. The patch itself is small and focused, modifying `line-range.c` to skip trailing empty lines after determining the function range. It also updates test expectations in `t/t4211-line-log.sh` to reflect the new behavior, including two new test cases that verify the fix works for both trailing empty lines at EOF and empty lines between functions.

The test updates are thorough, covering both SHA-1 and SHA-256 test vectors, and the new test cases are well-designed to isolate the issue. The patch looks well-motivated and uncontroversial, with no obvious downsides or edge cases left unaddressed. It should be a straightforward improvement for users of `git log -L`.