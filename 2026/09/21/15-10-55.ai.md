This is a trivial cleanup patch in a two-patch bugfix series for `git stash`. Junio C Hamano replies to D. Ben Knoble’s first patch, which removes an unused `#include "oid-array.h"` from `builtin/stash.c`. The patch is uncontroversial: the header is transitively included via other paths, and none of its direct symbols are used in the file. Junio’s response is brief and positive, calling the patch a "no-brainer to accept" and praising the validation of correctness. There is no substantive discussion here—just confirmation that the cleanup is safe and welcome. The real action in this series remains in the second patch, which addresses the autostash bug with staged index entries.