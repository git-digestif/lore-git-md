This is a **follow-up discussion** in the line-log bugfix thread, targeting the `git log -L` subsystem. Kristofer Karlsson acknowledges Junio C Hamano’s feedback about the patch’s incomplete handling of whitespace-only lines (not just literal `\n`), which creates a discrepancy with `git grep -W`. The exchange now shifts to a **design question**: whether `git log -L` and `git grep -W` should behave consistently in trimming trailing whitespace-only lines, despite neither command’s documentation explicitly promising this behavior.

Kristofer notes that the current documentation for both commands describes function ranges in terms of "funcname lines" without mentioning blank-line trimming, leaving the intended behavior ambiguous. This raises a broader question about whether the patch should align with `git grep -W`’s existing logic (which trims whitespace-only lines) or adhere strictly to the documented behavior (which implies no trimming). The discussion is now at a **crossroads**: the patch’s goal of consistency with `git grep -W` may require updating documentation as well, or the project may decide the two commands should diverge.

Key unresolved points:
- **Behavioral alignment**: Should `git log -L` mirror `git grep -W`’s whitespace-aware trimming, or should it follow the literal documentation?
- **Documentation**: If the behavior changes, should the man pages for `git log -L` and `git grep -W` be updated to reflect the new logic?
- **Edge cases**: The patch still lacks coverage for CRLF line endings (e.g., `\r\n`) and files where trailing whitespace is the only content after a function.

The thread remains **under review**, with no revised patch yet. Junio’s feedback suggests the current implementation is insufficient, but the discussion has expanded to include documentation and design intent. The outcome will likely require a v2 patch addressing both code and documentation, along with tests for whitespace-only lines and CRLF edge cases. The patch’s readiness hinges on resolving this design question.