This is a substantive review of Harald Nordgren’s `fetch.shallow` feature patch. Phillip Wood raises a design question about the patch’s scope and motivation, specifically whether the performance issue it addresses stems from sparse checkouts or from the presence of many remote-tracking branches in the local repository.

Phillip’s key point is that the commit message and documentation imply the problem occurs in a shallow, *sparsely checked-out* clone, but the actual implementation does not depend on sparseness—it only checks whether the repository is shallow. He asks whether the real issue is that `git remote add` in a sparse repository defaults to a wildcard fetch refspec, which then causes a refspec-less fetch to negotiate history for every remote branch. If so, he suggests the solution might be to improve `git remote add` behavior in sparse repositories rather than adding a new config option.

The review does not challenge the correctness or implementation of the patch itself, but it does question whether the feature is solving the right problem in the right place. This is a substantive design-level concern that could lead to a rethink of the approach if other reviewers agree. No code changes are requested yet, but the thread may now pivot toward discussing whether the current design is the most maintainable or user-friendly way to address the performance issue.