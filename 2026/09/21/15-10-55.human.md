This is a trivial cleanup patch in a two-patch bugfix series for `git stash`. Junio C Hamano acknowledges the removal of the unused `#include "oid-array.h"` from `builtin/stash.c` as correct and uncontroversial. The patch silences a Clang warning and has no functional impact—it’s purely a preparatory step for the substantive fix in the next patch. The change is mechanical, validated by the absence of any `oid_array*` usage in the file, and already carries the author’s `Signed-off-by`. No further discussion or objections are expected.