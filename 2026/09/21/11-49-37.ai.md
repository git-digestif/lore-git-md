This is a bugfix patch targeting the line-log subsystem (`git log -L`). The patch addresses an inconsistency in how `git log -L :funcname:` determines function boundaries: it currently includes trailing empty lines in the range, while `git grep -W` explicitly skips them. The fix trims trailing empty lines from the computed range in `parse_range_funcname()` by checking for lines that start with a newline character and adjusting the end pointer accordingly.

The commit message cites a prior fix (`8e5dd94e68`) that made `git grep -W` skip trailing empty lines, framing this change as a consistency improvement. The patch includes two new tests in `t/t4211-line-log.sh` that verify the behavior: one checks that trailing empty lines at EOF are excluded, and the other ensures empty lines between functions are not included in the range. The test updates also modify existing expected output files to reflect the new behavior, removing trailing empty lines from the diff hunks.

The change is small and focused, touching only `line-range.c` and its tests. The motivation is clear and the implementation straightforward, though the commit message does not explain why the inconsistency existed in the first place or whether there are potential edge cases (e.g., files with only empty lines after a function). The patch is under review and not yet merged.