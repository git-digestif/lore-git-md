This thread is about enabling Git’s `RUNTIME_PREFIX` support on OpenBSD by using the `getexecpath()` function to locate runtime files relative to the executable’s path. The patch, authored by Brad Smith, adds a new code path in `exec_cmd.c` and has been under review.

The discussion has fully resolved the only technical concern raised: Chris Torek initially questioned whether the new `HAVE_GETEXECPATH` method could conflict with the existing `HAVE_BSD_KERN_PROC_SYSCTL` method, but Johannes Sixt clarified that the `&&` chaining in `git_get_exec_path()` ensures only the first successful method is used, making additional documentation or build-system constraints unnecessary. Chris later acknowledged his misreading of the return value convention, confirming the patch’s logic is correct.

The patch itself is small and well-scoped, touching only `exec_cmd.c` and introducing no behavioral changes for other platforms. The only remaining loose end is whether the patch has been tested on OpenBSD, but the code change is otherwise validated. If testing is confirmed, this looks ready for Junio’s queue as a straightforward portability fix.