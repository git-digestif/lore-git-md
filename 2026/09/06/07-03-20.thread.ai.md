Thread identity: Meta-discussion on version numbering for the next major Git release after 2.56, initiated by Junio C Hamano. Thread type: design discussion (not a patch series or bug report).

Core problem or goal: Decide the version number for the next Git release after 2.56 (scheduled for late September 2026), focusing on whether to bump to 3.0, use a transitional version (e.g., 2.99), or continue with incremental numbering (e.g., 2.57). The discussion evaluates unresolved breaking changes, foundational work, and ecosystem compatibility, particularly SHA-256 support on major forges like GitHub and the readiness of downstream tools.

Current status: No decision reached; Junio outlined four options (3.0, 2.99, 2.98/2.97, or 2.57) and called for input. Brian M. Carlson advocated for a cautious delay (option 3: 2.97 or 2.95) to allow time for key work to stabilize, particularly the lowercase-only object IDs series. Patrick Steinhardt’s reply weakened two of Carlson’s technical blockers: libgit2’s SHA-256 and reftable support are now upstream and enabled by default. Emily Shaffer emphasized GitHub’s SHA-256 support as the most critical unresolved gap, given its market dominance, while noting the project is not obligated to wait indefinitely. Randall S. Becker’s recent update reveals progress toward Rust platform support (including NonStop), though details are under NDA and not yet public. This weakens Carlson’s explicit exclusion of Rust platform support as a blocker, though it does not alter the immediate timeline for Git 3.0. The thread remains in the discussion phase, with no consensus or maintainer decision yet.

Key technical details: No code or subsystems are directly implicated. The primary reference document is `BreakingChanges`. Carlson’s readiness criteria were: (1) SHA-256 support on major forges (status unclear; Carlson plans an update at Git Merge; Shaffer emphasizes GitHub’s dominance), (2) libgit2’s SHA-256 and reftable support (now resolved per Steinhardt), and (3) the lowercase-only object IDs series (still unresolved; Carlson plans to re-roll and suggests soak time). Shaffer adds that GitOxide has initial SHA-256 support, but Jujutsu (jj) still requires additional work, and Google’s internal Git distribution would likely disable SHA-256 for new repositories until GitHub support is available. Rust platform support, while not a blocker, is now acknowledged as progressing, with Becker expressing optimism about its availability soon after Git 3.0.

Open questions and loose ends:
- What breaking changes or foundational work remain unresolved in the `BreakingChanges` document, particularly the lowercase-only object IDs series? Will it be re-rolled and merged in time for the next release, or will it require additional soak time?
- Is the project prepared for a 3.0 release, or does it need more time to address outstanding issues, particularly GitHub’s SHA-256 support? If not 3.0, what version number (2.99, 2.98, 2.97, or 2.57) best signals the project’s intent while allowing time for stabilization?
- Are there external factors (e.g., compatibility guarantees, ecosystem expectations) that should influence the decision, such as coordination with JGit, GitOxide, or Jujutsu? Shaffer notes that downstream tools like Jujutsu may not be fully ready even if Git itself is.
- What is the timeline for GitHub’s SHA-256 support, and should the project wait for it? Shaffer suggests that the pressure of an impending 3.0 release may accelerate progress but cautions against indefinite delays.
- How might large organizations (e.g., Google) mitigate compatibility issues if 3.0 is released without GitHub support? Shaffer’s comment about disabling SHA-256 for new repositories via system configuration suggests a potential workaround.
- What are the implications of Becker’s update on Rust platform support? While not a blocker for Git 3.0, the progress may reduce long-term friction for the Rustification effort, though the NDA status limits public discussion.

Participant positions:
- **Junio C Hamano** (maintainer): Initiated the discussion, framed the options, and emphasized the need for a deliberate transition. No preference stated yet.
- **brian m. carlson**: Advocates for a cautious delay (option 3: 2.97 or 2.95) to allow time for the lowercase-only object IDs series and other foundational work to stabilize. Explicitly excluded Rust platform support as a blocker but acknowledged other SHA-256/reftable work as unlikely to be ready in time. Position remains substantive but is partially undermined by Steinhardt’s update on libgit2 and now by Becker’s update on Rust platform progress.
- **Patrick Steinhardt**: Provides new information weakening two of Carlson’s technical blockers (libgit2’s SHA-256 and reftable support are now upstream and enabled by default). Does not engage with Carlson’s advocacy for a cautious delay or the status of the lowercase-only object IDs series. Position is pragmatic, focusing on achievable progress toward 3.0 readiness.
- **Emily Shaffer**: Emphasizes GitHub’s SHA-256 support as the most critical unresolved gap for a 3.0 release, given its market dominance. Acknowledges the project’s independence from GitHub but notes the practical reality of user experience. Skeptical about waiting indefinitely for GitHub if there is no clear timeline, suggesting the pressure of an impending 3.0 release may motivate progress. Notes that downstream tools (e.g., Jujutsu) and alternative implementations (e.g., GitOxide) have varying levels of SHA-256 support, and that large organizations like Google may mitigate compatibility issues via configuration. Position is balanced and pragmatic, focusing on ecosystem compatibility and user experience.
- **Randall S. Becker**: Provides an update on Rust platform support, revealing progress toward compatibility on platforms that currently lack it (including NonStop). While details are under NDA, he expresses optimism that support could arrive soon after Git 3.0, if not in time for the release itself. This weakens Carlson’s exclusion of Rust platform support as a blocker but does not directly address the core version numbering question. Position is cautiously hopeful and focused on long-term platform compatibility.

Related work: The `BreakingChanges` document remains the primary reference. Carlson’s readiness criteria included the lowercase-only object IDs series, SHA-256 support on major forges (particularly GitHub), and libgit2’s SHA-256/reftable compatibility (now resolved). Shaffer’s input highlights the status of GitOxide and Jujutsu as downstream dependencies, as well as Google’s internal mitigation strategies. Becker’s update introduces Rust platform support as a developing area, though its immediate relevance to Git 3.0 is limited. No other in-flight topics or dependencies are mentioned.