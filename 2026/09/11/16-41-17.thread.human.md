This thread introduces a new `--matched-only` option for `git range-diff`, proposed by Harald Nordgren. The feature filters the output to show only commits present in both input ranges, making it easier to review changes in rebased or amended series without distraction from commits that were added or dropped. The implementation reuses existing suppression logic for `--left-only` and `--right-only`, ensuring consistency and minimizing new code paths. The patch includes documentation updates, command-line parser adjustments, mutual-exclusion checks to prevent conflicting flag combinations, and thorough test coverage.

So far, the patch appears uncontroversial and well-executed. It addresses a clear workflow need—focusing on surviving commits during review—without introducing complexity or risk. The reuse of existing logic and comprehensive testing suggest it’s ready for integration, but it hasn’t yet received any reviews or maintainer feedback. If no objections arise, this is likely to be merged in its current form.