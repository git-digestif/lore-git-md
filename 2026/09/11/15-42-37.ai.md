This is a CI/build system maintenance thread follow-up. Junio C Hamano briefly endorses Jeff King's rationale for rejecting the `oldstable` Docker tag proposal in favor of the current approach: hard-coding the Debian version number (e.g., `debian:12`) and updating it manually every few years when the release reaches end-of-LTS. The endorsement is concise and unqualified, signaling that the technical discussion about the update mechanism is now settled. The patch itself remains unchanged and is still queued in `seen` with intent to merge. No new technical details or open questions are introduced; this reply simply closes the loop on the last pending design decision in the thread.