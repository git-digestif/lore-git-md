This is a follow-up in the ongoing bugfix thread for merge driver temporary file cleanup on interrupt. Michal Koutný responds to Jeff King’s (Peff) alternative patch, which replaces the original signal-handling approach with Git’s `tempfile` API.

Michal confirms that Peff’s patch works as intended, successfully cleaning up temporary files (`.merge_file_*`) both after regular termination and after SIGINT (Ctrl+C). He tested the patch and reports no functional issues, though he notes a minor compiler warning about const-correctness when modifying the `tempfile` path buffer. The warning is harmless in this context because the code only trims trailing directory separators from filenames under Git’s control, not user-provided paths.

The email advances the thread toward resolution: Michal asks whether he should send Peff’s patch as a formal `v2` or if Peff will submit it himself. This leaves the next step ambiguous but implies the technical discussion is settled. The only remaining loose end is the minor compiler warning, which is cosmetic and unlikely to block acceptance.

Key technical details for follow-up:
- **File**: `merge-ll.c` (unchanged from previous patches).
- **APIs**: Peff’s patch uses `xmks_tempfile()`, `delete_tempfile()`, and `get_tempfile_path()`.
- **Behavior**: Cleanup is now handled by Git’s `tempfile` subsystem, which is signal-safe and atomic.
- **Test status**: Lightly tested by Michal; no automated test coverage for interrupt scenarios yet.
- **Compiler warning**: Non-blocking const-correctness issue in `get_temp_path()`.

The patch is now effectively a joint effort, with Michal’s original problem statement and Peff’s implementation converging on a robust solution. The thread is likely to conclude with a `v2` submission unless Peff opts to send it himself. No substantive objections remain.