This thread began with Junio C Hamano removing a risky Coccinelle rule that automatically converted `if (!E) free(E);` into an unconditional `free(E)`, citing safety concerns—even seemingly redundant guards might exist for a reason. That patch, which deleted the rule from `tools/coccinelle/free.cocci`, is now under review with no objections recorded.

Junio has now followed up with a small but logical extension: a new patch that explicitly allows `FREE_AND_NULL(E)` to be called unconditionally in Coccinelle’s static analysis. The change adds just two lines to the same semantic patch file, reinforcing the principle that unconditional freeing is safe when the macro (or function) is designed to handle `NULL` inputs. This aligns Coccinelle’s behavior with the documented semantics of `FREE_AND_NULL` and closes a minor gap in the tool’s rules. The patch is uncontroversial—it doesn’t introduce new risk or behavior, merely codifies what is already true—and is unlikely to draw debate, though it may prompt discussion about whether other custom free-like macros should receive similar treatment. No reviews or objections have been posted yet.