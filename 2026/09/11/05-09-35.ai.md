This is a CI/build system follow-up that closes the loop on the Debian 12 CI update thread. Patrick Steinhardt acknowledges Jeff King's (Peff) rationale for rejecting the `oldstable` Docker tag proposal and agrees to proceed with the original patch, which hard-codes `debian:12` in the GitHub Actions and GitLab CI workflows. The patch remains queued in `seen` with Junio C Hamano's intent to merge, and this reply effectively removes the last open technical question about the update mechanism.

The discussion about long-term CI maintenance—specifically the viability of the `linux32` and `linux-TEST-vars` jobs—remains unresolved but is explicitly deferred. Peff's original patch addressed only the immediate need (Debian 11 reaching end-of-LTS), and this reply confirms that the project will continue updating the numeric version manually when future releases go out of support. No further changes to the patch are expected.