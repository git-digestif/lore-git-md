This is a follow-up in Junio C Hamano’s recurring "What's cooking" integration report thread. The email records Junio’s responses to Patrick Steinhardt’s earlier replies about three specific topics.

Junio fast-tracks the one-patch documentation fix `bc/maintenance-doc-markup-fix-for-asciidoc` (an AsciiDoc bullet formatting correction in `git-config(1)`) directly to `next`, bypassing the usual one-week soak because it is trivially verifiable and uncontroversial. He then acknowledges Patrick’s withdrawal of the `ps/libgit-in-subdir` series (which proposed moving `libgit.a` sources into a `lib/` subdirectory) and accepts that it will be discarded for now, leaving the door open for possible revival at the Contributor’s Summit. Finally, Junio merges the v3 update of `ps/odb-stop-registering-in-memory-sources` (a 13-patch ODB cleanup removing ad-hoc alternate registration) to `next`; the only change was a single-word tweak in a commit message. All three decisions are procedural rather than technical and reflect the current state of the integration branches.