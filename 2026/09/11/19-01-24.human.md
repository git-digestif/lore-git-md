This is a brief follow-up in the ongoing discussion about adding a `--matched-only` option to `git range-diff`. Harald Nordgren, the author of the original patch, replies to Junio C Hamano’s conciliatory proposal to retain the current implementation and update only the documentation.

The exchange is now settled: Harald accepts Junio’s minimal alternative, which avoids any code refactoring or deprecation of existing options. The plan is to keep the v1 implementation as-is, with only the documentation adjusted to clarify the relationship between `--matched-only` and the existing `--left-only`/`--right-only` flags. This resolves the design debate in favor of stability and minimal churn, leaving the feature’s behavior unchanged from the initial patch. The next step will likely be a v2 patch that updates the documentation without touching the code.