This is a CI/build system patch that addresses a duplicate commit in two recently merged topics (`ps/ci-depends-on-ruby` and `jk/ci-use-system-asciidoctor`). The email highlights a minor but visible issue: both topics include an identical fix for a missing Ruby dependency in Git’s CI documentation job, and the earlier version (`ps/ci-depends-on-ruby`) contains a typo in its commit message ("in us" instead of "in, either").

The author, Tuomas Ahola, points out the duplication and suggests cleaning it up before the topics graduate to `master`. The patch in `jk/ci-use-system-asciidoctor` is the corrected version, so the fix is already in place; the concern is purely about avoiding redundant or typo-ridden history in the permanent branch. The email includes a `git range-diff` showing the only difference between the two commits is the corrected typo.

This is a low-stakes but housekeeping-worthy observation. The fix itself is trivial and uncontroversial, and the duplication is already resolved in `next`. The value here is in preventing a minor blemish from reaching `master`. No further action is required beyond noting the preference to drop the earlier, typo-containing commit when the topics are merged.