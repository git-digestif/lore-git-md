This is a brief administrative follow-up in the ongoing CI modernization thread. Junio C Hamano confirms that the third patch in Jeff King’s series—switching GitLab CI from a manually installed, version-pinned Asciidoctor (`gem install asciidoctor -v 1.5.8`) to the system-provided package (`apt-get install asciidoctor`)—was indeed overlooked during the initial merge window. He has now queued it on top of the two already-merged patches (Patrick Steinhardt’s Ruby dependency fix and Jeff’s dead-code removal), so the full dependency chain is preserved. The change is a straightforward, low-risk cleanup that eliminates an outdated version pin and aligns CI with real-world user environments. No further discussion or action is expected; the thread’s goal of restoring and modernizing the documentation build is now complete.