This is a brief follow-up in the `git range-diff --matched-only` feature thread. Harald Nordgren accepts Junio C Hamano’s minimal alternative: retain the v1 implementation as-is and update only the documentation to clarify the relationship between `--matched-only` and the existing `--left-only`/`--right-only` flags. No code changes are planned; the discussion is now settled on a documentation-only update. The thread can be considered resolved unless further objections arise.