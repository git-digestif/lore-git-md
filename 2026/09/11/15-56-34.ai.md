This is a brief follow-up in the ongoing "What's cooking" thread, specifically addressing the duplicate CI commit issue raised by Tuomas Ahola. Junio C Hamano acknowledges the observation with a simple "Thanks for catching" and a lighthearted ";-)" in response to Tuomas's comment about avoiding immortalizing typos in `master`.

The exchange confirms the preference to drop the earlier, typo-containing commit (`ps/ci-depends-on-ruby`) when the topics graduate to `master`, aligning with the prior suggestion. No new technical details or objections are introduced; this is purely a procedural confirmation. The resolution is now clear: the corrected version in `jk/ci-use-system-asciidoctor` will be retained, and the duplicate will be omitted. This tidies up the integration history without requiring further patch work.