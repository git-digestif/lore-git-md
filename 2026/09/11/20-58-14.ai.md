This is a CI/build system follow-up that closes the loop on the Asciidoctor modernization effort. Junio C Hamano clarifies that the patch Tuomas Ahola resent—replacing the manual `gem install asciidoctor -v 1.5.8` with `apt-get install asciidoctor`—has already been merged to `master` between the v2.56.0-rc0 tag and the current integration cycle. The release notes entry that claimed the change was added in commit `47ce80527c` ("A bit more for -rc1"), which postdates rc0, so the documentation accurately reflects the merged state.

The email resolves the apparent discrepancy Tuomas flagged: the patch was not overlooked but merged after the initial release candidate. No further action is needed on this patch, and the thread’s primary goal—modernizing GitLab CI’s Asciidoctor installation—is now complete. The broader discussion about dropping Asciidoc support remains open but is unrelated to this specific change.