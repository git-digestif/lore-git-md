This is a bugfix patch for low-level merge driver infrastructure, specifically addressing resource cleanup when the merge process is interrupted by SIGINT or SIGQUIT. The thread is now focused on adopting Jeff King’s alternative solution, which replaces the original signal-handling approach with Git’s established `tempfile` API.

Michal Koutný confirms that Peff’s patch works as intended, successfully cleaning up temporary files (`.merge_file_*`) both after normal termination and after interruption by SIGINT. The only minor issue noted is a compiler warning about constness in the `tempfile` path buffer, but this is harmless since the overwrite only occurs if trailing directory separators are present—and the filenames here are under Git’s control.

The discussion is now about logistics: Michal asks whether he should send Peff’s version as a v2 patch or if Peff will submit it himself. This is a procedural question rather than a technical one, and the answer will likely depend on Peff’s preference. The patch itself appears to be in good shape, with no substantive objections raised and light testing confirming its correctness.

This is a well-motivated fix for a real edge case (resource leaks on interrupt), and the shift to the `tempfile` API makes the implementation more robust and idiomatic. The only loose end is the lack of automated test coverage for interrupt scenarios, but that could be addressed in a follow-up if needed. For now, the patch looks ready for further review or integration.