This is the eighth patch in Patrick Steinhardt’s 13-patch series refactoring the object database (ODB) subsystem to remove ad-hoc in-memory source registration. The series is part of the ongoing ODB abstraction effort and continues the `the_repository` removal work.

This patch removes the now-unused `tmp_objdir_add_as_alternate()` function from `tmp-objdir.c` and its declaration in `tmp-objdir.h`. The function became obsolete after commit `bdee7b3013` (July 2026) switched `builtin/receive-pack` to use ODB transactions instead of temporary object directories for staging incoming objects. The change is mechanical and uncontroversial: the function has no remaining callers, and its removal aligns with the series’ goal of simplifying the ODB sources list by eliminating dual-use for alternates and temporary sources.

The patch deletes 11 lines across two files, with no functional changes or behavioral impact. It is a straightforward cleanup that depends on earlier patches in the series (notably the removal of the last caller in `builtin/receive-pack`). The commit message is clear and concise, explicitly citing the commit that made the function redundant.

This patch is fully reviewed and ready for merging, with no open questions or objections. It is one of several small, incremental steps in the series that collectively eliminate the ad-hoc source registration mechanism, paving the way for future ODB abstraction work.