**Summarizer brief mode**

This is a **substantive review reply** to the second patch in Tyler Cipriani’s `--force-if-includes` bugfix series (v3, 2/2). The patch introduces a new `advice.pushRefUnverifiable` config knob and updates the advice message for detached-HEAD rejections. Patrick Steinhardt’s feedback targets the **wording of the documentation** in `Documentation/config/advice.adoc`: he suggests replacing “available” with “integrated” to more accurately reflect that the check verifies whether the remote tip has been incorporated into the local history, not merely whether the ref exists. The comment is a single-line, non-blocking nit that does not affect code behavior or test coverage. No other aspects of the patch (the `ref->unverifiable` flag, transport-layer updates, or test cases) are addressed in this reply.