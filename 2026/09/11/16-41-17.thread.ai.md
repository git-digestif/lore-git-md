Thread identity: patch series introducing `--matched-only` option to `git range-diff`; originating author Harald Nordgren; thread type: feature patch; series version v1.

Core problem or goal: add a `--matched-only` flag to `git range-diff` that filters output to show only commits present in both input ranges, addressing the workflow pain point of reviewing rebases or range-diffs where users want to focus on how surviving commits changed rather than seeing added or dropped commits.

Current status: **resolved and ready for integration**. The v1 implementation is finalized with no code changes; only documentation was updated to clarify the relationship between `--matched-only` and the existing `--left-only`/`--right-only` flags. The patch is now in its final form, with Junio providing the exact wording for the man page update. No further changes are expected unless new objections arise.

Key technical details:
- Files touched: `builtin/range-diff.c` (v1 implementation), `Documentation/git-range-diff.txt` (updated in this email), `t/t3206-range-diff.sh` (v1 test coverage).
- New CLI option: `--matched-only` (v1 implementation).
- No refactor or deprecation of existing flags (`--left-only`, `--right-only`).
- Mutual exclusion: `--matched-only` cannot be combined with `--left-only` or `--right-only` (v1 implementation); enforced via open-coded error handling (nit noted but unaddressed).
- No new symbols (functions, structs, config keys) introduced; purely additive to the CLI and output filtering logic.
- No on-disk format changes; purely a presentation-layer feature.
- Test coverage: new test case in `t3206-range-diff.sh` verifies the new option, mutual exclusion, and interaction with existing flags (v1 implementation).
- Output behavior: numbered gaps where unmatched commits are omitted remain acceptable; documentation now accurately describes this behavior.

Open questions and loose ends: **None**. The thread is closed.

Participant positions:
- **Junio C Hamano (maintainer)**: Initially proposed a substantive refactor for internal consistency, then pivoted to a minimal alternative (documentation-only update). Provided the final wording for the man page update, signaling the design is settled. Position is substantive; prioritizes stability and minimal code churn. Notes a mechanical nit (use of `die_for_incompatible_opt3()`) but does not require it to be addressed.
- **Harald Nordgren (author)**: Initially resisted Junio’s refactor, proposing a lighter alternative. Accepted Junio’s minimal alternative (documentation-only update) and deferred to Junio’s final wording. Position evolved from substantive objection to agreement; focuses on minimizing long-term maintenance burden.