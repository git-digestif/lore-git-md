Thread identity: patch series introducing `--matched-only` option to `git range-diff`; originating author Harald Nordgren; thread type: feature patch; series version v1 (final, pending error-handling revision).

Core problem or goal: add a `--matched-only` flag to `git range-diff` that filters output to show only commits present in both input ranges, addressing the workflow pain point of reviewing rebases or range-diffs where users want to focus on how surviving commits changed rather than seeing added or dropped commits.

Current status: **resolved in principle, pending one mechanical revision**. The v1 implementation is finalized except for a layering violation in error handling: the patch must revert from `die_for_incompatible_opt3()` (which lives in the reusable library layer) back to the original `error()` call, leaving the CLI front-end (`builtin/range-diff.c`) responsible for calling `die()` if needed. No other changes are expected unless new objections arise.

Key technical details:
- Files touched: `builtin/range-diff.c` (CLI option, mutual-exclusion logic, and error-handling responsibility), `Documentation/git-range-diff.txt` (synopsis, description, and option documentation), `t/t3206-range-diff.sh` (test coverage for the new option, mutual exclusion, and output behavior).
- New CLI option: `--matched-only`.
- No refactor or deprecation of existing flags (`--left-only`, `--right-only`).
- Mutual exclusion: `--matched-only` cannot be combined with `--left-only` or `--right-only`; enforced via `error()` in the library layer, with the CLI front-end calling `die()` if needed.
- Implementation: reuses existing suppression logic for `--left-only` and `--right-only` by internally setting both flags when `--matched-only` is specified.
- No new symbols (functions, structs, config keys) introduced; purely additive to the CLI and output filtering logic.
- No on-disk format changes; purely a presentation-layer feature.
- Test coverage: new test case in `t3206-range-diff.sh` verifies the new option, mutual exclusion, and interaction with existing flags and output format.
- Output behavior: numbered gaps where unmatched commits are omitted remain acceptable; documentation accurately describes this behavior.

Open questions and loose ends: **One**: restore the original error-handling layering (library layer returns `error()`, CLI front-end calls `die()`). No other open items.

Participant positions:
- **Junio C Hamano (maintainer)**: Initially proposed a substantive refactor for internal consistency, then pivoted to a minimal alternative (documentation-only update). Provided the final wording for the man page update and approved the use of `die_for_incompatible_opt3()` for mechanical consistency, but later rejected it on architectural grounds (layering violation). Position is substantive; prioritizes architectural separation, stability, and consistency with existing Git command-line conventions. The reversal is framed as a principled decision, not a critique of the author.
- **Harald Nordgren (author)**: Initially resisted Junio’s refactor, proposing a lighter alternative. Accepted Junio’s minimal alternative (documentation-only update) and deferred to Junio’s final wording. Position evolved from substantive objection to agreement; focuses on minimizing long-term maintenance burden and addressing the user need with minimal disruption. Expected to implement the error-handling revision.