Thread identity: patch series introducing `--matched-only` option to `git range-diff`; originating author Harald Nordgren; thread type: feature patch; series version v1 (final).

Core problem or goal: add a `--matched-only` flag to `git range-diff` that filters output to show only commits present in both input ranges, addressing the workflow pain point of reviewing rebases or range-diffs where users want to focus on how surviving commits changed rather than seeing added or dropped commits.

Current status: **resolved and ready for integration**. The v1 implementation is finalized with documentation updates and mechanical consistency improvements (use of `die_for_incompatible_opt3()`). The patch is now in its final form, with Junio providing the exact wording for the man page update and no further changes expected unless new objections arise.

Key technical details:
- Files touched: `builtin/range-diff.c` (CLI option and mutual-exclusion logic), `Documentation/git-range-diff.txt` (synopsis, description, and option documentation), `t/t3206-range-diff.sh` (test coverage for the new option, mutual exclusion, and output behavior).
- New CLI option: `--matched-only`.
- No refactor or deprecation of existing flags (`--left-only`, `--right-only`).
- Mutual exclusion: `--matched-only` cannot be combined with `--left-only` or `--right-only`; enforced via `die_for_incompatible_opt3()` for consistency with other Git commands.
- Implementation: reuses existing suppression logic for `--left-only` and `--right-only` by internally setting both flags when `--matched-only` is specified.
- No new symbols (functions, structs, config keys) introduced; purely additive to the CLI and output filtering logic.
- No on-disk format changes; purely a presentation-layer feature.
- Test coverage: new test case in `t3206-range-diff.sh` verifies the new option, mutual exclusion, and interaction with existing flags and output format.
- Output behavior: numbered gaps where unmatched commits are omitted remain acceptable; documentation now accurately describes this behavior.

Open questions and loose ends: **None**. The thread is closed.

Participant positions:
- **Junio C Hamano (maintainer)**: Initially proposed a substantive refactor for internal consistency, then pivoted to a minimal alternative (documentation-only update). Provided the final wording for the man page update and approved the use of `die_for_incompatible_opt3()` for mechanical consistency, signaling the design is settled. Position is substantive; prioritizes stability, minimal code churn, and consistency with existing Git command-line conventions.
- **Harald Nordgren (author)**: Initially resisted Junio’s refactor, proposing a lighter alternative. Accepted Junio’s minimal alternative (documentation-only update) and deferred to Junio’s final wording. Position evolved from substantive objection to agreement; focuses on minimizing long-term maintenance burden and addressing the user need with minimal disruption.