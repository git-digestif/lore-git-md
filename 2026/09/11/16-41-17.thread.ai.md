Thread identity: patch series introducing `--matched-only` option to `git range-diff`; originating author Harald Nordgren; thread type: feature patch.

Core problem or goal: add a `--matched-only` flag to `git range-diff` that filters output to show only commits present in both input ranges, addressing the workflow pain point of reviewing rebases or range-diffs where users want to focus on how surviving commits changed rather than seeing added or dropped commits.

Current status: initial version (v1) posted; under review. No prior versions or merge status recorded.

Key technical details:
- Files touched: `builtin/range-diff.c`, `Documentation/git-range-diff.txt`, `t/t3206-range-diff.sh`.
- New CLI option: `--matched-only`.
- New behavior: when specified, internally sets both `--left-only` and `--right-only` suppression flags, reusing existing logic.
- Mutual exclusion: `--matched-only` cannot be combined with `--left-only` or `--right-only`.
- No new symbols (functions, structs, config keys) introduced; purely additive to the CLI and output filtering logic.
- No on-disk format changes; purely a presentation-layer feature.
- Test coverage: new test case in `t3206-range-diff.sh` verifies the new option, mutual exclusion, and interaction with existing flags.

Open questions and loose ends: none raised yet; the patch appears uncontroversial and self-contained.

Participant positions: none recorded yet beyond the author.

Related work: none mentioned.