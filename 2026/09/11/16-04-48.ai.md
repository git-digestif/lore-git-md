This is a pull request acknowledgment for the `gitk` GUI tool. Junio C Hamano confirms that he has pulled Johannes Sixt’s eight-patch series modernizing the color preference dialog and adding a README note about AI contributions. The series is now merged into Junio’s tree, meaning these changes will be part of the next Git release.

The patches themselves are mechanical UI improvements to `gitk`’s Tcl/Tk code: consolidating repetitive color-button code into loops, making button labels more natural, reordering dialog sections, and displaying color swatches directly on buttons. The only non-UI change is a one-line addition to `README.md` stating that AI-generated contributions are not accepted, aligning with the broader Git project’s stance. No technical controversy or conflicts were reported, and the changes are confined to `gitk` and its documentation.

Key details for follow-up discussion: the files touched are `gitk` (Tcl/Tk) and `README.md`, the behavior change is visual only (no on-disk format or functional changes), and there is no test coverage (UI-only changes). The series is now closed for review and ready for integration into `master`.