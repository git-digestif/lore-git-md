This is a CI/build system patch that completes the work begun in the earlier thread. Tuomas Ahola resends Jeff King’s patch to switch GitLab CI from manually installing Asciidoctor 1.5.8 via `gem` to using the system-provided version via `apt-get`. The patch is a one-line change in `ci/install-dependencies.sh` that replaces the explicit `gem install asciidoctor --version 1.5.8` with an `apt-get install asciidoctor` line, eliminating the outdated version pin.

The motivation is clear and uncontroversial: the version pin (1.5.8, from 2018) is no longer necessary after compatibility fixes for Asciidoctor 2.0.0 were merged in 2019. Using the system package ensures CI tests a version closer to what users actually have installed, reducing maintenance overhead and avoiding potential drift. The patch also removes the redundant Ruby installation (since `apt-get install asciidoctor` pulls in Ruby as a dependency) but keeps the explicit `ruby` package for clarity, matching Jeff King’s earlier series.

The patch is well-timed and directly addresses the loose end from the previous thread, where Jeff’s two-patch series (`peff/ci-asciidoctor-bump`) was already queued for merging. This resend appears to be a procedural step to ensure the change is properly attributed and merged, possibly because the original patch was overlooked or because the release notes draft for v2.56.0 mistakenly claimed the update was already in `master`. The commit message is concise and links back to the historical context, making the rationale easy to follow.

This is a low-risk, high-value maintenance update. It modernizes CI testing without altering behavior, and the technical details are straightforward. The patch is ready for merging and should be uncontroversial.