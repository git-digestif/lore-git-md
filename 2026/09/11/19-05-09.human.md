This is the final documentation-only patch that closes the `git range-diff --matched-only` feature thread. The patch updates the `git-range-diff` man page to clarify the behavior of the new `--matched-only` option, which was introduced in the v1 series to filter output to show only commits present in both input ranges. Junio C Hamano notes that the existing documentation is already clear once a misleading sentence is removed, so the change is minimal. The patch touches only `Documentation/git-range-diff.txt` and does not alter any code or tests. With this update, the feature is now fully documented and the thread is resolved. The implementation remains as shipped in v1, with no refactoring or deprecation of existing flags. This is a straightforward documentation patch that finalizes a small but useful feature for users reviewing rebases or range-diffs.