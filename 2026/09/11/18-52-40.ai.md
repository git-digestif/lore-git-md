This is a follow-up in the ongoing `--matched-only` feature discussion for `git range-diff`. Junio C Hamano responds to Harald Nordgren’s pushback on the proposed refactor, suggesting a minimal alternative: keep the current implementation and adjust only the documentation.

Junio’s reply is brief and pragmatic. He acknowledges Harald’s concern about the maintenance burden of introducing deprecated options and proposes a simpler path: retain the v1 implementation as-is, but update the documentation to clarify the behavior and relationship between `--matched-only` and the existing `--left-only`/`--right-only` flags. This would avoid any code churn while still addressing the original design critique. The tone is conciliatory, prioritizing stability over architectural purity. No new technical details or code changes are introduced; the focus is purely on process. The ball is now back in Harald’s court to either accept this lighter touch or press for a more ambitious refactor.