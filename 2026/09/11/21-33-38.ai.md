This is a CI/build system follow-up that clarifies the merge status of Jeff King’s `jk/ci-use-system-asciidoctor` series. Tuomas Ahola resends the second patch of that series—replacing the manual `gem install asciidoctor -v 1.5.8` with `apt-get install asciidoctor`—after noticing the release notes draft for v2.56.0 claimed the change was already in, yet the patch itself was not visible on `master`. Junio C Hamano confirms the patch was merged in commit `47ce80527c` ("A bit more for -rc1"), which landed after the v2.56.0-rc0 tag, so the release notes are accurate and no further action is needed. The thread’s primary goal—modernizing GitLab CI’s Asciidoctor installation—is now fully resolved.