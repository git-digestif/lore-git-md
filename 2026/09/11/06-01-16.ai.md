This is a reply from Patrick Steinhardt to Junio’s "What's cooking" report, addressing three topics: a documentation markup fix, the stalled `ps/libgit-in-subdir` series, and the ongoing `ps/odb-stop-registering-in-memory-sources` effort.

Patrick gives a green light to `bc/maintenance-doc-markup-fix-for-asciidoc` (a one-patch fix for AsciiDoc bullet formatting in `git-config(1)`), confirming it is ready for `next`.  The patch itself is uncontroversial and already reviewed; this is a procedural ack.

He then withdraws the `ps/libgit-in-subdir` series, which proposed moving `libgit.a` sources into a `lib/` subdirectory.  Patrick notes the series failed to gain consensus and suggests it be discarded for now, leaving the door open for a possible revival at the upcoming Contributor’s Summit.

Finally, he acknowledges a trivial commit-message tweak requested for `ps/odb-stop-registering-in-memory-sources` (a 13-patch ODB cleanup that removes ad-hoc alternate registration) and sends out v3.  The series is part of Patrick’s long-running ODB abstraction work and is otherwise ready to proceed.