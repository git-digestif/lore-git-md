This is a brief follow-up in the ongoing "What's cooking" thread. Junio C Hamano, the maintainer, acknowledges Tuomas Ahola’s observation about a duplicate commit in two recently merged CI topics (`ps/ci-depends-on-ruby` and `jk/ci-use-system-asciidoctor`). Both topics include the same fix for a missing Ruby dependency in Git’s CI documentation job, but the earlier version (`ps/ci-depends-on-ruby`) contains a typo in its commit message ("in us" instead of "in, either").

Junio’s reply is minimal: a simple "Thanks for catching" and a winking emoji, signaling agreement with Tuomas’s suggestion to clean up the duplication before the topics graduate to `master`. The corrected version (in `jk/ci-use-system-asciidoctor`) is already in `next`, so the fix itself is not in question—only the cosmetic issue of avoiding redundant or typo-ridden history in the permanent branch.

This exchange is purely procedural and low-stakes. No technical debate or further action is needed; the preference is simply to drop the earlier, typo-containing commit when merging to `master`. The thread remains focused on integration housekeeping rather than code changes.