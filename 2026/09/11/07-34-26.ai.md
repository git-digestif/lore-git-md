This is a **substantive review** of the second patch in the v4 series that defers auto maintenance until the end of sequencer-driven operations (`git rebase`, `git cherry-pick`, `git revert`). The patch moves the `run_auto_maintenance()` call from the sequencer to the built-in commands (`builtin/rebase.c` and `builtin/revert.c`), but Patrick Steinhardt challenges the design choice, arguing that the sequencer *does* have a single exit point (`pick_commits()`) where maintenance could be triggered consistently.

Patrick’s review identifies a technical oversight in the patch’s justification: the commit message claims there is no single exit point for the sequencer, but Patrick demonstrates that nearly all code paths (including `--continue`, `--skip`, and multi-commit sequences) eventually flow through `pick_commits()`. The only exception is when the sequencer is aborted, which could be handled separately. His alternative proposal—triggering maintenance in `pick_commits()` and on abort—would simplify the implementation by avoiding manual calls in each built-in command and reduce the risk of missing edge cases.

The review is **tested-level**: Patrick traces the code paths across `git-revert`, `git-cherry-pick`, and `git-rebase`, confirming his analysis by referencing specific functions (`run_sequencer()`, `sequencer_pick_revisions()`, `do_interactive_rebase()`, `complete_action()`, `sequencer_continue()`). This suggests he has either audited the code or tested the behavior locally.

Key details for follow-up:
- **Files/subsystems**: `sequencer.c` (specifically `pick_commits()`), `builtin/rebase.c` (`run_specific_rebase()`), `builtin/revert.c` (`run_sequencer()`).
- **Behavior change**: The patch currently scatters maintenance calls in built-ins; Patrick proposes consolidating them in `pick_commits()` and on abort.
- **Test impact**: The existing test coverage (using `GIT_TRACE2_EVENT` to verify maintenance runs once at sequence end) would still apply, but the implementation would be more robust.
- **Controversy**: This is the first substantive pushback on the series’ design. The author’s response will determine whether the patch needs a v5 or if Patrick’s suggestion is adopted.

The thread is now at a crossroads: the series was previously marked "Will queue" by Junio, but this review raises a legitimate architectural concern. If the author agrees with Patrick’s analysis, the patch will likely require a rewrite; if not, the discussion may pivot to whether the current design’s trade-offs (e.g., manual calls in built-ins) are justified. Either way, the outcome will affect how auto maintenance is handled across all sequencer commands.