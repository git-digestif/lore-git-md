This is a CI/build system follow-up that closes the loop on the Asciidoctor modernization effort. Junio C Hamano confirms that the patch Tuomas Ahola resent—replacing the manual `gem install asciidoctor -v 1.5.8` with `apt-get install asciidoctor`—has already been merged to `master` between the 2.56-rc0 tag and today’s integration cycle. The release notes claim about the update was accurate; the patch simply fell out of view during the earlier discussion.

The thread is now resolved. The original fix (Patrick Steinhardt’s Ruby dependency addition) and Jeff King’s two-patch series (dead code removal and system Asciidoctor installation) are all in `master`, eliminating the outdated version pin and simplifying CI configuration. No further action is needed on the CI front, and the broader discussion about dropping Asciidoc support remains exploratory.