This is the first patch in the **v7 series** that converts `USE_NSEC` to a runtime configuration (`core.useNanosec`). The patch addresses a long-standing inconsistency between Makefile and Meson builds by exposing a configurable `man_base_url` option in `meson_options.txt`. The default value (`file://$(htmldir)/`) now matches the Makefile’s behavior, ensuring correct rendering of relative links in manual pages (e.g., fixing broken paths like `/usr/share/manuser-manual.html`).

The change is straightforward: it adds a new `man_base_url` option to `meson_options.txt` and updates `Documentation/meson.build` to use it. The default includes a trailing slash, which was missing in the previous hard-coded path, preventing broken links. The commit message explains the rationale clearly, noting that all current uses of relative links in the documentation point to HTML files, making the default safe. While the author acknowledges that few distributions may currently use `MAN_BASE_URL`, the knob is provided for consistency and future-proofing.

This patch is **uncontroversial and well-motivated**, resolving a minor but persistent build-system inconsistency. It’s ready to advance independently and has no dependencies on the rest of the series. The conflict with `ty/repo-config-cleanups` (noted in the thread context) is unrelated to this patch and will be trivial to resolve if both topics graduate. No further changes are expected.