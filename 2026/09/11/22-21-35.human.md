This is a CI/build system patch. Junio C Hamano adds a Coccinelle rule to explicitly declare that `FREE_AND_NULL(E)` is safe to call on NULL pointers, just like `free(E)` itself.

The patch is a direct follow-up to the earlier thread where Junio removed a risky Coccinelle rule that converted `if (!E) free(E);` into an unconditional `free(E)`. That removal was motivated by safety concerns—freeing a pointer that was conditionally guarded for a reason, even if the guard appeared redundant. This new patch reinforces a safer pattern by teaching Coccinelle that `FREE_AND_NULL(E)` is always safe, regardless of whether `E` is NULL.

The change is minimal: two lines added to `tools/coccinelle/free.cocci`, matching the `FREE_AND_NULL(E)` pattern. No other files or subsystems are touched. The commit message is brief but clear, drawing an explicit parallel to the well-known safety of `free(E)` on NULL pointers.

This patch is uncontroversial in intent and implementation. It doesn’t introduce new behavior but rather documents an existing invariant in Git’s codebase, making Coccinelle’s static analysis more accurate and less likely to flag false positives. The timing—immediately after removing a risky rule—suggests a deliberate shift toward safer, more conservative Coccinelle rules in Git’s CI pipeline. No objections or review comments have appeared yet, but the patch is straightforward enough that none are expected.