This is a **platform compatibility / contributor housekeeping** update for `gitk`, the built-in Git history viewer. Johannes Sixt (the current `gitk` maintainer) sends a pull request with eight small patches that clean up the color preference dialog and add a note to the `README.md` discouraging AI-generated contributions.

The most user-visible change is the color preference dialog: buttons now show the actual color swatches instead of labels, and the layout is reorganized so generic colors (background, foreground) appear above diff-specific ones. The commit messages describe these as "more natural" wording and "condensed" code, suggesting the goal is usability and maintainability rather than new functionality.

The `README.md` addition is a brief policy statement asking contributors not to submit AI-generated patches. This aligns with Git's long-standing preference for human-authored code, though the phrasing is mild ("discourage" rather than "forbid").

The patches are mechanical and localized to `gitk` and its `README.md`; they do not touch core Git code or affect other tools. The series looks well-structured and uncontroversial, with no prior discussion or objections visible in the thread. If you maintain a Git GUI or care about `gitk`'s UI polish, this is worth a quick look; otherwise, it is safe to skip.