This is a brief follow-up in the long-running discussion about adopting `clang-format` for Git’s codebase. Tuomas Ahola asks whether the platform-compatibility blocker for the `BitFieldColonSpacing: None` rule—specifically, the requirement for clang-format v12 or newer—has been resolved now that four years have passed since the original concern was raised. Debian stable (and other conservative distributions) may now include a sufficiently recent clang-format version, but the email does not provide evidence one way or the other.

The question is narrowly scoped: it targets only the version constraint for this single `.clang-format` rule, not the broader debate about `clang-format` adoption, line-wrapping conventions, or Randall S. Becker’s pluggable-formatter counterproposal. It assumes the reader is familiar with the thread’s history, particularly brian m. carlson’s earlier objection that Debian stable (with clang 11) should remain a viable development target. The tone is neutral and inquisitive, seeking an update rather than advocating for a position.

For a busy reader, this email is a low-priority ping. It does not advance the discussion on its own but may prompt a factual reply about current clang-format availability on Debian stable or other key platforms. If the blocker is indeed resolved, it could remove one obstacle to merging Ævar’s patch; if not, the status quo remains. No actionable next steps are proposed.