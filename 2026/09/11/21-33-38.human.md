This is a CI/build system follow-up that tidies up a loose end in the recent Asciidoctor modernization effort. Tuomas Ahola resends Jeff King’s patch that replaces the manual, version-pinned `gem install asciidoctor -v 1.5.8` with a simple `apt-get install asciidoctor`, pointing out that the release notes for v2.56.0 already mention the change even though the patch itself was not yet in `master`. Junio C Hamano clarifies that the patch was in fact merged to `master` in commit `47ce80527c` (post-rc0), so the release notes are accurate and no further action is needed. The thread is now closed; the GitLab CI documentation job is fully modernized and uses the system-provided Asciidoctor package.