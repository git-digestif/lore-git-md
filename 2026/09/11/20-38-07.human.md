This is a brief follow-up in the ongoing CI modernization thread. Jeff King clarifies why Tuomas Ahola’s resend of his earlier patch fell through the cracks: the patch was intended to be part of the `jk/ci-use-system-asciidoctor` topic branch but was missed during integration, likely because the branch wasn’t based on Patrick Steinhardt’s earlier fix (`ps/ci-depends-on-ruby`). The explanation is purely logistical and doesn’t revisit the patch’s technical merits.

The patch itself—replacing the manual `gem install asciidoctor -v 1.5.8` with `apt-get install asciidoctor`—remains uncontroversial and ready for merging. Jeff’s note confirms the patch’s intended placement in the topic branch, which should resolve the discrepancy noted in the v2.56.0 release notes draft. No new objections or technical concerns are raised; this is a procedural clarification aimed at Junio C Hamano to ensure the patch is picked up before the release.