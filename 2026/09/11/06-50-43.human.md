This is a CI/build system patch that caught a minor but embarrassing duplication. Two topics—`ps/ci-depends-on-ruby` and `jk/ci-use-system-asciidoctor`—both landed the exact same one-line fix for a missing Ruby dependency in Git’s CI “documentation” job. The second topic (`jk/`) also cleaned up a stale `ALREADY_HAVE_ASCIIDOCTOR` variable, but the fix itself is identical. The author (Tuomas Ahola) points out the overlap and suggests dropping one copy when the topics graduate to `master` so the typo in the first commit message (“inus”) doesn’t get immortalized. Junio’s reply in the thread confirms the duplication and implies he’ll handle it during the next integration cycle. The patch is trivial, uncontroversial, and already cooking in `next`; the only action left is a small merge-time housekeeping chore.