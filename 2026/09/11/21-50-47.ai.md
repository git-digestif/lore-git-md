This is a CI/build system follow-up that closes the loop on Jeff King’s `jk/ci-use-system-asciidoctor` series. Junio C Hamano confirms that the third and final patch—replacing the manual `gem install asciidoctor -v 1.5.8` with `apt-get install asciidoctor`—was indeed overlooked in the initial merge but is now queued for application. The patch will land on `master` shortly, completing the modernization of GitLab CI’s Asciidoctor installation and eliminating the outdated version pin. The thread’s original goal—restoring and simplifying the documentation build job—is now fully resolved, with all three patches (Patrick Steinhardt’s Ruby fix, Jeff’s dead-code removal, and the system-package switch) in place. No further discussion or action is expected.