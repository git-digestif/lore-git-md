This is Junio C Hamano’s follow-up to Patrick Steinhardt’s reply in the "What's cooking" thread, closing the loop on three topics.

The documentation markup fix (`bc/maintenance-doc-markup-fix-for-asciidoc`) is now confirmed for fast-tracking into `next` and will likely graduate to `master` sooner than usual. Junio endorses the change as trivially verifiable and safe to merge early in the cycle.

The `ps/libgit-in-subdir` series, which proposed reorganizing `libgit.a` sources into a `lib/` subdirectory, is officially discarded for now. Junio acknowledges the intent was reasonable but notes that consensus on the concrete change never materialized. Patrick plans to revisit the idea at the upcoming Contributor’s Summit.

Finally, the 13-patch ODB cleanup series (`ps/odb-stop-registering-in-memory-sources`) is cleared for `next` after Patrick sent a v3 addressing a minor commit-message tweak. This series is part of Patrick’s ongoing ODB abstraction effort and appears uncontroversial beyond the cosmetic fix.