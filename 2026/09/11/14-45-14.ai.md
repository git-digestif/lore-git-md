This is the second patch in the v6 series adding path-related keys to `git repo info`. It introduces `path.superproject-root.absolute` and `path.superproject-root.relative`, which expose the working tree root of the superproject when the current repository is a submodule. The patch is **mechanically sound and correctness-fixed**, addressing the critical bug in `get_superproject_working_tree()` that previously ignored its `repo` parameter and relied on `xgetcwd()`.

The implementation updates `submodule.c` to respect the repository’s working directory, ensuring correct behavior in `--git-dir` cases and submodule contexts. The `builtin/rev-parse.c` caller is also updated to use the new function signature, maintaining consistency across Git’s codebase. Tests cover submodule execution, bare-repository edge cases, and `--git-dir` usage, including a new test for the previously broken `--git-dir` scenario.

This patch is a **key part of the series’ core functionality**, enabling scripts to identify superproject roots without direct access to Git’s internals. The fix for the correctness bug removes the primary blocker for merging, though the deferred architectural refactoring (consolidating shared logic into `repo-info.c`) remains an open question. Reviewers should focus on the correctness of the `get_superproject_working_tree()` refactor and its test coverage. The patch is unlikely to be controversial, but the broader architectural concern may prompt further discussion in the thread.