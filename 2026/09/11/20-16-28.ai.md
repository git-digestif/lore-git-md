This is a thread wrap-up from the author of the now-merged `dk/use-nsec-runtime` series. The email confirms that the v7 patches Junio received exactly match the version he had already locally amended (including the typo fix "comparisions" → "comparisons") and queued. The author’s intent was simply to nudge Junio to update the "What’s cooking" report, not to request further changes. The series—introducing the `core.useNanosec` config key, replacing the build-time `USE_NSEC` macro, and including a Meson build-system fix and a no-op refactor—is fully merged to `master` as of 2024-07-15. No unresolved technical issues remain; the only open architectural constraint (the latent bug in `is_racy_stat()` when operating on non-`the_repository` repositories) is documented and deferred. The discussion about the preprocessor guard’s trade-offs and the `repo_config_values()` limitation is now closed.