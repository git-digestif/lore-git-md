Thread identity
Subject: "What's cooking in git.git" (September 2026 report)
Originating author: Junio C Hamano
Thread type: integration status report

Core problem or goal
Junio’s periodic report summarizes the current state of all in-flight topics across Git’s integration branches (`next`, `seen`), highlighting trends, graduation candidates, and topics needing attention. This iteration focuses on the ongoing ODB refactoring led by Patrick Steinhardt, Rust integration challenges, reference-storage format unification, and a mix of performance fixes, new features, and documentation updates.

Current status
- **Graduated to master**: `ps/odb-stop-registering-in-memory-sources` (removed ad-hoc alternate registration API).
- **In next (cooking)**: `ps/odb-alternates-at-creation`, `ps/odb-pluggable-fsck`, `ps/ref-storage-format`, `tb/rerere-lock-grace`, `kn/receive-report-hook`, `kj/repo-info-more-path-keys`, `tc/last-modified-bloom`, `ta/command-list-guides-sync-lint`, `bc/restrict-hex-to-lowercase`, `en/no-amend-during-conflicts`, `hn/history-squash`.
- **In seen (under review)**: `jc/rust-cargo-build-target`, `mh/rust-crate-subdir` (broken).
- **New topics**: `pp/midx-write-skip-empty`, `jk/merge-ll-tempfile-cleanup`.
- **Discarded**: `ps/libgit-in-subdir`, `ps/fetch-packfile-uris-parallel`.

Key technical details
- **ODB refactoring**: `ps/odb-stop-registering-in-memory-sources` (master) removed the alternate registration API; `ps/odb-alternates-at-creation` defers alternate setup to ODB creation time during clone; `ps/odb-pluggable-fsck` moves fsck checks into backend-specific layers.
- **Rust topics**: `jc/rust-cargo-build-target` (cross-compilation), `mh/rust-crate-subdir` (Cargo integration, broken).
- **Ref storage**: `ps/ref-storage-format` standardizes terminology and CLI options (e.g., `--ref-storage-format`).
- **New symbols**: `receive-report` hook (`kn/receive-report-hook`), `git repo info` path keys (`kj/repo-info-more-path-keys`), Bloom filters for `git last-modified` (`tc/last-modified-bloom`).
- **Breaking changes**: `bc/restrict-hex-to-lowercase` rejects uppercase hex in preparation for Git 3.0.
- **Documentation**: `kh/doc-datamodel` links `gitdatamodel(7)` tutorial; `ta/command-list-guides-sync-lint` adds a linter for `command-list.txt`.

Open questions and loose ends
- **Rust topics**: `mh/rust-crate-subdir` is broken; platform compatibility (e.g., NonStop) remains contentious.
- **ODB topics**: `ps/odb-pluggable-fsck` needs review (touches core fsck logic).
- **New topics**: `pp/midx-write-skip-empty` and `jk/merge-ll-tempfile-cleanup` await responses.
- **Usability topics**: `kn/receive-report-hook` expects a reroll due to feedback.
- **Breaking changes**: No unresolved questions, but `bc/restrict-hex-to-lowercase` is a preparatory step for Git 3.0.

Participant positions
- **Junio C Hamano**: Maintainer report; flags topics needing review (e.g., `ps/odb-pluggable-fsck`), marks broken topics (`mh/rust-crate-subdir`), and notes graduation candidates (`ps/odb-stop-registering-in-memory-sources`).
- **Patrick Steinhardt**: Primary driver of ODB refactoring and ref-storage unification; topics in `next`/`seen` (`ps/`).
- **Taylor Blau**: Contributor to MIDX (`pp/midx-write-skip-empty`) and rerere (`tb/rerere-lock-grace`).
- **Jeff King**: Author of `jk/merge-ll-tempfile-cleanup`.
- **Elijah Newren**: Author of `en/no-amend-during-conflicts`.
- **Toon Claes**: Author of `tc/last-modified-bloom`.
- **Karthik Nayak**: Author of `kn/receive-report-hook`.
- **Kristoffer Haugsbakk**: Author of `kh/doc-datamodel`.
- **brian m. carlson**: Author of `bc/restrict-hex-to-lowercase`.
- **Ævar Arnfjörð Bjarmason**: Author of `ta/command-list-guides-sync-lint`.
- **Han-Wen Nienhuys**: Author of `hn/history-squash`.

Related work
- **ODB refactoring**: Prior topics include `ps/odb-stop-registering-in-memory-sources` (now in `master`).
- **Rust integration**: Build system topics (`jc/rust-cargo-build-target`, `mh/rust-crate-subdir`) follow earlier discussions about platform support (e.g., NonStop).
- **Documentation**: `kh/doc-datamodel` builds on Julia Evans’ pedagogical work on Git’s data model.