This is the final documentation patch in the `git range-diff --matched-only` feature thread. Junio C Hamano provides the exact wording for the updated `git-range-diff.txt` man page, removing a previously misleading sentence and leaving the rest of the description intact. The patch is purely editorial, reflecting the settled design direction: the v1 implementation of `--matched-only` will be retained as-is, with only documentation updated to clarify its relationship to the existing `--left-only` and `--right-only` flags. No code changes are involved, and the thread is now effectively closed unless new objections emerge. The files touched are limited to `Documentation/git-range-diff.txt`.