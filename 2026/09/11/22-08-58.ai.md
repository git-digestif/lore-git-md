This is a **maintainer feedback email** on the second patch in the v6 series adding `path.superproject-root` keys to `git repo info`. Junio C Hamano suggests a **substantive refactoring** to split the patch into two: one to fix the `get_superproject_working_tree()` function (with targeted tests in `t1500-rev-parse.sh`) and another to introduce the new keys (with tests in `t1900-repo-info.sh`). The goal is to **isolate the correctness fix** from the feature addition, improving clarity and bisectability.

The feedback is **high-weight** and **actionable**, directly addressing the patch’s structure rather than its design or behavior. The proposed split would make the correctness fix more visible and easier to review, while also ensuring the feature addition builds on a solid foundation. This aligns with Git’s preference for small, self-contained patches with clear commit messages. The author will need to implement this split before the patch can proceed further in review.

No objections to the feature itself are raised here; the focus is purely on patch organization. The feedback does not affect the series’ broader architectural concerns (e.g., the `path.cdup` edge case or the deferred refactoring into `repo-info.c`), but it does reinforce the importance of mechanical cleanliness in the submission process. Reviewers should note that this patch is now blocked on the refactoring, though the fix itself is already implemented and tested in v6.