This is a CI/build system follow-up to the ongoing effort to modernize GitLab CI’s Asciidoctor installation. Jeff King clarifies the status of Tuomas Ahola’s resend of his earlier patch, which replaces the manual `gem install asciidoctor -v 1.5.8` with `apt-get install asciidoctor`. The patch was intended to be part of Jeff’s two-patch series (`jk/ci-use-system-asciidoctor`) but was missed during integration, likely because the series wasn’t rebased onto Patrick Steinhardt’s earlier fix (`ps/ci-depends-on-ruby`).

The email is a brief technical note rather than a new patch or review. Jeff confirms that the patch was meant to follow his first patch (which removed dead `ALREADY_HAVE_ASCIIDOCTOR` code) and acknowledges that the oversight explains why the change wasn’t merged despite being claimed in the v2.56.0 release notes draft. No objections or new concerns are raised; the tone is matter-of-fact, treating the resend as a routine correction.

For a future AI summarizer, the key detail is the dependency chain: the patch must be applied *after* Jeff’s first patch (dead code removal) and Patrick’s original Ruby dependency fix. The resend itself is uncontroversial and ready for merging once the branch ordering is resolved. The thread’s broader goal—dropping the outdated Asciidoctor version pin and simplifying CI—remains on track.