This is a brief follow-up in the now-closed `dk/use-nsec-runtime` thread. The series, which converts the build-time `USE_NSEC` macro to a runtime configuration option (`core.useNanosec`), was merged to `master` on 2024-07-15. D. Ben Knoble sends this email simply to confirm that Junio C Hamano’s locally amended version of the series (which included a typo fix in the commit message) matches the final v7 patches. The message serves as a gentle nudge to update the "What’s cooking" report, though the topic is already fully integrated and no further changes are needed.

The thread itself is now complete, with all prior feedback addressed and the architectural constraint (a latent bug in `is_racy_stat()` when operating on non-`the_repository` repositories) documented as a forward-looking limitation. The discussion about the preprocessor guard’s trade-offs and the `repo_config_values()` dependency is also closed. This email has no technical substance—it’s purely procedural.