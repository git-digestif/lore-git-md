This is a CI/build system patch that is effectively complete. Junio C Hamano, the maintainer, replies to Jeff King's earlier message in the thread to explicitly endorse the rationale for sticking with hard-coded Debian version numbers (`debian:12`) rather than using the `oldstable` Docker tag. The message is brief and procedural: it signals agreement with Peff's reasoning and closes the last open technical question about the update mechanism. The patch itself, which updates GitHub Actions and GitLab CI workflows to use Debian 12 instead of Debian 11, remains queued in `seen` with Junio's intent to merge it. No further changes are expected unless new objections arise.