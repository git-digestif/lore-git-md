This is a follow-up in the ongoing discussion about adding a `--matched-only` option to `git range-diff`. Junio C Hamano responds to Harald Nordgren’s pushback on the proposed refactor of the option naming and combination logic.

Junio now suggests a simpler alternative: keep the current implementation as-is and adjust the documentation instead. This would avoid the complexity of introducing new options, deprecating old ones, or refactoring the internal logic. The tone is pragmatic, prioritizing minimal disruption over design elegance. The proposal leaves the existing `--matched-only` behavior unchanged (filtering out commits unique to either input range) while sidestepping the debate about `--hide-*` options entirely. No new code or tests are introduced in this reply—it’s purely a design pivot. The ball is back in Harald’s court to decide whether to proceed with the original implementation or explore this lighter approach.