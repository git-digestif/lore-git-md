This is a brief follow-up in the CI/build system thread updating Git's continuous integration from Debian 11 to Debian 12. Patrick Steinhardt acknowledges Jeff King's explanation for rejecting the `oldstable` Docker tag proposal and agrees to proceed with the original patch, which hard-codes `debian:12` in the CI configuration. The patch remains queued in `seen` with Junio C Hamano's intent to merge, and no further changes are expected. The thread's broader discussion about long-term CI maintenance—such as 32-bit testing and older Ubuntu LTS jobs—remains open but is not addressed here. This email effectively closes the tag-selection subtopic, leaving the patch's technical approach unchanged.