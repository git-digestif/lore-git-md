This is a pull request acceptance for `gitk`, the bundled Git repository browser GUI. Junio C Hamano has merged Johannes Sixt’s eight-patch series into his tree. The series modernizes the color preference dialog in `gitk` by consolidating repetitive Tcl/Tk UI code, improving button labels, reordering dialog sections, and displaying color swatches directly on buttons instead of adjacent labels. It also adds a one-line note to `README.md` stating that AI-generated contributions are not accepted, aligning with the broader Git project’s stance.

The changes are purely visual and confined to `gitk`’s Tcl/Tk code and documentation. No technical controversy arose during review, and the merge was straightforward with no conflicts reported. The patches were already integrated into Johannes’s repository before being pulled into Junio’s tree.

This is a low-risk, self-contained update to a non-core component of Git. If you use `gitk`, you’ll notice a cleaner, more intuitive color preference dialog in the next release. The addition to `README.md` is symbolic rather than functional, reflecting the project’s policy on AI contributions. No further discussion or follow-up work is expected.