This is patch 3/10 of Patrick Steinhardt’s v3 series refactoring Git’s object integrity verification (fsck) to make consistency checks pluggable per ODB backend. The patch is a pure refactoring that moves global fsck option variables (`show_root`, `show_tags`, `show_unreachable`, `check_strict`, `check_full`, `keep_cache_objects`, `name_objects`, `check_references`) and the option-parsing array (`fsck_opts`) into the `cmd_fsck()` function, converting them to local variables. This reduces global state and improves code clarity ahead of later patches that will rework option handling for pluggable ODB backends.

The diff is mechanical: 23 lines of global declarations are removed from the top of `builtin/fsck.c`, and 22 lines are added inside `cmd_fsck()`, with no behavior change. The patch touches only `builtin/fsck.c` and does not introduce or rename any symbols. Karthik Nayak’s earlier review of this patch acknowledged the design rationale, noting that the variables were only used locally and did not need to be global. Feedback was purely observational and did not engage with technical implementation or correctness.

This patch is part of the ongoing ODB abstraction effort led by Patrick Steinhardt. It prepares the codebase for the infrastructure introduced in patch 5/10, which adds pluggable fsck callbacks and the `struct odb_fsck_options` structure. The series is now complete with all feedback addressed, and this patch is ready for integration. No unresolved questions or review points remain for this specific change.