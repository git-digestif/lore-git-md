This is the first patch in a test modernization series (v4, 3/3). It updates `t4001-diff-rename.sh` and `t4009-diff-rename-4.sh` to align with current Git test conventions, focusing on mechanical improvements without altering the behavior under test.

The patch replaces legacy test patterns with modern equivalents: setup functions are merged into `test_expect_success` blocks, test names are rewritten for clarity (e.g., "diff rename favours same basenames over different ones" instead of "favour same basenames over different ones"), and output files are consistently named `expect`/`actual`. Indentation is standardized to tabs, and here-documents use `<<-` syntax for cleaner alignment. The changes reduce the total line count by 10 across both files while improving readability and maintainability.

Key technical details include the introduction of a helper function `initial_setup` to consolidate repetitive setup steps, and the adoption of modern assertion patterns like `test_cmp expect actual`. The patch touches the diff subsystem’s test suite, specifically rename detection logic, but does not modify the underlying functionality. All changes are purely stylistic and follow the project’s ongoing test modernization effort.

The patch appears well-scoped and uncontroversial, addressing prior review feedback (e.g., merging redundant `echo` commands, cleaning up here-docs). It builds on the series’ broader goal of updating legacy test scripts to use current conventions, as noted in the cover letter. No new test cases or command-line options are introduced.

Files modified: `t/t4001-diff-rename.sh`, `t/t4009-diff-rename-4.sh`. Key terms modernized: `test_expect_success` block structure, `expect`/`actual` file naming, tab-based indentation, and test name clarity. The patch is part of the v4 iteration of the series, which has iterated through feedback from earlier versions.