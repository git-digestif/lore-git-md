This is the third and final patch in Mark C. Chu-Carroll’s v4 series modernizing the `t40*` diff-rename test scripts. It updates `t4010-diff-pathspec.sh` to follow current Git test conventions: setup steps are now embedded directly in `test_expect_success` blocks, expected/actual output is written to files named `expect` and `actual`, here-documents use the `<<-` syntax for consistent indentation, and test names are made more descriptive. The patch is purely mechanical—no behavior changes—and reduces the file by two lines while improving readability and maintainability. This aligns with the broader test modernization effort and addresses all prior review feedback. The series as a whole looks uncontroversial and ready for integration.