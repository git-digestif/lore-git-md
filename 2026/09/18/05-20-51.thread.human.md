This thread is a brief, off-topic complaint from Junio C Hamano about GitHub’s CI webpage caching. He describes a frustrating issue where the GitHub Actions workflow page for the Git project often shows stale cached data—sometimes weeks old—when filtered by `event:push` and his username (`gitster`). While he mentions a potential workaround (adding a random query parameter to the URL), he clearly finds the situation unacceptable and unnecessary.

There’s no technical discussion, patch review, or project decision here—just a maintainer venting about infrastructure friction. The thread is unlikely to grow beyond sympathetic replies or similar war stories from others who’ve encountered GitHub’s caching quirks. If you’ve been away from the list, this is a minor footnote rather than anything actionable.