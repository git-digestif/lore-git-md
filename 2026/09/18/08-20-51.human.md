This is Junio’s final review of Mike Hommey’s patch to move Git’s Rust code into a dedicated `rust/` subdirectory. The patch is now technically complete and ready for integration.

Junio confirms that the patch applies cleanly on top of Git 2.56-rc1, avoiding conflicts with other recent Rust-related topics. He walks through the mechanical changes—updating paths in the Makefile, meson.build, and CI script—and notes that the reorganization is straightforward: everything previously in `src/` now lives in either `rust/` or `rust/src/`. The only new wrinkle is the repeated `--manifest-path rust/Cargo.toml` flag in the Makefile, which Junio questions but does not object to; Mike’s approach is correct, though Junio wonders if setting `CARGO_MANIFEST_DIR` might be cleaner.

The patch fixes the CI breakage Junio reported in v4, and all prior feedback has been addressed. No substantive objections remain. The only open discussion—how to handle downstream projects that vendor Git’s C code while excluding its Rust components when Git 3.0 makes Rust mandatory—is orthogonal and can proceed separately. Junio’s review signals that this patch is now a candidate for `next`.