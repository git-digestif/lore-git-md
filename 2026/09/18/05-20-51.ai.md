This is an off-topic grumble from Junio C Hamano about GitHub CI webpage caching issues.

Junio reports that the GitHub Actions workflow page for the Git project (`main.yml`) frequently serves stale cached content—sometimes showing data from as far back as early August—when filtered by `event:push` and `actor:gitster`. The caching behavior appears inconsistent and unpredictable. He notes a potential workaround (appending a random nonce to the URL) but expresses frustration that such a workaround should not be necessary.

No code changes, patches, or technical discussion are involved; this is purely a user-experience complaint about GitHub’s infrastructure. The email is unlikely to generate follow-up discussion beyond sympathetic acknowledgments or similar anecdotes from other contributors.