This is the first patch in a three-part test modernization series (v4). Mark C. Chu-Carroll updates `t4001-diff-rename.sh` and `t4009-diff-rename-4.sh` to align with current Git test conventions. The changes are purely mechanical: replacing legacy setup functions with `test_expect_success` blocks, standardizing test naming (e.g., "diff shows path0 renamed to path1 with edit" instead of terse descriptions), using consistent tab-based indentation, and adopting modern output file naming (`expect`/`actual` instead of `expected`/`current`).

The patch reduces the line count by 10 across the two files while preserving all test behavior. Test names now better reflect the functionality being verified, improving error messages and maintainability. This is a straightforward, uncontroversial contribution that follows the project’s ongoing test modernization effort. No new test cases or behavioral changes are introduced—just cleaner, more readable infrastructure. The author has iterated through four versions, addressing prior feedback, and the series appears ready for review.