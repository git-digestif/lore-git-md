This is an off-topic grumble from Junio C Hamano about GitHub CI's web caching behavior. He reports that the GitHub Actions workflow page for the Git project (`main.yml`) often serves stale cached content—sometimes weeks old—when filtered by `event:push actor:gitster`. His workaround is to append a random nonce to the URL, but he finds this annoying and unnecessary. The email is purely observational, with no patch, proposal, or call for action; it’s just a maintainer venting about a minor but persistent UI annoyance.