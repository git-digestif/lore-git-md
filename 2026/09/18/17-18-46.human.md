This is the second patch in Mark C. Chu-Carroll’s v4 series modernizing the `t40*` diff-rename tests. It updates `t4009-diff-rename-4.sh` to follow current Git test conventions: setup steps are now inside `test_expect_success` blocks, expected and actual output are written to `expect` and `actual` files, here-docs use `<<-` for consistent indentation, and test names are clearer. The patch is purely mechanical—no behavior changes, just improved readability and maintainability. The net result is a 6-line reduction in the file, aligning with the series’ goal of cleaner test infrastructure. This looks like a straightforward, uncontroversial modernization that addresses all prior review feedback.