This is Junio’s review of Mike Hommey’s v5 patch that moves Git’s Rust code into a `rust/` subdirectory. The patch is now technically complete and ready for integration.

Junio confirms the patch applies cleanly on top of Git 2.56-rc1, avoiding conflicts with recent Rust-related build-system topics. He walks through the mechanical changes—updating paths in the Makefile, meson.build, and CI script—and notes that the Rust sources are now consistently under `rust/src/`. The only minor question he raises is whether setting `CARGO_MANIFEST_DIR` in the Makefile would be cleaner than repeatedly passing `--manifest-path`; this is a stylistic point, not a blocker. He also double-checks that meson.build correctly descends into `rust/` rather than `rust/src/`, which it does.

No substantive objections remain. Junio’s review is surface-level, focusing on build-system mechanics and integration hygiene rather than the broader merits of the reorganization. The patch is now queued for `next` unless someone raises a last-minute technical concern. The orthogonal discussion about publishing the `gitcore` crate to crates.io for Git 3.0’s mandatory Rust components continues separately.