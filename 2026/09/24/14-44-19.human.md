This is the fourth patch in a seven-part documentation series introducing `gitmergeconflicts(7)`, a new man page consolidating merge conflict resolution guidance. The series aims to reduce duplication and improve clarity by replacing scattered advice across multiple command pages with a single, dedicated resource.

This patch updates `git-revert.adoc` to link to the new `gitmergeconflicts(7)` guide. The change is minimal: it adds a two-sentence note under the introductory description explaining when merge conflicts can occur during a revert, followed by a cross-reference to the new guide. The `SEE ALSO` section is also updated to include the new man page. The patch preserves all existing content, including the detailed explanation of `--skip` (which is unique to `git rebase` and not covered in the new guide), ensuring rebase-specific nuances remain documented.

The series as a whole is well-motivated, addressing a long-standing pain point for users who currently have to hunt across multiple man pages for conflict-resolution advice. Julia Evans’s approach—prioritizing practical examples and user feedback (e.g., Mastodon polls)—aligns with the project’s recent push for clearer documentation. Reviewers have not yet weighed in on this patch, but earlier feedback on the series has focused on technical accuracy (e.g., correctness of `diff3`/`zdiff3` examples) and man-page conventions (e.g., the role of `SYNOPSIS` in non-command guides). This patch is unlikely to be controversial, as it simply adds a cross-reference without altering existing behavior or removing substantive content.

The remaining patches in the series will update `git-cherry-pick`, `git-revert`, and `git-pull` in similar fashion. If the series lands, users will gain a centralized resource for conflict resolution, while maintainers will benefit from reduced duplication in the documentation. The patch itself is straightforward and ready for review.