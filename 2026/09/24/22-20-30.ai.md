This is a build-system follow-up to the `gitmergeconflicts(7)` documentation series. Junio reports that after merging the series into his `seen` branch, the `make check-docs` target fails with the error `no link: gitmergeconflicts`. The failure occurs because the new man page is not properly linked in the documentation linting infrastructure, which checks for cross-references and internal consistency.

The issue is mechanical: the new `gitmergeconflicts(7)` page exists but is not registered in the list of man pages that the linting tool expects. This is a routine integration step for new documentation pages, not a flaw in the content or structure of the series itself. The fix will likely involve updating a configuration file or script that enumerates the valid man page names, ensuring the linting tool recognizes `gitmergeconflicts` as a valid target.

The report is brief and factual, with no substantive discussion of the guide’s content or design. It serves as a heads-up to the author (Julia Evans) that one final adjustment is needed before the series can graduate from `seen` to `next`. The tone suggests this is a minor, expected step in the integration process. Reviewers or downstream consumers should treat this as a technical loose end rather than a red flag about the series’ readiness.