This is a documentation patch discussion, refining the rationale for the ongoing cross-reference modernization in Git's man pages. Jeff King (Peff) replies to Julia Evans' proposed commit message revision, confirming its accuracy and clarifying a minor technical detail about where the quoted rendering issue appears.

The email does not alter the patch's status—it remains approved and queued in `next`—but it provides a final technical endorsement. Peff notes that the quoted rendering issue (e.g., `"EXAMPLES"`) occurs only in man pages, not HTML output, and includes additional context ("the section called"). This distinction is minor and does not affect the patch's core justification for using the two-part AsciiDoc link form (`<<target,text>>`). The discussion remains focused on the trade-off between link stability (manual anchors) and readability (auto-generated anchors), though no new options or objections are raised.

Key technical details carried forward:
- The patch touches 38 files in `Documentation/`, replacing informal references with formal AsciiDoc cross-references.
- The two-part form (`<<target,text>>`) is necessary for consistent rendering across AsciiDoc and Asciidoctor backends.
- The patch uses manual anchors (e.g., `[[EDITING_PATCHES]]`), preserving existing HTML fragment identifiers but diverging from toolchain conventions.
- The anchor style trade-off (link stability vs. readability) remains an open design question, though the current patch is uncontroversial and ready for integration.

The email advances the thread by providing a final technical clarification, but no substantive changes or new concerns are introduced. The patch is on track for `next`, and the broader discussion about anchor style may continue separately.