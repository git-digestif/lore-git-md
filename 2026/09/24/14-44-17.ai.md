This is the second patch in Julia Evans' seven-patch series introducing `gitmergeconflicts(7)`, a new man page consolidating merge conflict resolution guidance. The patch surgically removes the "HOW CONFLICTS ARE PRESENTED" and "HOW TO RESOLVE CONFLICTS" sections from `git-merge.adoc` (122 lines deleted) and replaces them with a single cross-reference to the new `gitmergeconflicts(7)` guide added in the first patch.

The change is mechanical and precise: the removed sections are exactly the ones now covered in the new guide, and the replacement link is inserted in the same location where conflict-resolution advice was previously given. The patch also adds `linkgit:gitmergeconflicts[7]` to the `SEE ALSO` section at the end of the file, ensuring the new guide is discoverable from the `git merge` man page.

This patch is uncontroversial in isolation, but it concretely demonstrates the series' goal of eliminating duplication. The removed text is now centralized in `gitmergeconflicts(7)`, which reviewers will likely scrutinize for accuracy and completeness. The patch itself is unlikely to draw substantive feedback beyond stylistic nitpicks (e.g., placement of the cross-reference), but it serves as a tangible example of the series' refactoring approach. Future patches will apply the same pattern to `git-rebase`, `git-revert`, `git-cherry-pick`, and `git-pull`.