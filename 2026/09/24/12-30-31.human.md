This is a documentation patch, part of the ongoing effort to modernize cross-references in Git's man pages. Julia Evans responds to Jeff King's (Peff) technical review, which confirmed the necessity of the two-part AsciiDoc link syntax (`<<target,text>>`) for consistent rendering across backends but also highlighted a trade-off: the patch changes some HTML fragment identifiers (e.g., from `_examples` to `EXAMPLES`), potentially breaking existing external links.

Julia acknowledges the link stability issue and outlines three options: (1) accept the breakage as-is, (2) manually align anchors with auto-generated IDs (e.g., `_editing_patches`) to preserve link compatibility, or (3) find a way to use the simpler `<<PRUNING>>` syntax without rendering quirks. She leans toward option 1 for practicality but notes the ideal solution would be option 3, though she lacks a clear path to implement it. Her revised commit message now explicitly states the trade-off—better HTML navigation at the cost of some broken links—and clarifies the rationale for the two-part syntax (to avoid rendering artifacts like `[EXAMPLES]` in HTML).

The patch remains approved and queued for `next`, but this exchange surfaces a broader documentation maintenance question: should the project prioritize link stability (manual anchors) or readability (auto-generated anchors or simpler syntax)? The discussion is now framed as a conscious design choice rather than a technical oversight, with no immediate resolution. Julia’s response is pragmatic and transparent, focusing on the trade-offs rather than dismissing the concern. For readers, this patch is a well-motivated improvement to man page usability, but the anchor style question may resurface in future documentation work.