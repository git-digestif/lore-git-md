This is a documentation patch, the final step in a minor grammar cleanup. Junio C Hamano replies to the second version (`v2`) sent by first-time contributor parovozik, pointing out that the patch still lacks a real-name Signed-off-by trailer. The patch itself is uncontroversial and already addresses the only technical feedback from the first round (removing unnecessary commas in the `--` option descriptions of `git-rm` and `git-add` man pages). Junio’s reply is brief and procedural: he explains where the changelog should go in a single-patch series and reiterates the project’s real-name policy. Once the author resends with a real name, the patch is expected to be accepted.