This is a documentation patch, the second iteration of a trivial grammar fix. The author (parovozik) addresses Junio C Hamano’s feedback from the first version by removing unnecessary commas in two locations: `Documentation/git-add.adoc` and `Documentation/git-rm.adoc`, exactly where `git grep` had revealed the same phrasing. The Signed-off-by trailer still uses the pseudonym “parovozik” instead of the contributor’s real name, so the patch remains out of compliance with `Documentation/SubmittingPatches`; Junio will almost certainly request a resend once he notices.

The patch is purely stylistic, touching only AsciiDoc markup in the man-page synopses. No new symbols, behavior changes, or test coverage are introduced. It applies cleanly and is uncontroversial; the only remaining blocker is the real-name requirement. Once that is fixed, the patch is likely to be accepted.