Here’s a concise human-readable summary of the Git mailing list thread covering Junio’s September 2026 "What’s cooking" report:

The thread is Junio C Hamano’s authoritative update on the state of all in-flight topics for the upcoming Git 2.56 release, now in its final release candidate phase. The most notable news is the project’s shift to a new versioning scheme: Git 2.56 will be followed by Git 2.98, skipping intermediate numbers. No new feature topics are expected before 2.56 final, so the focus is on stabilizing what’s already queued.

Key developments include a mix of fixes, optimizations, and refactoring efforts. Several topics are now cooking in `next`—meaning they’re under integration testing and likely to graduate to `master` soon. These include a fix for a `git pull` segfault (`jk/pull-null-merge-head`), a protocol v2 ordering fix for shallow clones (`rr/upload-pack-swap-shallow-wanted-ref`), and a major refactoring of repository initialization (`ps/setup-enforce-repo-passed-to-create-repository-has-no-state`). The `ps/ref-storage-format` series has also matured, standardizing terminology around reference storage formats and extending the `--ref-storage-format` option to accept payloads.

A few topics are still under active discussion or awaiting review. Patrick Steinhardt’s Meson build improvements (`ps/meson-improvements`) and a new centralized merge conflicts guide (`je/doc-merge-conflicts`) are notable examples. The `mm/diff-process-hunks` series, which introduces external hunk providers via `diff.<driver>.process`, remains a significant but contentious addition. Meanwhile, stalled topics like `fz/rebase-autosquash-empty` (handling empty commits during autosquash) haven’t seen recent activity.

No topics have been merged to `master` yet, as the 2.56-rc2 cycle is still open. The report also highlights smaller fixes and CI improvements, such as Rust support for GitLab’s Windows CI (`js/gitlab-ci-windows-rust`) and a fix for unit tests not running on Windows (`kn/ci-unit-tests-on-windows-fix`). The overall tone suggests a focus on polish and stability as the project prepares for the 2.56 release.