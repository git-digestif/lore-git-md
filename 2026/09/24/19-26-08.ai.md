This is a **CI/build system** clarification reply in the ongoing thread fixing GitLab CI breakage for Windows after Rust was enabled. Johannes Schindelin responds to Patrick Steinhardt’s substantive review question about the "GNU Rust" toolchain introduced in the first patch.

The exchange addresses the single open technical question from the prior review: what "GNU Rust" means and why the MinGW build requires a different Rust target (`gnu`) than the MSVC build (`msvc`). Johannes clarifies that "GNU Rust" refers to the Rust toolchain producing GCC-compatible output (as opposed to Visual C-compatible libraries), not a distinct GNU project. He acknowledges the poor wording in the original commit message and commits to rewording it using `git history reword`. He also confirms Patrick’s observation that the current MinGW CI job uses `install-sdk.ps1` rather than `install-dependencies.ps1`, and agrees to clarify this in the commit message.

The reply is narrowly scoped to the toolchain selection question and does not introduce new technical concerns or alter the series’ direction. The clarification is straightforward and resolves the ambiguity Patrick raised, leaving no loose ends on this point. The series remains validated by Karthik Nayak’s internal GitLab pipeline, and the unrelated GitLab Runner regression (credential-clearing timeout) is still with GitLab’s infrastructure team. Future discussion may focus on Patrick’s second substantive question—whether enabling the `Gcc` feature in Rust’s MSI installer introduces a redundant GCC toolchain—but that topic is not addressed in this email.