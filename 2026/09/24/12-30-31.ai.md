This is a documentation patch in the ongoing effort to modernize cross-references in Git's man pages. Julia Evans responds to Jeff King's (Peff) substantive review, which identified why the two-part AsciiDoc link form (`<<target,text>>`) is necessary for consistent rendering across backends. The email does not alter the patch's integration status—it remains approved and queued in `next`—but it clarifies the trade-offs around link stability and readability.

Julia acknowledges Peff's findings about the edge cases in AsciiDoc/Asciidoctor rendering (e.g., duplicated text in man pages or `[REMOTES]` in HTML) and proposes a revised commit message that explicitly states the rationale for the two-part form. She also outlines three options for handling HTML fragment identifiers: (1) accept breaking some external links (current approach), (2) manually align anchors with auto-generated IDs (error-prone and confusing), or (3) find a way to use the single-form (`<<PRUNING>>`) reliably (ideal but unclear how to implement). Her preference leans toward option 1, citing the lower effort involved, but she leaves the decision open for further discussion.

The email advances the thread by surfacing the anchor style trade-off as a conscious design choice rather than an oversight. While the patch itself is uncontroversial and ready for integration, the broader question of whether to prioritize link stability (manual anchors) or readability (auto-generated anchors) remains unresolved. This could spark follow-up discussion, though no immediate action is required—the current patch is still on track for `next`. The revised commit message improves clarity but does not change the technical content of the patch.