This is a CI/build system bugfix targeting Windows. The patch corrects a mismatch in test-slice indexing that prevented unit tests from running in GitLab and GitHub CI workflows.

The issue stems from commit 3141df7ec4 (ci: don't skip smallest test slice in GitLab, 2026-02-19), which switched CI workflows to use one-indexed slices for Windows tests. However, the unit test execution logic in `ci/run-test-slice.sh` still checked for slice "0", causing it to be skipped entirely. The fix updates the condition to check for slice "1" instead, aligning it with the new indexing scheme.

The change is minimal—just a two-line modification to `ci/run-test-slice.sh`—and directly addresses the regression. The commit message clearly explains the problem and solution, and the patch is based on a recent fix (2d486439fe) that included cargo-related adjustments for Windows. This looks like a straightforward, well-motivated correction for a CI oversight.