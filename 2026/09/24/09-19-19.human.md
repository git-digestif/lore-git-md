This is the first patch in Patrick Steinhardt’s seven-part refactoring series to simplify the `create_repository()` interface. It’s a small, mechanical cleanup in the path-handling subsystem.

The patch removes `safe_create_leading_directories_1()`, a helper function that was just a thin wrapper around `safe_create_leading_directories()`. The wrapper added no value—it was literally a one-line call to the wrapped function—so Patrick inlines it into the two places it was used. The change touches only `path.c` and reduces the code by six lines.

This is a straightforward refactoring with no behavior change. It’s clearly motivated by the desire to reduce indirection before the larger interface changes that follow in the series. The commit message is concise and to the point, and the patch itself looks clean. No controversy or open questions here; it’s a safe, incremental step toward the series’ overall goal.