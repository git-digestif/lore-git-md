This is the sixth patch in Julia Evans' seven-part documentation series introducing `gitmergeconflicts(7)`, a new man page consolidating merge conflict resolution guidance. The series aims to replace scattered, duplicated advice across multiple command pages with a single, authoritative resource.

This patch updates `git-pull.adoc` to replace a brief mention of conflict-abort commands (`git merge --abort` or `git rebase --abort`) with a cross-reference to the new `gitmergeconflicts(7)` guide. The change is minimal—just two lines—but it completes the pattern established in earlier patches (for `git merge`, `git rebase`, `git revert`, and `git cherry-pick`), ensuring users are consistently directed to the centralized guide for conflict-resolution help.

The patch is uncontroversial and mechanically sound. It follows the same approach as the rest of the series: removing redundant, command-specific conflict advice and replacing it with a pointer to the new guide. The cross-reference is correctly formatted and placed, and the change aligns with the series' goal of reducing maintenance burden while improving user experience. No new questions or edge cases are raised here; this is a straightforward application of the established refactoring.

With this patch, the series is nearly complete. Only the final patch (updating `git-merge-file.adoc`) remains. The series as a whole is well-motivated and addresses a clear gap in Git's documentation. Reviewers have already engaged with earlier patches on technical accuracy and man-page conventions, so this patch is unlikely to spark new discussion. It’s a small but necessary step in delivering a more cohesive and user-friendly documentation experience.