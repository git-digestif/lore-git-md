This is a documentation patch series introducing a new `gitmergeconflicts(7)` man page. The series adds a dedicated guide explaining merge conflict resolution with examples, then updates existing command man pages (`git merge`, `git rebase`, `git revert`, `git cherry-pick`, and `git pull`) to link to the new guide instead of duplicating conflict-resolution advice.

The motivation is clear: Git’s current documentation scatters merge-conflict guidance across multiple pages, often repeating or omitting key details. The new page consolidates this information, addresses user-reported pain points (e.g., confusion about "ours" vs. "theirs"), and provides practical examples. Julia Evans explicitly cites feedback from Git users (via Mastodon polls and direct comments) as the driving force behind the design choices, such as de-emphasizing `*_HEAD` references and prioritizing `diff3`/`zdiff3` context over low-level merge-state details.

The series touches 9 files, including a new `Documentation/gitmergeconflicts.adoc` (294 lines) and surgical updates to existing man pages to replace redundant sections with cross-references. The commit messages note open questions (e.g., whether `git commit` and `git merge --continue` are always equivalent, or if `git apply`/`git am` conflicts should be covered), signaling areas where reviewer input is sought. The patch also adds the new file to `.gitattributes`, `Makefile`, and `meson.build` to ensure it’s processed correctly.

This is a well-motivated, user-focused effort. The new guide’s structure—prioritizing examples and practical workflows over technical minutiae—aligns with the project’s recent push to improve documentation clarity (e.g., Jean-Noël Avila’s synopsis-style conversion). The series is likely to spark discussion about edge cases (e.g., patch application conflicts) and man-page conventions (e.g., the role of the `SYNOPSIS` section in non-command guides), but the core idea—a centralized conflict-resolution resource—addresses a long-standing gap. Reviewers will likely focus on the accuracy of technical details (e.g., `diff3` vs. `zdiff3` explanations) and the balance between brevity and completeness in the new page.