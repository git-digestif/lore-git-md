This is a CI/build system bugfix addressing a Windows-specific regression in Git's CI infrastructure. The patch has received a tested, substantive review from Patrick Steinhardt, who confirms the fix aligns the unit test execution logic with the one-indexed slice scheme introduced in commit 3141df7ec4.

The issue was straightforward: after CI workflows switched to one-indexed slices for Windows tests, the unit test runner in `ci/run-test-slice.sh` still checked for slice "0", causing unit tests to be skipped entirely. The fix updates the condition to check for slice "1" instead, a minimal two-line change that directly resolves the regression. Patrick's review explicitly acknowledges missing this detail during the original slice-indexing change, and his "The diff looks good to me" carries weight given his domain expertise in CI infrastructure.

No open questions remain, and the patch is self-contained with no dependencies beyond the earlier slice-indexing commit. The commit message clearly explains the problem and solution, and the fix is already validated by Patrick's review. This appears to be a clean, uncontroversial correction that should restore unit test coverage on Windows CI workflows. The only file touched is `ci/run-test-slice.sh`, and the change is limited to the slice-indexing logic for unit tests.