This is a brief procedural follow-up in the `http.sslVerifyStatus` thread. Junio C Hamano acknowledges SZEDER Gábor’s two test-infrastructure suggestions: tightening the `SSL_VERIFYSTATUS` prerequisite’s error-message check to look for the absence of “could not enable OCSP status verification” (the clearer second half of the error message) and renaming the prerequisite from `SSL_VERIFYSTATUS` to `CURL_SSL_VERIFYSTATUS` to better reflect its scope (libcurl support, not server support). Junio agrees both changes are reasonable and understandable. No new issues are raised; the thread remains focused on test robustness rather than the core feature. The next step is for the author (graysongordon-gl) to post a follow-up patch implementing these two minor improvements. The feature itself remains merged in `master` and is not in question.