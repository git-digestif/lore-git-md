This is a brief design discussion reply in the ongoing thread about improving `git fetch` behavior for shallow repositories. Junio C Hamano acknowledges that introducing a `fetch.<remote>.refmap` configuration (as previously proposed) would naturally enable the desired behavior where `git fetch second main` automatically creates a remote-tracking branch (`refs/remotes/second/main`) without requiring explicit refspecs. This aligns with the consensus reached earlier in the thread, where the `remote.X.refmap` design was endorsed as the right approach to implement the "matching-inspired" fetch mode.

The email is a small but meaningful clarification: it confirms that the `fetch.<remote>.refmap` configuration (a subset of the broader `remote.X.refmap` design) would handle the specific case of single-branch fetches elegantly, preserving backward compatibility while enabling the new dynamic behavior. This removes any lingering ambiguity about how the feature would work in practice. The discussion remains focused on implementation details, with no new objections or open questions introduced. The upcoming v3 patch series is expected to implement this design.