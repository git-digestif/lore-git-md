This is a platform-compatibility report from Ramsay Jones testing Git’s experimental Rust support on Cygwin. The email documents Ramsay’s attempt to build Git with the `NO_RUST` flag disabled, using an unofficial Cygwin Rust package (version 1.91.0) marked as experimental and unmaintained.

Ramsay’s build succeeds on the current `master` branch (v2.56.0-rc2 plus one commit), producing a working `git` binary. The Rust compiler (`cargo`) is invoked to build `libgitcore.a`, and the resulting binary passes all static checks (`sparse`, `static-check.pl`, `hdr-check`). The test suite fails in the same two places as the non-Rust build (unit test 254 and `t9904-url-parse.sh`), indicating no new regressions from the Rust code. Ramsay notes that the Rust compiler is only lightly exercised by the current codebase but calls the result “slightly encouraging.”

The context is the Git 3.0 timeline discussion, where Rust support is a stated goal but not guaranteed due to portability concerns—particularly for platforms like NonStop and, now, Cygwin. Ramsay’s report provides concrete evidence that Rust can compile on Cygwin, though the toolchain’s experimental status and lack of official support remain caveats. He also mentions that Brian Carlson likely has more extensive Rust code in a branch, hinting at future testing opportunities.

Ramsay closes by noting he will be unavailable for some time due to surgery, so follow-up testing will be delayed. The email does not propose changes or request action; it is purely informational, offering data for the ongoing Rust-in-Git discussion.