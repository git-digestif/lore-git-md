This is a maintainer review of a small feature patch for the `git-contacts` contrib script, now in its third iteration. Junio C Hamano responds to Brigham Campbell’s v3 implementation of the `-` idiom for stdin input with a concrete alternative design. The review is substantive: it proposes a different code structure that avoids introducing a separate `$read_from_stdin` flag and instead treats `-` as a special filename throughout the code path. Junio’s suggestion would push the stdin logic into `scan_patch_file` rather than scattering it across argument parsing, making the behavior more localized and potentially easier to maintain. The patch is still narrowly scoped to `contrib/contacts/git-contacts` and does not introduce new symbols or CLI options, but the review raises a design question about how to best integrate the `-` idiom into the existing codebase. No functional objections are raised; this is a stylistic and structural refinement. The thread remains under review, with the ball back in the author’s court to either adopt Junio’s suggestion or explain why the current approach is preferable.