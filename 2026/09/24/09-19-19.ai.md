This is the first patch in Patrick Steinhardt’s seven-patch series refactoring `create_repository()` to enforce stateless input. It is a small, mechanical cleanup in the path subsystem.

The patch removes the helper function `safe_create_leading_directories_1()`, which was just a thin wrapper around `safe_create_leading_directories()`. Both callers (`safe_create_leading_directories()` and `safe_create_leading_directories_no_share()`) now call the main function directly. The change is purely internal to `path.c`; no behavior or interface changes are introduced. The diff is minimal—nine lines removed, three lines adjusted—and the commit message clearly explains the redundancy.

This patch is uncontroversial and looks like a straightforward tidy-up. It sets the stage for the later patches in the series, which will introduce new variants of these directory-creation helpers. No review feedback has appeared yet, but the change is unlikely to draw objections. The only file touched is `path.c`.