This is a follow-up in the documentation cross-reference modernization thread. Jeff King (Peff) provides a detailed technical explanation of why the two-part AsciiDoc link form (`<<EXAMPLES,EXAMPLES>>`) is necessary, clarifying the rendering behavior across different backends (AsciiDoc vs. Asciidoctor) and output formats (HTML vs. man pages). His analysis traces the issue to the docbook layer, where the single-form link (`<<EXAMPLES>>`) is expanded into verbose or inconsistent text (e.g., `the section called "EXAMPLES"` or `[EXAMPLES]`), while the two-part form bypasses this by explicitly providing the link text.

The email is purely explanatory and does not propose changes to the patch itself, which remains approved and queued in `next`. Peff’s breakdown confirms Julia Evans’s earlier rationale and adds precision about the underlying toolchain behavior, though he notes this level of detail isn’t needed in the commit message. This contribution strengthens the technical foundation of the patch by documenting the edge cases and implementation quirks for future reference. The discussion remains focused on the already-resolved design choice (two-part links) and does not reopen the broader anchor style trade-off (manual vs. auto-generated anchors). No further action is required on this thread unless someone later revisits the docbook layer to override the default rendering.