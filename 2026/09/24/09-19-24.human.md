This is part six of Patrick Steinhardt’s seven-patch refactoring series that prepares the `create_repository()` interface for a stateless input. The patch overhauls `repo_clear()` to guarantee that every field in the `struct repository` is zeroed out, eliminating any risk of stale state leaking between calls. The change is mechanical—replacing individual `FREE_AND_NULL()` calls with plain `free()` followed by a final `memset()`—but it is a prerequisite for the final patch, which will enforce that `create_repository()` starts with a blank slate. The commit message is clear, the diff is straightforward, and the patch looks like a clean, low-risk step toward the series’ goal.