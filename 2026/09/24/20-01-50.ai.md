This is the first patch in Johannes Schindelin’s v2 CI/build system series, fixing GitLab CI breakage for Windows builds after Rust support was enabled. It prepares the `ci/install-dependencies.ps1` script to provision the correct Rust toolchain for MinGW-based builds by introducing a `-Mingw` switch that selects the `gnu` target (GCC-compatible) instead of the default `msvc` target.

The patch moves the Rust installation logic earlier in the script and makes it conditional on the `-Mingw` flag, ensuring the MinGW build environment gets the appropriate toolchain. The existing Rust installation for MSVC builds remains unchanged but is now deferred until after the MinGW-specific block. This change is purely preparatory—it does not yet alter the CI pipeline’s behavior, as the MinGW job still uses `install-sdk.ps1` rather than `install-dependencies.ps1`. The follow-up patches will activate this logic by updating the job configuration.

The commit message is clear and well-motivated, explaining why the `gnu` target is necessary for MinGW builds and how the script’s structure accommodates both MinGW and MSVC environments. The patch itself is small and focused, touching only the dependency installation script. No new symbols or behavior changes are introduced at this stage. The use of the `-Mingw` switch and the `gnu` Rust target are key technical details for follow-up discussion in the thread.