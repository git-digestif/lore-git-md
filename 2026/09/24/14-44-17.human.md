This is the second patch in Julia Evans' seven-part series introducing `gitmergeconflicts(7)`, a new man page consolidating merge conflict resolution guidance. The patch updates `git-merge.adoc` to replace its lengthy, duplicated conflict-resolution section with a concise cross-reference to the new guide.

The change is surgical: 122 lines of redundant documentation are removed, and a single link to `gitmergeconflicts(7)` is added near the top of the "HOW CONFLICTS ARE PRESENTED" section. The commit message is minimal but clear: "All of the info about merge conflicts has been moved to the new guide." This aligns with the series' goal of centralizing conflict-resolution advice to reduce maintenance burden and improve user experience.

The patch is well-structured and uncontroversial. It preserves the existing flow of the `git merge` man page while eliminating duplication, and the cross-reference is placed where users would naturally look for conflict-resolution guidance. The removed section covered conflict markers, resolution workflows, and tooling (e.g., `git mergetool`, `git diff AUTO_MERGE`)—all of which are now addressed in the new guide introduced in the first patch.

Reviewers are likely to focus on whether the remaining text in `git-merge.adoc` still provides enough context for users who might not immediately follow the link to `gitmergeconflicts(7)`. For example, the patch retains the brief mention of `--abort` and `--continue` but removes the detailed explanation of how to use them. However, this trade-off is intentional and consistent with the series' design: the new guide is meant to be the authoritative resource, while command-specific pages provide only high-level signposts.

This patch is a clean, mechanical step in the series. It doesn’t introduce new technical content or edge cases, so substantive feedback is unlikely. The main discussion points will probably revolve around whether the remaining text in `git-merge.adoc` is sufficient to orient users before they click through to the new guide. If the series lands, this patch will be a straightforward win for documentation clarity.