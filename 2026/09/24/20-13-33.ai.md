This is the **author's reply** in the ongoing architectural discussion about the reference-transaction hook regression fix. Maciej Ciemborowicz acknowledges Junio C Hamano’s request for clarity in the commit message and explains that `refs_delete_refs()` lacks a parameter to pass pre-resolved old OIDs into the transaction, making them unavailable to the hook. He then signals a **major pivot**: the v6 series will abandon the v5 approach of extending `refs_delete_refs()` and instead adopt Patrick Steinhardt’s suggestion to resolve missing old OIDs in the transaction hook layer itself. This sidesteps the API debate entirely but introduces a new design (on-demand resolution of old OIDs) that has not yet been posted or discussed in detail.

The reply is brief and does not address the performance trade-off of resolving old OIDs at the hook layer (which could add latency for large batches) or Junio’s request for concrete code comparisons. The thread is now poised for the v6 series, which will touch the transaction system, refs subsystem, and all ref-modifying commands (`git branch -d`, `git tag -d`, `git remote prune`, `git fetch --prune`, etc.), restoring pre-regression hook behavior without modifying `refs_delete_refs()` or its callers. Key technical details to watch: the new mechanism for caching hook-observed old OIDs, its impact on transaction semantics, and whether the performance overhead is quantified.