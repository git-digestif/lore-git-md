This is a **CI/build system** follow-up in the thread working around Debian 12’s HTTP/2 authentication failures in Git’s CI. Jeff King (Peff) replies to Johannes Schindelin’s confirmation that the bug was introduced in curl 7.88.0 (not 7.88.1) and approves the adjusted version range (7.88.0–8.3.0) in the upcoming patch iteration. His brief "looks good to me" signals the version-range-aware runtime prereq (`HAVE_CURL_HTTP2_BUG`) is now technically settled.

The patch targets `t/t5551-http-fetch-smart.sh`, marking `t5559.15` and `t5559.16` (half-auth clone tests) with `!HAVE_CURL_HTTP2_BUG` to skip them on affected curl versions. The version boundary correction (7.88.0 instead of 7.88.1) tightens the prereq’s scope without changing the fundamental approach. No production code or on-disk formats are touched; this remains a minimal, test-only workaround. The thread’s earlier debate—CI-only skip vs. runtime prereq—is now resolved in favor of the version-range runtime check, with Peff’s feedback incorporated. The patch is ready for integration.