This is a **CI/build system** patch thread reaching closure. Jeff King (Peff) reviews and approves Johannes Schindelin’s latest iteration of the patch to work around Debian 12’s HTTP/2 authentication failures in Git’s CI. The patch now correctly identifies curl 7.88.0 as the first affected version, refining the version-range prereq (`HAVE_CURL_HTTP2_BUG`) that skips the flaky `t5559.15` and `t5559.16` tests on curl 7.88.0 through 8.3.0.

The patch is narrowly scoped to the test suite (`t/t5551-http-fetch-smart.sh`) and introduces no production code or on-disk format changes. It uses a runtime version check (via `build_option` and `awk`) to detect the problematic curl versions, ensuring the workaround applies only where needed. Peff’s approval signals the implementation is technically sound and ready for integration, with no remaining open questions about version boundaries or approach. The thread’s earlier discussion about trade-offs between CI-only skips and runtime prereqs is now settled in favor of the version-range-aware runtime solution.