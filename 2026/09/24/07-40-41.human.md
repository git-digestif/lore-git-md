This is a documentation patch from a first-time contributor. The author resends the patch (v2) to address Junio C Hamano’s feedback on the initial version: the unnecessary comma in the description of the `--` option is now removed from both `git-add` and `git-rm` man pages, and the Signed-off-by trailer still uses the pseudonym "parovozik" instead of a real name. The patch is trivial, uncontroversial, and touches only two documentation files. Once the author fixes the Signed-off-by line, it should be ready for merging.