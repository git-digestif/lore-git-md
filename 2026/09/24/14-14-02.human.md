This is a CI/build system bugfix for Windows. The patch itself was already well-explained in the original submission: it fixes a regression where unit tests were being skipped on Windows CI due to a mismatch between one-indexed test slices (introduced in an earlier commit) and the zero-indexed logic in the test script.

Patrick Steinhardt’s reply is brief but substantive. He acknowledges missing the issue during the original change and confirms the fix is correct. The review is minimal—just a "looks good"—but given the narrow scope of the patch (a two-line change to a CI script) and the clear explanation in the commit message, this is sufficient. The patch is self-contained, addresses a real problem, and has no open questions. It’s ready to merge once Junio picks it up.