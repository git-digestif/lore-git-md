Thread identity: Documentation patch series introducing `gitmergeconflicts(7)`, a new man page consolidating merge conflict resolution guidance. Series version: initial (v1). Originating author: Julia Evans (via GitGitGadget). Thread type: patch series.

Core problem or goal: Git’s current documentation scatters merge-conflict advice across multiple command man pages (`git merge`, `git rebase`, `git revert`, `git cherry-pick`, `git pull`), leading to duplication, omissions, and user confusion. The series introduces a dedicated `gitmergeconflicts(7)` guide to centralize this information, improve clarity (e.g., clarifying "ours" vs. "theirs"), and provide practical examples based on user feedback (e.g., Mastodon polls). The goal is to replace redundant sections in existing man pages with cross-references to the new guide.

Current status: Under review (v1 posted). No prior versions. The series adds a new `Documentation/gitmergeconflicts.adoc` (294 lines) and updates 8 existing man pages to link to it. Build system files (`.gitattributes`, `Makefile`, `meson.build`) are also updated to ensure the new page is processed correctly. Open questions include whether `git commit` and `git merge --continue` are always equivalent and whether `git apply`/`git am` conflicts should be covered.

Key technical details:
- Files touched: `Documentation/gitmergeconflicts.adoc` (new), `Documentation/git-{merge,rebase,revert,cherry-pick,pull}.adoc` (updates), `.gitattributes`, `Makefile`, `meson.build`.
- New symbols: `gitmergeconflicts(7)` man page (no CLI options or config keys).
- Behavior changes: Replaces duplicated conflict-resolution sections in existing man pages with cross-references to the new guide. No on-disk or runtime behavior changes.
- Test coverage: None (documentation-only series).

Open questions and loose ends:
1. Are `git commit` and `git merge --continue` always equivalent in conflict resolution? (Julia seeks reviewer input.)
2. Should `git apply`/`git am` conflicts be covered in the new guide? (Julia notes this as an open question.)
3. Accuracy of technical details (e.g., `diff3` vs. `zdiff3` explanations) and balance between brevity and completeness in the new page.

Participant positions: None yet (initial posting). Expected reviewers may focus on:
- Technical accuracy of conflict-resolution examples (e.g., correctness of `diff3`/`zdiff3` explanations).
- Man-page conventions (e.g., role of `SYNOPSIS` in non-command guides).
- Edge cases (e.g., patch application conflicts).

Related work: Aligns with Jean-Noël Avila’s ongoing synopsis-style conversion effort to improve documentation clarity. No dependencies or conflicts with other in-flight topics.