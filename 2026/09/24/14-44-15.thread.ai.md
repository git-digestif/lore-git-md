Thread identity: Documentation patch series introducing `gitmergeconflicts(7)`, a new man page consolidating merge conflict resolution guidance. Series version: v1 (initial, now complete). Originating author: Julia Evans (via GitGitGadget). Thread type: patch series.

Core problem or goal: Git’s current documentation scatters merge-conflict advice across multiple command man pages (`git merge`, `git rebase`, `git revert`, `git cherry-pick`, `git pull`), leading to duplication, omissions, and user confusion. The series introduces a dedicated `gitmergeconflicts(7)` guide to centralize this information, improve clarity (e.g., clarifying "ours" vs. "theirs"), and provide practical examples based on user feedback (e.g., Mastodon polls). The goal is to replace redundant sections in existing man pages with cross-references to the new guide, reducing maintenance burden and improving user experience.

Current status: Under review (v1 posted and complete). The series consists of seven patches: the first adds the new `gitmergeconflicts(7)` guide and build system updates, the second through sixth update `git-merge.adoc`, `git-rebase.adoc`, `git-revert.adoc`, `git-cherry-pick.adoc`, and `git-pull.adoc` to replace redundant conflict-resolution sections with cross-references to the new guide, and the seventh (this update) adds `Documentation/gitmergeconflicts.adoc` to `.gitattributes` with `conflict-marker-size=32` to ensure proper conflict detection during future edits. Changes between versions: N/A (initial version).

Key technical details:
- Files touched: `Documentation/gitmergeconflicts.adoc` (new, added in first patch), `.gitattributes` (this patch), `Makefile`, `meson.build` (first patch), `Documentation/git-merge.adoc`, `Documentation/git-rebase.adoc`, `Documentation/git-revert.adoc`, `Documentation/git-cherry-pick.adoc`, `Documentation/git-pull.adoc`.
- New symbols: `gitmergeconflicts(7)` man page (no CLI options or config keys).
- Behavior changes: Replaces duplicated conflict-resolution sections in existing man pages with cross-references to the new guide. No on-disk or runtime behavior changes. The new guide is purely additive and does not alter existing functionality.
- Structure of the new guide: Organized as a practical, example-driven document with sections covering:
  - When merge conflicts occur (`git merge`, `git rebase`, `git cherry-pick`, `git pull`, `git revert`).
  - Resolution paths (fix or abort).
  - Merge conflict markers (`<<<<<<<`, `=======`, `>>>>>>>`) and the four-step resolution process (identify, edit, stage, continue).
  - Tools for handling conflicts (graphical merge tools, `merge.conflictstyle=diff3`/`zdiff3`, `git show :1:filename`).
  - Terminology clarification ("ours" vs. "theirs") and edge cases (e.g., `git rebase` inverts "ours"/"theirs").
  - `git diff AUTO_MERGE` for tracking progress.
  - Side-by-side examples of `diff3` vs. `zdiff3` output.
  - Sample `git status` output to guide users on `--abort`/`--continue` commands.
- Retained command-specific content: The series preserves explanations of options unique to individual commands (e.g., `--skip` for `git rebase`, `--no-commit` for `git cherry-pick`), while centralizing general conflict-resolution advice in the new guide.
- Pull-specific details: The updated `git-pull.adoc` replaces a brief mention of aborting merge conflicts with a cross-reference to the new guide, which covers resolution tools and examples but does not explicitly mention pull-specific edge cases (e.g., `--no-commit` or `--no-ff` conflicts).
- `.gitattributes` addition: The final patch adds `Documentation/gitmergeconflicts.adoc conflict-marker-size=32` to `.gitattributes`, aligning with existing practice for other documentation files to ensure proper conflict detection during future edits.

Open questions and loose ends:
1. Are `git commit` and `git merge --continue` always equivalent in conflict resolution? (Julia seeks reviewer input.)
2. Should `git apply`/`git am` conflicts be covered in the new guide? (Julia notes this as an open question.)
3. Accuracy of technical details (e.g., correctness of `diff3`/`zdiff3` explanations and examples).
4. Balance between brevity and completeness in the new guide (e.g., whether the conversational tone and example-heavy approach are appropriate for a man page).
5. Man-page conventions (e.g., role of `SYNOPSIS` in non-command guides like `gitmergeconflicts(7)`).
6. Whether the cross-reference placement in updated man pages (e.g., `git-pull.adoc`) is optimal for user discoverability.
7. Sufficiency of retained command-specific details (e.g., whether the `--skip` explanation for `git rebase` is adequate or if additional pull-specific guidance, such as `--no-commit` or `--no-ff` edge cases, should be preserved).
8. Potential omissions in the new guide (e.g., whether `git pull --no-commit` conflicts or `MERGE_HEAD`/`REBASE_HEAD` behavior warrant explicit mention).

Participant positions: None yet (initial posting and follow-ups). Expected reviewers may focus on:
- Technical accuracy of conflict-resolution examples (e.g., correctness of `diff3`/`zdiff3` output representations).
- Man-page conventions and style (e.g., whether the guide’s conversational tone aligns with project norms, placement of cross-references).
- Completeness of coverage (e.g., whether `git apply`/`git am` conflicts or pull-specific edge cases like `--no-commit` should be included).
- Mechanical correctness of the refactoring (e.g., whether all redundant sections are removed and cross-references are correctly inserted).

Related work: Aligns with Jean-Noël Avila’s ongoing synopsis-style conversion effort to improve documentation clarity. No dependencies or conflicts with other in-flight topics. The series is self-contained and does not interact with code changes.