This is a **translation (l10n)** patch adding a complete Afrikaans translation for `git-gui`. The v2 iteration addresses the procedural feedback raised in the initial review: the Afrikaans language (`af`) is now registered in `po/meson.build`, and the `po/af.po` file has been stripped of source location metadata (the line numbers like `#: git-gui.sh:793` that were present in v1). The translation itself remains unchanged—it still covers all 2,733 strings in the `git-gui` interface, from menu items and dialogs to error messages and tooltips.

The patch is purely additive, touching only the `po/` directory, and has no functional impact on Git’s behavior. It follows the standard `.po` file conventions and includes translator metadata. With the procedural requirements now met, the patch is ready for a final review by Afrikaans speakers or the l10n maintainers to verify translation accuracy. No technical objections have been raised, and the changes align with the project’s existing localization workflow.