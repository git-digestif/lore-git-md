This thread introduces a new `gitmergeconflicts(7)` man page, authored by Julia Evans, to consolidate Git’s scattered merge-conflict documentation into a single, user-focused guide. The series replaces redundant conflict-resolution advice in existing man pages (`git merge`, `git rebase`, `git revert`, etc.) with cross-references to the new guide, which prioritizes practical examples and workflows over low-level technical details. The motivation stems from user feedback (via Mastodon and direct comments), particularly around confusion over "ours" vs. "theirs" and the role of `diff3`/`zdiff3` context.

The patch touches nine files, including the new 294-line `Documentation/gitmergeconflicts.adoc`, and updates build configurations to ensure proper processing. Open questions remain about edge cases (e.g., whether `git commit` and `git merge --continue` are always equivalent, or if `git apply`/`git am` conflicts should be included), signaling areas where reviewer input is sought. The series aligns with the project’s broader push for clearer documentation, though it may spark discussion about man-page conventions (e.g., the role of `SYNOPSIS` in non-command guides) and the balance between brevity and completeness. No reviews or decisions have been recorded yet.