This is a **CI/build system** follow-up in the ongoing thread about working around Debian 12’s HTTP/2 authentication failures in Git’s CI. Johannes Schindelin replies to Jeff King’s (Peff) earlier clarification about the version-range trade-offs, confirming that the bug was introduced in curl 7.88.0 (not 7.88.1) after further investigation. He states he will adjust the version range in the next patch iteration to reflect this new information.

The core technical detail here is the corrected version boundary: the affected range now starts at 7.88.0 instead of 7.88.1, which tightens the scope of the workaround slightly. This adjustment does not change the fundamental approach (runtime prereq vs. CI-only skip) but refines the version check logic. The thread’s unresolved question—whether to adopt the original CI-only skip, Peff’s single-version prereq, or the version-range prereq—remains open, though this reply edges the discussion closer to consensus on the version-range approach with accurate bounds. Key symbols: `HAVE_CURL_HTTP2_BUG` prereq, curl 7.88.0–8.3.0 range, `t5559.15`/`t5559.16` tests.