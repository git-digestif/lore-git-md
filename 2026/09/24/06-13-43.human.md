This is a CI/build system patch under review, part of a two-patch series addressing resource exhaustion in GitHub Actions. Patrick Steinhardt raises two substantive questions about the first patch, which replaces `test_cmp` with `test_cmp_bin` and deletes temporary files early in `t4205-log-pretty-formats.sh`.

Patrick asks for evidence of the failure (a link to a CI log showing the OOM kill) and for benchmark data comparing peak RSS and runtime between `test_cmp` and `test_cmp_bin`. He also questions the practical benefit of early file deletion, noting that the files are overwritten shortly afterward in subsequent tests. The review is substantive but not yet critical; it seeks clarification rather than rejecting the approach outright.

The patch itself remains a targeted fix for a real CI bottleneck, but the review highlights that the justification could be stronger. No objections to the technical direction have been raised—only requests for more data. The thread is still open, and the author’s next step will likely be to provide the requested evidence.