This is a **test-infrastructure follow-up** in the `http.sslVerifyStatus` security feature thread. Patrick Steinhardt replies to SZEDER Gábor’s post-merge test failure report with a diagnostic question: whether Gábor’s system might be using a libcurl backend that does not fully support OCSP stapling. Steinhardt notes that even if the backend lacks support, the test suite should detect this and skip the relevant tests rather than failing. The question is aimed at isolating the root cause of the test failure Gábor observed — whether it stems from an environmental difference (e.g., libcurl backend) or a gap in the test’s prerequisite logic. No new technical issues with the core feature are introduced; the focus remains on test robustness.