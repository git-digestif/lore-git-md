This is a **CI/build system** follow-up in the thread working around Debian 12’s HTTP/2 authentication failures in Git’s CI. Junio C Hamano replies to Johannes Schindelin’s confirmation that the bug was introduced in curl 7.88.0 (not 7.88.1), asking for a simple clarification: is the affected range now 7.88.0 through 8.3.0? This is a minor but necessary detail to finalize the version-range-aware runtime prereq (`HAVE_CURL_HTTP2_BUG`) that Peff proposed as an alternative to the original CI-only skip.

The thread is now very close to resolution. The core diagnosis (early 401 responses treated as transport errors by curl 7.88.0–8.3.0) and the preferred implementation (runtime prereq over CI-only skip) are settled; only the exact version bounds remain to be confirmed. Junio’s question is purely mechanical—he is not revisiting the trade-offs or the approach, just ensuring the version check in the next patch iteration is accurate. The affected tests (`t5559.15` and `t5559.16`) and the workaround’s scope (no production behavior changes) remain unchanged.