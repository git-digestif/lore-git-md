This is a **CI/build system** follow-up in the thread working around Debian 12’s HTTP/2 authentication failures in Git’s CI. Junio C Hamano replies to Johannes Schindelin’s confirmation that the bug was introduced in curl 7.88.0 (not 7.88.1), asking for a simple clarification: whether the affected version range is now correctly understood as 7.88.0 through 8.3.0.

The email is a brief, surface-level check-in—no new technical details, no objections, and no alternative proposals. It serves as a final confirmation before Johannes sends the next patch iteration, ensuring the version boundary is accurate. The thread’s unresolved question (which implementation to adopt) remains unchanged, but this reply signals Junio’s readiness to proceed once the version range is settled. Key symbols: curl 7.88.0–8.3.0 range, `HAVE_CURL_HTTP2_BUG` prereq, `t5559.15`/`t5559.16` tests.