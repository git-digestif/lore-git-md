This is a brief follow-up in the ongoing CI/build system thread fixing GitLab CI breakage for Windows builds after Rust was enabled. The series as a whole is narrowly focused on restoring pipeline functionality for GitLab's MinGW and MSVC jobs, with no changes to Git's core behavior.

Patrick Steinhardt raises a substantive question about the fourth patch in the series, which adds the `Gcc` feature to the Rust MSI installation for MinGW builds. His concern is whether this introduces redundancy: the MinGW environment already provides GCC, and now Rust's MSI is also installing its own version. The question touches on a potential overlap in toolchain dependencies and whether the fix might create unnecessary duplication or conflicts in the build environment.

The patch itself is minimal and well-justified in its immediate goal—restoring the ability to link host executables like `build.rs`—but Patrick's question highlights a deeper design consideration. The answer could influence how the project manages toolchain dependencies in CI, particularly as Rust adoption progresses. No resolution is offered in this email; it simply flags the issue for further discussion.

This is a targeted technical inquiry rather than a critique of the patch's correctness. The series remains under review, with no objections to its core purpose, but this point may prompt Johannes Schindelin to clarify the rationale for the `Gcc` feature in the MinGW context. For now, the question is open and worth watching in follow-up replies.