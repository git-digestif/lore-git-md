This is a documentation patch addressing AsciiDoc cross-reference formatting in Git's man pages. Julia Evans revises the commit message for her previously approved patch to clarify why the two-part AsciiDoc link form (`<<EXAMPLES,EXAMPLES>>`) is necessary, responding to Junio C Hamano's request for editorial refinement.

The patch itself remains unchanged and is still queued in `next`. The update focuses solely on explaining the rendering behavior: the short form (`<<EXAMPLES>>`) can produce inconsistent output like `the section called "EXAMPLES"` or `[EXAMPLES]` in HTML, while the two-part form ensures predictable rendering as plain `EXAMPLES`. Julia acknowledges some uncertainty about the exact conditions triggering these edge cases but confirms the two-part form reliably avoids them. Jeff King (Peff) previously validated this behavior through testing with both AsciiDoc and Asciidoctor backends.

This is part of the ongoing documentation modernization effort led by Jean-Noël Avila, though the patch stands alone as a systematic improvement to cross-references across 38 man pages. The broader discussion about anchor style trade-offs (manual vs. auto-generated) remains unresolved but is not reopened here. The only remaining action is this commit message update, which should finalize the patch for integration. The contribution is well-tested and uncontroversial, with no technical changes expected.