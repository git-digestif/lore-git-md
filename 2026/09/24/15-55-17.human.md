This is a documentation patch, part of the ongoing effort to modernize cross-references in Git’s man pages. The email is a review reply from Jeff King (Peff) that wraps up the discussion on the patch originally sent by Julia Evans. The patch itself remains approved and queued in `next`; this exchange does not alter its integration status but clarifies and endorses the technical rationale.

Peff confirms Julia’s revised commit message, which explains why the two-part AsciiDoc link form (`<<EXAMPLES,EXAMPLES>>`) is necessary: it avoids rendering quirks in both HTML and man page output, where single-form links (`<<EXAMPLES>>`) can produce broken or nonsensical text like `[EXAMPLES]` or duplicated section names. The discussion also surfaces the trade-off between link stability (manual anchors like `[[EXAMPLES]]`) and readability (auto-generated anchors like `_examples`), but both agree the current approach—accepting some broken external links—is acceptable for now. Peff notes that any future change to align with AsciiDoc’s auto-generated IDs would be a separate effort.

The patch touches 38 files in `Documentation/`, systematically replacing informal references with formal cross-references. No functional changes to man page content are introduced; the improvement is purely navigational in HTML-rendered output. The patch is well-tested: Julia built all man pages before and after, and Peff independently verified the behavior across both AsciiDoc and Asciidoctor backends. The thread leaves one open design question—whether to prioritize link stability or readability—but the current patch is uncontroversial and ready for integration. This email effectively closes the review loop with a clear endorsement.