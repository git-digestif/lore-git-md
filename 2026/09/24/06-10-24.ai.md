This is a **surface-level review** in the regression-fix thread for `git commit --amend` during interactive rebase. Johannes Sixt (Hannes) notes a minor localization concern: reverting commit 6257588252 will reintroduce a translatable error message ("You are in the middle of a rebase -- cannot amend.") that was previously removed from `.po` files by translators after the original series (0f8e75abebff) was merged. This is a housekeeping detail rather than a technical objection; it does not affect the decision to revert or the patch’s correctness. The observation is aimed at the l10n coordinator (Jiang Xin) and has no bearing on the core regression or architectural discussion. No code changes or test updates are involved.