This is a **platform compatibility and translation (l10n) heads-up** in the `git commit --amend` regression fix thread. Johannes Sixt (Hannes) points out that the original series (commit 0f8e75abebff) added a translatable error message that will reappear if the regressing commit (6257588252) is reverted. Translators may have already removed this string from their `.po` files, so reintroducing it could create extra work for the l10n teams.

The observation is brief and technical, targeting the practical consequences of the revert rather than the regression itself. It does not propose a solution or oppose the revert, but it flags a minor coordination issue for the Git 2.56 release cycle. The thread remains focused on the revert as the agreed path forward, with no new code or architectural changes introduced here.