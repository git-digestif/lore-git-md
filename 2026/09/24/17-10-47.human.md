This is a design discussion reply in the ongoing thread about improving `git fetch` behavior for shallow repositories. D. Ben Knoble acknowledges Junio C Hamano’s clarification about the long-standing "single-shot fetch" semantics and agrees that the proposed `remote.X.refmap` configuration is the right approach for the matching-inspired fetch mode.

The exchange confirms that the design will proceed with `remote.X.refmap` as the mechanism for defining namespace mappings, separating it from the dynamic branch selection logic. D. Ben Knoble’s earlier suggestion—automatically creating remote-tracking branches for named remotes—is now off the table due to backward compatibility concerns, but no objections are raised to the refmap-based design itself. The thread remains focused on refining the implementation details for the v3 patch series, which will likely include the refmap configuration, dynamic branch enumeration, and heuristics for shallow repositories.

The discussion is constructive and aligns with the earlier consensus: the feature is still under active design, with no fundamental disagreements about its direction. The next step is for Harald Nordgren to send a v3 series implementing Junio’s proposed four-commit plan.