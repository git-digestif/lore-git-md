This thread is a 7-patch series from Patrick Steinhardt aimed at improving Git’s Meson build system—speeding up clean builds, fixing test failures, and stabilizing GitLab CI. The first six patches (already summarized) tackle build optimizations (reducing clean build time by ~35%), shell completion test fixes, and subproject updates. The just-posted seventh patch resolves a long-standing GitLab CI hang in MSVC jobs by disabling PortableGit’s default credential helper, which was causing cleanup operations to block indefinitely.

The series is now complete and under review. No feedback has arrived yet, so the patches remain unmerged. Key optimizations include refactoring HTTP sources into a static library (8% speedup), avoiding redundant recompilation for HTTP aliases (1%), and introducing precompiled headers for test helpers and unit tests (24% combined speedup). The CI fix is minimal and Windows-specific, with no impact on other platforms. If you’ve been waiting for faster Meson builds or more reliable CI, this series delivers both—once it lands.