This thread is about a small but important fix to Git’s CI infrastructure on Windows. Karthik Nayak identified a regression where unit tests were being skipped due to a mismatch between one-indexed test slices (introduced in a recent commit) and the zero-indexed logic in the CI script. The patch is a minimal two-line change to `ci/run-test-slice.sh` that aligns the script with the new indexing scheme, restoring unit test coverage on Windows CI workflows.

Patrick Steinhardt, a key contributor with deep CI expertise, reviewed the patch and confirmed it resolves the issue. His review noted that the oversight in the original slice-indexing change was his own, and he endorsed the fix as correct. There are no open questions or dependencies—the patch is self-contained and directly addresses the regression. Given the straightforward nature of the fix and Patrick’s substantive review, this looks ready to merge. The thread is effectively resolved, pending Junio’s integration.