This is a substantive review of the first patch in Tamir Duberstein’s CI resource-exhaustion series. Patrick Steinhardt engages with the technical rationale and questions two aspects of the proposed fix.

The review asks for evidence: a link to a CI failure showing the SIGKILL, and benchmark data comparing `test_cmp_bin` to `test_cmp` in terms of peak RSS and runtime. Without these, the claim that `test_cmp_bin` is a better choice remains unsupported. Patrick also notes that the early file deletion may not reclaim as much memory as the commit message suggests, since the files are rewritten shortly afterward in subsequent tests. The question is whether the deletion is worth the added complexity if the memory is only freed briefly.

The thread context shows this is the first review of the series; no other feedback has been recorded. The exchange is technical and focused on the patch’s effectiveness, not its style or correctness. If Tamir provides the requested data, the discussion is likely to converge quickly; if not, the patch may need revision or a stronger justification.