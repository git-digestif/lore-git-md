**Summarizer brief mode**

This is a **substantive review reply** in the ongoing discussion about the first patch of Maciej Ciemborowicz’s three-patch bugfix series (v5), which refactors `refs_delete_refs()` to allow callers to supply old OIDs for batch deletion. The series addresses a regression in the reference-transaction hook where all-zero OIDs are passed on branch/tag deletion, breaking external tools like Gerrit and GitLab.

Junio C Hamano responds directly to Karthik Nayak’s **substantive technical concern** about the safety of replacing `is_null_oid()` checks with `NULL` in `ref_transaction_delete()`. Karthik had argued that a zeroed-out `null_oid` typically signals a user bug (e.g., uninitialized `struct object_id` or ignored return codes), and removing the `BUG()` macro weakens safety by masking potential misuse. Junio counters this by pointing out a **legitimate use case**: when callers use an `object_array` to selectively pass object names, a pointer to `null_oid` is a valid way to represent "N/A" for optional parameters. He argues that forcing callers to convert `null_oid` to `NULL` manually would add unnecessary complexity, and the callee (`ref_transaction_delete()`) can handle this conversion centrally without sacrificing safety.

The reply **does not resolve the architectural debate** about whether `refs_delete_refs()` should remain a simple wrapper or support conditional logic, nor does it address Patrick Steinhardt’s broader concerns about the patch’s design. However, it **defends the implementation choice** to treat `null_oid` and `NULL` equivalently, which is critical for the patch’s correctness. The discussion remains focused on the **low-level API semantics** of `ref_transaction_delete()` and how callers interact with it.

Key technical details referenced:
- **Function**: `ref_transaction_delete()`
- **Data structures**: `struct object_id`, `object_array`
- **Symbols**: `null_oid`, `NULL`
- **Prior concern**: Karthik’s argument that `is_null_oid()` → `NULL` weakens safety by masking misuse
- **Junio’s counterargument**: `null_oid` is a valid representation of "N/A" in selective object passing, and centralizing the conversion in the callee is cleaner than forcing callers to handle it.

The reply is **high-weight** due to Junio’s role as the maintainer and his direct engagement with Karthik’s technical objection. It does not dismiss the concern outright but provides a rationale for the current implementation, which may influence whether the patch requires further changes before graduating from `next` to `master`. The thread remains open, with the architectural direction (Patrick’s concerns) and other technical nits (redundant failure counting, loop variable) still unresolved.