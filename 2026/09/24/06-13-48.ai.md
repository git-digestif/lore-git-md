This is a substantive CI/build system review reply in the ongoing thread about reducing resource exhaustion in GitHub Actions. The thread targets Linux runners where the full test suite—including long-running tests—is enabled for pull requests, leading to out-of-memory (OOM) kills and "no space left on device" (ENOSPC) errors.

Patrick Steinhardt’s reply focuses on the second patch, which dynamically sets parallelism (`-j`) for `make` and `prove` to the number of available CPU cores on Linux runners. He raises two key questions: first, he requests a link to a CI failure demonstrating the ENOSPC issue (echoing his earlier request for evidence in Patch 1); second, he asks for benchmark data comparing CI runtime before and after the change, and whether oversubscribing (running more jobs than CPU cores) might be a better trade-off. He also suggests aligning GitLab and GitHub CI logic, noting there’s no clear reason for the two to differ.

The review is substantive: it engages with the patch’s technical rationale, questions the performance impact, and proposes broader consistency across CI platforms. It does not contest the problem (ENOSPC errors on small runners) but seeks data to validate the solution. The tone is constructive, and the questions are precise—typical of Patrick’s thorough, evidence-driven style. No tested review yet; the focus is on design and measurement. The ball is now in Tamir’s court to provide the requested links and benchmarks.