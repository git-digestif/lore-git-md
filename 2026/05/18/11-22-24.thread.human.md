This thread is about Erik Cervin-Edin's patch series that extends `git commit --fixup` to support non-interactive message specification via `-m`, `-F`, `-c`, and `-C` options across all `--fixup` variants. The series is now complete at version 3, with both patches posted and all prior feedback addressed.

The key improvement in this latest update is the second patch, which enables `-c` and `-C` options for all `--fixup` variants, closing the last gap in the feature matrix. The implementation is clean, reusing existing message-handling logic and maintaining backward compatibility—editors still launch when no message options are provided. The patch removes redundant option-checking code and extends the fixup logic to handle commit references from `-c`/`-C`, mirroring the behavior of `--fixup=amend`. Documentation and test coverage have been updated to reflect these changes, with new test cases ensuring consistency across all message specification methods.

The series has been thoroughly reviewed, with no remaining open questions or objections. Junio Hamano has actively solicited final review, and Phillip Wood's prior feedback on code reuse and test hygiene has been incorporated. Erik has dogfooded the feature for months, confirming its production readiness. The series is now under active review for merging into Junio's integration branches.