Git thread summary:

This thread covers Erik Cervin-Edin's patch series enhancing `git commit --fixup` to support non-interactive message specification via `-m`, `-F`, `-c`, and `-C` options for all `--fixup` variants (`--fixup`, `--fixup=amend:`, `--fixup=reword:`). The series is now at v3 (2/2 patches posted), with all prior feedback addressed, including the final code hygiene issue identified in v2.

Current status:
- v3 series (2/2) fully posted, completing the feature matrix by enabling `-c` and `-C` for all `--fixup` variants.
- All substantive feedback from v1 and v2 addressed: author identity consistency, terminology, code reuse, test hygiene, redundant option-checking logic, and the now-obsolete `die_for_incompatible_opt3()` function.
- Backward compatibility maintained (editor launches when no message options provided).
- Junio Hamano explicitly solicited final review for the second patch after it sat unreviewed in v2.
- Implementation now considered production-ready with no remaining open questions or loose ends.

Key technical details:
- Unified message handling through `prepare_to_commit()` for all variants and message options (`-m`, `-F`, `-c`, `-C`).
- Special handling for amend-style messages (strips "amend!" lines) to ensure clean final commit messages.
- Comprehensive test coverage in `t/t7500-commit-template-squash-signoff.sh` and `t/t7501-commit.sh`, including new cases for `-C` with plain `--fixup`, `-c` with `--fixup=amend:`, message equivalence between `-C` and interactive amend, and nested squash/amend scenarios.
- Documentation updates (`git-commit.adoc`) reflecting expanded capabilities for all four message specification methods.
- Preserves message origin information for debugging.
- Files touched: `builtin/commit.c` and test suite only; no changes to core data structures or on-disk formats.
- Complete feature matrix: all four message specification methods (`-m`, `-F`, `-c`, `-C`) work consistently across all three `--fixup` variants (`--fixup`, `--fixup=amend:`, `--fixup=reword:`).

Reviewer positions:
- **Junio Hamano**: All prior concerns addressed; actively solicited final review for the second patch in v2; no new comments in v3. The series is now awaiting final maintainer review for integration.
- **Phillip Wood**: Prior suggestions on code reuse and test hygiene implemented; sequencer/rebase expertise applied to message handling. No outstanding objections.

The series is production-ready, with Erik having dogfooded the feature for several months. The implementation maintains high code quality, thorough test coverage, and consistent behavior across all message specification methods. No open questions or design objections remain. The series is under active review for merging.