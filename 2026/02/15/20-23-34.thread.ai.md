Thread identity: Bugfix patch series (2 patches) addressing `git fetch --prune-tags` behavior inconsistency between documentation and implementation. Originating author is Orgad Shaneh.

Core problem: The `--prune-tags` flag is documented to perform both (1) pruning local tags not present on remote and (2) force-updating tags that differ between local and remote, but current implementation only does (1). Historical analysis shows this may stem from timing - the flag was added (commit 97716d21) before tag clobbering protection (commit 0bc8d71b), suggesting an oversight rather than intentional design. This creates practical issues where tag conflicts cause fetch failures.

Current status: Series at v1, awaiting resolution on design intent. First patch adds test coverage for `git fetch --force` behavior with tags (currently only verifies exit status, not tag content). Second patch modifies `builtin/fetch.c` to pass `--force` flag to tag-fetching logic when `--prune-tags` is active. Junio C Hamano has raised concerns about historical intent and test coverage sufficiency but has not yet revisited the thread after acknowledging the need to refresh his memory; Orgad Shaneh has responded to test coverage feedback and maintains the behavior was likely unintentional.

Key technical details:
- First patch adds test in `t5516-fetch-push.sh` verifying `--force` behavior with tags (exit status only)
- Second patch changes `builtin/fetch.c` to pass `TAG_FETCH_FORCE` flag in prune-tags case (one-line change)
- Existing pruning tests in `t/t5510-fetch.sh` cover tag deletion but not force-update
- Original implementation by Ævar Arnfjörð Bjarmason (2018); tag clobbering protection added later same year
- No new symbols or on-disk format changes

Open questions:
- Was the current behavior (no force-update) an intentional design choice by Ævar?
- Should the code change to match docs, or docs change to match code?
- Is the current test coverage sufficient, or should it verify tag content after force-update?
- Potential addition of `fetch.force` config option as alternative solution

Participant positions:
- Orgad Shaneh: Author proposing code change to match documentation; provides historical context suggesting likely oversight; responsive to test coverage feedback; maintains position that behavior was unintentional
- Junio C Hamano: Maintainer questioning historical intent and test coverage sufficiency; has not yet ruled on substance; acknowledged need to refresh memory before further comment
- Ævar Arnfjörð Bjarmason: Original implementer (not yet participating in thread)

Related work: Standalone fix not tied to larger efforts. The fetch machinery and tag handling are stable subsystems with few recent changes that would interact with this fix. Resolution awaits input from Ævar or other fetch/tag experts to clarify original design intent.