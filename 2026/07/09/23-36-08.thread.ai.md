Here is the dense, loss-free AI-mode summary for the next invocation:

---
Thread identity: patch series introducing unified `post-worktree` hook (subcommand-style: `add`, `remove`, `move`) as a compromise to replace the original `post-worktree-add`, `post-worktree-remove`, and `post-worktree-move` hooks; series version 2 under design revision; originating author Domen Kožar; thread type patch series.

Core problem or goal: provide a reliable, Git-native mechanism for notifying external tools (IDEs, AI agents, devenv) of worktree lifecycle events (creation, removal, movement) in a way that is observable even when the event is triggered by an uncontrolled caller (e.g., user running `git worktree remove` directly or IDE invoking `git worktree prune`). The compromise proposal narrows the interface to a single hook with subcommands, addressing Junio C Hamano’s design objection about hook necessity while retaining the core observability requirement.

Current status: under review; version 2 design in flux following Junio’s fundamental objection; Domen has proposed a compromise (single `post-worktree` hook with `add`/`remove`/`move` subcommands) and explicitly seeks Junio’s feedback on whether this meets the bar for a Git-native hook. Series remains in `seen` (proposed updates) pending resolution of the design question. No merge status change; Junio’s objection remains unresolved.

Key technical details: proposed unified `post-worktree` hook with subcommand-style interface (`post-worktree add`, `post-worktree remove`, `post-worktree move`). Hook signature: `post-worktree <subcommand> <worktree-id> <worktree-path> [<old-path>]` (old-path only for `move`). Runs in the worktree directory for `add` and `move` (with `GIT_DIR` and `GIT_WORK_TREE` cleared), in the main worktree for `remove`. Exit status propagates to caller but does not affect operation outcome. Covers all removal paths (manual deletion, `git worktree prune`, `git worktree remove`), including damaged entries (empty string for path if gitdir file is missing/corrupt). Implementation would touch `builtin/worktree.c` (hook invocation points), `worktree.c`/`worktree.h` (path/ID resolution), and `Documentation/githooks.adoc` (documentation). Test coverage would extend `t/t2400-worktree-add.sh`, `t/t2401-worktree-prune.sh`, and `t/t2403-worktree-move.sh`. Changes from v2: (1) replaces three separate hooks with one unified hook; (2) passes worktree ID and path for all subcommands (addressing Caleb White’s consistency feedback); (3) retains prune coverage and edge-case behavior for `remove`.

Open questions and loose ends: (1) **design justification**: Junio C Hamano’s fundamental objection remains unresolved. Domen’s compromise argues observability (racing with other operations, damaged entries, uncontrolled callers) and ties the proposal to emerging use cases (coding agents automating worktree lifecycle), but Junio has not yet responded to this counterargument. (2) undocumented relative order of `post-worktree add` and `post-checkout` (Phillip Wood’s prior request remains unaddressed); (3) whether the compromise sufficiently narrows the interface to meet Junio’s bar for a native hook (Domen explicitly seeks clarity before further iteration); (4) whether the subcommand-style interface is preferable to separate hooks (no substantive feedback yet).

Participant positions:
- Domen Kožar (author): proposes compromise (unified `post-worktree` hook with subcommands) to address Junio’s design objection, arguing observability requirements and emerging use cases (IDEs, AI tools, devenv) justify a Git-native hook. Cites real-world pain points (racing, damaged entries, uncontrolled callers) as evidence that wrappers are insufficient. Open to further iteration but seeks explicit feedback from Junio on whether the compromise meets the bar for acceptance.
- Junio C Hamano (maintainer, substantive review): has not yet responded to Domen’s compromise or the observability argument. Prior objection challenged the necessity of any hooks, arguing user-written wrappers can achieve the same functionality. His critique applies to the unified hook proposal until explicitly withdrawn or revised.
- Phillip Wood (substantive review): prior feedback (unconditional execution of `post-worktree-add`, hook execution order documentation) remains relevant but unaddressed in the compromise. No new position on the unified hook.
- Caleb White (surface-level): prior feedback (passing worktree path/ID to `post-worktree-add` for consistency) is addressed by the compromise. No substantive engagement with Domen’s observability arguments or the unified hook design.

Related work: none mentioned.
---