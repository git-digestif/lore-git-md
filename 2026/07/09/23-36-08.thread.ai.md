Here is the dense, loss-free AI-mode summary for the next invocation:

---
Thread identity: patch series introducing unified `post-worktree` hook (subcommand-style: `add`, `remove`, `move`) as a compromise to replace the original `post-worktree-add`, `post-worktree-remove`, and `post-worktree-move` hooks; series version 2 under design revision alongside Kristoffer Haugsbakk’s `--id=<string>` counterproposal; originating author Domen Kožar; thread type patch series with competing and potentially complementary design approaches.

Core problem or goal: provide a reliable, Git-native mechanism for notifying external tools (IDEs, AI agents, devenv) of worktree lifecycle events (creation, removal, movement) in a way that is observable even when the event is triggered by an uncontrolled caller (e.g., user running `git worktree remove` directly, IDE invoking `git worktree prune`, or manual deletion). The discussion now explores two distinct but not necessarily mutually exclusive approaches: Domen’s unified `post-worktree` hook (subcommand-style) for observability, and Kristoffer’s `--id=<string>` option for ownership-based enforcement. Domen argues that observability (hooks) and ownership (IDs) address different but complementary needs—hooks enable tools to react to events they do not control, while IDs prevent tools from interfering with each other’s worktrees.

Current status: under active design discussion; no merge status change. Domen’s version 2 compromise remains in `seen` (proposed updates) pending resolution of Junio C Hamano’s fundamental objection. Kristoffer’s counterproposal is now positioned as potentially complementary rather than mutually exclusive, though no concrete integration has been proposed. Junio’s objection to hooks remains unresolved; no feedback yet from Junio, Domen, or other participants on the framing of hooks and IDs as complementary. The thread is evolving into a design discussion about whether and how to combine observability and ownership mechanisms.

Key technical details:
- Domen’s proposal: unified `post-worktree` hook with subcommand-style interface (`post-worktree add`, `post-worktree remove`, `post-worktree move`). Hook signature: `post-worktree <subcommand> <worktree-id> <worktree-path> [<old-path>]` (old-path only for `move`). Runs in the worktree directory for `add` and `move` (with `GIT_DIR` and `GIT_WORK_TREE` cleared), in the main worktree for `remove`. Exit status propagates to caller but does not affect operation outcome. Covers all removal paths (manual deletion, `git worktree prune`, `git worktree remove`), including damaged entries (empty string for path if gitdir file is missing/corrupt). Implementation touches `builtin/worktree.c` (hook invocation points), `worktree.c`/`worktree.h` (path/ID resolution), and `Documentation/githooks.adoc`. Test coverage extends `t/t2400-worktree-add.sh`, `t/t2401-worktree-prune.sh`, and `t/t2403-worktree-move.sh`.
- Kristoffer’s counterproposal: `--id=<string>` option for `git worktree add`, storing the ID in the worktree and enforcing ownership. Any `git worktree` command (remove, prune, move) would only act on worktrees that either lack an ID or match the provided `--id`. No new hooks, no new config, no on-disk format changes beyond storing the ID. Tools can implement additional metadata (e.g., expiry timestamps) on top of the ID without further Git changes. Implementation would touch `builtin/worktree.c` (ID handling), `worktree.c`/`worktree.h` (ID storage/retrieval), and `Documentation/git-worktree.txt` (documentation). Test coverage would focus on ID enforcement in `t/t2400-worktree-add.sh` and `t/t2401-worktree-prune.sh`.
- Domen’s clarification: hooks and IDs serve distinct but complementary purposes. Hooks provide observability for tools that do not own the worktrees (e.g., devenv reacting to `git worktree prune` or manual deletion), while IDs prevent interference between tools that do own worktrees (e.g., IDEs or agents). No concrete proposal yet for integrating the two approaches, but Domen suggests they could coexist.

Changes from prior summary: (1) updated core problem/goal to reflect Domen’s clarification that hooks and IDs address complementary needs (observability vs. ownership); (2) revised current status to note that the proposals are now seen as potentially complementary rather than mutually exclusive; (3) expanded key technical details to include Domen’s framing of hooks and IDs as distinct but complementary mechanisms; (4) updated open questions to reflect the new framing and unresolved integration points.

Open questions and loose ends:
- **Design justification (Domen’s proposal)**: Junio C Hamano’s fundamental objection remains unresolved. Domen’s compromise argues observability requirements (racing with other operations, damaged entries, uncontrolled callers) and ties the proposal to emerging use cases (coding agents automating worktree lifecycle), but Junio has not yet responded to this counterargument or the framing of hooks and IDs as complementary.
- **Design justification (Kristoffer’s counterproposal)**: whether the ID-based ownership model sufficiently addresses the original problem (uncontrolled callers) without hooks, and whether it can coexist with hooks. No feedback yet from Domen, Junio, or other participants on the viability of combining the two approaches or the trade-offs of doing so (e.g., complexity, edge cases).
- **Integration of hooks and IDs**: whether and how the two proposals could be combined. Domen suggests they are complementary but does not propose a concrete integration. Key unresolved questions include: (1) whether hooks should be extended to include ID information in their arguments; (2) whether IDs should be optional or mandatory for hook execution; (3) whether tools could opt into observability for worktrees they do not own (e.g., devenv reacting to an IDE-created worktree with an ID).
- Domen’s proposal: (1) undocumented relative order of `post-worktree add` and `post-checkout` (Phillip Wood’s prior request remains unaddressed); (2) whether the compromise sufficiently narrows the interface to meet Junio’s bar for a native hook (Domen explicitly seeks clarity before further iteration); (3) whether the subcommand-style interface is preferable to separate hooks (no substantive feedback yet).
- Kristoffer’s counterproposal: (1) whether the ID-based model is sufficiently flexible for all tooling use cases (e.g., whether tools can reliably manage worktrees without hooks); (2) whether the lack of observability for uncontrolled callers (e.g., manual `rm -rf`) is an acceptable trade-off; (3) whether the ID storage mechanism introduces new edge cases (e.g., ID collisions, ID corruption).

Participant positions:
- Domen Kožar (author): proposes unified `post-worktree` hook (subcommand-style) to address observability requirements, arguing that hooks and ownership IDs serve complementary purposes—hooks for observability (tools reacting to events they do not control), IDs for ownership (tools preventing interference). Cites real-world pain points (racing, damaged entries, uncontrolled callers) as evidence that wrappers are insufficient. Open to further iteration but seeks explicit feedback from Junio on whether the compromise meets the bar for acceptance. No concrete proposal yet for integrating hooks and IDs.
- Junio C Hamano (maintainer, substantive review): has not yet responded to Domen’s compromise, the observability argument, the framing of hooks and IDs as complementary, or Kristoffer’s counterproposal. Prior objection challenged the necessity of any hooks, arguing user-written wrappers can achieve the same functionality. His critique applies to Domen’s unified hook proposal until explicitly withdrawn or revised; no position yet on the ID-based counterproposal or the complementary framing.
- Kristoffer Haugsbakk (substantive counterproposal): proposes replacing hooks entirely with an ID-based ownership model (`--id=<string>` option for `git worktree add`), arguing that ownership (not observability) is the real problem. No feedback yet on Domen’s framing of hooks and IDs as complementary or on the potential for integration.
- Phillip Wood (substantive review): prior feedback (unconditional execution of `post-worktree-add`, hook execution order documentation) remains relevant to Domen’s proposal but unaddressed in the compromise. No position yet on Kristoffer’s counterproposal or the complementary framing.
- Caleb White (surface-level): prior feedback (passing worktree path/ID to `post-worktree-add` for consistency) is addressed by Domen’s compromise. No substantive engagement with Domen’s observability arguments, the unified hook design, or Kristoffer’s counterproposal.

Related work: none mentioned.
---