This thread is about a patch series by Paulius Zaleckas that adds a configurable option (`fetch.submoduleErrors`) to control whether `git fetch --recurse-submodules` fails hard or just warns when a submodule’s gitlink points to an unreachable OID. The series also fixes a pre-existing logic flaw where submodule fetch errors were recorded prematurely, even if a later retry could succeed.

The current version (v6) is technically complete and addresses all prior feedback, including Junio Hamano’s mechanical requests about array indexing and naming. The series touches `submodule.c`, `fetch.c`, documentation, and tests, and is marked as "Needs review" in Junio’s "What's cooking" report with no merge branch assigned. Junio has explicitly invited additional reviewers to step in, as he hasn’t performed a full review yet.

Paulius recently pinged the thread to re-engage prior reviewers, specifically Ramsay Jones (who noted a test portability issue now fixed) and Jean-Noël Avila (who contributed documentation fixes). The series is narrowly scoped and appears ready for `next` once it gets broader review. No new technical concerns have surfaced, and the implementation preserves backward compatibility by defaulting to `fail`.