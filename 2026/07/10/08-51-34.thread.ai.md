Thread identity: Patch series replacing the `GIT_NO_LAZY_FETCH=fromAccepted` proposal with `uploadpack.lazyFetchTrusted`, a server-side protected configuration variable to mark repositories as trusted for lazy fetching. Series version: v3. Originating author: Christian Couder.

Core problem or goal: Provide a server-side mechanism for operators to explicitly declare which repositories are safe to lazy-fetch from, addressing the security objection that untrusted repositories could trigger arbitrary code execution via lazy-fetch hooks or configuration. The new approach shifts trust decisions entirely to the server operator, modeled after Git’s `safe.directory` configuration, and replaces the earlier client-side `fromAccepted` proposal.

Current status: Under review. The v3 series is a major refactoring of v2, addressing Junio’s API design critique and adding safeguards against infinite recursion. The series is correctly threaded (no misthreading issues) and rebased on recent `master` (3cb9185f65). No merge status reported; series remains in review, not in `seen`, `next`, or `master`. Junio’s prior substantive reviews of v2 (1/5 design/scalability, 3/5 API rigidity, 4/5 conceptual doubt, 5/5 documentation ambiguities) are partially addressed in v3: the callback-based `path_allowlist_apply()` resolves the API rigidity, and the recursion safeguard mitigates a key risk, but the scalability concern (memory overhead in `lazy_fetch_objects()`) and conceptual doubt about using a configuration variable remain unresolved. Documentation ambiguities (multi-valued syntax, repository specification format) are clarified in v3. The unresolved high-weight security objection from brian m. carlson persists but is mitigated by the server-side trust model.

Key technical details:
- Files touched: `promisor-remote.c` (refactoring in 1/5), `setup.c` (callback-based allowlist logic in 2/5, configuration reading in 4/5), `upload-pack.c` (configuration integration and recursion safeguard in 4/5 and 5/5), `git-upload-pack.adoc`, `git.adoc`, `Documentation/config/uploadpack.adoc` (documentation in 5/5), test script `t/t5710-promisor-remote-capability.sh` (expanded in 5/5).
- New or modified symbols: `path_allowlist_config_apply()` (callback-based wrapper for allowlist logic, replacing the rigid `path_allowlist_apply()` from v2), `upload_pack_lazy_fetch_trusted()` (checks repository trust status, now using the callback-based allowlist), `GIT_INTERNAL_LAZY_FETCH_DEPTH` (new environment variable to limit recursion depth). The `lazy_fetch_objects()` helper in `promisor-remote.c` is refactored to simplify control flow (addressing Junio’s v2 stylistic feedback). Repositories are now identified by their git directory (e.g., `/srv/repo/.git`) rather than worktree path, affecting configuration syntax.
- New behavior: `uploadpack.lazyFetchTrusted` (protected configuration variable) allows server operators to mark repositories as trusted for lazy fetching. The configuration is read from protected files only, and relative paths (including `.`) are rejected. The callback-based allowlist design enables caller-specific validation rules: `uploadpack.lazyFetchTrusted` requires absolute paths, while `safe.directory` retains its existing behavior. Recursion safeguards prevent infinite lazy-fetch loops when a repository is configured as its own promisor remote. Documentation clarifies that the configuration is multi-valued, specifies repository paths as git directories, and warns against self-referential promisor remotes. Tests verify recursion limits and path validation.

Open questions and loose ends: The unresolved high-weight security objection from brian m. carlson remains, though the server-side trust model directly addresses it. Junio’s v2 scalability concern about memory overhead in `lazy_fetch_objects()` (holding missing object names in memory) is unaddressed; the sketched streaming API alternative (`bulk_download_begin()`, etc.) remains unimplemented. Junio’s conceptual doubt about using a configuration variable for this purpose persists. The series is rebased and correctly threaded, but no new reviews have been posted since v3. CI tests pass except for an unrelated Debian 11 environment issue.

Participant positions: Christian Couder (author) has submitted v3, addressing Junio’s API design critique (callback-based allowlist), adding recursion safeguards, and clarifying documentation. Junio C Hamano’s prior reviews of v2 (1/5 design/scalability, 3/5 API rigidity, 4/5 conceptual doubt, 5/5 documentation ambiguities) are partially addressed: the API rigidity and recursion risks are resolved, but the scalability concern and conceptual doubt remain. No other substantive reviews have been posted for v3. The security objection from brian m. carlson predates v3 and remains unresolved but is mitigated by the server-side design.

Related work: The series builds on Git’s existing `safe.directory` mechanism and protected configuration infrastructure. It replaces the earlier client-side `GIT_NO_LAZY_FETCH=fromAccepted` proposal.