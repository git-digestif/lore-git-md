Thread identity
Subject: [PATCH 0/3] teach `git history` to sign rewritten commits
Series version: v3 (v1 and v2 previously)
Originating author: Souma
Thread type: patch series

Core problem or goal
Teach `git history` to sign rewritten commits (`drop`, `fixup`, `reword`, `split`) using the same configuration (`commit.gpgsign`) and command-line options (`-S/--gpg-sign`, `--no-gpg-sign`) already supported by `git commit`.

Current status
Series is at v3; no maintainer decision yet. All substantive review feedback from Patrick Steinhardt (v1 and v2) has been addressed: the replay API plumbing remains split into a dedicated preparatory patch (1/3), the documentation and tests are folded into the main feature patch (3/3 in v2, now 2/2 in v3), and the commit messages are now trimmed and the `OPT_HISTORY_GPG_SIGN` macro formatting fixed. The series remains under active review, with only surface-level cleanups requested in the latest round.

Key technical details
- Files touched: `builtin/history.c`, `replay.c`, `replay.h`, `Documentation/git-history.txt`, `t/t3451-history-reword.sh`, `t/t3452-history-split.sh`, `t/t3453-history-fixup.sh`, `t/t3454-history-drop.sh`
- New behavior: `drop`, `fixup`, `reword`, `split` now respect `commit.gpgsign`, `-S[=<key-id>]`, `--gpg-sign[=<key-id>]`, and `--no-gpg-sign`; command-line options override configuration, and the last option wins
- New symbols: `sign_commit` field in `replay_revisions_options` struct, plumbing through `pick_regular_commit`, `create_commit`, `commit_tree_ext`, `commit_tree_with_edited_message`, `split_commit`
- On-disk format: unchanged; only the commit object’s signature trailer is added when signing is enabled
- Test coverage: GPG-gated tests in `t3451`, `t3452`, `t3453`, `t3454` verify config-driven signing, command-line overrides, explicit key selection, and the no-new-commit case for `drop`; all flag/config combinations are covered

Open questions and loose ends
- **Conceptual trade-off**: whether signing rewritten commits that were originally authored by someone else is desirable or misleading; this is discussed in the commit message but remains unresolved.
- **Further iteration**: the series is likely to see additional minor changes based on ongoing review.

Participant positions
- **Patrick Steinhardt**: Substantive review in v1; requested mechanical cleanups (parameter ordering, config callback), a preparatory split of `replay.c` changes, and a discussion of the conceptual trade-off around signing others’ commits. These requests were fully addressed in v2. In v2, provided surface-level review: suggested trimming the commit message’s in-body documentation of the `sign_commit` parameter (patch 1/3), removing redundant commit message sentences (patch 2/3), and fixing the `OPT_HISTORY_GPG_SIGN` macro formatting. All v2 feedback has now been addressed in v3; no technical concerns raised; the review signals the patch is close to ready pending any remaining minor cleanups.
- **Souma**: Addressed all v1 and v2 feedback in v3, including splitting the replay API plumbing into a dedicated preparatory patch (1/3), folding documentation and tests into the main feature patch (2/2), trimming commit messages, and fixing the macro formatting.

Related work
None mentioned.