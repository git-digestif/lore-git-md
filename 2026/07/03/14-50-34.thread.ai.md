Thread identity
Subject: [PATCH 0/3] teach `git history` to sign rewritten commits
Series version: v2 (v1 previously)
Originating author: Souma
Thread type: patch series

Core problem or goal
Teach `git history` to sign rewritten commits (`drop`, `fixup`, `reword`, `split`) using the same configuration (`commit.gpgsign`) and command-line options (`-S/--gpg-sign`, `--no-gpg-sign`) already supported by `git commit`.

Current status
Series is at v2; no maintainer decision yet. Patrick Steinhardt’s v1 review feedback has been fully addressed: the replay API plumbing is now split into a dedicated preparatory patch (1/3), and the documentation and tests are folded into the main feature patch (2/3 in v2, previously 3/3 in v1). The series remains under active review, with only minor surface-level cleanups requested in the latest round.

Key technical details
- Files touched: `builtin/history.c`, `replay.c`, `replay.h`, `Documentation/git-history.txt`, `t/t3451-history-reword.sh`, `t/t3452-history-split.sh`, `t/t3453-history-fixup.sh`, `t/t3454-history-drop.sh`
- New behavior: `drop`, `fixup`, `reword`, `split` now respect `commit.gpgsign`, `-S[=<key-id>]`, `--gpg-sign[=<key-id>]`, and `--no-gpg-sign`; command-line options override configuration, and the last option wins
- New symbols: `sign_commit` field in `replay_revisions_options` struct, plumbing through `pick_regular_commit`, `create_commit`, `commit_tree_ext`, `commit_tree_with_edited_message`, `split_commit`
- On-disk format: unchanged; only the commit object’s signature trailer is added when signing is enabled
- Test coverage: GPG-gated tests in `t3451`, `t3452`, `t3453`, `t3454` verify config-driven signing, command-line overrides, explicit key selection, and the no-new-commit case for `drop`; all flag/config combinations are covered

Open questions and loose ends
- **Conceptual trade-off**: whether signing rewritten commits that were originally authored by someone else is desirable or misleading; this is now discussed in the commit message but remains unresolved.
- **Commit message trimming**: Patrick Steinhardt suggested trimming the in-body documentation of the `sign_commit` parameter in patch 1/3 (preparatory refactoring) and, in the latest round, removing two sentences from the commit message of patch 2/3: one about the `drop` edge case (no commit created, nothing to sign) and another summarizing test coverage. These suggestions have not yet been addressed.
- **Further iteration**: the series is likely to see additional changes based on ongoing review.

Participant positions
- **Patrick Steinhardt**: Substantive review in v1; requested mechanical cleanups (parameter ordering, config callback), a preparatory split of `replay.c` changes, and a discussion of the conceptual trade-off around signing others’ commits. These requests were fully addressed in v2. In v2, provided surface-level review on both patches: for patch 1/3, approved the changes but suggested trimming the commit message’s in-body documentation of the `sign_commit` parameter; for patch 2/3, flagged redundant commit message sentences and a style nit in the `OPT_HISTORY_GPG_SIGN` macro definition. No technical concerns raised; the review signals the patch is close to ready pending these minor cleanups.
- **Souma**: Addressed all v1 feedback in v2, including splitting the replay API plumbing into a dedicated preparatory patch (1/3), folding documentation and tests into the main feature patch (2/3), and discussing the conceptual trade-off in the commit message.

Related work
None mentioned.