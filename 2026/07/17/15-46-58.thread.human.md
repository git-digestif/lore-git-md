Here’s the human-mode narrative summary for a developer catching up on the thread:

The thread is about Toon Claes’s patch series to speed up Git’s experimental `git last-modified` command by reusing Bloom filters, bringing its performance close to `git log` for pathspec queries (including wildcards like `*.c`). The series is now at v4, which is complete and ready for integration after addressing all prior feedback—except for two remaining review comments from Patrick Steinhardt on patches 3/4 and 4/4.

The core optimization reuses Git’s Bloom filter machinery to avoid redundant work, achieving a 3.7× speedup (60.7 ms → 16.2 ms) without `--show-trees` and 1.95× (61.9 ms → 31.8 ms) with it on the ziglang/zig repository. Key changes include fixing a `--show-trees` correctness bug (where ancestor trees could leak into results), adding support for wildcard pathspecs, and instrumenting trace2 counters to verify the Bloom filter fast-path is triggered correctly. The series also resolves a test environment interaction that blocked v3 by explicitly controlling `GIT_TEST_COMMIT_GRAPH` in the test suite.

Patrick Steinhardt’s latest review focuses on patch 5/4, which adds a pre-filter to discard commits that definitely didn’t touch any relevant path early. He questions whether the pre-filter might sometimes do redundant work (since Bloom filter checks could be repeated later) and asks for a benchmark to justify the performance trade-off. While he acknowledges the logic is sound, he wants the commit message to explicitly state the expected benefit. This joins two earlier comments from him on patches 3/4 and 4/4: a correctness issue in `bloom_filter_contains_any_vec()` (where an empty Bloom filter could return `-1`, breaking boolean assumptions) and a naming clarity issue in `revs_maybe_changed_in_bloom_with_parents()` (where "parents" could be misread as commit parents rather than parent directories). Toon will need to address these in a v5 reroll.

Other reviewers have signed off: Jeff King accepted the current approach for managing `bloom_filter_settings`, Taylor Blau confirmed the `--show-trees` fix, and Junio C Hamano incorporated a test environment fix. The series is otherwise technically complete, with all other feedback resolved. The remaining work is narrowly scoped to Patrick’s comments—no broader design objections remain.