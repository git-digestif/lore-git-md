Thread identity: `[PATCH v2 0/2] format-patch: add --[no-]range-diff-notes` series. Originating author: Kristoffer Haugsbakk. Thread type: patch series (feature addition).

Core problem or goal: Enable independent control over Git notes inclusion in `git format-patch` output, specifically allowing users to suppress notes in the range-diff section while retaining them in the patch output. The v2 redesign eliminates the ambiguous option semantics, unintuitive fallback behavior, and problematic toggle mechanism of v1 by adopting Junio C Hamano’s suggested rule: `--no-range-diff-notes` means "no notes for range-diff" without fallback, and `--range-diff-notes=<ref>` requires an explicit ref argument, enforcing explicit separation between patch notes and range-diff notes.

Current status: **Proposed redesign (v2)**. The series is now complete at v2 with two patches: a preparatory refactoring (posted) and the new implementation (now posted). The v2 design directly addresses Junio’s usability concerns by simplifying the CLI semantics, eliminating the toggle mechanism, and making the behavior predictable. The thread remains closed in terms of active development, but the v2 proposal provides a viable path forward if the feature is revived. No maintainer decision has been made on the redesign, and the series is not yet under review for integration.

Key technical details:
- Files touched: `builtin/log.c` (refactoring and implementation), `log-tree.c`, `revision.c`, `revision.h`, `Documentation/git-format-patch.txt`, and tests in `t/t3206-range-diff.sh`.
- CLI options (v2): `--range-diff-notes=<ref>` (requires an explicit ref argument) and `--no-range-diff-notes` (means "no notes for range-diff" without fallback). The arg-less `--range-diff-notes` variant and the toggle mechanism are removed.
- New struct: `struct rdiff_notes` introduced to track whether the user wants to override the default notes behavior for range-diffs. The `rev_info` struct is simplified by removing the `rdiff_override_notes` flag and renaming `rdiff_notes_arg` to `rdiff_log_arg` for clarity.
- Refactoring: `get_notes_args` updated to take only `struct rev_info *` as a parameter, removing the redundant `struct strvec *arg` parameter. The function now directly accesses `rev->rdiff_log_arg` (introduced in commit 85bd88a7, 2025-09-25) instead of receiving it as a separate argument. The custom `revision.c` option handler is replaced with a standard `parse-options` callback (`rdiff_notes_cb`).
- Behavior (v2): The interaction rule between patch notes and range-diff notes remains unchanged: if no range-diff notes options are given, the range-diff uses the same notes as the patches; otherwise, it uses only the explicitly specified notes (including none, via `--no-range-diff-notes`). `--no-notes` no longer suppresses range-diff notes unless explicitly requested via `--no-range-diff-notes`. `--range-diff-notes` without `--range-diff` has no effect but does not error, avoiding silent failures.
- Test coverage: Comprehensive tests verify scenarios such as suppressing all range-diff notes, using different notes refs for patches and range-diffs, and ensuring `--range-diff-notes` requires a value. Two additional tests specifically address the single-patch case, serving as regression tests for a bug fixed in a prior commit (155986b4, 2025-09-25). Tests also confirm that `--range-diff-notes` has no effect when `--range-diff` is not used.
- Prior commit dependencies: 85bd88a7 (2025-09-25) introduced `rdiff_log_arg` to `struct rev_info`; 155986b4 (2025-09-25) fixed a bug in the single-patch case.
- Documentation: Updated to reflect the simplified behavior, removing references to the toggle mechanism and clarifying the explicit separation between patch notes and range-diff notes.

Open questions and loose ends:
- The v2 redesign resolves the usability issues raised in v1, but the thread remains closed, and no maintainer decision has been made on whether to accept the redesign. The feature’s revival would require explicit reopening of the discussion.
- The tangential issue of `format-patch`’s default notes handling (potentially aligning it with `format.notes` instead of `notes.displayRef`) remains unaddressed but is not blocking.
- The v2 implementation avoids the problematic edge cases of v1 (e.g., `--no-notes --range-diff-notes` suppressing notes in both contexts, confusing alias behavior), but the interaction between `--notes` and `--range-diff-notes` is now explicit and predictable.

Participant positions:
- Kristoffer Haugsbakk (author): Proposed the v2 redesign after abandoning v1 due to usability concerns. The v2 implementation adopts Junio’s suggested rule, eliminates the toggle mechanism and arg-less `--range-diff-notes` variant, and enforces explicit separation between patch notes and range-diff notes. Position: the v2 design addresses the usability issues of v1 and provides a viable path forward if the feature is revived. The implementation is now simpler and more predictable, aligning with Junio’s feedback. Both patches are uncontroversial and well-motivated.
- Junio C Hamano (maintainer): Raised substantive usability concerns about v1’s option semantics, fallback behavior, and toggle mechanism. Position (implied): the v2 redesign directly addresses these concerns by simplifying the CLI semantics and eliminating the problematic toggle mechanism. No explicit position on v2 has been stated, but the redesign aligns with his suggested rule.
- D. Ben Knoble (reviewer): Provided surface-level feedback on v1’s documentation and engaged in follow-up discussion about the toggle mechanism and default notes handling. Position (implied): the v2 design’s explicit separation and simplified behavior are improvements, but no technical review of v2 has been provided.

Related work: None mentioned in the thread. The v2 redesign is self-contained and does not depend on or conflict with other in-flight efforts. The refactoring in the first patch builds on the prior commit 85bd88a7 (2025-09-25). The feature’s goal—allowing independent control over notes in range-diffs versus patches—remains unmet but is now addressed by a viable design. If revived, the v2 proposal would need maintainer approval and further review. The tangential issue of `format-patch`’s default notes handling is not blocking but could be addressed in future work.