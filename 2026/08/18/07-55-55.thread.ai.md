Mode: ai

Thread identity
Subject: [PATCH v6 0/4] receive-pack: add new `receive-report` hook for server-side status filtering
Series version: v10 (4 patches)
Originating author: Karthik Nayak
Thread type: feature patch series

Core problem or goal
Git’s `receive-pack` currently sends a fixed status report to the client after ref updates are committed, with no mechanism for server administrators to intercept or modify this report. The patch series introduces a new `receive-report` hook that acts as a bidirectional filter: it receives the pkt-line encoded status report on stdin, replaces it with its stdout, and forwards stderr to the client via the sideband channel. The hook is motivated by GitLab’s need to implement MVCC (multi-version concurrency control) on the server side, where the status report must reflect the repository state *after* all server-side operations (including those beyond `ref_transaction_commit()`) are complete. Junio C Hamano’s earlier comment questioned the necessity of a separate hook, noting that the `reference-transaction` hook already fires in the "committed" phase and could theoretically be extended to allow rejecting the transaction. Karthik Nayak’s replies clarify that the `reference-transaction` hook cannot meet GitLab’s requirements because it lacks access to push-specific context (e.g., per-ref failures, sideband messages) and cannot convey the final status report to the client. The hook’s ability to decouple reported status from actual repository state is intentional and aligns with GitLab’s use case, where conservative client-side behavior (not updating remote-tracking branches when the hook reports `ng`) is the desired outcome. Junio’s latest reply highlights a **fundamental consistency concern**: this decoupling violates Git’s principle that remote-tracking branches reflect the server’s state, as a "failed" push (reported as `ng`) may still commit ref updates, leaving the client’s view inconsistent with reality.

Current status
**Queued in `next`** (Junio C Hamano, 2026-09-05, "What's cooking" report). The series is **feature-complete and ready for final review**, with all prior feedback addressed, including the **critical design flaw** in patch 2/4 resolved in v9, the **memory leak** in the error-handling path fixed in v10, and the **BUG() flaw** in patch 2/4 confirmed as untestable due to infrastructure limitations. The v10 iteration includes the following changes:
1. **Memory leak fix in patch 4/4**: The `override_cmds_error()` helper now conditionally frees the existing `cmd->error_string` before overwriting it, using a new `error_string_owned` flag to track ownership. This addresses Junio C Hamano’s substantive review identifying a leak in the non-zero hook exit path.
2. **Test coverage for error path**: Added a test case in `t/t5412-receive-report-hook.sh` to trigger the error path in `override_cmds_error()`, ensuring the memory leak fix is verifiable. The test suite now spans **257 lines**.
3. **Confirmation of untestable BUG() flaw**: Jeff King’s review demonstrates that the `BUG()` in patch 2/4 (resolved in v9) was real but could not be triggered by the existing test suite because older Git clients do not request status reports. The absence of test coverage for this edge case is now explicitly acknowledged as a practical limitation of the test infrastructure, not a design flaw. Karthik Nayak declines to add an interop test, citing the impracticality of maintaining such tests and the rarity of clients that do not request status reports.
4. **Commit message refinement (pending)**: Oswald Buddenhagen’s **substantive review** critiques the verbosity of the commit message in patch 4/4, arguing it duplicates documentation and obscures the core rationale ("why?"). The feedback suggests trimming technical details (e.g., stdin/stdout mechanics, pkt-line format) in favor of emphasizing GitLab’s MVCC use case and the consistency trade-off. The review is **non-blocking** and does not imply code changes, but the author may revise the message for conciseness. **Latest update**: The author has not yet revised the commit message, but the feedback remains non-blocking.
5. **Documentation gap filled**: The v10 series includes a **documentation-only patch** (1/4) that adds an eight-line `PROC-RECEIVE HOOK` section to `git-receive-pack.adoc`, explaining the purpose of the `proc-receive` hook and linking to `githooks(5)`. This plugs a prior gap in the documentation and aligns with the surrounding hook documentation style. The patch is uncontroversial and includes a `Helped-by` credit to Patrick Steinhardt.

All four patches remain **applied**:
1. **Documentation-only**: Adds the `PROC-RECEIVE HOOK` section to `git-receive-pack.adoc` and documents the `receive-report` hook in `githooks.adoc`. Styled consistently with surrounding hook documentation. Includes a `Helped-by` credit to Patrick Steinhardt.
2. **Refactoring (critical fix applied)**: Replaces static variables tracking report status version with an `enum report_status_version` to make the protocol-version switch more explicit and extensible. The enum includes a `REPORT_STATUS_UNKNOWN` sentinel value, which now triggers a silent `break` instead of a `BUG()` call when the client does not request a status report. The patch is purely internal, with no user-visible behavior changes or test coverage. Credits Junio C Hamano with a `Helped-by` for the enum refactoring. **Latest update**: The patch is now explicitly described as **internal-only**, with no user-visible behavior changes, and the enum-based approach is confirmed to improve maintainability by making the mutually exclusive nature of report status versions explicit.
3. **Refactoring**: Extracts the status-report generation logic into a reusable `generate_report()` helper in `builtin/receive-pack.c`. The `report()` and `report_v2()` functions are now unified into a single `report()` helper that takes an `enum report_status_version` parameter, eliminating redundancy. This addresses Patrick Steinhardt’s substantive review feedback from v6. The patch is purely internal and does not change user-visible behavior. Credits Patrick Steinhardt with a `Helped-by` for the refactoring. **Latest update**: The refactoring is now complete, with the redundant `report_v2()` function removed and the `report()` function delegating to `generate_report()` with the appropriate version parameter. The change is mechanical and well-motivated, reducing code duplication and paving the way for the hook implementation in patch 4/4.
4. **Core implementation (memory leak fixed)**: Introduces the `receive-report` hook with the agreed-upon exit semantics. The commit message explicitly acknowledges the consistency trade-off and justifies the divergence between reported status and actual repository state with GitLab’s MVCC use case. The hook’s design as a single-script filter is documented, along with its three ways of affecting push outcome: rewriting ref status lines, aborting the push (non-zero exit), and modifying the `unpack` status line. The error-handling logic now conditionally frees the existing `cmd->error_string` before overwriting it, addressing Junio’s memory leak concern. **Test coverage**: 257 lines in `t/t5412-receive-report-hook.sh` covering passthrough, failure modes, stdin/stdout handling, sideband relay, and the hook’s ability to modify the `unpack` status line and individual ref statuses.

Key technical details
- Files touched: `builtin/receive-pack.c`, `Documentation/githooks.adoc`, `Documentation/git-receive-pack.adoc`, `t/meson.build`, `t/t5412-receive-report-hook.sh`
- New hook name: `receive-report`
- New helper functions: `generate_report()`, `run_receive_report_hook()`, `override_cmds_error()`
- New enum: `report_status_version` (replaces the `detailed_reports` boolean parameter; values: `REPORT_STATUS_UNKNOWN`, `REPORT_STATUS_V0`, `REPORT_STATUS_V2`)
- New flag: `error_string_owned` (tracks ownership of `cmd->error_string` to prevent memory leaks)
- Invocation point: after all ref updates are committed, before the status report is sent
- Input: pkt-line encoded status report on stdin (terminated by a flush packet)
- Output: replacement status report on stdout; stderr forwarded to client via sideband
- Exit code semantics: non-zero exit rewrites all ref status lines to `"receive-report hook failed"` and overwrites all `cmd->error_string` fields (now with conditional free); zero exit uses the hook’s stdout as the report. The regenerated error report uses the same protocol version as the original call.
- Hook is **not integrated with the config-based hook infrastructure**
- Test coverage: 257 lines in `t/t5412-receive-report-hook.sh` covering passthrough, failure modes, stdin/stdout handling, sideband relay, and the hook’s ability to modify the `unpack` status line and individual ref statuses. **Test coverage limitation**: The test suite does not verify behavior when the client does not request a status report (i.e., when `version` is `REPORT_STATUS_UNKNOWN`). Jeff King’s review confirms this gap is a practical limitation: older Git clients (e.g., v1.0.0) do not request status reports, so the `BUG()` flaw in patch 2/4 was invisible to them. The built-in `git send-pack` client always requests a status report if the server advertises the capability, and there is no mechanism to disable this behavior. Karthik Nayak explicitly declines to add an interop test, citing the impracticality of maintaining such tests and the rarity of clients that do not request status reports.
- Documentation: The hook is documented in `githooks.adoc` and `git-receive-pack(1)`, with the latter linking to `githooks(5)`. The `proc-receive` hook is now documented in `git-receive-pack.adoc` with an eight-line `PROC-RECEIVE HOOK` section explaining its purpose and linking to `githooks(5)`. The commit message documents **three ways the hook can affect the push outcome**:
  1. Rewriting individual ref status lines.
  2. Aborting the entire push (by exiting non-zero).
  3. Modifying the initial `unpack` status line.
- **Consistency concern**: The hook’s design intentionally decouples the reported status from the actual repository state, allowing the server to report a push as failed (`ng`) while still committing ref updates. This violates Git’s traditional principle that remote-tracking branches reflect the server’s state, as a client’s `git fetch` after a "failed" push may reveal the server *did* accept the update. The divergence is justified by GitLab’s MVCC use case, where conservative client-side behavior (not updating remote-tracking branches when the hook reports `ng`) is the desired outcome. The commit message explicitly states that the hook owner is responsible for undoing ref changes if needed and that the hook is only appropriate for servers where rejected updates are not immediately visible to other readers.

Open questions and loose ends
- **Design rationale for a separate hook**: Junio’s earlier comment noted that the `reference-transaction` hook could theoretically be extended to meet GitLab’s requirements, but Karthik’s replies clarify that it lacks access to push-specific context (e.g., per-ref failures, sideband messages) and cannot convey the final status report to the client. No further action is required.
- **Consistency trade-off**: Junio’s latest reply surfaces a **philosophical tension** between GitLab’s use case and Git’s traditional consistency guarantees. The hook’s ability to report a state divergent from the actual repository state is intentional but introduces a novel inconsistency in Git’s push/fetch model. The commit message now explicitly acknowledges this trade-off and justifies it with GitLab’s MVCC use case. No code changes are proposed or needed; the exchange documents the design choice for future reviewers.
- **Commit message verbosity (pending)**: Oswald Buddenhagen’s **substantive review** critiques the commit message in patch 4/4 for duplicating documentation and obscuring the core rationale. The feedback suggests trimming technical details (e.g., stdin/stdout mechanics, pkt-line format) in favor of emphasizing the "why" (GitLab’s MVCC use case and the consistency trade-off). The review is **non-blocking** and does not imply code changes. **Latest update**: The author has not yet revised the commit message, but the feedback remains non-blocking.

Participant positions
- Karthik Nayak (author): Implemented the hook, documented the use case (GitLab’s MVCC system), and provided test coverage. Addressed all prior feedback, including Junio’s maintainability concern about the `detailed_reports` boolean parameter (now replaced with an `enum`), Patrick Steinhardt’s exit status proposal, documentation ambiguities, the hook’s ability to modify the `unpack` status line, the **copy-paste oversight** in the error-handling logic (fixed in v8), the **stale comment** in the `generate_report()` helper (fixed in v8), the **typo in the `enum report_status_version`** (`REPORT_STATUS_UNKOWN` -> `REPORT_STATUS_UNKNOWN`, fixed in v8), the **critical design flaw** in patch 2/4 (resolved in v9), and the **memory leak** in `override_cmds_error()` (fixed in v10). Confirmed Junio’s manual conflict resolution with Justin Tobler’s "pluggable writes" series is correct. **Strengthened the commit message to explicitly acknowledge the consistency trade-off and justify the divergence between reported status and actual repository state.** Added documentation for the `proc-receive` hook in `git-receive-pack.adoc` to address a prior gap. **Incorporated all remaining feedback in v10**, including the memory leak fix, test coverage for the error path, and commitment to address the commit message verbosity in patch 4/4. The series is now **ready for final review** and graduation to `master`.
- Junio C Hamano (maintainer): Initially raised concerns about exit status semantics and the hook name. Endorsed Patrick Steinhardt’s proposal to align the exit status behavior with the `pre-receive` hook. Queued the series in `seen` and manually resolved a conflict with Justin Tobler’s series. Raised a maintainability concern about the `detailed_reports` boolean parameter, suggesting an `enum` would be more explicit. **Graduated the series to `next` in the 2026-09-05 "What's cooking" report**, but **identified a critical design flaw in patch 2/4**: the `BUG()` call in the `switch` statement assumed the client’s requested protocol version was always valid, but clients may omit the `report-status` or `report-status-v2` capability entirely, leaving `version` as `REPORT_STATUS_UNKNOWN`. The fix (replacing `default: BUG(...)` with an explicit `REPORT_STATUS_UNKNOWN` case that `break`s) is now applied in v9. **Identified a copy-paste oversight in the error-handling logic in v7**, a **stale comment in the `generate_report()` helper in v8**, and a **typo in the `enum report_status_version` in v8**, all of which were fixed in v8. **Latest email**: Identified a **memory leak** in the `override_cmds_error()` helper, where the function overwrites `cmd->error_string` without freeing the existing string if it is owned by the `command` struct. Proposed a fix using a new `error_string_owned` flag to track ownership. The issue is **resolved in v10**; no further objections raised. The series remains on track for `master`.
- Patrick Steinhardt: Provided **substantive reviews** clarifying the hook’s necessity, tightening documentation, and proposing the exit status alignment with `pre-receive`. His proposal is fully adopted. Agreed with Junio’s naming objection and proposed `receive-report` as an alternative. Provided surface-level and substantive reviews of the documentation and refactoring patches, all of which have been incorporated. Raised and resolved two new points in v4:
  - Documentation ambiguities (grammatical fixes, optional markers).
  - Technical question about overwriting `cmd->error_string` fields (resolved in favor of overwriting all fields). **Helped-by credit** for the `proc-receive` hook documentation patch. **Latest reviews in v6**:
    - **Surface-level suggestions**:
      - Use a `switch` statement for handling the `enum report_status_version` in the second patch (subjective, now incorporated in v7).
      - Remove unnecessary curly braces around the single-statement loop body in `override_cmds_error()` in the fourth patch (purely stylistic; now addressed in v7).
    - **Substantive review** of the third patch, identifying a **missed refactoring opportunity**: the `report()` and `report_v2()` functions were redundant and are now unified in v7. **Final approval in v7**, confirming all prior feedback—both surface-level and substantive—has been addressed. **Latest review in v9**: Flagged a **test coverage gap** in the 257-line `t/t5412-receive-report-hook.sh` script, noting that the absence of test failures for the `BUG()` flaw in patch 2/4 suggests the test suite does not verify behavior when the client does *not* request a status report. Junio’s and Jeff King’s latest emails now clarify this gap as a practical limitation of the test infrastructure rather than a design flaw.
- Jeff King (Peff): **Substantive review** demonstrating that the `BUG()` flaw in patch 2/4 was real but untestable with the existing test infrastructure. Provided an interop test using an older Git client (v1.0.0) to show that the flaw was invisible to clients that do not request status reports. Explicitly described the test as a "fun answer" rather than a serious suggestion for the series, confirming the v9 fix (silently skipping the hook when no report is requested) remains valid. **Key takeaway**: The `BUG()` flaw was a real issue but could not be caught by the existing test suite due to infrastructure limitations, not design oversight. The series remains on track for `master`.
- Kristoffer Haugsbakk: Provided **surface-level reviews** focusing on documentation clarity. Suggested linking to `gitprotocol-v2(5)` for pkt-line format (resolved by linking to `gitprotocol-pack[5]` instead).
- Phillip Wood: Questioned the single-script design rationale but was addressed by Karthik’s justification in the commit message. No further objections raised.
- Oswald Buddenhagen: **Substantive review** of the commit message in patch 4/4, critiquing its verbosity and redundancy with the documentation. Argues the message should focus on the "why" (GitLab’s MVCC use case and the consistency trade-off) rather than duplicating technical details (e.g., stdin/stdout mechanics, pkt-line format). The feedback is **non-blocking** and does not imply code changes. **Latest update**: The author has not yet revised the commit message, but the feedback remains non-blocking.

Related work
- Justin Tobler’s "pluggable writes" series (concurrent with this one) required manual conflict resolution by Junio. No functional overlap; the two series are complementary. Karthik Nayak has confirmed the resolution is correct. The v9 rebase resolves any remaining conflicts.