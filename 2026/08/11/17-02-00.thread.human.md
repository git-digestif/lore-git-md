Here’s the human-mode summary for a developer catching up on the thread:

The thread is about a security feature patch that adds `http.sslVerifyStatus`, a boolean config option (defaulting to `false`) to enable OCSP staple validation in Git’s HTTPS certificate checks. The patch was motivated by government customers like the US Department of Defense, who require OCSP stapling and found that Git’s OpenSSL-linked builds ignored staples, creating a security gap where revoked certificates could be accepted. The feature was merged to `master` on June 12, 2025, but reverted from `next` on June 18 due to test failures reported by SZEDER Gábor.

The core functionality remains intact, but the new test `t5585-http-ssl-ocsp.sh` fails on Gábor’s system for two reasons: first, the test assumes a revoked certificate would be accepted without `http.sslVerifyStatus`, but libcurl 7.81.0 rejects it via CRL checks regardless of stapling; second, the `SSL_VERIFYSTATUS` prerequisite’s error-message check is misleading, though the prerequisite itself works. Gábor suggests tightening the error-message check and renaming the prerequisite to `CURL_SSL_VERIFYSTATUS` to clarify its scope. Two minor code-quality nits in the test script (exact error-message checks and `export` usage) also remain open.

Key participants include the author (graysongordon-gl), who addressed all review feedback and provided the final combined patch; Junio C Hamano, who merged the patch but later reverted it from `next` due to test issues; Patrick Steinhardt, who provided a test harness and confirmed the fail-closed behavior is covered; and Gábor, who reported the test failures and proposed fixes. The open questions now focus on test robustness—how to isolate OCSP stapling from CRL checks in the test environment and whether the default for `http.sslVerifyStatus` should eventually change to `true`. No objections to the feature itself remain, and the revert is procedural to allow test fixes before the next release cycle.