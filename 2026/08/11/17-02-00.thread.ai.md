Thread identity
Subject: [PATCH v4] http: add http.sslVerifyStatus to check OCSP staples
Series version: standalone patch (v6 submitted with documentation tweak)
Originating author: graysongordon-gl
Thread type: security feature patch

Core problem or goal
Git’s HTTPS certificate verification omits OCSP staple validation, leaving it vulnerable to accepting revoked certificates. The patch adds a boolean `http.sslVerifyStatus` option (default `false`) that enables staple validation via libcurl’s `CURLOPT_SSL_VERIFYSTATUS`, causing connections to fail if the server does not provide a staple. The design is fail-closed for security but defaults to `false` to avoid breaking remotes that do not support stapling. The feature is motivated by government and government-adjacent customers (e.g., US Department of Defense PKI) who mandate OCSP stapling and encounter Git servers with stapled responses; OpenSSL-linked Git binaries currently ignore staples, creating a security gap where revoked certificates are accepted.

Current status
**Graduated to `next`; merge to `next` confirmed by Junio C Hamano.** Junio’s latest email explicitly states the patch is ready for merge, resolving the last procedural question (test coverage of fail-closed behavior) with Patrick Steinhardt’s confirmation that Grayson Gordon’s existing tests in `t5551-http-fetch-smart.sh` already cover the negative case. The v6 iteration addressed Junio’s final documentation wording tweak, and the author has now confirmed they will send a final combined patch incorporating the best parts of both their original tests and Patrick Steinhardt’s test harness. This resolves the last procedural step before the feature can graduate to `master`. No further changes are anticipated, and the patch is now **in `next`**, awaiting Junio’s merge action to finalize its inclusion.

Key technical details
- New config key: `http.sslVerifyStatus` (boolean, default `false`)
- Per-URL override: `http.https://example.com/.sslVerifyStatus`
- Libcurl requirement: `CURLOPT_SSL_VERIFYSTATUS` (libcurl ≥ 7.41.0, already below Git’s minimum)
- Files touched: `http.c`, `Documentation/config/http.adoc` (updated in v6), `t/t5551-http-fetch-smart.sh` (four new tests covering fail-closed behavior, per-URL scoping, and edge cases), `t/lib-httpd.sh`, `t/t5585-http-ssl-ocsp.sh` (Patrick Steinhardt’s test harness, now merged)
- Test integration: The final combined patch will merge Grayson’s original tests (which verify fail-closed behavior when no staple is present, per-URL scoping, and edge cases) with Patrick’s test harness (which exercises success and failure paths for stapled responses). The `SSL_VERIFYSTATUS` lazy prerequisite uses `test_might_fail` to handle expected failures gracefully. Junio’s latest reply confirms the negative case (no staple) is already covered by the original tests, and Patrick’s harness covers the positive and invalid staple cases.
- Behavior: fail-closed (connection fails if staple missing or invalid)
- Backend differences: GnuTLS and OpenSSL now behave identically in libcurl ≥ 8.10 (GnuTLS ≥ 3.8), obsoleting prior backend-specific caveats
- Error handling: failure messages include libcurl error strings (`curl_easy_strerror()`) to distinguish missing features (`CURLE_NOT_BUILT_IN`) from other errors
- Symbols: none renamed; new config key only
- Deployment context: GitLab’s Helm charts default to OpenSSL on UBI-based images (FIPS compliance requirement), leaving government customers exposed without this feature

Open questions and loose ends
- Whether the default (`false`) should eventually change to `true` or if users should be warned when staples are ignored.
- Whether the feature’s maintenance burden (given OCSP stapling’s declining relevance) is justified by the specific use case of government customers.

Participant positions
- **Author (graysongordon-gl)**: Introduced the feature with clear rationale, design trade-offs, and thorough testing. Addressed all review feedback, including Junio’s procedural requests (test integration, Meson build updates, prerequisite robustness) and Patrick Steinhardt’s substantive review (documentation accuracy, error message clarity, test coverage). Provided concrete motivation: government customers (e.g., US DoD PKI) mandate OCSP stapling, and OpenSSL-linked Git binaries (common in FIPS-compliant deployments) currently ignore staples, creating a security gap. Resolved Junio’s final documentation wording tweak in v6 by rephrasing the `http.sslVerifyStatus` documentation to focus on the user-facing outcome (refusing connections to servers with revoked certificates) rather than technical jargon. **Confirmed in the latest email that they will send a final combined patch merging their original tests with Patrick Steinhardt’s test harness**, resolving the last procedural step before graduation to `master`.
- **Junio C Hamano**: Surface-level review on v1 (requested comment removal) and v2 (test numbering conflict). Provided procedural follow-up in v3 (Meson build integration, `SSL_VERIFYSTATUS` prerequisite robustness). Accepted the v4 iteration as technically complete and confirmed in v6 that no further changes are needed. Final request in v5 (rephrasing the `http.sslVerifyStatus` documentation) was addressed in v6. **Explicitly confirmed in his latest email that the patch is ready for merge to `next`**, resolving the last procedural question (test coverage of fail-closed behavior) with Patrick Steinhardt’s input. No objections to the feature itself; feedback was purely about documentation clarity and procedural completeness.
- **Patrick Steinhardt**: Substantive review in v3 questioning the patch’s purpose, initially arguing the feature does not resolve backend differences, provides no security benefit by default, and may not be worth maintaining. The author’s response addressed the use case concern, but the maintenance burden question remains open. In v4, requested actionable improvements: documentation updates (backend parity in recent libcurl versions), error message clarity (libcurl error strings), and test coverage (positive case). All requested changes except the positive test case were resolved in v5. Provided a complete, self-contained test harness for OCSP stapling validation, addressing the last major test-coverage gap. **Explicitly confirmed in his latest reply that the fail-closed behavior (connections fail when no staple is present) is already covered by the original tests in `t5551`**, resolving Junio’s lingering question. Position is now fully supportive of graduation; no further feedback expected.