```
Thread identity: patch series (12 patches), "[PATCH v3 0/12] MinGW/Windows build and runtime adjustments", originating author Johannes Schindelin (Dscho).
Core problem or goal: upstream Git for Windows-specific build and runtime adjustments into the main Git codebase, addressing hard-coded assumptions, MSYS2 integration, linking behavior, deprecated build artifacts, a locale-handling regression in the MSYS2 runtime, and direct `git.exe` invocation. The series aims to improve Windows compatibility without affecting non-Windows platforms.
Current status: v3 posted; under review. All patches (1–12) are uncontroversial and ready for integration, with no blocking review feedback. Changes from v2 are mechanical: compiler-definition hunks (`ENSURE_MSYSTEM_IS_SET` and `MINGW_PREFIX`) moved from Patch 8 to Patch 12 (where they are consumed by the C code), and a fly-by style cleanup in `t0060-path-utils.sh` dropped, addressing Johannes Sixt’s feedback about logical ordering and maintainability. Johannes Sixt reports running a subset of these patches (2–6, 8, and either 9 or 12) in his personal Windows builds since November 2025 without issues, and the full series passes GitHub CI on Windows, providing real-world validation of stability. The unresolved discussion about guarding against an empty `MSYSTEM` export in Patch 12 continues, with Junio C Hamano reinforcing the precedent for defensive programming in the build system by pointing to existing `ifneq (,$(MSYSTEM))` guards in `config.mak.uname`; Johannes Schindelin has not addressed this restatement in v3.
Key technical details:
- Files touched: `compat/mingw.c`, `config.mak.uname`, CMake/Meson build files (`contrib/buildsystems/CMakeLists.txt`, `meson.build`, `meson_options.txt`), test `t0060-path-utils.sh`.
- New/renamed symbols: none.
- Behavior changes: none for non-Windows platforms. Windows-specific adjustments include:
  - Enabling Python buildability by removing `NO_PYTHON` for MINGW in `config.mak.uname` (Patch 1), allowing users to leverage MSYS2-provided Python interpreters in Git for Windows’ SDK environment.
  - Removing the hard-coded `CC = gcc` assignment in the MINGW section of `config.mak.uname` to allow alternative compilers (e.g., Clang) (Patch 2).
  - Removing the `-D_USE_32BIT_TIME_T` compiler flag from the MinGW section of `config.mak.uname` (Patch 3), as it is incompatible with the UCRT64 runtime and was only needed for the original MinGW runtime (deprecated since 2015).
  - Refining the linker flag `-Wl,--large-address-aware` to apply only to 32-bit builds (`MINGW32`), omitting it for 64-bit builds (`MINGW64` and `CLANGARM64`) where it is irrelevant or unsupported (Patch 4).
  - Removing redundant `-e` entry-point specifications from the `-Wl,--pic-executable` linker flag in `config.mak.uname` (Patch 5), as the specified entry points (`_mainCRTStartup` for 32-bit, `mainCRTStartup` for 64-bit) are already the default according to upstream binutils documentation.
  - Replacing hard-coded `prefix` and `HOST_CPU` values in `config.mak.uname` with dynamic settings derived from MSYS2’s environment variables (`MINGW_PREFIX` and `MINGW_CHOST`), guarded by `ifneq (,$(MSYSTEM))` (Patch 6).
  - Making the `-DDETECT_MSYS_TTY` compiler flag conditional on the presence of the `MSYSTEM` environment variable in `config.mak.uname` (Patch 7), ensuring it is only enabled when compiling in an MSYS2 environment.
  - Replacing hard-coded defaults for `MSYSTEM` and installation prefix in `config.mak.uname`, CMake, and Meson with values derived from MSYS2’s environment variables (`MINGW_PREFIX` and `MSYSTEM`), guarded by `ifneq (,$(MSYSTEM))`. The v2 iteration aligned Meson’s behavior with the Makefile’s by removing the leading slash from `MINGW_PREFIX`. The compiler-definition hunks (`ENSURE_MSYSTEM_IS_SET` and `MINGW_PREFIX`) are now in Patch 12, where they are consumed by the C code. The patch also adds a fallback for the minimal Git for Windows SDK (where only `MSYSTEM` is guaranteed) by deriving the prefix from the down-cased `MSYSTEM` value (Patch 8).
  - Disabling generation of hard-linked `git-<command>` executables (e.g., `git-commit`) in `libexec/git-core/` for Windows builds by setting `SKIP_DASHED_BUILT_INS = YabbaDabbaDoo` in `config.mak.uname` (Patch 9). The value `YabbaDabbaDoo` is a non-empty placeholder; the build system only checks whether the variable is defined, not its content. This change eliminates ~30 seconds of build overhead and avoids spurious Windows Explorer warnings due to hard-link awareness limitations. The dashed form (`git-commit`) has been deprecated since Git 1.5.4 (February 2008) and is a historical artifact from when commands were shell scripts wrapping low-level plumbing.
  - Simplifying the logic for setting `ETC_GITCONFIG` and `ETC_GITATTRIBUTES` paths by unconditionally defining them for all MSYS2 environments (Patch 10), removing a redundant conditional block that previously only set these paths when the prefix matched `/mingw64` (or similar) and replacing it with a single `ifneq (,$(MINGW_PREFIX))` guard.
  - Fixing a locale-handling regression in the MSYS2 runtime (introduced between versions 2.4.1 and 2.6.0) that caused Git to mishandle command-line arguments containing non-ASCII characters when shelling out to subprocesses. The patch adds a three-line guard in `compat/mingw.c`’s `setup_windows_environment()` function to ensure a valid `LC_CTYPE` is set (`C.UTF-8`) if none of `LC_ALL`, `LC_CTYPE`, or `LANG` are already defined. This prevents the MSYS2 runtime from defaulting to a locale based on the current code page, which would corrupt non-ASCII characters in command-line arguments (e.g., paths with non-ASCII characters passed to `git-upload-pack` during `git clone` or `git fetch`). The regression is documented in GitHub issue [git-for-windows/git#1036](https://github.com/git-for-windows/git/issues/1036) (Patch 11).
  - Allows `git.exe` to be called directly instead of through the "Git wrapper" (`C:\Program Files\Git\cmd\git.exe`), eliminating an extra process hop. The patch adds logic in `compat/mingw.c` to detect when `MSYSTEM` is unset (indicating the wrapper hasn’t run) and initializes it, along with `PLINK_PROTOCOL` and a carefully constructed `PATH` that prepends `$HOME/bin`, the MSYS2 system directories (`/mingw64/bin` and `/usr/bin`), and the original `PATH`. The build system (`config.mak.uname`, CMake, and Meson) is updated to pass `MSYSTEM` and `MINGW_PREFIX` as compiler definitions. A new test in `t0060-path-utils.sh` verifies the environment setup (Patch 12).
- On-disk format: unchanged.
- Test coverage: one existing test (`t0060-path-utils.sh`) updated for Windows compatibility; the locale regression fix (Patch 11) is implied to be covered by GitHub issue #1036; Patch 12 adds further test coverage for the environment setup.
Open questions and loose ends: Junio C Hamano has suggested that the deprecation of dashed-form `git-<command>` executables (e.g., `git-commit`) could be completed by removing them entirely at the Git 3.0 boundary. This is a forward-looking proposal and not a request for changes to the current series. The environment-variable initialization issue in Patch 12 (empty `MSYSTEM` export) remains unresolved, with Junio reinforcing the precedent for defensive programming by pointing to existing `ifneq (,$(MSYSTEM))` guards in `config.mak.uname`; Johannes Schindelin has not addressed this restatement in v3.
Participant positions:
- Johannes Schindelin (Dscho): author; positions the series as minimal, routine build-system adjustments enabling Python support, compiler flexibility, UCRT64 compatibility, MSYS2 integration, phased deprecation of dashed-form executables, a targeted fix for a locale-handling regression, and direct `git.exe` invocation. Addressed Johannes Sixt’s feedback about logical ordering in v3 by moving compiler-definition hunks to Patch 12. Declines to address the environment-variable initialization issue in Patch 12, arguing the empty-`MSYSTEM` scenario is too unlikely to warrant additional build-system guards.
- Junio C Hamano: maintainer; surface-level review of Patch 9, raising the possibility of removing dashed-form executables entirely in Git 3.0; substantive review of Patch 8, identifying a cross-build-system inconsistency that was addressed in v2; substantive review of Patch 12, identifying a potential correctness issue in the environment-variable initialization logic (empty `MSYSTEM` export) and suggesting a build-system guard (`ifneq (,$(MSYSTEM))`), now reinforced by pointing to existing precedent in `config.mak.uname`. No objections to the current series. No new positions on Patches 1–7, 10, or 11.
- Johannes Sixt: substantive review of Patch 8, proposing that the CMake and Meson hunks introducing compiler definitions (`ENSURE_MSYSTEM_IS_SET` and `MINGW_PREFIX`) be moved to Patch 12, where the definitions are consumed by the C code, to improve logical ordering and maintainability. The feedback was addressed in v3. Reports running a subset of the patches (2–6, 8, and either 9 or 12) in personal Windows builds since November 2025 without issues, and confirms the full series passes GitHub CI on Windows. The feedback is framed as a cleanup suggestion and not a blocking issue, but provides real-world validation of the series’ stability.
Related work: GitHub issue [git-for-windows/git#2283](https://github.com/git-for-windows/git/issues/2283) (direct `git.exe` invocation) and GitHub issue [git-for-windows/git#1036](https://github.com/git-for-windows/git/issues/1036) (locale-handling regression).
```