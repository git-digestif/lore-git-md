```
Thread identity: patch series (13 patches), "[PATCH v4 0/13] MinGW/Windows build and runtime adjustments", originating author Johannes Schindelin (Dscho).
Core problem or goal: upstream Git for Windows-specific build and runtime adjustments into the main Git codebase, addressing hard-coded assumptions, MSYS2 integration, linking behavior, deprecated build artifacts, a locale-handling regression in the MSYS2 runtime, direct `git.exe` invocation, and build-system hygiene. The series aims to improve Windows compatibility without affecting non-Windows platforms and is urgently needed for Git for Windows' migration from MINGW64 to UCRT64.
Current status: **merged to `next`** (Junio C Hamano confirmed integration of v3 with minor editorial cleanup; v4 is a polish iteration). Changes from v3 are mechanical: a fly-by style fix split into its own commit (13/13), `Helped-by` trailers added, a paragraph moved from patch 8/12’s commit message to 12/12, and **Patch 4/13 now restricts the `-Wl,--large-address-aware` linker flag to 32-bit MinGW builds only** (removing it from 64-bit and ARM64 configurations where it is ignored or unsupported). The series explicitly notes that CMake and Meson do not automatically inherit the `MSYSTEM` setting from the build environment. Johannes Sixt’s validation (personal builds since November 2025, GitHub CI passes) still applies.
Key technical details:
- Files touched: `compat/mingw.c`, `config.mak.uname` (Patch 3/13 removes `-D_USE_32BIT_TIME_T`; **Patch 4/13 moves `-Wl,--large-address-aware` into the `MINGW32` conditional block**), CMake/Meson build files (`contrib/buildsystems/CMakeLists.txt`, `meson.build`, `meson_options.txt`), test `t0060-path-utils.sh` (updated for Windows compatibility and new test coverage for `MSYSTEM`/`PATH` adjustment).
- New/renamed symbols: none.
- Behavior changes: none for non-Windows platforms. Windows-specific adjustments include:
  - Enabling Python buildability via removal of `NO_PYTHON` in `config.mak.uname` (Patch 1), compiler flexibility via removal of hard-coded `CC = gcc` (Patch 2), **UCRT64 compatibility via removal of `-D_USE_32BIT_TIME_T` (Patch 3)**, **32-bit-only `-Wl,--large-address-aware` linker flag (Patch 4)**, refined linker flags (Patch 4), redundant entry-point removal (Patch 5), dynamic `prefix`/`HOST_CPU` settings (Patch 6), conditional `DETECT_MSYS_TTY` (Patch 7), MSYS2 environment-variable integration (Patch 8), phased deprecation of dashed-form executables (Patch 9), simplified `ETC_GITCONFIG`/`ETC_GITATTRIBUTES` logic (Patch 10), locale-handling regression fix (Patch 11), direct `git.exe` invocation with automatic `MSYSTEM`/`PATH` initialization (Patch 12), and a fly-by style fix to `t0060-path-utils.sh` (Patch 13).
- On-disk format: unchanged.
- Test coverage: `t0060-path-utils.sh` updated for Windows compatibility; Patch 12 adds test coverage for environment setup when `MSYSTEM` is unset.
Open questions and loose ends:
- Junio’s suggestion to add a build-system guard (`ifneq (,$(MSYSTEM))`) for the empty-`MSYSTEM` edge case in Patch 12 remains unresolved but is not blocking integration. Dscho declined to address it, arguing the scenario is too unlikely.
- Junio’s forward-looking proposal to remove dashed-form executables entirely at the Git 3.0 boundary (not a request for changes to this series).
Participant positions:
- Johannes Schindelin (Dscho): author; positioned the series as minimal, routine adjustments. Addressed Johannes Sixt’s feedback in v3, agreed to remove the stale CMake/Meson paragraph from Patch 8/12’s commit message, declined to address the empty-`MSYSTEM` guard, and added `Helped-by` trailers. Split the fly-by style fix into its own commit in v4.
- Junio C Hamano: maintainer; **proposed the v3 series ready for `next`** and confirmed all review feedback addressed. Handled the final commit message cleanup directly during merge. Reinforced the empty-`MSYSTEM` guard suggestion (citing precedent in `config.mak.uname`) but did not block progression.
- Johannes Sixt: substantive review of Patch 8 (logical ordering feedback addressed in v3). Reported real-world validation of stability (personal builds since November 2025, GitHub CI passes). Credited with helping refine Patches 1, 2, 3, and **4** (Python buildability, compiler flexibility, `-D_USE_32BIT_TIME_T` removal, and `-Wl,--large-address-aware` restriction).
Related work: GitHub issues [git-for-windows/git#2283](https://github.com/git-for-windows/git/issues/2283) (direct `git.exe` invocation) and [git-for-windows/git#1036](https://github.com/git-for-windows/git/issues/1036) (locale-handling regression).
```