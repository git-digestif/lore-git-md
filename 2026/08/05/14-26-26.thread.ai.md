```
Thread identity: patch series (12 patches), "[PATCH v3 0/12] MinGW/Windows build and runtime adjustments", originating author Johannes Schindelin (Dscho).
Core problem or goal: upstream Git for Windows-specific build and runtime adjustments into the main Git codebase, addressing hard-coded assumptions, MSYS2 integration, linking behavior, deprecated build artifacts, a locale-handling regression in the MSYS2 runtime, and direct `git.exe` invocation. The series aims to improve Windows compatibility without affecting non-Windows platforms.
Current status: v3 posted; **ready for `next`** as per Junio C Hamano's explicit proposal. All patches (1–12) are uncontroversial and address prior review feedback. Changes from v2 are mechanical: compiler-definition hunks (`ENSURE_MSYSTEM_IS_SET` and `MINGW_PREFIX`) moved from Patch 8 to Patch 12 (where they are consumed by the C code), and a fly-by style cleanup in `t0060-path-utils.sh` dropped. Johannes Sixt reports running a subset of these patches (2–6, 8, and either 9 or 12) in his personal Windows builds since November 2025 without issues, and the full series passes GitHub CI on Windows. The only pending item is a **surface-level commit message cleanup** in Patch 8/12: Johannes Sixt noted a stale paragraph about CMake/Meson behavior that no longer reflects the patch’s final state and suggested removing it (placement in Patch 8 vs. Patch 12 left ambiguous).
Key technical details:
- Files touched: `compat/mingw.c`, `config.mak.uname`, CMake/Meson build files (`contrib/buildsystems/CMakeLists.txt`, `meson.build`, `meson_options.txt`), test `t0060-path-utils.sh` (updated for Windows compatibility and new test coverage for `MSYSTEM`/`PATH` adjustment).
- New/renamed symbols: none.
- Behavior changes: none for non-Windows platforms. Windows-specific adjustments include:
  - Enabling Python buildability (Patch 1), compiler flexibility (Patch 2), UCRT64 compatibility (Patch 3), refined linker flags (Patch 4), redundant entry-point removal (Patch 5), dynamic `prefix`/`HOST_CPU` settings (Patch 6), conditional `DETECT_MSYS_TTY` (Patch 7), MSYS2 environment-variable integration (Patch 8), phased deprecation of dashed-form executables (Patch 9), simplified `ETC_GITCONFIG`/`ETC_GITATTRIBUTES` logic (Patch 10), locale-handling regression fix (Patch 11), and direct `git.exe` invocation with automatic `MSYSTEM`/`PATH` initialization (Patch 12).
- On-disk format: unchanged.
- Test coverage: `t0060-path-utils.sh` updated for Windows compatibility; Patch 12 adds test coverage for environment setup when `MSYSTEM` is unset.
Open questions and loose ends:
- Junio C Hamano’s suggestion to add a build-system guard (`ifneq (,$(MSYSTEM))`) for the empty-`MSYSTEM` edge case in Patch 12 remains unresolved but is not blocking progression.
- Junio’s forward-looking proposal to remove dashed-form executables entirely at the Git 3.0 boundary (not a request for changes to this series).
- **New**: Johannes Sixt’s editorial feedback on Patch 8/12’s commit message (stale CMake/Meson paragraph) requires cleanup; placement (Patch 8 vs. Patch 12) is ambiguous.
Participant positions:
- Johannes Schindelin (Dscho): author; positions the series as minimal, routine adjustments. Addressed Johannes Sixt’s feedback in v3. Declines to address the empty-`MSYSTEM` guard, arguing the scenario is too unlikely.
- Junio C Hamano: maintainer; **proposed the series ready for `next`**, confirming all review feedback is addressed. Reinforced the empty-`MSYSTEM` guard suggestion (citing precedent in `config.mak.uname`) but did not block progression. No objections to Patches 1–11.
- Johannes Sixt: substantive review of Patch 8 (logical ordering feedback addressed in v3). Reports real-world validation of stability (personal builds since November 2025, GitHub CI passes). **New**: surface-level feedback on Patch 8’s commit message (stale paragraph).
Related work: GitHub issues [git-for-windows/git#2283](https://github.com/git-for-windows/git/issues/2283) (direct `git.exe` invocation) and [git-for-windows/git#1036](https://github.com/git-for-windows/git/issues/1036) (locale-handling regression).
```