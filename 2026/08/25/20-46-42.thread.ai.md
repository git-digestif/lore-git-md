Thread identity: patch series extending `git var` to provide unified identity and signing configuration, replacing the earlier `git ident` proposal. Originating author: Andrew Pleeter (via GitGitGadget). Thread type: patch series (implementation of design consensus).

Core problem or goal: provide a unified, scriptable interface to Git’s identity (author/committer names/emails/dates) and signing configuration (GPG/SSH/X.509 keys, `commit.gpgsign` status) by extending the existing `git var` plumbing command. The solution must support both interactive use and scripting, with composable arguments and machine-readable output.

Current status: **active review; approaching readiness for integration**. The v6 patch addresses all substantive feedback from Junio C Hamano’s review of v5, including **multi-valued variable implementation, error handling robustness, documentation clarity, and output format backward-compatibility**. The series remains in `seen`; no maintainer has explicitly signaled readiness to graduate it, but the technical blockers raised in v5 are now resolved. Junio’s prior feedback suggests minor adjustments may still be needed before integration, but the patch is now **technically sound and review-converged**.

Key technical details:
- **Files touched**: `builtin/var.c`, `Documentation/git-var.adoc`, `t/t0007-git-var.sh`.
- **New variables**: `GIT_AUTHOR_NAME`, `GIT_AUTHOR_EMAIL`, `GIT_AUTHOR_DATE`, `GIT_COMMITTER_NAME`, `GIT_COMMITTER_EMAIL`, `GIT_COMMITTER_DATE`, `GIT_SIGNING_KEY` (resolves the key that would be used for a commit, regardless of `commit.gpgsign` status).
- **Behavior**: supports multiple variable arguments in a single invocation, `-z` for NUL-terminated output (aligned with `git config -z`: `key\nvalue\0`), and strict argument ordering (options precede variable names, enforced via `parse_options()` with `PARSE_OPT_STOP_AT_NON_OPTION`). Maintains backwards compatibility with existing `git var` usage, though scripts relying on the current output format may need updates for `-z` mode.
- **Code structure**: uses `enum ident_part` for type safety, table-driven variable handling, and Git’s standard argument parsing infrastructure. The `GIT_SIGNING_KEY` variable uses Git’s standard `get_signing_key()` (from `gpg-interface.c`) for consistency with actual commit signing.
- **Output format**: `-z` mode matches `git config -z` (`key\nvalue\0` or `key\0` for no-value variables). Multi-valued variables are handled via a `string_list`-based `multiread` callback, avoiding edge cases with empty strings. Single-variable queries and `git var -l` do not print extra delimiters after multi-valued variables, while multi-variable queries include an extra NUL delimiter to disambiguate the stream. Non-`-z` output remains unchanged.
- **Error handling**: prints an empty record (blank line or NUL) for any unset variable and continues processing remaining variables, exiting with code 1 if any variable was unset. `BUG()` is triggered for unknown `ident_part` values, and redundant NULL checks in `ident_part()` are removed to catch programming errors.
- **Multi-valued variables**: implemented via a `string_list`-based `multiread` callback, replacing the prior NUL-delimited string approach. This avoids edge cases with empty strings and aligns with Git’s standard practices.
- **Argument parsing**: enforces strict ordering (options before variable names) and rejects ambiguous invocations (e.g., `git var GIT_AUTHOR_NAME -z`). `--` is not currently handled but could be added in a future iteration.
- **Signing key resolution**: uses `get_signing_key()` (from `gpg-interface.c`) to ensure consistency with actual commit signing. The documentation for `GIT_SIGNING_KEY` remains clear and sufficient.
- **Documentation**: expanded descriptions of identity variables (`GIT_AUTHOR_NAME`, etc.) to explicitly state they reflect the values that would be used by `git commit`, addressing Junio’s prior terseness concerns.
- **Edge cases**: empty variable lists (e.g., `git var` with no args) still exit with code 129 and print usage, though this may be revisited in a future iteration. Multi-valued variables are explicitly handled in `-z` mode to avoid parsing failures when values contain newlines.
- **Test coverage**: comprehensive, including `-z` output format, multi-valued variables, argument ordering, unset variables, and signing key resolution. Tests verify alignment with `git config -z` behavior for edge cases (e.g., no-value variables, multi-valued variables with newlines).
- **No on-disk format changes or new configuration options**; the patch extends an existing plumbing command predictably.

Open questions and loose ends:
- **Output format backward-compatibility**: clarified in v6 that single-variable queries and `git var -l` do not print extra delimiters after multi-valued variables, while multi-variable queries do. Junio’s prior concerns about script breakage are addressed, but minor adjustments may still be needed.
- **Usability**: whether to allow empty variable lists (e.g., `git var` with no args) remains deferred to Junio’s broader usability argument. `--` handling is not implemented but could be added later.
- **Long-term CLI rules**: whether Git 3.0 should enforce stricter flag/variable ordering across all commands, and whether `git var` should stop handling `git config -l` output. Neither is a blocker for the current patch.

Participant positions:
- **Andrew Pleeter**: implemented v6, addressing all substantive feedback from Junio’s review of v5. Key changes include switching to a `string_list`-based `multiread` callback for multi-valued variables, adding `BUG()` for unknown `ident_part` values, expanding documentation for identity variables, and clarifying the `-z` output format. The patch is now **technically robust and ready for final review**.
- **Junio C Hamano**: **high-weight review** on v5 raised concerns about **documentation clarity, output format backward-compatibility, error handling robustness, and multi-valued variable implementation**. All points are addressed in v6, including expanded documentation, `string_list`-based multi-valued variable handling, and stricter error handling. The patch is now **approaching readiness for integration**, though minor adjustments may still be needed.
- **Phillip Wood**: **substantive, high-weight review** on v3 and v4 identified critical issues with output format ambiguity (`-z` mode), argument parsing, multi-valued variable handling, error behavior, and signing key resolution. All points are addressed in v5/v6, including alignment with `get_signing_key()`, script-friendly error handling, and finalization of the `-z` output format. No new feedback on v6.
- **Jeff King (Peff)**: prior **high-weight review** endorsed extending `git var` instead of introducing a new command. No new feedback on the implementation.
- **brian m. carlson**: prior **high-weight review** requested machine-readable output, removal of `the_repository`, and X.509 support; all are satisfied in v3/v6. No position on the implementation yet.
- **Ben Knoble**: **surface-level review** on v3 noted the current behavior of `git var` on empty argument lists. The feedback is practical but defers to Junio’s broader usability argument; the behavior remains unchanged in v6.

Related work: the patch directly implements the design consensus reached in the prior thread, contrasting with the earlier `git ident` proposal. No other alternatives are under discussion. The focus is now on finalizing the patch for integration.