Thread identity: Refactoring ODB alternates handling during repository creation. 9-patch series (`[PATCH v3 0/9]`) by Patrick Steinhardt, part of the ongoing ODB abstraction effort. Thread type: patch series.

Core problem or goal: Remove the ability to write ODB alternates after repository creation, simplifying the ODB interface. Currently, alternates can be added at any time via `odb_source_write_alternate()`, but Git only uses this during initial setup (e.g., `git clone --reference`). The series introduces a new option in `odb_source_create_on_disk()` to handle alternates at creation time, eliminating the need for a separate `write_alternates()` callback. This change prepares for alternates to become an implementation detail of the ODB backend, giving backends more control over their configuration. The v3 iteration refactors `init_db()` into `create_repository()`, which now only creates the repository skeleton without setting up the refdb or ODB, eliminating the need for skip flags and moving command-specific logic (e.g., initialization messages) into the commands themselves. Patch 2 defers ODB initialization in `git clone` until after the clone source URI is resolved, addressing two key issues: forward compatibility with alternate ODB backends (which may require object hash information at creation time) and the need to resolve alternates paths before ODB setup. Patch 4/9 (updated) renames `setup_reference()` to `collect_alternates()` and refactors it to gather alternates paths into a `strvec` instead of immediately writing them to the ODB, preparing for later patches that consolidate alternates setup into `create_object_database()`. Patch 5/9 (new email) extends `collect_alternates()` to handle shared local clones (`git clone --shared`), consolidating all alternates logic into a single code path by moving the manual alternates setup from `clone_local()` into `collect_alternates()` and adding `src_repo` and `is_local` parameters to support this.

Current status: Under review. The series is built on `2c3adbb2c4` (The 18th batch, 2026-08-24) with `ps/odb-eagerly-load-alternates` merged in. All 9 patches have been posted as v3, incorporating feedback from v2: the primary change is the refactoring of `init_db()` into `create_repository()` (patches 1-2), which simplifies the interface by removing skip flags and moving command-specific logic out of the core initialization function. Patches 3-9 are largely unchanged from v2, except for minor typo fixes and the refactoring in patches 4-5/9. Patch 3/9 is a preparatory refactoring that moves `setup_reference()` closer to `copy_alternates()` to avoid a forward declaration, enabling the subsequent renaming to `collect_alternates()` in patch 4/9. Patch 5/9 consolidates alternates logic for shared local clones into `collect_alternates()`, removing the manual alternates setup in `clone_local()` and preparing for the lockfile API adoption in patch 6. The series retains the lockfile API adoption for writing the `info/alternates` file (patch 6), resolving TOCTOU race and concurrent access concerns raised by Junio C Hamano and Toon Claes in v1. The refactoring in patches 1-2 addresses Justin Tobler’s forward-looking architectural concern about the growing complexity of `init_db()`’s skip flags by replacing them with a cleaner separation of concerns: `create_repository()` handles skeleton creation, while `create_object_database()` and `initialize_repository()` (a new helper) handle ODB and refdb setup, respectively. This design avoids the awkwardness of accumulating skip flags and better reflects the narrowed scope of repository initialization in `git clone`. Patch 2 specifically defers the `create_object_database()` call in `cmd_clone()` until after URI resolution and transport setup, ensuring alternates can be written with full context and enabling subsequent patches to write alternates during ODB creation.

Key technical details:
- Files touched: `builtin/clone.c` (patches 1-2 refactored to defer ODB setup and consolidate alternates logic into `collect_alternates()`; patch 1 introduced the `create_repository()` refactoring and deferred ODB/refdb setup until after URI resolution; patch 2 moved the `create_object_database()` call to occur after URI resolution and transport setup, deferring `setup_reference()` until after ODB creation; patch 3/9 moved `setup_reference()` closer to `copy_alternates()` to avoid a forward declaration; patch 4/9 renamed `setup_reference()` to `collect_alternates()` and refactored it to gather alternates paths into a `strvec` using `struct add_one_alternate_data` to track required/optional alternates; patch 5/9 extended `collect_alternates()` to handle shared local clones by adding `src_repo` and `is_local` parameters and moving the manual alternates setup from `clone_local()` into `collect_alternates()`), `setup.c`/`setup.h` (patches 1-2 split `init_db()` into `create_repository()`, `create_object_database()`, and `initialize_repository()`; `create_object_database()` now accepts a `strvec` of alternates paths and an `odb_create_on_disk_options` struct for ODB creation; patch 6 updated `create_object_database()` to pass an empty options struct; patch 7 updated it to accept the alternates `strvec` directly), `odb/source-files.c` (patch 6 added support for writing alternates during ODB creation via `odb_source_files_create_on_disk()` with `odb_create_on_disk_options` using the lockfile API; patch 9 removed the `write_alternate` callback and its lockfile mechanism), `odb/source-inmemory.c`, `odb/source-loose.c`, `odb/source-packed.c` (patch 9 removed backend-specific `write_alternate` implementations), `odb/source.h` (patch 6 added `struct odb_create_on_disk_options`, adjusted header organization; patch 9 removed the `write_alternate` field from the `odb_source` struct and its associated inline helper function), `odb.c`/`odb.h` (patch 9 removed `odb_add_to_alternates_file()` and its supporting infrastructure), and `t/t5604-clone-reference.sh` (updated tests for worktree alternates handling and relative path resolution, including two new tests in patch 5: one verifying the bug fix for cloning from a linked worktree and another ensuring relative alternates paths are properly resolved).
- New or renamed symbols: `create_repository()` (replaces `init_db()` for skeleton creation, introduced in patch 1), `initialize_repository()` (new helper for refdb setup, introduced in patch 1), `collect_alternates()` (renamed from `setup_reference()` in patch 4/9, now gathers alternates paths into a `strvec` with `src_repo` and `is_local` parameters to handle shared local clones), `add_one_alternate()` (replaces `add_one_reference()` and uses `struct add_one_alternate_data` to track required/optional alternates), `struct add_one_alternate_data` (tracks whether an alternate is required/optional and holds the `strvec` of paths), `read_alternates()` (renamed from `copy_alternates()`), and `odb_create_on_disk_options` (new struct carrying alternates `strvec` for ODB creation). The `write_alternates()` callback, `odb_add_to_alternates_file()` helper, and lockfile mechanism have been removed. `odb_source_write_alternates()` is now unused and deleted. The `INIT_DB_SKIP_ODB` flag is no longer needed due to the `create_repository()` refactoring.
- Subsystems: ODB (object database), alternates handling, repository creation, worktree handling.
- Old-vs-new behavior: No user-visible changes. The series defers ODB and refdb setup in `git clone` until after the clone source URI is resolved, ensuring alternates are written with full context. Patch 1 introduces `create_repository()` to handle skeleton creation, while `create_object_database()` and `initialize_repository()` handle ODB and refdb setup, respectively. Patch 2 specifically defers the `create_object_database()` call in `cmd_clone()` until after URI resolution and transport setup, enabling alternates to be written with full context. Patches 1-5 addressed a latent bug in worktree alternates handling (fixing unresolved common directory paths) and consolidated alternates logic into a single code path. Patch 3/9 is a mechanical refactoring that moves `setup_reference()` closer to `copy_alternates()` to avoid a forward declaration. Patch 4/9 refactored `--reference` and `--reference-if-able` logic to gather alternates paths into a `strvec` instead of immediately writing them to the ODB. Patch 5/9 extends `collect_alternates()` to handle shared local clones (`git clone --shared`), removing manual alternates updates in `clone_local()` for shared clones. Patch 6 implements the core design change: alternates are now written during ODB creation via `odb_create_on_disk_options` using Git’s standard lockfile API, ensuring atomic writes and resolving TOCTOU race and concurrent access concerns. Patch 7 consolidates alternates setup into `create_object_database()` by passing the alternates `strvec` directly, removing the last manual alternates writing step in `cmd_clone()`. Patch 8 removes the unused `write_alternates()` infrastructure, which is now redundant due to the lockfile API adoption in patch 6. Patch 9 removes the `write_alternate` callback and its supporting infrastructure across all ODB backends.
- Test coverage: Updated in `t/t5604-clone-reference.sh` to verify new behavior, including edge cases for cloning from worktrees and resolving relative alternates paths. The lockfile API adoption in patch 6 ensures safety against partial writes or corruption, though additional test coverage for concurrent repository creation scenarios may be added in future iterations.

Open questions and loose ends: Justin Tobler’s forward-looking architectural question about the ODB interface’s long-term flexibility—whether `git clone --shared` and similar options should be restricted to the "files" backend or left to the backend’s discretion—remains unresolved. Patrick Steinhardt’s response in v2 clarified that the `odb_create_on_disk_options` interface’s `alternates` field is intentionally flexible, allowing backends to define their own semantics for user-facing options like `--shared`. This aligns with the ODB abstraction goal but may prompt further discussion about how to document or enforce consistency for such options. The series may also prompt broader discussion about safety and concurrency in the ODB abstraction effort, particularly around backend-specific options and interface clarity. The refactoring of `init_db()` into `create_repository()` addresses Justin Tobler’s concern about skip flags but may require additional review to ensure the new helpers (`create_object_database()` and `initialize_repository()`) are used consistently across the codebase. Reviewers may verify that the reordering in patch 2 doesn’t introduce regressions in edge cases (e.g., template config rewriting URIs). The consolidation of alternates logic in patches 4-5/9 may attract scrutiny regarding error handling for invalid alternates paths, though the patches appear uncontroversial given their incremental nature. The `--no-shared` case for local clones is deferred to patch 6, which may prompt questions about whether the consolidation in patch 5/9 is complete.

Participant positions: Junio C Hamano raised a security and correctness concern in v1 about patch 6’s use of append mode instead of the lockfile API for writing the `info/alternates` file, prompting Patrick Steinhardt to adopt the lockfile API in v2, resolving the primary blocker. Toon Claes provided reviews of patches 3, 4, 5, 6, and 8 in v1. His earlier comments (patches 3-5) were surface-level, noting minor observations about code structure without engaging with technical substance, correctness, or edge cases. His review of patch 5 included a suggestion to move `fclose(in)` before the `out` label, which Patrick Steinhardt declined, citing reduced fragility for future extensions. Toon’s review of patch 8 raised a substantive concern about the removal of locking, which is now resolved by the lockfile API adoption in patch 6. In v2, Toon Claes provided a surface-level approval of the cover letter, stating “This version looks good to me!” without elaborating on technical substance, testing, or specific patches. This approval carries low weight due to its lack of engagement with the series' core changes. Justin Tobler’s reviews are substantive and forward-looking. His feedback on patch 1 in v2 engaged with the patch’s technical rationale and design implications, accepting the core motivation but questioning the growing complexity of `init_db()`’s skip flags. Patrick Steinhardt addressed this in v3 by refactoring `init_db()` into `create_repository()`, eliminating the need for skip flags and moving command-specific logic into the commands themselves. Justin’s review of patch 6 in v2 raised a thoughtful question about the ODB interface’s exposure of alternates to backends that may not use them, probing the abstraction’s long-term flexibility. His surface-level review of patch 7 in v2 offered brief approval without engaging with technical details. Both comments on patches 1 and 6 focus on interface clarity and extensibility rather than implementation bugs or edge cases. Patrick Steinhardt has addressed all substantive feedback, and no other reviewers have weighed in on the locking issue or the broader design. The series is expected to proceed to integration once the remaining ODB-focused contributors (e.g., Karthik Nayak) have had an opportunity to review patches 6-9.

Related work: Part of Patrick Steinhardt’s ODB abstraction effort; built on `ps/odb-eagerly-load-alternates`. The series is foundational for moving alternates handling into the "files" backend, aligning with the goal of making alternates an ODB backend implementation detail. The latent bug fix in patch 5 (worktree alternates handling) highlights the importance of consolidating alternates logic before further ODB abstraction. The broader effort continues to progress incrementally, with this series representing a key step toward simplifying the ODB interface. The lockfile API adoption may influence the design of future ODB abstraction work, particularly around safety and concurrency. Justin Tobler’s feedback on the ODB interface’s flexibility may inform future discussions about backend-specific options and interface clarity. The refactoring of `init_db()` into `create_repository()` could simplify the interface for future work, though it may require additional review to ensure consistency across the codebase. The clarification about backend-specific semantics for `--shared` and similar options may prompt further discussion about how to document or enforce consistency in the ODB abstraction.