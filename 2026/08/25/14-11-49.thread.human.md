Patrick Steinhardt’s nine-patch series to refactor ODB alternates handling during repository creation is now complete. The thread began as a proposal to simplify the ODB interface by removing the ability to write alternates after repository creation, consolidating all alternates logic into the ODB creation process. The series has evolved through four iterations, with v4 incorporating all substantive review feedback and adding clarifying documentation for the new repository creation helpers.

The core change—writing alternates during ODB creation via `odb_create_on_disk_options` using Git’s lockfile API—was implemented in patch 7, resolving earlier security concerns about TOCTOU races. Patches 1-6 consolidated alternates logic into a single code path, fixed a latent bug in worktree alternates handling, and deferred ODB setup in `git clone` until after URI resolution to ensure alternates are written with full context. Patch 9, the final patch, removes the now-unused `odb_source_write_alternates()` function and its supporting infrastructure, completing the series’ goal of making alternates an ODB backend implementation detail.

Key participants include Justin Tobler, whose substantive reviews identified a critical correctness issue in patch 5 (ensuring alternates are only added for shared local clones) and prompted improved documentation for the new helpers in `setup.h`. Junio C Hamano’s early concern about the lockfile API adoption in patch 7 was addressed in v2, and Toon Claes provided surface-level reviews that did not engage with the series’ technical substance. The series is now under review and appears ready for integration, with no remaining open questions or loose ends. It is built on `ps/odb-eagerly-load-alternates` and represents a foundational step in Patrick Steinhardt’s ongoing ODB abstraction effort.