**Human mode summary**

This thread is about a 9-patch series from Patrick Steinhardt that refactors how Git handles ODB (object database) alternates during repository creation. The goal is to simplify the ODB interface by removing the ability to write alternates after repository creation, making alternates an implementation detail of the ODB backend. This is part of Patrick’s broader ODB abstraction effort, which aims to give backends more control over their configuration.

The series is now at v4, with all patches posted and under review. The key changes since v3 are improved documentation for the new repository creation functions (`create_repository()`, `create_reference_database()`, and `create_object_database()`) and clarifications around the `reinit_ok` parameter, addressing Justin Tobler’s feedback. The series also fixes a latent bug in worktree alternates handling (patch 6) and consolidates all alternates logic into a single code path, ensuring alternates are only added for shared local clones (patch 5). The core design change (patch 7) now uses Git’s standard lockfile API for atomic alternates writes, resolving earlier security concerns raised by Junio C Hamano.

Justin Tobler has been the most substantive reviewer, raising questions about interface clarity and correctness, including a critical fix in patch 5 to ensure alternates are only added for shared local clones. His feedback has been fully addressed, and no substantive objections remain. Karthik Nayak recently provided a surface-level review of patch 1, confirming the initialization order is preserved but adding no new technical feedback. The series appears ready for Junio’s queueing, with all major concerns resolved and the code well-documented. The only open question is a forward-looking architectural discussion about whether user-facing options like `--shared` should be backend-specific, but this doesn’t block the current work.