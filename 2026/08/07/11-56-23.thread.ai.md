Mode: ai

Thread identity
Subject: [PATCH 0/3] convert USE_NSEC to runtime config
Series version: v7 (applied under topic `dk/use-nsec-runtime`; **graduated to `next` as of 2024-07-10**, **merged to `master` as of 2024-07-15**)
Originating author: D. Ben Knoble
Thread type: patch series

Core problem or goal
Replace the build-time `USE_NSEC` macro with a runtime configuration option (`core.useNanosec`) to allow users to enable nanosecond precision on supported filesystems without recompiling Git, reducing "racy Git" problems. The series also includes a Meson build-system fix (`MAN_BASE_URL` configuration) and a no-op refactor of `repo_config_values_init`.

Current status
**Merged to `master` as of 2024-07-15**. The series is complete and all prior feedback has been addressed. Junio’s requested **final reroll (v7)** was posted and served as a final polish to confirm the series was technically complete; no further changes were intended or made. The author’s latest email confirms the v7 patches Junio received exactly match the version he had already locally amended (including the typo fix "comparisions" → "comparisons") and queued. The author’s intent was to nudge Junio to update the "What’s cooking" report, not to request further changes. The topic (`dk/use-nsec-runtime`) is now fully merged, and the discussion is closed. The first patch (Meson build-system fix) and the second patch (no-op refactor of `repo_config_values_init`) advanced independently and are uncontroversial.

Key technical details
Files touched: `Documentation/config/core.adoc`, `Documentation/meson.build`, `meson_options.txt`, `environment.c`, `environment.h`, `read-cache.c`, `statinfo.c`, `Documentation/technical/racy-git.adoc`, `compat/posix.h`, `Makefile`, `configure.ac`, `builtin/update-index.c`.
New config key: `core.useNanosec` (defaults to false).
Behavior change: nanosecond precision becomes configurable at runtime rather than compile time; performance impact documented for `git diff` in new worktrees. The patch preserves existing behavior on platforms lacking nanosecond support by treating the `use_nanosec` field as false when `NO_NSEC` is defined, preventing silent corruption. **No runtime warning will be emitted** when `core.useNanosec=true` is set on a `NO_NSEC` platform; users must rely on documentation to understand the risks. The `#ifndef NO_NSEC` guard around the `core.usenanosec` config handler in `environment.c` is retained to prevent reading nanosecond values on unsupported platforms, avoiding false negatives in racy checks. Jeff King (Peff) notes this guard introduces a subtle correctness risk: an index generated by a non-`NO_NSEC` build could store real nanosecond values, which a `NO_NSEC` build would misinterpret as zero, leading to false "racy" comparisons. This edge case is unlikely in practice but may warrant documentation in the future. The guard’s practical impact is minimal: users on `NO_NSEC` builds cannot enable `core.useNanosec` without recompiling, even if their platform supports it. The discussion highlights a trade-off between build-time restrictions and runtime flexibility, with both Peff and Knoble confirming the guard is reasonable given the complexity of eliminating `NO_NSEC` entirely.
On-disk format: unchanged.
Test coverage: platform-dependent; existing tests (e.g., `t4060`) pass when `core.useNanosec=true` is set, but systematic testing is complicated by platform variability. The Meson build-system fix (first patch) addresses a long-standing inconsistency by exposing a configurable `man_base_url` option in `meson_options.txt`, defaulting to `file://$(htmldir)/` (with a trailing slash) to match the Makefile’s behavior. The fix resolves broken links in manual pages (e.g., `/usr/share/manuser-manual.html` → `/usr/share/man/user-manual.html`) and is motivated by the need to ensure correct rendering of links added in later patches (e.g., to `racy-git.adoc`). The no-op refactor in the second patch reorders assignments in `repo_config_values_init()` to group them by config section ("core", "sparse", "branch") for maintainability, improving code readability and easing future maintenance as new fields are added. The change is purely cosmetic and introduces no behavioral differences.
Performance: the v1 performance regression (~41× slowdown) is resolved by caching the config value in `repo_config_values`. The nested `#ifndef NO_NSEC` guards in `is_racy_stat()` are replaced with a single ternary expression for clarity.
Code structure: the `use_nanosec` field is unconditionally present in `struct repo_config_values` but ignored (treated as false) when `NO_NSEC` is defined. The `#ifndef NO_NSEC` guard in `environment.c` is retained, and the author has abandoned plans to consolidate `NO_NSEC` build-time differences using `ST_XTIME_NSEC(st)` in `is_racy_stat()`. The Meson build-system fix adds a configurable `man_base_url` option to `meson_options.txt`, defaulting to `file://$(htmldir)/` (with a trailing slash) to match the Makefile’s behavior. The no-op refactor in the second patch reorders assignments in `repo_config_values_init()` to group them by config section ("core", "sparse", "branch") for maintainability, improving code readability and easing future maintenance as new fields are added.
**Architectural constraint**: the new call to `repo_config_values()` in `is_racy_stat()` introduces a latent bug when operating on a repository that is not `the_repository`, as the function currently only works for the global repository. The author defers deeper investigation, citing time constraints and the complexity of testing across subsystems. The series adopts a safe default (treating `core.useNanosec` as false) when `istate->repo != the_repository` as a stopgap. This is not a regression but makes the existing limitation more visible. Junio explicitly flagged this as a blocker for future recursive submodule operations (e.g., `git diff` or `git status` traversing submodules without spawning subprocesses), noting that `git grep --recurse-submodule` does not exercise the racy-entry code paths and thus offers no assurance against latent bugs in other recursive contexts. Patrick Steinhardt suggested refactoring to `struct repo_settings` as a potential mitigation if future edge cases arise. **The discussion between Knoble and Junio confirmed the stopgap is a temporary measure and may reduce the impetus to fix the underlying limitation**, but no immediate changes were proposed. Knoble argued that papering over the issue with a safe default might delay deeper refactoring, but acknowledged the trade-off was acceptable given the series’ goals.

Open questions and loose ends
- **Forward-looking constraint**: the new dependency on `repo_config_values()` in `is_racy_stat()` is explicitly identified as a blocker for recursive submodule operations. The latent bug (treating `core.useNanosec` as false when `istate->repo != the_repository`) remains unresolved but is documented as a stopgap. Deeper refactoring (e.g., forwarding the repository argument through the config subsystem or migrating to `struct repo_settings`) is deferred due to complexity. D. Ben Knoble’s partial control-flow analysis of 214 call paths to `repo_config_values()` confirmed all terminate at `the_repository`, but the latent bug remains for future work. Junio’s latest reply underscored that this limitation will obstruct features requiring recursive submodule traversal without subprocesses. **The discussion is now closed**; the stopgap’s practicality and its potential to delay deeper refactoring were acknowledged, but no consensus on next steps was reached.
- **Edge case documentation**: Jeff King (Peff) surfaced a subtle correctness risk involving mixed builds (one with `NO_NSEC` and one without) on a system that supports nanosecond timestamps. The risk is unlikely in practice but may warrant a note in `Documentation/technical/racy-git.adoc` or a "don’t do that" warning in the config documentation. The discussion between Peff and Knoble confirmed the guard’s necessity and its trade-offs, but neither participant pushed for immediate documentation changes. The exchange is treated as a theoretical footnote and is now closed.
- **Meson build-system fix and no-op refactor**: both the first and second patches in v7 are **uncontroversial, self-contained, and ready to advance independently**. The Meson fix resolves a long-standing inconsistency between Makefile and Meson builds, ensuring relative links in manual pages render correctly. The no-op refactor improves maintainability by grouping assignments in `repo_config_values_init()` by config section. Neither patch has dependencies on the rest of the series, and both are backward-compatible. No further changes are expected.

Participant positions
- **D. Ben Knoble (author)**: confirmed the series was technically complete and ready for `master`. Acknowledged the architectural constraint introduced by the new `repo_config_values()` call but deferred deeper refactoring, citing time constraints and the complexity of testing across subsystems. **Did not produce v8**; the v7 cover letter explicitly stated no further changes were planned. Agreed with Jeff King (Peff) that the preprocessor guard (`#ifndef NO_NSEC`) was not worth removing at this stage. Reported that a partial control-flow analysis of 214 call paths to `repo_config_values()` showed all terminated at `the_repository`, confirming the limitation was not new. Proposed `struct repo_settings` or a global variable as potential stopgaps if future edge cases revealed problems. Accepted Junio’s and Patrick’s design decisions (no runtime warning, unconditional initialization of `use_nanosec`) and updated documentation to warn about platform/filesystem limitations. Described the second patch (no-op refactor) and the Meson build-system fix (first patch) as "well-crafted, uncontroversial, and ready to advance to `master`". Incorporated Patrick Steinhardt’s requested cosmetic change (replacing "defined" with "enabled" in a comment in `builtin/update-index.c`) in the follow-up patch. Noted that both the Meson fix and the no-op refactor were self-contained, uncontroversial, and ready to advance independently, resolving a long-standing inconsistency between Makefile and Meson builds. Engaged with Peff’s follow-up discussion, confirming the `#ifndef NO_NSEC` guard’s role in preventing false negatives in racy checks on `NO_NSEC` platforms while acknowledging its trade-off: users on `NO_NSEC` builds cannot enable `core.useNanosec` without recompiling, even if their platform supports nanoseconds. Acknowledged the guard’s practical impact was minimal and the edge case (mixed builds leading to false "racy" comparisons) was unlikely, but did not propose documentation changes. Confirmed the editorial typo fix ("comparisions" → "comparisons") and included it in v7. **In the latest exchange, Knoble confirmed the v7 patches Junio received matched his locally amended version and nudged Junio to update the "What’s cooking" report, signaling the discussion is closed.**
- **Junio C Hamano (maintainer)**: identified the performance regression in v1 and proposed caching the config value in `repo_config_values` as the fix. Flagged the cross-platform correctness blocker in v2 and approved the author’s solution (unconditionally including the `use_nanosec` field but treating it as false when `NO_NSEC` is defined). Rejected the idea of a runtime warning for `core.useNanosec=true` on `NO_NSEC` platforms, prioritizing user experience for shared configurations. Raised the forward-looking architectural concern about `repo_config_values()` but did not block graduation. **Merged the series to `master` as of 2024-07-15**, confirming the v7 patches matched his locally amended version of v6 (which included the typo fix). **Locally handled the routine conflict resolution with `ty/repo-config-cleanups` in `seen`**. In the latest reply, Junio acknowledged the author’s nudge to update the "What’s cooking" report, effectively closing the discussion. The architectural constraint (latent bug in `is_racy_stat()` for non-`the_repository` repositories) remains documented but deferred.
- **Patrick Steinhardt**: confirmed the series was technically sound and ready for `next`. Endorsed the plan to eliminate preprocessor directives in `is_racy_stat()` and agreed with Junio’s rejection of a runtime warning. Expressed dissatisfaction with the current state of `repo_config_values()` but acknowledged refactoring it to support arbitrary repositories was non-trivial. Suggested `struct repo_settings` as a potential mitigation if future edge cases arose. Requested the trivial cosmetic change (replacing "defined" with "enabled" in a comment in `builtin/update-index.c`) as the final action before merging, which the author addressed.
- **Jeff King (Peff)**: raised a subtle correctness risk in the final patch: the `#ifndef NO_NSEC` guard around the `core.usenanosec` config handler in `environment.c` does not eliminate the `NO_NSEC` build knob and introduces a hypothetical edge case where mixed builds (one with `NO_NSEC` and one without) on a system that supports nanosecond timestamps could lead to false "racy" comparisons. Acknowledged the scenario was unlikely and did not block graduation, but suggested documenting the risk or leaving it as a "don’t do that" footnote. Clarified that the guard prevents reading nanosecond values on unsupported platforms, avoiding false negatives in racy checks, but also restricts users on `NO_NSEC` builds from enabling `core.useNanosec` without recompiling. Confirmed the guard was reasonable given the complexity of eliminating `NO_NSEC` entirely, but the discussion highlighted a trade-off between build-time restrictions and runtime flexibility. Engaged in follow-up discussion with Knoble, treating the edge case as theoretical and not warranting immediate code or documentation changes. **Agreed with Knoble that the preprocessor guard was not worth removing at this stage**, effectively closing the discussion.
- **SZEDER Gábor**: provided a surface-level review on commit message formatting, suggesting the `Best-viewed-with: --ignore-space-change` hint be retained. Did not engage with technical substance.

Related work
- `ty/repo-config-cleanups` (in `seen`): removed comments in `struct repo_config_values`, causing a conflict with this series. The conflict was routine and resolved by Junio during integration. The first and second patches in v7 (Meson build-system fix and no-op refactor) were unaffected by this conflict, as they operated in separate layers (build system and initialization logic).