Mode: human

The thread is now complete. D. Ben Knoble’s patch series converting Git’s nanosecond precision from a build-time option (`USE_NSEC`) to a runtime configuration (`core.useNanosec`) has been accepted and queued in Junio C Hamano’s `next` branch, where it has been cooking since July 10, 2024. The final version (v7) addressed all prior feedback, including a performance regression fix, platform compatibility concerns, and a documentation typo that Junio locally amended before formal inclusion.

The series is functionally complete and ready for the next release cycle. The only remaining step is routine conflict resolution with another in-flight topic (`ty/repo-config-cleanups`), which Junio has already handled. Two early patches—a Meson build-system fix and a no-op refactor of configuration initialization—are uncontroversial and could advance independently if needed. The core change, `core.useNanosec`, defaults to false and allows users to enable nanosecond precision on supported filesystems without recompiling, reducing "racy Git" issues.

One architectural limitation remains unresolved: the new dependency on `repo_config_values()` in `is_racy_stat()` only works for the global repository, not submodules. This was documented as a forward-looking constraint, with a safe default (treating the setting as false for non-global repositories) adopted as a stopgap. Junio and Knoble agree this may delay deeper refactoring but is an acceptable trade-off for now. Jeff King also noted a theoretical edge case with mixed builds on nanosecond-capable systems, but this was deemed unlikely and not worth further changes. No additional iterations are expected.