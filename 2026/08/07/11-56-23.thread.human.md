Mode: human

The thread is about converting Git’s nanosecond timestamp support from a compile-time option (`USE_NSEC`) to a runtime configuration (`core.useNanosec`). The series, authored by D. Ben Knoble, has now graduated to Junio’s `next` branch for integration testing and is effectively complete. The final patch (3/3) replaces preprocessor guards with a runtime check, making nanosecond precision configurable without recompiling Git, which helps reduce "racy Git" issues on supported filesystems.

Key participants include Junio (the maintainer), who guided the series through several iterations, addressing a performance regression and cross-platform correctness concerns. Jeff King (Peff) raised a subtle edge case involving mixed builds but agreed the trade-offs were acceptable. Patrick Steinhardt and SZEDER Gábor also reviewed, with Patrick endorsing the approach and SZEDER offering minor commit-message feedback. The series is uncontroversial and ready to advance, though it introduces a latent limitation: the new dependency on `repo_config_values()` doesn’t work for non-global repositories, which could block future submodule features. This is documented as a forward-looking constraint but not a blocker for graduation.

The first two patches—a Meson build-system fix and a no-op refactor of `repo_config_values_init()`—are self-contained and could advance independently. The series is now in `next` pending routine conflict resolution with another topic (`ty/repo-config-cleanups`). No further changes are expected.