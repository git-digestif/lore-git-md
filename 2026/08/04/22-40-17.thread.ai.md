Thread identity: patch series (single patch), originating author Daniel Martí, subject "bugfix for contrib/credential/libsecret: avoid crash and silent data loss".

Core problem or goal: fix a crash and silent data loss in the `contrib/credential/libsecret` credential helper. The current implementation uses the `SECRET_SEARCH_LOAD_SECRETS` flag with `secret_service_search_sync()`, which silently discards failures (e.g., locked or concurrently deleted items), leading to NULL secret values. Subsequent calls to `secret_value_get_text()` or `secret_value_unref()` trigger assertion failures, causing the password to be lost even if it was retrievable.

Current status: patch v1 posted two weeks ago, under review. Junio C Hamano has provided a substantive review proposing an alternative implementation that preserves the batch-fetch optimization while addressing the crash. Daniel Martí has defended his original design, arguing that dropping `SECRET_SEARCH_LOAD_SECRETS` in favor of explicit `secret_item_load_secret_sync()` maintains the same round-trip count in the common case, unifies the code path, and ensures all errors are surfaced. Junio’s latest reply clarifies that the discussion has progressed through a substantive review cycle, and the next step is for Daniel to revise the patch to address open questions—particularly the clarity of the commit message and code—so that future reviewers won’t need to rehash the same points. No consensus has been reached on the design trade-off, but the procedural path forward is now clear: a v2 patch is expected.

Key technical details:
- File: `contrib/credential/libsecret/git-credential-libsecret.c`
- Changed symbols: replaces `SECRET_SEARCH_LOAD_SECRETS` flag with explicit `secret_item_load_secret_sync()` call (v1 approach). Junio’s alternative suggests retaining the flag and conditionally calling `secret_item_load_secret_sync()` only if `secret_item_get_secret()` returns NULL.
- Behavior change: prevents silent data loss and assertion crashes by properly reporting errors for inaccessible items. Daniel’s approach eliminates the two-step (SearchItems + GetSecrets) process that silently discards errors, replacing it with a single SearchItems call plus one explicit GetSecret for the relevant item. Junio’s alternative would add a third round trip in rare cases (when `secret_item_get_secret()` returns NULL) and leave the error-prone path untested.
- No new CLI options, config keys, or on-disk format changes.
- No test coverage included; problem is reproducible with concurrent credential operations and occurs several times per week for the author.

Open questions and loose ends:
- Whether to drop the `SECRET_SEARCH_LOAD_SECRETS` flag entirely (Daniel’s preferred approach) or retain it and conditionally load secrets (Junio’s alternative). The discussion centers on design philosophy: optimization (preserving the batch-fetch path for the common case) vs. simplicity and reliability (eliminating silent failures). Daniel argues that `SECRET_SEARCH_LOAD_SECRETS` is not a true optimization (it performs two round trips: SearchItems + GetSecrets) and silently discards errors, including client-side skipped items. His approach replaces this with a single SearchItems call plus one explicit GetSecret, keeping the round-trip count identical in the common case while ensuring all errors are surfaced.
- Clarity of the commit message and code: Junio requests that the v2 patch be self-contained, with a commit message and implementation that clearly explain the rationale and design choices without requiring reviewers to reference prior discussion. This is now the primary blocker for further review.

Participant positions:
- **Daniel Martí**: Substantive defense of his original design. Argues that `SECRET_SEARCH_LOAD_SECRETS` is not a true optimization and silently discards errors, including client-side skipped items. His approach replaces the two-step process with a single SearchItems call plus one explicit GetSecret, maintaining the same round-trip count in the common case while ensuring all errors are surfaced. Prefers simplicity and reliability over preserving an error-prone path. Has nudged the discussion after two weeks of silence and is expected to post a v2 patch addressing Junio’s feedback on clarity.
- **Junio C Hamano**: Substantive review proposing an alternative implementation that retains `SECRET_SEARCH_LOAD_SECRETS` and conditionally loads secrets only when `secret_item_get_secret()` returns NULL. Prioritizes preserving the batch-fetch optimization for the common case, arguing the race condition is rare. Accepts the lack of test coverage due to reproducibility challenges. In his latest reply, emphasizes the need for a self-contained v2 patch with a clear commit message and implementation, so that future reviewers won’t need to rehash the same points.

Related work: none mentioned. This remains a standalone bugfix for the `contrib/credential/libsecret` helper.